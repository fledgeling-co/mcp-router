The two captures DEF-010 is about, kept because they are its reproduction.

    shasum -a 256 SURF-001.build.png SURF-003.build.png

returns one digest twice. The shell's picture and the Activity board's picture were the same
image, because the capture step photographed "whatever destination is restored" and filed it as
SURF-001. The other 23 superseded captures were deleted rather than kept: filenames that claim a
subject they do not show are the hazard this campaign just closed, and storing more of them beside
the corrected wall reopens it.

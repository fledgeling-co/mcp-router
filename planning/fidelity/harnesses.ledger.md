# Breadth ledger — harnesses (generated by scripts/acceptance/mock-fidelity-gate.sh)

Every row is derived on the run that wrote this file: the mock side from
`scripts/acceptance/mock-affordances.py`, the build side from the measurement harness's
dump. Nothing here is hand-maintained, which is what stops a row being deleted to make a
finding disappear.

## Layers

| Layer | Result | Detail |
|---|---|---|
| `tokens` | clean | 70 matched, 19 pending, of 89 rows |
| `literals` | clean | scanning 146 files |
| `structure` | clean | 193 nodes across 4 states · 57 declared axis/axes corroborated against child geometry |
| `geometry` | clean | 193 frames |
| `type-metrics` | clean | 65 per-role comparison(s) over 70 text nodes · 6 roles · Caption=13pt Subheadline=14pt Callout=15pt Body=16pt Title3=19pt Title1=26pt · 5 multi-line node(s) excluded from the per-role check |
| `copy` | 1 finding(s) | 4 paired strings |
| `census` | 1 finding(s) | 142 mock element(s) across 4 state(s) · 1 outside every derivation rule, 0 of them waived |
| `breadth` | 162 finding(s) | absent 16 · covered-by-pair 25 · covered-unoracled 21 · divergent 1 · extra-cited 88 · present 3 · structure-unpaired 15 · unclassified 36 |
| `font-weight-face` | inconclusive (substituted) | font-weight-face: String(describing:) of a Font built through Font.system(size:weight:) returns "Font(provider: SwiftUI.FontBox<SwiftUI.Font.SystemProvider>)" — no size, no weight, no face |

## Present / divergent / absent

| State | Affordance | Kind | Status | Mock value | Build value | Citation |
|---|---|---|---|---|---|---|
| ideal | v-ideal/heading/harnesses | heading | present | mock="Harnesses" kind=heading | build=harnesses.ideal/board-column/board-header/board-head/board-title text="Harnesses" role=board-title kind=text |  |
| ideal | v-ideal/sentence/which-ai-tools-on-this-mac-actually-route-throug | sentence | present | mock="Which AI tools on this Mac actually route through here. Poin" kind=sentence | build=harnesses.ideal/board-column/board-header/board-head/board-subtitle text="Which AI tools on this Mac actually route through here. Poin" role=board-subtitle kind=text |  |
| ideal | v-ideal/button/rescan | button | unclassified | mock="Rescan" kind=button | build=harnesses.ideal/board-column/board-header/board-actions/rescan text="Rescan" role=board-action kind=leaf |  |
| ideal | v-ideal/button/reconcile-all | button | unclassified | mock="Reconcile all…" kind=button | build=harnesses.ideal/board-column/board-header/board-actions/reconcile-all text="Reconcile all…" role=board-action kind=leaf | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| ideal | v-ideal/banner/antigravity-runs-18-servers-of-its-own-ten-of-wh | banner | unclassified | mock="Antigravity runs 18 servers of its own, ten of which this ro" kind=banner | build=harnesses.ideal/board-column/harnesses-populated/finding-band text="" role=finding-band kind=hstack |  |
| ideal | v-ideal/icon/unlabelled | icon | unclassified | mock="" kind=icon | build=harnesses.ideal/board-column/harnesses-populated/finding-band/finding-icon text="" role=finding-icon kind=leaf | D11 · scripts/acceptance/mock_fidelity.py:791 — a DECLARED residue, not a defect: "the harness records no glyph identity for an icon node, so an icon pair has nothing to compare and lands as unclassified rather than as agreement. That is the honest outcome, not the audited one." The pairing is real and is left standing; what cannot run is the comparison. Same shape as the font-weight-face row, and the remedy is an instrument that can read a glyph rather than an edit to this file |
| ideal | v-ideal/button/show-the-diff | button | unclassified | mock="Show the diff…" kind=button | build=harnesses.ideal/board-column/harnesses-populated/finding-band/finding-action text="Reconcile…" role=finding-action kind=leaf | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| ideal | v-ideal/section-header/detected-on-this-mac | section-header | unclassified | mock="Detected on this Mac" kind=section-header | build=harnesses.ideal/board-column/harnesses-populated/harness-list/section-detected text="Detected on this Mac" role=section-header kind=text |  |
| ideal | v-ideal/card/claude-code-claude-json-v2-1-204-routed-over-htt | card | unclassified | mock="Claude Code ~/.claude.json · v2.1.204 Routed over HTTP 1 ent" kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/claude-code | heading | unclassified | mock="Claude Code" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-name text="Claude Code" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/button/open-config | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/button/verify-now | button | absent | mock="Verify now" | build=no paired node | D3 · planning/fidelity/harnesses.layers.json §3 — the board reads every harness in one call, so the control it earns is the board-level Check again, which is built |
| ideal | v-ideal/card/cursor-cursor-mcp-json-v1-8-2-routed-over-http-1 | card | unclassified | mock="Cursor ~/.cursor/mcp.json · v1.8.2 Routed over HTTP 1 entry " kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/cursor | heading | unclassified | mock="Cursor" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-name text="Cursor" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled#2 | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/button/open-config#2 | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/button/verify-now#2 | button | absent | mock="Verify now" | build=no paired node | D3 · planning/fidelity/harnesses.layers.json §3 — the board reads every harness in one call, so the control it earns is the board-level Check again, which is built |
| ideal | v-ideal/card/grok-cli-grok-config-toml-v4-6-1-routed-through- | card | unclassified | mock="Grok CLI ~/.grok/config.toml · v4.6.1 Routed through a stdio" kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/grok-cli | heading | unclassified | mock="Grok CLI" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-name text="grok" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled#3 | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/sentence/grok-has-no-http-mcp-transport-so-it-reaches-the | sentence | unclassified | mock="Grok has no HTTP MCP transport, so it reaches the router thr" kind=sentence | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-sentence text="It reaches the router through mcp-remote, which is one extra" role=status-sentence kind=text |  |
| ideal | v-ideal/button/open-config#3 | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/button/why-a-shim | button | unclassified | mock="Why a shim?" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/explain-shim text="Why a shim?" role=row-action kind=leaf |  |
| ideal | v-ideal/card/codex-chatgpt-cli-codex-config-toml-v0-61-0-rout | card | unclassified | mock="Codex · ChatGPT CLI ~/.codex/config.toml · v0.61.0 Routed, p" kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/codex-chatgpt-cli | heading | unclassified | mock="Codex · ChatGPT CLI" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/harness-name text="Codex CLI" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled#4 | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/sentence/four-of-those-seven-duplicate-upstreams-this-rou | sentence | unclassified | mock="Four of those seven duplicate upstreams this router already " kind=sentence | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-sentence text="4 of its 7 own servers duplicate upstreams this router alrea" role=status-sentence kind=text |  |
| ideal | v-ideal/button/reconcile | button | unclassified | mock="Reconcile…" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-actions/reconcile text="Reconcile…" role=row-action kind=leaf | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| ideal | v-ideal/button/open-config#4 | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/card/antigravity-agy-gemini-settings-json-v3-7-0-18-d | card | unclassified | mock="Antigravity · agy ~/.gemini/settings.json · v3.7.0 18 direct" kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/antigravity-agy | heading | unclassified | mock="Antigravity · agy" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-name text="Gemini CLI" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled#5 | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/sentence/a-session-here-spawns-eighteen-children-at-init- | sentence | unclassified | mock="A session here spawns eighteen children at init to reach a c" kind=sentence | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-sentence text="It runs 18 servers of its own, 10 of which this router alrea" role=status-sentence kind=text |  |
| ideal | v-ideal/button/reconcile#2 | button | unclassified | mock="Reconcile…" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/reconcile text="Reconcile…" role=row-action kind=leaf | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| ideal | v-ideal/button/open-config#5 | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/card/opencode-opencode-mcp-json-v0-4-9-not-wired-inst | card | unclassified | mock="OpenCode ~/.opencode/mcp.json · v0.4.9 Not wired Installed, " kind=card | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode text="" role=harness-card kind=vstack | D1 · planning/plans/plan-M22.md §2.2 — nothing in this product observes a harness version; HTTPCapability names the binary a probe was taken against on 2026-08-21, not the one on this machine — the mock's card carries a version line the build's does not |
| ideal | v-ideal/heading/opencode | heading | unclassified | mock="OpenCode" kind=heading | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-name text="opencode" role=harness-name kind=text |  |
| ideal | v-ideal/indicator/unlabelled#6 | indicator | absent | mock="" | build=no paired node | D11 · scripts/acceptance/mock_fidelity.py:44 and ai/m16's own note on the same dict — the harness records no text for a dot, so both sides fail readable() and a pairing lands `unclassified`, which is that file's phrase for agreement between two absences. M16 declines to vouch `indicator` for the same reason and leaves the mock's indicator affordances `absent` with a citation naming what the instrument cannot do; this follows that precedent rather than inventing a second answer. The build DRAWS the dot — status-dot under each card's status-pill — and §6 pairs it with a word, which status-label carries and which IS compared |
| ideal | v-ideal/sentence/installed-speaks-streamable-http-and-has-no-mcp- | sentence | unclassified | mock="Installed, speaks streamable HTTP, and has no MCP servers co" kind=sentence | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-sentence text="Nothing in this harness points at the router, and it declare" role=status-sentence kind=text |  |
| ideal | v-ideal/button/wire-to-the-router | button | absent | mock="Wire to the router" | build=no paired node | D4 · planning/specs/spec-R7.md §7 — nothing in this product writes or selects a harness configuration file, and scripts/lint/no-harness-config-writes.sh keeps the seam free of a writer |
| ideal | v-ideal/button/open-config#6 | button | unclassified | mock="Open config" kind=button | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions/open-config text="Reveal config in Finder" role=row-action kind=leaf |  |
| ideal | v-ideal/section-header/not-found | section-header | absent | mock="Not found" | build=no paired node | D5 · planning/fidelity/harnesses.layers.json §5 — the route reports every harness in MCPClient.allCases with its own exists and unreadable, so a harness that is not installed is a row rather than a paragraph |
| ideal | v-ideal/card/claude-desktop-is-installed-but-cannot-be-wired- | card | absent | mock="Claude Desktop is installed but cannot be wired: its per-ser" | build=no paired node | D5 · planning/fidelity/harnesses.layers.json §5 — the route reports every harness in MCPClient.allCases with its own exists and unreadable, so a harness that is not installed is a row rather than a paragraph |
| ideal | v-ideal/sentence/claude-desktop-is-installed-but-cannot-be-wired- | sentence | absent | mock="Claude Desktop is installed but cannot be wired: its per-ser" | build=no paired node | D5 · planning/fidelity/harnesses.layers.json §5 — the route reports every harness in MCPClient.allCases with its own exists and unreadable, so a harness that is not installed is a row rather than a paragraph |
| ideal | harnesses.ideal/board-column | board-column | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column (2 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/board-header | board-header | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column/board-header (2 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/board-header/board-head | board-head | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column/board-header/board-head (2 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/board-header/board-actions | board-actions | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column/board-header/board-actions (2 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/harnesses-populated | board-body | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column/harnesses-populated (3 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/finding-band/finding-text | finding | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/finding-band/finding-text text="Gemini CLI runs 18 servers of its own, 10 of which this rout" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list | harness-list | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.ideal/board-column/harnesses-populated/harness-list (7 children, at least one paired) |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-path text="~/.claude.json" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-label text="Routed over HTTP" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-sentence | status-sentence | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-sentence text="Sessions reach every server this router fronts through one e" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-path text="~/.cursor/mcp.json" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-label text="Routed over HTTP" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-sentence | status-sentence | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-sentence text="Sessions reach every server this router fronts through one e" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-path text="~/.grok/config.toml" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-label text="Routed through a stdio shim" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/harness-path text="~/.codex/config.toml" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-header/status-pill/status-label text="Routed, plus 7 direct servers" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates | duplicate-list | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-dossier | duplicate-entry | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-dossier text="dossier" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-obscura | duplicate-entry | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-obscura text="obscura" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-Ref | duplicate-entry | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-Ref text="Ref — the router calls it ref-tools-mcp" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-google-search | duplicate-entry | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-duplicates/duplicate-google-search text="google-search" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-codexCLI/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-path text="~/.gemini/config/mcp_config.json" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-label text="Not routed" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header | card-header | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-path | config-path | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-path text="~/.config/opencode/opencode.json" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill | status-pill | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-dot | status-indicator | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-dot |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-label | status-word | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-label text="Not routed" |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions | card-actions | covered-by-pair | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.ideal/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions |  |
| ideal | harnesses.ideal/board-column/harnesses-populated/harnesses-footer | board-footer | extra-cited | mock=nothing | build=harnesses.ideal/board-column/harnesses-populated/harnesses-footer | D8 · planning/features-to-triage/M22-harnesses-and-insights-boards.md — "a stale reading here is worse than no reading"; the route stamps readAt and the board says how old the answer is, which the mock has no counterpart for |
| ideal | harnesses.ideal/board-column/harnesses-populated/harnesses-footer/read-at | freshness | extra-cited | mock=nothing | build=harnesses.ideal/board-column/harnesses-populated/harnesses-footer/read-at | D8 · planning/features-to-triage/M22-harnesses-and-insights-boards.md — "a stale reading here is worse than no reading"; the route stamps readAt and the board says how old the answer is, which the mock has no counterpart for |
| ideal | harnesses.ideal/board-column/harnesses-populated/harnesses-footer/scope-note | scope | extra-cited | mock=nothing | build=harnesses.ideal/board-column/harnesses-populated/harnesses-footer/scope-note | D9 · planning/specs/spec-R7.md §3 R7-C4 — project-scoped entries are not read, and the board says so once rather than leaving a reader to assume their own project was consulted |
| empty | v-empty/icon/unlabelled | icon | unclassified | mock="" kind=icon | build=harnesses.empty/board-column/harnesses-empty/message-state/message-icon text="" role=state-illustration kind=leaf | D11 · scripts/acceptance/mock_fidelity.py:791 — a DECLARED residue, not a defect: "the harness records no glyph identity for an icon node, so an icon pair has nothing to compare and lands as unclassified rather than as agreement. That is the honest outcome, not the audited one." The pairing is real and is left standing; what cannot run is the comparison. Same shape as the font-weight-face row, and the remedy is an instrument that can read a glyph rather than an edit to this file |
| empty | v-empty/heading/no-ai-harnesses-found | heading | present | mock="No AI harnesses found" kind=heading | build=harnesses.empty/board-column/harnesses-empty/message-state/message-copy/message-title text="No AI harnesses found" role=state-title kind=text |  |
| empty | v-empty/sentence/nothing-on-this-mac-looks-like-an-agent-cli-or-e | sentence | divergent | mock="Nothing on this Mac looks like an agent CLI or editor that s" kind=sentence | build=harnesses.empty/board-column/harnesses-empty/message-state/message-copy/message-detail text="Nothing on this Mac looks like an agent CLI or editor that s" role=state-detail kind=text |  |
| empty | v-empty/button/choose-a-config-file | button | absent | mock="Choose a config file…" | build=no paired node | D4 · planning/specs/spec-R7.md §7 — nothing in this product writes or selects a harness configuration file, and scripts/lint/no-harness-config-writes.sh keeps the seam free of a writer |
| empty | v-empty/button/scan-again | button | unclassified | mock="Scan again" kind=button | build=harnesses.empty/board-column/board-header/board-actions/rescan text="Rescan" role=board-action kind=leaf |  |
| empty | harnesses.empty/board-column | board-column | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column (2 children, at least one paired) |  |
| empty | harnesses.empty/board-column/board-header | board-header | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column/board-header (2 children, at least one paired) |  |
| empty | harnesses.empty/board-column/board-header/board-head | board-head | extra-cited | mock=nothing | build=harnesses.empty/board-column/board-header/board-head | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| empty | harnesses.empty/board-column/board-header/board-head/board-title | board-title | extra-cited | mock=nothing | build=harnesses.empty/board-column/board-header/board-head/board-title | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| empty | harnesses.empty/board-column/board-header/board-head/board-subtitle | board-subtitle | extra-cited | mock=nothing | build=harnesses.empty/board-column/board-header/board-head/board-subtitle | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| empty | harnesses.empty/board-column/board-header/board-actions | board-actions | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column/board-header/board-actions (2 children, at least one paired) |  |
| empty | harnesses.empty/board-column/board-header/board-actions/reconcile-all | board-action | extra-cited | mock=nothing | build=harnesses.empty/board-column/board-header/board-actions/reconcile-all | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| empty | harnesses.empty/board-column/harnesses-empty | empty | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column/harnesses-empty (1 children, at least one paired) |  |
| empty | harnesses.empty/board-column/harnesses-empty/message-state | state-container | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column/harnesses-empty/message-state (2 children, at least one paired) |  |
| empty | harnesses.empty/board-column/harnesses-empty/message-state/message-copy | state-copy | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.empty/board-column/harnesses-empty/message-state/message-copy (2 children, at least one paired) |  |
| loading | v-loading/card/unlabelled | card | absent | mock="" | build=no paired node | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, because neither side carries a string and the comparison would read `present` off agreement between two absences. The build draws a skeleton card at the harness card's own geometry so nothing moves when the read lands (DESIGN.md §5); what cannot be established is that the two are the same control |
| loading | v-loading/card/unlabelled#2 | card | absent | mock="" | build=no paired node | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, because neither side carries a string and the comparison would read `present` off agreement between two absences. The build draws a skeleton card at the harness card's own geometry so nothing moves when the read lands (DESIGN.md §5); what cannot be established is that the two are the same control |
| loading | harnesses.loading/board-column | board-column | extra-cited | mock=nothing | build=harnesses.loading/board-column | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, so the loading frame's two cards are unpaired and every build node under them is a node the mock's census cannot reach through a pairing |
| loading | harnesses.loading/board-column/board-header | board-header | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-head | board-head | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-head | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-head/board-title | board-title | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-head/board-title | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-head/board-subtitle | board-subtitle | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-head/board-subtitle | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-actions | board-actions | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-actions | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-actions/rescan | board-action | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-actions/rescan | D7 · design/mcp-router-console.html:2362-2372 — the mock draws the board head OUTSIDE every .v-* frame, so a per-state census cannot contain it; the build's header is one view that persists across states, which is what stops the title moving when data lands |
| loading | harnesses.loading/board-column/board-header/board-actions/reconcile-all | board-action | extra-cited | mock=nothing | build=harnesses.loading/board-column/board-header/board-actions/reconcile-all | D2 · planning/specs/spec-M22.md header — M18 owns the reconcile panel; the control is drawn and disabled with its reason (DESIGN.md §3.4) |
| loading | harnesses.loading/board-column/harnesses-loading | loading | extra-cited | mock=nothing | build=harnesses.loading/board-column/harnesses-loading | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, so the loading frame's two cards are unpaired and every build node under them is a node the mock's census cannot reach through a pairing |
| loading | harnesses.loading/board-column/harnesses-loading/harness-skeleton | skeleton | extra-cited | mock=nothing | build=harnesses.loading/board-column/harnesses-loading/harness-skeleton | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, because neither side carries a string and the comparison would read `present` off agreement between two absences. The build draws a skeleton card at the harness card's own geometry so nothing moves when the read lands (DESIGN.md §5); what cannot be established is that the two are the same control |
| loading | harnesses.loading/board-column/harnesses-loading/harness-skeleton/skeleton-card-0 | skeleton-card | extra-cited | mock=nothing | build=harnesses.loading/board-column/harnesses-loading/harness-skeleton/skeleton-card-0 | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, because neither side carries a string and the comparison would read `present` off agreement between two absences. The build draws a skeleton card at the harness card's own geometry so nothing moves when the read lands (DESIGN.md §5); what cannot be established is that the two are the same control |
| loading | harnesses.loading/board-column/harnesses-loading/harness-skeleton/skeleton-card-1 | skeleton-card | extra-cited | mock=nothing | build=harnesses.loading/board-column/harnesses-loading/harness-skeleton/skeleton-card-1 | D10 · scripts/acceptance/mock_fidelity.py:55-58 — VOUCHED_CONTROLS refuses a mock `card` answered by a skeleton in its own words, because neither side carries a string and the comparison would read `present` off agreement between two absences. The build draws a skeleton card at the harness card's own geometry so nothing moves when the read lands (DESIGN.md §5); what cannot be established is that the two are the same control |
| error | v-error/banner/codex-s-configuration-would-not-parse-codex-conf | banner | unclassified | mock="Codex’s configuration would not parse ~/.codex/config.toml:1" kind=banner | build=harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI text="" role=harness-card kind=vstack | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's error frame is a partial read, and MeasureDump maps (.harnesses, .error) to the partial fixture |
| error | v-error/icon/unlabelled | icon | absent | mock="" | build=no paired node | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's error frame is a partial read, and MeasureDump maps (.harnesses, .error) to the partial fixture; the build names the failure in words on the card rather than with a mark beside it |
| error | v-error/button/open-in-editor | button | unclassified | mock="Open in editor" kind=button | build=harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI/unreadable-open-codexCLI text="Reveal config in Finder" role=row-action kind=leaf | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's error frame is a partial read, and MeasureDump maps (.harnesses, .error) to the partial fixture |
| error | v-error/callout/nothing-was-written-a-file-this-app-cannot-read- | callout | unclassified | mock="Nothing was written. A file this app cannot read is a file i" kind=callout | build=harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-note text="Nothing was written. A file this app cannot read is a file i" role=callout kind=text |  |
| error | harnesses.error/board-column | board-column | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.error/board-column (2 children, at least one paired) |  |
| error | harnesses.error/board-column/board-header | board-header | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-head | board-head | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-head | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-head/board-title | board-title | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-head/board-title | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-head/board-subtitle | board-subtitle | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-head/board-subtitle | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-actions | board-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-actions/rescan | board-action | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-actions/rescan | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/board-header/board-actions/reconcile-all | board-action | extra-cited | mock=nothing | build=harnesses.error/board-column/board-header/board-actions/reconcile-all | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated | board-body | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.error/board-column/harnesses-populated (4 children, at least one paired) |  |
| error | harnesses.error/board-column/harnesses-populated/finding-band | finding-band | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/finding-band | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/finding-band/finding-icon | finding-icon | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/finding-band/finding-icon | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/finding-band/finding-text | finding | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/finding-band/finding-text | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/finding-band/finding-action | finding-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/finding-band/finding-action | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list | harness-list | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/section-detected | section-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/section-detected | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode | harness-card | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header | card-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-name | harness-name | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-name | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-path | config-path | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/harness-path | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill | status-pill | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-dot | status-indicator | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-dot | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-label | status-word | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-header/status-pill/status-label | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-sentence | status-sentence | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-sentence | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions | card-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions/open-config | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-claudeCode/harness-actions/open-config | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor | harness-card | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header | card-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-name | harness-name | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-name | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-path | config-path | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/harness-path | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill | status-pill | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-dot | status-indicator | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-dot | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-label | status-word | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-header/status-pill/status-label | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-sentence | status-sentence | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-sentence | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions | card-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions/open-config | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-cursor/harness-actions/open-config | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI | harness-card | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header | card-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-name | harness-name | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-name | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-path | config-path | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/harness-path | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill | status-pill | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-dot | status-indicator | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-dot | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-label | status-word | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-header/status-pill/status-label | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-sentence | status-sentence | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-sentence | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions | card-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/open-config | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/open-config | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/explain-shim | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-grokCLI/harness-actions/explain-shim | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI | harness-card | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header | card-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-name | harness-name | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-name | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-path | config-path | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/harness-path | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill | status-pill | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-dot | status-indicator | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-dot | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-label | status-word | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-header/status-pill/status-label | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-sentence | status-sentence | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-sentence | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions | card-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/open-config | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/open-config | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/reconcile | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-geminiCLI/harness-actions/reconcile | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode | harness-card | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header | card-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-name | harness-name | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-name | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-path | config-path | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/harness-path | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill | status-pill | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-dot | status-indicator | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-dot | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-label | status-word | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-header/status-pill/status-label | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-sentence | status-sentence | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-sentence | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions | card-actions | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions/open-config | row-action | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harness-list/harness-card-opencode/harness-actions/open-config | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/unreadable-section | unreadable-list | structure-unpaired | mock=no affordance of a declared kind | build=harnesses.error/board-column/harnesses-populated/unreadable-section (3 children, at least one paired) |  |
| error | harnesses.error/board-column/harnesses-populated/unreadable-section/section-unreadable | section-header | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/unreadable-section/section-unreadable | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI/unreadable-name-codexCLI | harness-name | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI/unreadable-name-codexCLI text="Codex CLI" |  |
| error | harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI/unreadable-reason-codexCLI | read-failure | covered-unoracled | mock=inside a paired affordance, at a granularity the mock's census does not reach | build=harnesses.error/board-column/harnesses-populated/unreadable-section/unreadable-card-codexCLI/unreadable-reason-codexCLI text="~/.codex/config.toml:14812 — unexpected key after table head" |  |
| error | harnesses.error/board-column/harnesses-populated/harnesses-footer | board-footer | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harnesses-footer | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harnesses-footer/read-at | freshness | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harnesses-footer/read-at | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |
| error | harnesses.error/board-column/harnesses-populated/harnesses-footer/scope-note | scope | extra-cited | mock=nothing | build=harnesses.error/board-column/harnesses-populated/harnesses-footer/scope-note | D6 · planning/fidelity/harnesses.layers.json §6 — the mock's v-error frame draws the banner and the callout only, and its own callout says the other five harnesses were read normally and are shown above; the build draws them, so every row of that list is a build node the mock's error census cannot contain |

# M18 — thirteen sheets, one inventory, and the gate each decision gets

Branch `ai/m18`, worktree `.worktrees/M18`, cut from `main` at `87e16dc`. Not rebased and not
merged: the work order stops this runner before verify and before merge, and the orchestrator
serialises both.

Plan: `planning/plans/plan-M18.md` (committed `706cefb`, tier Large). Spec at
`planning/specs/spec-M18.md`, flipped to `Ready for AI` at `c4bd64d`.

## What was built

| Commit | What |
|---|---|
| `706cefb` | The plan, and the out-of-family review that changed its design |
| `c4bd64d` | Spec → Ready for AI, with the plan's sha |
| `a9dcb71` | The inventory, the gate table, the seven call sites, the two new sheets |
| `4c320a8` | Destructive is a role; Cleanup gets its one filled primary |
| `fe63d5e` | The gate table asserted where it can actually fail |

**One enum, nested per host.** `RouterSheet` in `MCPRouterKit/Shell/` replaces five per-board
`Sheet` enums and two `Bool`-shaped presentations. Each board types its `sheet` as its own nested
group, so `inboxBoard.sheet = .addServer` does not compile rather than rendering something at
runtime. `RouterSheet.Kind` is the flat inventory — sixteen: the mock's thirteen plus the three the
build draws and the mock does not.

**The gate table is routed through, not merely declared.** `SheetGate` carries the brief's seven
rows plus `resetCallHistory`, and each board model gained `request(_:)`. The destructive call sites
ask for the *action*; the table decides the friction. That was the plan review's sharpest finding —
as first planned the table was a lookup nobody consulted, and its tests would have passed at full
green while a button called `remove(name)` directly.

**All seven `.sheet(` sites use `item:`.** The three that were `isPresented:` kept their by-id
lookup: `InboxBoardModel.sheet` and `DiscoverBoardModel.sheet` are **computed** from the existing
`sheetItemID` / `sheetEntryID` storage rather than stored beside it, so there is still exactly one
place each answer lives. A captured value would have satisfied the brief's letter and reintroduced
the stale-subject bug both models carry a comment about closing.

**`EvalsBoardModel.Sheet` deleted.** `recheckAll` was never assigned and never presented, and
`EvalsBoard` has no `.sheet(` at all, so `escape()`'s first arm was unreachable too. Both went.

**Two sheets the build did not have**, and both are honest about what they cannot say — see
*What could not be drawn*.

**Destructive is a role.** `StandardButtonStyle` reads `configuration.role`; four call sites
stopped naming a colour. The token moved with it, and that part is a measured fix rather than a
tidy-up: `ColorToken.fail.contrastRole` is `pairedWithAWord` while `failInk` is `text`, and these
labels sit on `--raised`, one of `DESIGN.md` §2's four text grounds. Every one of those four was
shipping a label in the indicator hue on a text ground.

## What could not be drawn, and why

Three things the mock draws are not in the build, each measured rather than assumed.

1. **`path` ships as a refusal.** The router computes the search path it hands its children
   (`src/pool.ts:168`, `augmentPath`) and publishes it nowhere: `ControlAPIClient` declares
   nineteen methods and none returns a path or an environment. Reading *this app's* login-shell
   `PATH` instead would be a different list under the same heading — the app is a GUI process, the
   router is a launchd agent, and R6 exists because those differ. So the sheet is built, attached
   to the Settings window, and says what it will show and why it cannot yet. The first draft of the
   plan parked it outright; the out-of-family review called that an excuse against this repo's own
   two precedents (`SURF-010`'s pair sheet, `HeldVersionSheet`'s dimmed offers) and was right.
2. **The 30-day undo is not a thing this router does.** `ConfigWriter.backUp` keeps
   `backupsKept = 5` — five writes, not thirty days — and no control-API method reports a backup
   count, an age or a policy, so even "the last five are kept" would be quoting a constant the app
   cannot read. **Nothing was added**: the build never carried the claim, and this item did not
   introduce it. The gate table's "30-day undo stated on the surface" is therefore unmet by
   decision rather than by omission.
3. **`official` is drawn without the publisher grid.** `RegistryEntry` has no publisher field;
   `source` is which index supplied the row, not who published it. The fixture's names are
   `github`, `deepwiki` and `ai.smithery/Hint-Services-obsidian-github-mcp`, so a namespace parse
   would invent the ownership claim this sheet exists to disclaim. Grouping by `source` instead was
   rejected on this module's own note: a figure must come "never off `sources.official`, which is a
   pre-merge, pre-slice count of a different set." The omission is stated **on the surface**
   (`OfficialMarkCopy.publishersUnavailable`), not hidden.

## The gate table, row by row

| Row | Gate | Built? | Proved by |
|---|---|---|---|
| Remove duplicates from a harness config | `sheet(.reconcile)` | no — **M22** | `unhostedRowsNameTheirOwner` |
| Remove selected capabilities | `sheet(.confirmRemove)` | yes, on two hosts | `removeFrom{Servers,Cleanup}OpensConfirmRemove` |
| Accept held schema changes | `sheet(.quarantine)` | yes | `acceptHeldChangesOpensQuarantine` |
| Disable a server | `quietDestructiveControl` | **no, and unclaimed** | `unbuiltRowsAreVisible` |
| Trip breaker / wake now | `ungated` | yes | `tripBreakerOpensNothing` |
| Approve a phone-queued install | `sheet(.queuedDetail)` | yes | `approveQueuedInstallOpensQueuedDetail` |
| Stop Router | `menuItem(accelerator: nil)` | no — **M20** | `stopRouterHasNoAccelerator` |
| *Reset the call history* | `sheet(.resetHistory)` | yes, on two hosts | `resetHistoryFrom{Cleanup,Activity}` |

Two findings in that table are worth reading rather than counting.

**"Disable a server" has no owner at all.** There is no disable operation on the control API —
`ServerPatch` carries `projects`, `warm`, `idleMs` and `placard` and nothing that stops a server
answering — the mock draws the button in the quarantine sheet, and no ledger item claims it. It is
recorded as `unclaimed` with that reason so the absence is visible rather than reading as built.
This is new; triage did not have it.

**The eighth row is the build's own.** Forgetting the call record is destructive, irreversible and
drawn twice, and the brief's table does not cover it. Added rather than omitted — a gate table with
a hole in exactly the place this item exists to close would be worse than no table.

## Gates

**`make lint` did not run, and the reason is the machine rather than the change.** It requires
`make tools`, which requires `node_modules` and `dist/index.js`; this is a fresh worktree and has
neither. Two `npm install` runs were queued through `harbourmaster` — twenty attempts at weight 4
and a further run at weight 1 — and neither was ever granted a berth.

**`swift build` did not complete**, and half the reason was this runner's own doing. Two builds
were queued — a Kit-only one and a whole-package one — and both were granted berths. They then
deadlocked on the same `app/.build` lock: after nine and a half minutes the first had resolved its
dependencies into `.build/checkouts` and was sitting at **2.36 seconds of CPU with zero
`swift-frontend` processes alive**, which is SwiftPM waiting on a lock rather than compiling.

The first reading of that was "other fleets are holding the lock", and `harbourmaster`'s berth
listing says otherwise: all six occupied slots carried this project's label. Killing the redundant
Kit-only build released the lock and the whole-package build began compiling immediately. Recorded
because the wrong diagnosis was the more comfortable one, and a runner that queues two builds of one
package will do it again.

What is true about the machine, from `harbourmaster` and `uptime`: load average between 75 and 288
across 16 cores throughout, 19 logged-in users, pressure `critical`, and the ceiling reduced to 3
berths. Other fleets were running — two `make lint` recipes from other projects were live — but they
were not what blocked this. Exit 75 is scheduling information rather than a failing gate.

**Final state of the attempt.** After the deadlock was cleared the build was requeued three times
— weight 3, then weight 2 — and never got its slots: `swiftlint` held one berth for the rest of the
session and the ceiling stayed at 3 under `critical` pressure, with 15 `swift-frontend` processes
live that belonged to other projects. One earlier attempt also died to a mistake in the wrapper
rather than the machine: `swift build … | grep … | head -60` closes the pipe after sixty lines and
SIGPIPEs the build, which is why two runs produced a zero-byte log and no exit code. The command
was rewritten to redirect to a file.

A `swift build` remains queued through the governor writing to `/tmp/m18-build.log`; whoever picks
this up next should read that file before re-running anything.

**So this branch is committed and unbuilt.** That is stated plainly because a runner reporting
"ready to verify" over an uncompiled tree is the failure this repo's null-run gate exists to catch.
What is owed, in order: `swift build`, `make test`, `make lint`, then the four fidelity surfaces.

## Deviations from the plan, each with its reason

- **Phase E — the fidelity surfaces — was not started.** The plan scoped six
  (`quarantine`, `confirm-remove` on both hosts, `queued-detail`, `official`, `path`). None was
  written. A `<surface>.layers.json` is only worth committing once its gate has been run against it,
  and `mock-fidelity-gate.sh` needs the `MEASURE` build — a *second* compilation of the UI target,
  when the first never completed. A manifest committed without a run is the stale-ledger failure
  that script opens by warning about. Owed.
- **`SheetFrame` was not unified**, as planned (§6 of the plan). Six sheet frames remain.
- **Multi-select removal on Cleanup was not built**, as planned (§4.2). It is a Cleanup write-path
  feature rather than a sheet.
- **Error text still reads `ColorToken.fail`.** Four *buttons* were moved to the role; the error
  strings on five surfaces were left. That is `DESIGN.md` §10's open per-surface item, not this
  brief's clause about button roles, and moving them would widen the diff past what the item asks.
- **`SheetGate` lives in its own file** rather than inside `RouterSheet.swift` as the plan says.
  One 450-line file for three types read worse than two.

## Implementation assumptions

- **Host ownership is a test-time fact, not a compile-time one** — assuming the nested-group shape
  (rather than one exhaustive host over a flat enum, which would need `ShellModel` threaded into
  the four boards deliberately built without it, or a `default:` arm in each of six hosts, which
  the review objected to correctly).
- **`Kind` marks the three build-only sheets with a leading `-`** so the mock comparison can
  exclude them by prefix — assuming that over a second enum, which would be two inventories.
- **The `official` sheet's entry point is the mock's quiet `What is official?` control alone**
  (`design/mcp-router-console.html:2851`) — assuming that rather than also building the Publisher
  segmented filter beside it, which the build does not have and which is Discover's scope.
- **`request(_:)` returns the sheet it opened and leaves `sheet` untouched for a non-sheet gate** —
  assuming an ungated action should not shut a sheet the user opened (asserted by
  `ungatedRequestDoesNotCloseAnOpenSheet`).

## Out-of-family review

Three lanes attempted in the ordered sequence. `codex`/`gpt-5.6-sol` is recorded down until
2026-08-27 and was not retried. `grok-4.6` returned `402 Payment Required — Grok Build usage balance
exhausted` and a zero-byte report. **`agy`/`gemini-3.7-flash-high` answered**, so the gate ran out
of family and is not a downgrade. Verdict AMEND, seven findings; the full disposition — six
accepted, three rejected with reasons, one confirmed — is `planning/plans/plan-M18.md` §14.

## What is open

1. **The tree is unbuilt.** Everything below waits on that.
2. `make test` — the Swift suite's count must not fall, and none of the new tests has been observed
   passing *or* failing. The three drift guards (`closingPairingStopsTheTicker`,
   `ungatedRequestDoesNotCloseAnOpenSheet`, `inventoryMatchesTheMock`) have not been broken-and-
   restored, so by `SWIFT_PRACTICES.md` §7 they are not yet known to work.
3. `make lint` — blocked on `npm install`, above.
4. **Four of the thirteen sheets have no host** and are owed to M19 (`readme`) and M22
   (`reconcile`, `recommendation`, `analyzer`). Recorded in `Kind.owner`, not in prose.
5. **Phase E, the fidelity surfaces** — six, none written.
6. **Rendered verification** — two newly drawn sheets and four changed buttons want a `proctor`
   pass; `planning/evidence/M18-acceptance.md` records it as owed with the reason.
7. **"Disable a server" is an unclaimed gate-table row.** Needs an owner or a decision that the
   action is not wanted.

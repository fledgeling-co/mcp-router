# LEDGER — MCP Router feature pipeline

Allocation is a read-modify-write on this file: **one triage at a time**, and any runner
creating a child spec takes the ledger lock first.

**This file is not the only allocator, and a scan of its table is not a free-id check.**
It said "ids are allocated here and nowhere else" until 2026-08-21, and that sentence
produced two near-misses from two sessions inside one hour — one session reached for I6
while it was merged at `ef4f615`, another for X7/X8 against a table that records neither
X4 nor X5. `ORCHESTRATOR.md` carries live rows this table has never held (P5, P6, R2-R,
R2-W, R4-C, and the M9-M12 deferred children), and merged branches exist for ids recorded
in neither file (`ai/x4`, `ai/x5`). Before allocating, scan **both files plus
`git branch --merged main --list 'ai/*'`**, and expand any range notation first — see the
allocation notes below for why a range is the one place a reconciliation reports agreement
it never tested.

| ID | Title | Brief | Spec | Plan | Status |
|---|---|---|---|---|---|
| F1 | Swift workspace, shared kit, three targets | `F1-swift-workspace.md` | `spec-F1.md` | `plan-F1.md` | **Merged** `0924040` |
| F2 | The design system in SwiftUI | `F2-design-system.md` | `spec-F2.md` | `plan-F2.md` | **Merged** `22d1802` |
| F3 | Typed control-API client and models | `F3-control-client.md` | `spec-F3.md` | `plan-F3.md` | **Merged** `13825c9` |
| F4 | ServerStateTracker cannot report failure | `F4-tracker-failure-states.md` | — | — | **Merged** `aba30bd` |
| R1 | Swift router: core, config, manifest | `R1-router-core.md` | `spec-R1.md` | `plan-R1.md` | **Merged** `c30eac9` |
| R2 | Swift router: lazy pool, relay, passthrough | `R2-router-pool-relay.md` | — | — | **Merged** `a8091bb` |
| R3 | Swift router: control, usage, registry | `R3-router-control-registry.md` | — | — | **Merged** `e154bae` |
| R5 | Swift router: OAuth and the auth routes | `R5-router-auth.md` | — | — | **Merged** `b7c527c` |
| R4 | Differential parity harness and cutover | `R4-router-parity-cutover.md` | — | — | **Merged (harness only)** `e129779` |
| R4-C1 | The installer points at Swift; the TypeScript tree stays | `R4-C1-installer-points-at-swift.md` | — | — | **Done** (ai/r4c) |
| R4-C2 | Retire `src/*.ts` — held, and what it waits on | `R4-C1-installer-points-at-swift.md` | — | — | Held (owner: not on a green streak) |
| M1 | Mac window shell, menu bar, keyboard | `M1-mac-shell.md` | — | — | **Merged** `10cad44` |
| M2 | Activity: the live call log | `M2-activity.md` | — | — | **Merged** `c39c891` |
| M3 | Servers: the breaker board | `M3-servers-board.md` | — | — | Done (`589ab2e`, `3b11f33`, `af77200` on main) |
| M4 | Skills and marketplaces | `M4-skills.md` | — | — | **Merged** `7a28de8` |
| M5 | Discover: the registry | `M5-discover.md` | — | — | **Merged** `2a81c87` |
| M6 | Inbox and phone pairing (Mac side) | `M6-inbox-pairing.md` | — | — | **Merged** `6b3e940` |
| M7 | Evals and Cleanup | `M7-evals-cleanup.md` | — | — | **Merged** `85d8331` |
| M8 | Settings, menu-bar popover, quarantine | `M8-settings-quarantine.md` | — | — | **Merged** `affaed6` |
| I1 | iPhone: shell and pairing | `I1-ios-shell-pairing.md` | — | — | **Merged** `d582d43` |
| I2 | iPhone: Discover and detail | `I2-ios-discover.md` | — | — | **Merged** `ba139d4` |
| I3 | iPhone: Triage, Queue, Library, Settings | `I3-ios-triage.md` | — | — | **Merged** `b50aa8d` |
| P1 | Make the two auth routes reachable | `P1-auth-routes-reachable.md` | — | — | **Merged** `496f88c` |
| P2 | The `import` verb and the config rewrite | `P2-import-verb.md` | — | — | **Merged** `95d16f9` |
| P3 | Oracles for the usage stream and registry search | `P3-stream-and-registry-oracles.md` | — | — | **Merged** `f466020` |
| P4 | Derive the manifest rows; fix the directory-dependent normaliser | `P4-derive-manifest-rows.md` | — | — | **Merged** `8686fd6` |
| M13 | The scroll-edge separator, A34 | `M13-scroll-edge.md` | — | — | **Merged** `08b9bdf` |
| G1 | Stop the checks blaming the app for being out of date | `G1-gate-hygiene.md` | — | — | **Merged** `8cfb9e3` |
| V1 | Re-run the out-of-family review on the router items (grok) | `V1-outside-review-router.md` | — | — | **Merged** `29af3eb` |
| I4 | Let the phone install directly | `I4-phone-direct-install.md` | — | — | **Retired** — replaced by I5 (`4157bc4`) and I6 (`ef4f615`) |
| D1 | Deferred register: router side (12 children) | `D1-deferred-router.md` | — | — | **Merged** `997f7af` |
| D2 | Deferred register: Mac surfaces and design authority (14) | `D2-deferred-mac.md` | — | — | **Merged** `9e8a754` |
| D3 | Deferred register: phone copy and the harness limit (4) | `D3-deferred-phone-harness.md` | — | — | **Merged** `67ae4f5` |
| — | **BLOCKED: the Apple developer identity** | `BLOCKED-apple-identity.md` | — | — | **Needs input — `as-found`, not confirmed** |
| M14 | A shipped menu tells the user the app is not built | `M14-menu-says-not-built.md` | — | — | **Merged** `7e7ed70` |
| R6 | Children inherit launchd's minimal PATH | `R6-child-process-path.md` | `spec-R6.md` | `plan-R6.md` | **Done** — merged to `main` at `1d958b4` (`ai/r6` `7a4f15a`; rung `effect-witness`; 9 follow-ups `D-r6-d`…`D-r6-k`) — both routers append the user's own `bin` directories to a child's PATH; `r6-child-path.sh` examined=6 failures=0; parity census untouched at 83 rows, cli 17/17, pool 5/5 |
| R8 | An upstream that refuses our credentials must say so | `R8-auth-rejection-visible.md` | — | — | **Done** (ai/r8 → main; owner unfroze `src/`, A38 rewritten to guard the reference's existence; Swift half unblocked by R9; parity 82/83 control 16/16 0 diverged; auth gate examined=8 failures=0) |
| R7 | The router's thesis is unmet for every harness but Claude Code | `R7-harness-reconciliation.md` | `spec-R7.md` | `plan-R7.md` | **Merged** — **Verified — Done** (`effect-witness`, 2026-08-21). B1 closed and cross-checked against `agy mcp list`'s own twenty rows; both real Gemini configs byte-identical on six attributes across the run. All five write-gate walk-throughs now exit 1, re-planted independently. B3 and B4 closed in both directions with a seven-shape sweep finding no remaining silent zero. The stripper was checked by construction and three string-literal shapes still miss — latent, since the whole tree opens exactly one block comment and it is genuine. `make lint`'s three-pass block is a false dependency. Awaiting merge; follow-ups `D-r7-z`…`D-r7-ai` — prior-pass detail: **Ready to verify** — gap-fix 2 complete (`ai/r7` `d285298`). B1 closed against the real machine: the run reads `~/.gemini/config/mcp_config.json` and its 19 entries plus the router match `agy mcp list`'s twenty rows. The panel again caught two defects in the fix — a key rewrite that made stdio entries read as HTTP, and byte-for-byte endpoint comparison turning `/mcp` vs `/mcp/` into B4's silent loss. **The write gate's own comment stripper was blanking every line to EOF** on a Swift string containing a slash-star, so an applier under one reported clean. Selftest 22→27 cases, lane 55→59 checks. Two findings overruled on measurement. grok down as a lane at 16.5 KB, substituted with fable. Follow-up `D-r7-y` |
| R7-C1 | The Harnesses board and the `GET /harnesses` route behind it | — | — | — | Deferred — **absorbed by M22** at its 2026-08-22 triage. The two rows named each other as blockers and neither could be scheduled; A36 forbids the board reading a harness config itself, so the route is not optional and the work is one piece |
| R7-C2 | Apply a reconciliation plan to a harness config, behind a human | — | — | — | Deferred (child of R7; the write R7 refuses) |
| R7-C3 | opencode's transport is unestablished | — | — | — | Deferred (child of R7) |
| R7-C4 | Project-scoped harness entries | — | — | — | Deferred (child of R7) |
| R11 | Skills write endpoint (remove/disable) with preconditions and undo | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R7, renumbered 2026-08-21) |
| R12 | Server soft-delete with a restore endpoint | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R8, renumbered 2026-08-21) |
| R13 | Router-side behavioural eval runner — servers only | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R6, renumbered 2026-08-21) |
| G2 | The ledger table holds two row shapes, and every reader silently drops one | `G2-orchestrator-table-is-heterogeneous.md` | — | — | Ready for AI — deferred from 2026-08-21 while agents were mid-read **Deferral re-affirmed 2026-08-22 ~18:2x, and the condition is now stronger than when it was written.** G2 restructures the tables in `ORCHESTRATOR.md` and `LEDGER.md`; it was deferred on 2026-08-21 *while agents were mid-read*. **Eight runners are reading both files right now** — six build items, two verifiers — so dispatching it would recreate the exact hazard it was parked for, against four times the readers. It is not blocked on capacity, information or a decision: it is blocked on the wave draining, and it goes out as the first item after the last runner reports. Recorded here rather than left as a silent skip, because *Ready for AI* on a row nobody dispatches reads as an oversight three sessions later. |
| G3 | `make test` is not deterministically green | `G3-make-test-is-not-deterministic.md` | — | — | **Merged** `4e18cc0` — **Verified — Done** (`metamorphic`, 2026-08-21), awaiting merge. All three shapes flip, confirmed by building the scanner standalone at both the pre-fix and delivered commits; exactly 5 call sites, all bounded, over 508 files. The directional count is wrong about which scanner it measured — nine shapes at 6/3 against the delivered artifact, not twelve at 8/4 — and the population is a convenience sample the verifier doubled in one session, though the conclusion it supports is unaffected. Nine new shapes, none blocking, because the artifact declared its bound honestly. **The title cannot be met by this item**: the one red in five runs was a port collision, not a sleep, so the residue is three mechanisms and wants its own item — prior-pass detail: **Ready to verify — gap-fix 3 complete** (`ai/g3` `94ddd73`, 4 commits on `e8c20e0`). The property is unchanged and every gate held. This pass fixed the layer the rebuild deliberately did not replace — Swift's statement and trailing-closure grammar — where a verifier measured **two misses and one false fire**, each pinned by a one-token control: a statement label made a control-flow body read as the wrapper's trailing closure; a labelled string-literal final argument produced no call site at all, because a literal blanked to whitespace is the shape of an unapplied method reference; and `Task` inside a string interpolation reddened a correct wrap. **The previous pass's directional claim is corrected where it was made** — "fails toward a red on correct source rather than toward a miss" was true of the three unreachable shapes it named and false as a statement about the layer. Measured: 2 misses to 1 false fire in the blocking shapes, and 6 misses to 3 reds across the nine further shapes the lanes found and this pass did not take (`D-g3-ah`). Stated as a count: of the twelve shapes measured against the delivered scanner, eight fail toward a miss and four toward a red, so the layer fails both ways and the direction is not predictable from it. **The completeness claim is bounded rather than closed** (`D-g3-ae`): Family A is 19 controls on a citable production list, Family B is 12 on brace nesting, and **Family C is 38 with an open population** — the count is now given family by family and the mutation matrix stated separately. 69 controls, each red under at least one of twelve mutations. **`D-g3-q`'s derivation withdrawn**: it does not reproduce, and the verifier got both sites red 4 of 4 under heavier load; deferred on scope alone. **Five duplicated register ids removed** (`D-g3-ad`), and `ledger-reconcile.py` — taken from `main` for **check K** — exits 0 at 172 register rows. Gates: `make test` **0** and **0** (7.126 s, 12.513 s at 1587 tests in 199 suites), `make lint` **0** at 0 violations in 500 files, `make parity` **0** at 358/358, `make acceptance-r6` **0** at `examined=6 failures=0`, the assigned mutation red at **exit 2** naming its own condition at `PoolReapingTests.swift:98:29` in 13.189 s. Two further `make test` runs went red in files this item does not touch — `CallbackLifecycleTests.swift:238` and `ControlStreamTests.swift:72`, `D-g3-c`'s class, recorded under `D-g3-s` as its fourth and fifth instances; **`make test` is still not deterministically green on this machine, which is this item's own title**, and what G3 fixed is the pool suite, the residue being `D-g3-c`'s ~60 unclassified sleeps elsewhere. The scan finds exactly 5 call sites, all bounded, over 499 files. Load average 421 and 0.0% idle throughout, so the timings are not representative. Two out-of-family lanes asked to break it found eight more, two of them regressions this pass had introduced; five taken, the rest registered with their direction. New follow-ups `D-g3-aa`…`D-g3-ah`. Prior: The property is unchanged and established: **a regression in this class produces a named red inside the CI bound, not a timeout** — the assigned mutation reds at exit 2 after 11.280 s naming its own condition. This pass fixed the guard, not the property. The scanner was **rebuilt rather than patched**: all seven defects the panel and the verifier found are instances of two approximations of Swift's grammar — comments recognised by a line's first three characters, and block structure read from indentation. A delexer implementing the real comment and literal grammar and a brace-balance walk replace both, and three named residues close with them. Held by **53 controls** asserting both directions, every one seen to fail: **34 single-mechanism mutations, 34 of 34 red, all 53 controls covered**. **Three out-of-family rounds asked to break it rather than review it and found twelve more defects, all fixed here** (`D-g3-y`) — including a raw literal read as a multi-line opener that was silently blanking a real file, and a CRLF desync that kept the readability guard green. `D-g3-l`/`m`/`n`/`o`/`p`/`r`/`v` closed; `D-g3-q` re-measured wider and deferred with a probe-tested reason (`awaitSessionEnded` takes its early return at 3 of 3 call sites, so the remedy is `D-g3-g`); acceptance criterion 3 deleted with its reason, as the orchestrator's error. New: `D-g3-w`, `D-g3-x`, `D-g3-y`, `D-g3-z`. `make test` **0** twice at `1587 tests in 199 suites` (4.941s, 4.130s); `make lint` **0** at `0 violations, 0 serious in 497 files`; `make parity` **0** at 358/358; `make acceptance-r6` **0** at `examined=6 failures=0`; the assigned mutation reds at **exit 2** in 10.589 s naming its own condition at `PoolReapingTests.swift:98:29`. One unrelated red in six runs — `CallbackLifecycleTests.swift:238`, a third measured `D-g3-c` instance, recorded under `D-g3-s`. Idle 0.0-44.6% with two sibling runners live What is still approximated is named — Swift's statement and trailing-closure grammar, regex literals, `#if` branches read as if all compile, and lexical containment standing in for an execution bound. All mutated files restored from `cp` backups and `git diff app/Sources/` empty across the pass |
| G4 | Assertions that do not read the quantity they are named for | `G4-assertions-that-do-not-read-their-own-quantity.md` | — | — | Merged `de1315 **Gap-fix 2 verified Done 2026-08-22 and merged at `de1315d`.** Oracle rung **differential measurement with an armed control** — the verifier armed its own sweep against a synthetic residue before trusting its clean sheet, and **every figure came from a command it ran** rather than from the runner's account. §6's arithmetic holds (`1 + 22 + 6 + 7 = 36` case lines; `31 behaved + 5 did not = 36`), every non-elided line appears verbatim in its own run, and the two absent are make's echo and make's error because it invoked the script directly. **`D-g4-b`'s thesis proved itself a fourth time while being checked**: the aligned diff of the two pastes differs in exactly one line, `merged ai/*` 29 → 27, and the verifier's own run read **28** — a count that has moved 29, 27, 27, 28 with **no tracker file edited**. `ai/g5` has since added a third commit past the merge point, so *committed twice* is already stale, and chasing it is the row's own point. **The sweep loads `claim-sweep.py` from `main` rather than copying it** — absent from this HEAD, present there, `exec`'d — and its accounting closes: **1118 scanned + 157 skipped = 1275 = `git ls-files`**, so nothing is dropped in silence. Non-blocking imprecision left: §6's diagnostic sentence attributes all five selftest failures to the stale count, and `div-r1-d3` fails a different check — the *expectation* is stale in all five, so the sentence is right about the fixture and loose about the output. |
| G4-B | The two gates G4 shipped, and the doctrine they each broke | `G4-gapfix-3.md` | — | — | Merged `92a348d` — **verified Done 2026-08-22 at `bc41e13`.** `main`'s `make lint` was red from `de1315d` and both causes were G4's own rules broken by G4's own gates. (1) `null-run-gate.py`'s three RAW arms built their scratch tree from a **hardcoded** directory list; M15 widened `GEOMETRY_DIRS` to four by adding `Settings`, so `no-raw-design-values.sh` exited early, the plant was never reached and each arm honestly reported it could not discriminate — G4's own thirteenth instance verbatim. The arms now read the list out of the script. (2) `reader-accounting.py` walked the filesystem, so it named a silent drop inside `planning/test-campaign/bin/witness-fixture/` — **untracked, another session's work-in-progress** — and reddened a shared branch over a file not in the repository. Its population is now `git ls-files`, and the docstring states the cost: a drop written this morning is invisible until it is added. **Five arms held.** The one that settles it is the boundary control: the real fixture (sha256 matched) reads **22 files exit 0** untracked and **23 files exit 1** after `git add` alone, with no byte changed — so the boundary is tracked-vs-untracked and not a `planning/test-campaign/` exclusion. The RAW derivation followed two novel directory names while `main`'s pre-fix gate **HELDs on all three poison arms** against the same widened script, which is what makes it a derivation rather than a coincidental fit. The registry row was **forced out, not removed to pass**: `analyse_file` finds `::main`'s silent drop at `:519` before and no reader after, unresolved 9 on both sides, and restoring the row exits 1 as a stale declaration. **A defect the write-up understated**: `NULL-accounting-empty` was passing on the git-failure path, since exit 2 plus `usage error` is satisfied by any non-repository; `repo=True` moves it onto the null condition it is named for. **Merged-tree gates re-run by the orchestrator**: `make lint` exit 0 with all three previously-HELD arms biting, 28 armed / 28 changed verdict / 0 held; reader-accounting 0; reconciler 0 across A–L. **Two residuals, neither blocking**: `null-run-gate.py:395` names *the Swift suite (1684 tests)* where `main` is **1725** — a stale literal in the instrument built to catch stale literals, filed as G4's **twenty-second** instance; and `script_dirs` misses a `"${ROOT}/…"` brace spelling, measured as failing loud rather than quiet. |
| G5 | Vendor the `test-campaign` version the gates actually run | `G5-vendor-the-campaign-version-the-gates-run.md` | — | — | Needs More Work (2nd) — gap-fix **Verified 2026-08-22 (2nd); verdict Needs More Work — gap-fix 2 queued (`G5-gapfix-2.md`), documents only, no gate re-run needed.** **Oracle rung metamorphic**: the verifier re-executed **seven versions of the instrument against two registry snapshots**, extracting scripts with `git archive` at each version-bump commit, and moved single properties to see the denominators and controls respond. **The block is closed and independently reproduced**, the controls armed both ways (`grep -Fc` 0 in the file each guards, normalised match 1, and mutating two phrases made them print `FAIL`), and **zero files changed under `planning/test-campaign/`**. **BL-1: `planning/progress/G5-gapfix.md` is deleted from the working tree** — the commit holds it, the rows point at it, and it is not on disk; the swiftformat move-aside that isolated the 284→285 skip count never moved back, so every gate afterwards ran on a tree missing the item's own record. Filed as G4's **twentieth** instance: a measurement technique with a side effect on its subject that nothing checked was undone. **BL-2**: the backtick paragraph's `main` clause is wrong — the parity argument stands on `64e1631` alone and `main` reads **1128**. **BL-3**: the hazard row's *the cache's 0.9.1* reads as present tense about a cache that today holds **0.9.6** and reads 62. Registered not fixed: `D-g5-a` (the sweep's ABSENT half was never repository surface — the withdrawn claims were checked over four hand-listed files, and the verifier's own corpus check over **1366 tracked files** found nothing leaked, *which was luck confirmed afterwards rather than coverage*), `D-g5-b` (the fixed point is **per-file, not general** — W1 is quoted unwrapped at `G5-gapfix.md:84` and a corpus-wide sweep would collapse on it), `D-g5-c` (`strict-ratchet.json` still 58, should be 62). **Gap-fix 2 verified Needs More Work 2026-08-22 at `1335ec2`.** Five arms confirmed and the item's central claim is sound: the vendor checksum `b9acf616…` reproduces **three ways** — worktree, plugin cache, and `git archive` at `28ecd67` — 85 files each, `diff -r` zero lines, so the owner's upstream-and-re-pull decision for X7/X8 rests on a real property. **What blocks it is BL-3 replacing a stale present-tense claim with a fresh one**: *the cache holds 0.9.6 today* was written at 16:18 and was false by 17:38 when the cache took `0.9.8` (re-confirmed by the orchestrator — nine versions cached, newest 0.9.8). The defect is not the number. **One section earlier the same document derives the rule that a moving reference is withdrawn rather than re-numbered, and applies it to `main`** — then BL-3 re-numbers one. Re-numbering again to 0.9.8 repeats it on a one-day clock. Asserted at `ORCHESTRATOR.md:571`, `:752` and `G5-gapfix-2.md:80`. Three smaller findings ride with it, none moving a measurement: `G5.md:19-20` records *no differences* beside a `diff -r` whose two `Only in` lines predate the vendoring; `D-g5-d` says two documents carry the stale `0.9.4` label where **four** do, one the G5 row above it in the same file; and `vendor/README.md:35` documents a `tar -x -C` that fails without a `mkdir -p`, so the reproduction instruction does not reproduce. **Gap-fix 3 dispatched.** **Gap-fix 3 delivered 2026-08-22 at `43b44a2`, one commit, documents only — in verification.** The fix is the rule the document already owned: the moving reference is **withdrawn, not re-numbered**. `0.9.8` is what the number would have become — the cache has read it since 17:38 — and writing it down would have restarted the same one-day fuse. What replaces it is the reading per *named* version, which cannot move, plus the command that reads the cache for anyone who needs today's state. **Four sites carried the claim, not the brief's three**: `ORCHESTRATOR.md:751` (the `D-g5-c` register row) and `G5-gapfix-2.md:112` (the `D-g5-d` bullet) also asserted it. Every version re-measured against a `/tmp` copy: **0.5.0 and 0.9.1 read 58 of 76 with no `effect-witness` rung; 0.9.2 through 0.9.8 and the vendored copy all read 62.** The three smaller findings were confirmed by **reproduction rather than taken on report** — `diff -r` does print two `Only in` lines whose four filesystem entries were created 2026-08-21, a day before the vendoring, so that command never printed what was recorded beneath it; and `tar -x -C` does exit 1 with *could not chdir*. `vendor/README.md` gained an `rm -rf` **before** the `mkdir -p`, because the second run of that procedure is the X7/X8 re-pull case the owner has just committed to. **`D-g5-d`'s count is four documents in ten places**; the count was corrected and the ten labels left alone, on the stated grounds that re-numbering them would be BL-3 again at ten sites. **A finding that bears on the X7/X8 decision**: the manifest disagreement recurs at 0.9.8 (top-level `0.9.7` against `.claude-plugin` `0.9.8`), which makes it an upstream *pattern* rather than one slip. **The guard is built against the defect rather than the number** — the sweep asserts *it holds 0.9.8 today* absent alongside *it holds 0.9.6 today*, so a document repeating the mistake with tomorrow's version still fails. Two honest reports rather than omissions: the reconciler exits 1 on check E (`G4-B`) because this branch predates that merge, and `null-run-gate.py` and `reader-accounting.py` **do not exist on this branch** — the previous verdict reached them through the merged tree. **Gap-fix 3 verified Needs More Work 2026-08-23 at `3e63811`, tagged `verdict/G5-gapfix-3`; gap-fix 4 dispatched.** The verifier **committed its instrument** at `planning/verification/G5-gapfix-3-sweep.py` (139 lines) rather than leaving it in `/tmp` — **`G6`'s rule applied the same day it was filed**. It handled the degraded arm exactly as framed: pre-crash results recorded as *testimony carrying no load because they cannot be re-run*, the property re-established with its own sweep, and it says which is which. **Eight plants, eight caught**, including a hard wrap mid-claim and a second file — it built on `claim-sweep.py`'s `normalise`/`quote_marker` reader, which is what made those two catchable. **The blocker is already false, not merely at risk**: `G5.md:170` says `installed_plugins.json` records **0.9.4** since 2026-08-21; the orchestrator read the live file and found **zero occurrences of 0.9.4, two of 0.9.9**, mtime `19:54:03`. **And the defect is the pattern, not the line** — `G5.md` binds a number to the word *installed* at four sites (`:105`, `:134`, `:154`, `:170`), each asserting a fact about the machine now, while **the cache moved three times in 4h35m** (0.9.6 at 15:19, 0.9.8 at 17:38, 0.9.9 at 19:54). Two further sites use *installed* with **no number** (`:8`, `:178`) and are already correct — they name a role rather than a value and cannot rot, so they are the model for the fix rather than more work. **A citation claim this row made and then had to withdraw**: it said the verdict's `:159` was simply wrong because `:159` is blank at `43b44a2`, `3e63811` and in the worktree. **All three frames are post-edit.** `:159` is *exactly right* at `a9603e5` — gap-fix 3's base, and the frame `D-g5-d`'s own ten citations use — and becomes `:170` only afterwards. Three concordant readings from one side of a change are one reading, not three: an absence check with no presence control, which is the defect class this very document set carries a guard for. The citation was true when written and false where a reader lands, so **the fix is to carry the frame, not to renumber** — `G5.md:159` at `a9603e5` stays checkable from either side of an edit. Corrected in the in-flight brief before the runner committed. Raised by `lukerhodes-2f`, which had made the same error and reported it unprompted; the orchestrator then repeated it in this row and in `dc2f3d4`. G5V2 corrected at `f085242` **on top of `3e63811` rather than amending**, because that SHA had already been cited — which is the same rule, applied to the correction itself. On gap-fix 2 the verifier agrees it is not reopened but records it as finding **F2** rather than leaving it implicit, with arm 1's three-way checksum re-deriving independently and only the fixture evidence lost. **Gap-fix 4 delivered 2026-08-23 at `6a3dbb8`, three commits — ready to verify.** **Twelve sites in six documents bound a version number to *installed*, not the brief's four**, and the reasons it found more are the item's own subject twice over. `G5.md:87` carries `Installed 0.9.4` with a **capital I** in a table header — **the orchestrator's brief undercounted because it ran a case-sensitive `grep`**: 6 lowercase hits against 7 case-insensitive, the same instrument class as the line-anchored sweep already filed as `G4`'s tenth. And two sites bind **`0.9.2`**, not `0.9.4` (`M28-decision-docket.md:244`, the vendor triage row at `:17`) — *which is why a sweep keyed on the number that had gone wrong had never found them*. **Counts as asked: 12 bound and fixed; 7 name a role with no number and were left** (five pre-existing, two added in the same shape); **2 quote a withdrawn claim and stay as records**. Nothing re-numbered, no measurement moved — *0.9.4 reads 62 of 76* is true whatever the cache holds. **The guard is `planning/installed-claim-sweep.py`, committed**, and it fixes a defect in its predecessor: gap-fix 3's sweep imports `planning/claim-sweep.py` from ROOT and **that file is `main`-only, so it could not run on this branch as delivered**. The new one imports the reader when present and inlines it otherwise, printing which. Twelve blocking patterns all keyed on `\d+\.\d+\.\d+`; `T1` catches the table-cell form crossing a `|` that no sentence pattern sees. **13 blocking rows at `43b44a2` → 0.** `--selftest` holds 105 in-memory cases and **found a real hole while being written** — `B9` was built against the one em dash that had gone wrong and the hyphen form walked past it. Seven tree plants caught, files restored byte-for-byte, both negative controls silent. **On `D-g5-d` it followed the brief's correction over the verdict's F4**, presence-controlled: **10/10 citations resolve at `a9603e5`, 4/10 at `43b44a2`**, so the row carries its frame with numbers unchanged rather than being renumbered into a pointer that works today and breaks next edit. One further off-by-one recorded not acted on: the verdict's F5 cites the vendor row at `:16`; it is `:17` at both frames. It could not reply to `g5v2-a1` — stale socket, session gone — so the divergence is in the note and the commit message. **Gap-fix 4 verified Needs More Work 2026-08-23 at `78ce229`; gap-fix 5 owed — four record findings, none touching a measurement.** **The substance holds, confirmed three ways**: the committed guard against both trees (13 blocking → 0), an independently designed proximity reader (11/12 plus one structural blind spot it named and hand-confirmed), and again after merging `main` with three conflict hunks resolved. **The predecessor's crash reproduces** (`FileNotFoundError` on `planning/claim-sweep.py`) and the new guard runs inline on-branch then switches to main's imported reader after merge, exiting 0 both ways. All 12 selftest patterns load-bearing under mutation, the B9 hole reproduces, 8 of 9 plants land at the exact stated site **including the hard wrap at `G5.md:107`**. **The premise is live-read rather than testimony** — four cache dirs born 05:19:05Z / 07:38:26Z / 09:54:03Z, `installed_plugins.json` holding `0.9.9` with zero `0.9.4`. **F1 is the finding worth keeping**: `\binstalled\b` **cannot match inside `_installed_`** because `_` is a word character, and `G5-gapfix.md:24` introduces that emphasis style — the guard handles emphasis on the *version* and not on the word, so it would walk silently past the next occurrence. The verifier **validated the fix rather than proposing it**. F2: `G5-gapfix-3.md:267` is filed among the seven *with no number* while `0.9.2` sits one hard wrap above — **6/3, not 7/2**. F3: the gates table's `1211` and `6172` **hold at no commit on this branch**; the tip reads 1217 and 6180. F5: the presence-control table names no reader and no predicate, and its `10/10` contradicts the same report's *nine of the ten bound*. **On `D-g5-d` the choice was right and stronger than the report argued**: renumbering to `43b44a2` would not have *worked today* — `:134`→`:139` and `:154`→`:159` **inside the same gap-fix that would have written the numbers** — and **five of the six broken citations land on prose that reads perfectly well** while one lands on a blank line, which is `G7`'s dangerous variety quantified. **It committed its normaliser beside the verdict rather than leaving it in `/tmp`** — `G6`'s rule applied unprompted. |
| G6 | Evidence kept in `/tmp` is not evidence | `G6-evidence-that-lives-in-tmp-is-not-evidence.md` | — | — | Untriaged — filed 2026-08-23 after the crash. **Every `/tmp` artifact this fleet cited is gone**: `sweep3.py`, `sweep2.py`, `sweep.py` and `m18-build2.log`. These were not scratch files — a sweep is the instrument proving a guard is *armed*, which is the distinction most of G4's twenty-two instances turn on. Two consequences already real: **G5 gap-fix 2's accepted verdict rests on an artifact nobody can re-run** (it recorded four planted faults producing four `FAIL` rows, plus three more the verifier planted itself, all against `/tmp/g5gf2/sweep2.py`); and **`G5V2` was sent to run a file that does not exist** — its brief opens arm 1 with *Run it* against `/tmp/g5gf3/sweep3.py` and calls that arm the claim that decides the item. **Worse than a stale literal**: G4's instances are wrong and *checkable*, but a dead path produces no wrong answer to catch — only an absence, which reads identically to *not yet run*. The fix is already precedent here: R17's `planning/claim-sweep.py` is committed and survived; G5's lived in `/tmp` and did not, and the two were written days apart for the same shape of problem. **The orchestrator's defect, not a runner's** — the briefs citing `/tmp` were written here, `GAPFIX-BRIEF.md:64` included. **Two directions, one root — measured 2026-08-23.** The matching rule (`lukerhodes-2f`) is **a brief cites a repo path or it cites nothing**; two briefs failed it *hours after being written here* — `GAPFIX-BRIEF.md:64` and `VERIFY-BRIEF.md:29`. The opposite error is the dispatch artifacts themselves: `WORK-ORDER.md`/`GAPFIX-BRIEF.md`/`VERIFY-BRIEF.md` were written **inside the tree under test** and are now **tracked in 4 of 12 worktrees** (M12, M19, M20, G5V2) and untracked in the other eight — so a third of them would merge into `main` carrying the orchestrator's prompts as project artifacts, and nobody decided that. M20 caught it and isolated the file in `bcc69dd` so it can be dropped before merge, but that is a per-runner workaround for a dispatcher's mistake and eleven runners did not each invent it. **`/tmp` is too volatile to cite, the worktree too durable to write into**, and this fleet never decided where its own working material lives. Triage must settle both directions together: fixing only the `/tmp` half moves briefs into worktrees and makes the tracked-artifact problem universal instead of one-third. |
| G7 | A citation that does not resolve where it is read | `G7-a-citation-that-does-not-resolve-where-it-is-read.md` | — | — | Untriaged — **split out of `G6` on 2026-08-23, on `G6`'s own coupling test rather than on taste.** `G6`'s two halves are one item because each fix displaces the other: move a brief out of `/tmp` and it lands in a tree under test. This has no such relationship — fix `G6` completely and `planning/progress/G5.md` still rots at `:159`, because that file already satisfies `G6`'s rule perfectly (committed, durable, repo-pathed, and the exact artifact the defect lives in); carry the frame everywhere and not one `/tmp` file returns. **`G6` is about whether the artifact survives; this is about whether the pointer resolves**, and an abstraction shared is not a remedy shared — `G6`'s is a location decision a dispatcher makes once, this is a citation discipline every writer applies every time. **The measurement**: `G5.md:159` is correct at `a9603e5` and becomes `:170` at `43b44a2`; two agents independently ruled out revision drift after checking three frames **all on the same side of the edit**. Systemic rather than incidental — six of `D-g5-d`'s ten citations fail the same way, and gap-fix 3's own verdict carries the bare number in its opening sentence, its arm-4 paragraph and its work order, the three places a reader acts. **Do not fix by renumbering**: a number chased forward is correct until the next edit and carries no way to tell. Argument and decomposition test both `lukerhodes-2f`'s. |
| G8 | A question answered at the wrong scope returns a clean answer | `G8-a-question-answered-at-the-wrong-scope.md` | — | — | Untriaged — filed 2026-08-23 beside `G7` on the test that split `G7` from `G6`: shared shape, unshared remedy. **`G7`'s fix is carry the frame; this one's is establish the set before answering about members of it.** Harder to catch because **a citation that does not resolve announces itself when someone follows it, while a question answered at the wrong scope returns a clean answer** — no dangling pointer, no red, just a correct answer to a narrower question. **Three occurrences in one evening** before either party named it: the `:159` citation ruled non-drift on three frames **all one side of the edit**; `RouterSheet.Kind`'s cases verified to exist and their *meaning* inferred without reading the tests that define it; and the `VOUCHED_CONTROLS` collision asked of *the two parties already in conversation* rather than *which branches touch the file* — complete about the wrong set, missing the largest party at `+29/−6`, and **caught only because M19's report mentioned the table in passing**. A fourth, same shape: a line-anchored `grep` as an absence sweep over hard-wrapped prose returned 0 for a phrase present twice, and the orchestrator reported a verbatim quotation as a paraphrase **to the party who had quoted it correctly, who then adopted the wrong version**. **M16 produced the positive form the same evening**: rather than asking whether a merge conflicts it derived the union of all three trees, walked its own dumps against it, and named the one thing it could not check — reducing an open-ended cross-branch question to *does the other branch introduce a role I use*, mechanical and runnable before any merge. That belongs in `mock_fidelity.py`'s header, where it **retires** the merge hazard rather than answering it once. **Routing decided 2026-08-23 — this item carries M16's rule, and it lands after all three merge.** M16 wrote the vouching-asymmetry rule into `planning/progress/M16.md` at `f83c7d8` rather than `mock_fidelity.py`'s header, **deliberately and correctly**: `ai/m16` is finished and ready to verify, and a fourth edit to a file two live branches are still rewriting buys them a conflict and reopens a closed branch to purchase a comment. It named the header as the right home and asked that the timing be the orchestrator's rather than taken from a finished branch. **Not M22's to carry**, despite being last: M22 has **zero diff** on that file today, so handing it the comment creates a conflict surface it does not have and makes its rebase harder — and *whichever merges last* is fragile, because merge order can change and a stalled M22 would strand the rule indefinitely. It lands instead as **one dedicated change once M19, M16 and M22 are all in**: zero conflict surface, no branch reopened, no dependence on anyone's timing. |
| G9 | Two gates `cd` into a worktree that no longer exists | `G9-two-gates-pin-a-worktree-that-no-longer-exists.md` | — | — | Untriaged — swept for 2026-08-23 after egress rescoped the family as *a destructive git call whose target is established somewhere other than immediately before the call*. **This repo has the shape, mitigated but not harmless.** `app/Scripts/pool-mutation-gate.sh:15` and `planning/run-gate.sh:10` both `cd` into `.worktrees/R2`, **which does not exist** — no R2 in `git worktree list`, directory gone. Both are 0755 and both are **permanently dead**: every invocation exits 90 without reaching a check. **The `|| exit 90` is what makes this better than DEF-253**, whose script proceeded into whatever repository it landed in and ran `git reset --hard`, destroying a gate patch. **What is not saved is the gate**: `planning/specs/spec-R2.md` cites `pool-mutation-gate.sh` as runnable, so the spec asserts a mutation gate that cannot produce a result — this fleet's recurring shape once more. **It was correct when written** and a routine worktree cleanup falsified script, gate and citation without touching any file, with nothing going red because nothing invokes them. An absolute path is the slow-acting version of the fault and it decays untouched. **Also swept**: four other files carry absolute paths — none pins a worktree, but `planning/watch-fleet.sh` pins a `~/.claude/projects/…` session directory, which decays the same way. **A sweep-scope note against this session**: the first pass covered `scripts/` only and returned zero, missing `app/Scripts/` and `planning/` — `G8`'s wrong-scope failure, in its own sweep, minutes after filing G8. |
| M15 | Settings becomes its own window | `M15-settings-window.md` | `spec-M15.md` | `plan-M15.md` | Merged `29d511 **Gap-fix verified Done 2026-08-22 at the metamorphic rung and merged at `29d5111`** — BL-3's load-bearing claim flipped by a controlled single-property mutation and flipped back rather than re-run, BL-1 re-derived by regenerating the artifact, BL-2 by an independently written sweep. **A correction the orchestrator owes**: this row previously credited M15's runner with diagnosing the check-E false-RED correctly where R17's had not. `aae3033` gets the **mechanism** right and repeats the **specific** error — *seventeen commits, M21's merge and G5's among them*. There was no G5 merge: `4de2080..52f0c5c` is **18 commits with exactly one merge**, `e121801`, and `ai/g5` was cut from `2fbe062` having committed nothing, so its tip **was** a main commit. Today's green is not main moving again — `ai/g5` committed `3cd45c6` at **14:40:51, six minutes after `aae3033`**, and that is what stopped check E firing. **And the guard's claim is narrower than its comment**: armed by re-chording `Add server…` to `,`, AppKit **strips the chord from the loser**, so A19b read 35 chords on one item each against a 36/23 baseline and saw nothing; A20 caught it instead. The guard bites on the case it was built for, and `AXMenuItemCmdChar` cannot see a collision that has already been resolved — filed as G4's **nineteenth** instance. Two fixes named and not blocking: the gap-fix's reconciler paragraph, and A19b's comment. **Merged-tree gates re-run by the orchestrator**: `make lint` **0 violations in 549 files**, `0/556 require formatting`; `make test` **1725 tests in 215 suites passed**, 0 failures; reconciler **0 across A–L**. |
| M16 | The Signal Path replaces the Breaker Column | `M16-signal-path.md` | `spec-M16.md` | — | **To Do** — design of record settled 2026-08-22 as `design/mcp-router-console.html`. Needs More Info — triaged 2026-08-22. One essential question, and it is M21's fork applied to the signature element: does the breaker column stay. `BreakerGeometryParityTests` asserts exact equality against `DESIGN.md`'s nineteen rows, so removing the levers reddens a parity test whose oracle is the contested document. M23's Servers ledger has already enumerated the work and cites this brief by name **Ready to verify 2026-08-23 at `c50c44a`, twelve commits from `87e16dc`** — *not* `f83c7d8` with nine, which was a stale head that would have sent a verifier at the wrong tree. **No behaviour has changed since `753670e`**; everything after it is records: `87387a5` progress note and acceptance evidence, `f83c7d8` the cross-branch rule, `5ef14ef` citation frames, `1e229d9` the G8 placement ruling *with its argument rather than only its outcome*, `c50c44a` the sweep-count correction. `planning/evidence/M16-acceptance.md` still describes the tree. **It produced this fleet's most reusable result**: adding entries can only make more pairings vouched, never fewer, so no already-vouched pairing can be un-vouched by another branch's edit, and the whole cross-branch hazard is the quota rule keying on the **build node's role** — reducing an open-ended merge question to *does the other branch introduce a role my surface uses*, mechanical and runnable before any merge. Routed to `G8` for the header rather than carried by a finished branch. **It also caught its own citations**: three frame-dependent ones in its note now carry anchor and tree, and it flags that two of the three **landed on plausible wrong text rather than on nothing**, which is the variety that gets believed. **Verified Needs More Work 2026-08-23 at `115e0f9`; gap-fix 1 dispatched. The code is right; the record is not — all six defects are prose, each a one-line edit.** Every behavioural claim reproduced on the merged tree: **1743 tests in 216 suites**, all eight `lint` components exit 0, the fidelity gate returns 137 findings with the breadth composition matching exactly, and **regenerating the tracked ledger produces byte-identical output** — which the verifier calls the strongest single confirmation that the acceptance evidence still describes the tree. Claim 1 holds exactly. **The cross-branch rule is sound and was settled by execution rather than argument**: patching `VOUCHED_CONTROLS` to the three-branch union changed nothing on this surface, and a **presence control** (vouching `indicator`) moved `covered-by-pair` **24→20** and `extra` **10→14**, so the zero is measured rather than blind. **And the verifier refined the rule**: it survives a screen it does not mention — **Servers' mock census *does* name a `callout` affordance**, so screening on the *kind* would report it exposed when it is not, and keying on the **build node's role** is what matches the code. **Two of the six are the class M16 filed against `spec-M22.md` and then committed itself** — its note says the harness column has three states while listing four case names for a four-case enum, and one citation names its tree and still lands **two lines off, on another dict entry of identical shape**, which is `G7`'s dangerous variety resolving onto plausible text. **Two are false rows in the acceptance file and that is what tipped the call**: `AC6` describes output its case-sensitive `grep` does not produce, and `AC7`'s *§1, §7 and §8 no longer name the element* is false for §1 and §7 — **and that file instructs a reader to accept a row in place of re-running the check**, so a false row there is an instruction to believe something untrue rather than measure it. **The verdict overrides the only out-of-family lane still up**: `gemini` said `Done`, arguing over-blocking on prose, and the downgrade is logged rather than passed silently. **`make lint` never ran** — `tools` wants `node_modules` and `dist/index.js`, absent — so its body is green from components run directly but the target was not observed to pass. One early reading that `ORCHESTRATOR.md` looked 33 lines short was **`main` moving, not a lost merge**; `git merge-tree` cleared it and the false alarm is recorded. **Gap-fix 1 delivered 2026-08-23 at `b03d8ee`, 18 commits — ready to verify. Documents only**: `git diff 753670e..HEAD -- app scripts DESIGN.md` is still empty. **All six findings reproduced before being patched and each fix names its reader.** F1: `HarnessState` declares four cases (anchor `enum HarnessState` at `:52`, cases `:55-58`, same blob `f190de0` on this branch and `main`). **F2 corrects the verdict's own citation** — `"icon"` is at `:85` on this branch, not `:87`, and `:87` is `"column-header"`: `G7`'s dangerous variety, in the verdict that reported it. F3: AC6's case-sensitive `grep` returns 10 hits, all doc comments or forbidding assertions, **0 under `RouterCore`** — the deadlock breaker is lowercase and needs `-i` (29 total), presence control 22/32/9. F4: four breaker mentions survive in `DESIGN.md` and **§8 is the only named section that is clean**. F5: `extra-cited` restored so both columns reconcile, 116 over 149 at the base and 116 over 157 here, **the base column read out of the ledger blob tracked at `87e16dc`** rather than by rebuilding the gate. F6: 1725 is now a per-file `@Test` census over each tree's git objects, controlled against the tip's executed 1743 and against zero parameterised cases at both trees, with the diff's net +18 agreeing. **The cross-branch refinement is folded into the rule's bullet**: 0 build nodes of role `callout` on Servers against `jack` 4 / `signature` 1 / `row-indicator` 4 as the control. **Two things came out differently from the brief.** `make lint` **exits 0 here** — this worktree carries the `node_modules` and `dist/index.js` its `tools` prerequisite wants, so the target was observed; the verifier's failure was a **fresh-worktree condition** and both files now say which worktree. And **its own `ORCHESTRATOR.md:219-232` citation went stale mid-run** — `main` moved to `1f5ad55`, the text is at `:350`, **131 lines off, landing on another item's verdict line**: *the defect I was fixing, arriving on schedule*, caused by the orchestrator appending to that file concurrently. **It fixed one thing beyond the six and one beside it**, and it **agrees with the verdict while pushing back on two findings** — 5 and 6 would not have blocked alone — noting that *my agreement carries little weight, I am the graded party, so the argument is what holds it up*. |
| M17 | Four states on every surface, and chrome that follows | `M17-surface-states.md` | `spec-M17.md` | — | To Do — triaged 2026-08-22. The brief's declared dependency on M1 alone is wrong: forty cells needs M22's two boards and M15's window, so it lands after both. Nine-versus-four states produces the same build either way and is recorded as an assumption. `StateContainer` already models nine and is used only in the debug gallery. Cites `D-m23-g`, `DEF-015`, `DEF-014`, `DEF-034` and `SURF-003` rather than re-deriving them. Nine assumptions, no essential question |
| M18 | Twelve sheets, and the gate each decision gets | `M18-sheets-and-gates.md` | `spec-M18.md` | — | To Do — triaged 2026-08-22. Measured thirteen sheets in the mock, not twelve — `official` was added by M24 — and two of the mock's own sheet links resolve to nothing. The build has seven sheet sites, three still on `isPresented:`, five per-board enums and one dead case. Five of the thirteen have no host surface until M15, M19 and M22 land. Only `pair` has measured evidence and its transport is `DEF-001`, still open. Ten assumptions, no essential question **Built 2026-08-22 at `6b620b3`, eight commits — NOT ready to verify: the tree has never compiled.** The runner reported that plainly rather than dressing it up, which is the only reason it is actionable. `swift build` failed across five attempts; **two of those failures were the runner's own** and are recorded in its commits rather than quietly fixed — it queued two builds of one package which deadlocked on `app/.build`, and its wrapper piped through `head -60`, which SIGPIPEs the build. It first blamed other fleets for the lock and then corrected itself (`ee3ed39`) after finding all six occupied berths were this project's. `make lint` never ran at all: it needs `make tools`, which needs `node_modules`, and two `npm install` runs got no berth. **Out-of-family plan review (`agy`/`gemini-3.7-flash-high`, AMEND on seven findings) changed the design rather than the prose on one**: `SheetGate` as first planned was a lookup table **no call site consulted**, so its tests would have passed at full green while a button called `remove(name)` directly — the boards now route destructive actions through `request(_:)` and the acceptance test moved to the board-model seam. Five per-board `Sheet` enums and two `Bool`-shaped presentations became one `RouterSheet` nested per host, so `inboxBoard.sheet = .addServer` **fails to compile** rather than rendering something. All seven `.sheet(` sites use `item:`; the three that were `isPresented:` kept their by-id lookup by deriving the enum from existing storage, because a captured value would have satisfied the brief's letter and reintroduced the stale-subject bug both models carry a comment about closing. **Three things the mock draws that cannot be drawn as drawn**, measured here and absent from triage: the router computes its children's PATH at `src/pool.ts:168` and **publishes it nowhere**, so `path` ships as a refusal in the shape `SURF-010` established; `ConfigWriter.backupsKept = 5`, so the gate table's *30-day undo* is **a promise this router does not keep** and nothing was added to fake it; and `RegistryEntry` has no publisher field, so `official` drops its publisher grid and says so on the surface. **Also open**: Phase E's six fidelity surfaces and rendered verification of the two new sheets. Next step is a compile, not a verifier. **Gap-fix delivered 2026-08-23 at `6721e5c`, 12 commits — ready to verify.** `swift build` exit 0, `swift test` **1757 in 219 green twice**, `make lint` 0 (swiftlint 0/557, no-raw-design-values clean/127, null-run-gate 28 armed 0 held) — **+32 tests across 4 suites with nothing dropped**. **The measurement the brief asked for is still owed, and the runner said so itself rather than letting it pass**: the brief asked it to *measure* that `@Environment` actually updates a `ButtonStyle`, warning that a style is not a `View` and that a disabled button rendering at full strength is a silent defect no compile catches. It instead **removed the question by construction** — both styles now read `isEnabled` from a nested `View` where support is unambiguous — and recorded that in `planning/evidence/M18-acceptance.md` as **an argument rather than a measurement**. Right call and right disclosure, but **the property is argued, not observed**, and if the warning was worth writing the measurement is worth taking before this is called Done. **A flake named rather than absorbed**: `CallbackLifecycleTests` *a listener binds once* failed once with *the callback listener was cancelled before it bound*, passing on the same tree either side — R5's loopback socket bind, untouched by the diff, neither fixed nor folded into its own gate result. **Remaining scope**: the four hostless sheets (M19 owns readme, M22 the other three, `Kind.owner` names them), Phase E's six fidelity surfaces, and rendered verification of the two new sheets. `RouterSheetTests` compares enum and mock both ways, so a sheet shipping unmatched reddens it. Its worktree carries `GAPFIX-BRIEF.md` and `WORK-ORDER.md` untracked — `G6`'s second direction, in the eight-worktree bucket rather than the four. **Verified Needs More Work 2026-08-23 at `d482562` (on merge `c85a105`); gap-fix 2 dispatched.** **The blocker is a destructive-action defect the gap-fix introduced**: `4c320a8` moved `.keyboardShortcut(.cancelAction)` **off Cancel and onto the destructive Remove button** (`CleanupSheets.swift:106`, anchor `.keyboardShortcut(.cancelAction)`, tree `6721e5c`), **so Escape removes the installed capability instead of dismissing the sheet**. It was correct before M18. The verifier **measured it with a presence control** rather than reading it — a single-file SwiftUI reproduction of that shape fires `REMOVE` on posted keycode 53 and **the pre-M18 shape fires `CANCEL`**. **No test in `app/Tests` mentions either shortcut**, and that zero carries a planted probe, so the guard is part of the fix. **The measurement the runner owed and declined now exists, and it holds**: rendering the shipped styles through `ImageRenderer` and naming the nearest palette token, a live prominent button paints the accent hue, a disabled one paints `--f3` at `rgb(248,248,248)`, and a bare `View` reading `@Environment(\.isEnabled)` separates green from magenta. So the by-construction fix was sound and only the observation was missing. **Two instrument limits recorded as could-not-run rather than as passes**: `ImageRenderer` draws nothing inside a `ScrollView`, which is where `SheetFrame` puts every sheet body, so the empty bodies in the new sheets' renders are the instrument and not the product. **Three non-blocking**: a destructive button filled where the mock draws it unfilled; **the two new sheets have no Escape path at all, alongside six older ones — 8 of 14 sheets**, so fixing the blocker alone leaves the class one sheet over; and the `29 btn primary` count's normaliser was never named — the verifier gets **29, 33 and 35** over that one phrase and the conclusion survives all three, which is the counting discipline finding its next instance within hours of being written. `swift test` 1757/219 with **R5's ephemeral loopback bind red once (`EADDRINUSE`), green on runs 2 and 3** — *a second costume for the flake M18 had already named* — counts read from the xUnit report rather than the summary line. **Gap-fix 2 delivered 2026-08-23 at `3317f00`, seven commits plus a `main` merge — ready to verify.** **The blocker is fixed and it reproduced it first**, with a **committed** probe at `planning/evidence/M18-gapfix-2/escape-shortcut-probe.swift` (in the repo, not `/tmp`) building seven action-row shapes and posting keycode 53/36 at each: the shipped shape fires `REMOVE` on Escape, the pre-M18 control fires `CANCEL`. **That matrix killed two candidate fixes before one was written** — **one control cannot hold both shortcuts** (SwiftUI keeps the innermost and silently drops the other, both orders) and **`.onExitCommand` on a sheet's own body is inert**. So Cancel takes Escape, Remove takes no key, nothing is the default. **The cost is stated**: Return no longer dismisses the four surfaces touched — correct for a destructive confirmation with no non-destructive affirmative, and a deliberate trade on the two informational sheets, where a hidden zero-opacity twin keeping both keys was **measured working and declined on cost**. **Finding 3 scoped to four**, including `RemoveServerDialog` as the blocker's sibling because *fixing only the sheet would have left the pair disagreeing in the other direction*; six left as named open defects, guarded so a name that stops matching a sheet fails. **Nine of fifteen sheets now have an Escape path, up from six.** The guard is five source-level assertions plus a scanner armed against the shipped defect, eight arms all red. **Three figures corrected, two of them the verdict's**: `MissingSubjectSheet` sat outside its table so *8 of 14* is **9 of 15**, and `make lint` exits **0** here for the whole target because this worktree has `node_modules` and `dist/index.js` on disk **untracked** — a worktree fact saying nothing about a fresh checkout. **The third is its own and it is `G4`'s shape in its own measurement**: its first per-component lint check **read `tail`'s exit code**, so all eight read 0 while `reader-accounting.py` was actually failing on its new script — `make lint` caught it. `swift test` **1768/221, 0 failures, 0 skipped, six green runs**, R5's flake absent from all of them. **A design question it raised and declined to decide — now settled by the orchestrator from the design authority rather than left open**: it made Cancel a filled primary on `RemoveServerSheet` while Cleanup's other two confirmations kept a standard Cancel, so three sheets disagree. `DESIGN.md:441` rule 4 reads *One prominent accent-filled action per view, trailing. Cancel leads. Destructive is never the default.* **Cancel leads and the prominent action is trailing, so Cancel cannot be the accent-filled one** — and since destructive is never the default, a destructive confirmation has **no** prominent action, the rule being a ceiling rather than a requirement. **So the two standard Cancels conform and the filled one does not.** For the verifier to check rather than for an owner to decide. |
| M19 | The in-app GitHub-flavoured Markdown viewer | `M19-gfm-viewer.md` | `spec-M19.md` | — | To Do — triaged 2026-08-22. No Markdown rendering of any kind exists in the app and nothing in the campaign measures this feature at all, so M23's gate is the only thing that will catch drift. The mock's readme sheet gained a product header and a facts strip after the brief was written. One acceptance line waits on M21 for the badge colour — a dependency, not an owner question. Eight assumptions, no essential question **Ready to verify 2026-08-23 — six commits, tree clean, base `87e16dc`.** Runner measured **1776 tests in 219 suites** (up from 1725/215, re-measured at its own base rather than quoted), swiftlint `--strict` 0 over 570 files, reconciler 0 across A–L. `mock-fidelity-gate.sh readme` **exits 1 with 30 findings and that is the correct outcome** — every finding is a declared divergence; 29 ledger rows all carry a `Dn` tag and the manifest holds the recipe to re-derive the split. **Three things a verifier must not be surprised by, all recorded by the runner rather than found later**: there is **no entry point in the shipped app and that is deliberate** — `src/control.ts:279-283` serves no document, so a wired *Read me…* button would read *no document* for every capability in a Release build, which is the *no number the router does not observe* constraint one surface over (out-of-family record at `planning/evidence/M19-review-agy.md`; grok 402, codex down to 2026-08-27, `agy` answered); therefore **no driven pass**, only a real headless render plus the gate, **and the ledger opens by saying that is weaker**; and the gate measures **one of the panel's three designed states** because the mock draws one frame and `mock-affordances.py` exits 3 rather than inventing another. **It extended the shared M23 instrument** — additively and with a control: this surface has no `.v-*` frame so the census could not reach it at all, `mock_fidelity.py` now reads an optional `stateFrames` map, and five census kinds knew only the boards' spelling (`badge` had no rule, so **the shields this item exists to draw were invisible to the instrument meant to check them**). All six existing frames re-measured before and after — 87/6/8/6 and 52/4, unchanged. **An id collision, and it is the orchestrator's**: the runner filed `M29-capability-document-source.md` at `06dc629` (20:25) while `M29` had been taken on `main` at `78a27e7` (18:47) during the crash window — allocation is a read-modify-write the pipeline serialises for exactly this reason. Renumbering to **M30**; no ledger row of the runner's is affected. Verifier not yet dispatched. **A coverage asymmetry, stated before any verifier sees it.** `readme.layers.json`'s `stateFrames` is exactly `['ideal']` — **one** state frame — where M16's servers surface is measured on **four** (ideal, empty, loading, error), and `readme.ideal.json` is the only `readme.*.json` in the repo. So *M19's fidelity is measured* and *M16's fidelity is measured* are **not the same claim**, and only one of them covers the empty, loading and error states. Not a merge hazard and not a defect in the work — the gate measured what exists — but a verdict must not rest on a coverage word meaning two different things. Found by `lukerhodes-2f`; M19 has exited, so it is the orchestrator's to carry. **Cross-branch reclassification measured to zero for these two surfaces** — and *only* for these two, a limit this row previously overstated as *in both directions*. **M22 corrected it**: the role-walk hazard is **per-surface, not a property of the merge**. Over M22's 42 build roles the intersection with what the merge adds is **`callout`** — harmless there and checked rather than assumed, since the only node carrying it (`unreadable-note`, error state only) is already paired. **Anyone re-measuring after the merge runs it on their own surface rather than inheriting *empty*.**: M19 introduces eight roles (badge, callout, codeblock, heading, list-item, sentence, tab, verified-mark) and **none** intersects M16's 39 build roles, and `signature`/`jack` appear **zero** times across 21 build roles on the readme surface. |
| M20 | Menu bar, status item, and the notification banner | `M20-menubar-status-notification.md` | `spec-M20.md` | — | To Do — triaged 2026-08-22. The app declares six command groups with no Router or Library menu and `⌘1`-`⌘7` over seven destinations; the mock wants nine, which waits on M22 and M15. The accelerator map disagrees three ways and the mock wins, recorded as an assumption. The status item has never been measured — `SURF-009` carries three `n/a` cases on a structural limit — and M23's harness is the route that reaches it. Eight assumptions, no essential question **Recovered and reported 2026-08-23 after the crash.** All 17 outstanding paths committed on `ai/m20`, tree clean, three commits from `main`: `f3cc64d` the plan, `0bdfcbe` the menu-bar work labelled in-body as an **unverified crash checkpoint**, and `bcc69dd` isolating `WORK-ORDER.md` so the dispatch artifact can be dropped before merge. **Four of the 17 were untracked and existed nowhere else** — `KeyChord.swift`, `MenuCommandAvailability.swift`, `MenuCommandRuleTests.swift` and the work order. It had *finished its turn at 08:10 owing that commit* rather than being cut, which is the DISPATCH hazard biting a third time. Not pushed, not verified — the checkpoint label is honest and the item still owes a verifier. **Verified Needs More Work 2026-08-23 at `984f00a`; gap-fix 1 dispatched. Two blocking reasons.** **The delivered tree does not compile** — a bare `nil` in statement position at `MenuCommandAvailability.swift:110` fails `MCPRouterKit`, `make test` exits **2** at its enumeration guard, and **no test in the suite has ever run against this branch**. That is the crash-checkpoint risk the brief named, realised: this branch's contents were committed on recovery by a session that never gated them. **And three of the plan's four slices produced nothing** — seven promised names each occur in exactly one file, and **that file is `plan-M20.md`**. Adding `return nil` turns everything green — 1730 tests in 216 suites, the macOS target builds, and the running menu bar holds every Slice A claim reachable: eight menus in the order `mac-shell.sh:394` expects **and had never run**, eleven dimmed items carrying nine distinct unbuilt sentences, `⌃W` the only new chord, `⌘R` still on Reset Server. Probe reverted and hash-verified. **An uncommitted 548-line verdict draft from a dead verifier session was already in the worktree**; this verifier re-derived rather than carrying figures across, and **six of its numbers were wrong** — `make lint` exits 2 not 1, `EXPECTED_ORDER` is line 394 not 395, its *1734 discovered* is one draw of a figure that returns **1742/1734/1733 on the same tree because it counts SwiftPM build chatter**, its return-line list dropped `:74`, and *nine dimmed items* is eleven. Its two blocking conclusions and its `.featureUnbuilt` arithmetic survived. **The load-bearing non-blocking finding is `DESIGN.md:589`**: it refuses `⌥⌘Q` under a rule that does not reach it — *Review Held Changes…* can fire — and *eight of their ten commands* is **nine of twelve**, repeated wrongly in three more places while the same source file gets the same arithmetic right twice. It also **armed the A3 and A4 drift guards the plan claimed were seen red and never were**; both bite under mutation, so the gap-fix owes the record rather than the guards. `bcc69dd`'s isolation of `WORK-ORDER.md` **does** permit dropping it — reverse-applies cleanly. |
| M21 | The token layer, the split accent, and `DESIGN.md` | `M21-token-layer-and-design-md.md` | `spec-M21.md` | `plan-M21.md` | Merged `e12180 **Verified Done 2026-08-22 and merged at `e121801`.** `ColorToken` 40 cases over four resolved appearance contexts, `MetricToken` 21, `DESIGN.md` §1-2 re-authored to *Patchbay*, register regenerating **byte-identical** to the committed file at **89 rows — 70 matched, 19 pending, 0 uncited** against a plan expecting 64+. **The verification is stronger than the delivery in the two places the runner said it could not reach from its own worktree.** `mac-shell.sh`'s one scroll-edge failure is **not this branch's**: `main`'s build reports the identical `0.872 uniform` against the same 0.90 bar and passes 52/52. What M21 does move there is the separator — `main` **0.0742** to the branch's **0.0921** against a token going 7.5% → 9%, nominal 1.200 and measured 1.241, while `main` at the unchanged token differs from M13's reading by 1.9%, the run-to-run spread. An order of magnitude outside the noise and landing where the token says. And all 40 colour rows were reparsed from `DESIGN.md` alone with all **78** documented ratios recomputed in WCAG arithmetic: **zero disagreements beyond 0.05**. **Arm 7a's no-bit confirmed to four decimals** — `#FF5A5D` is 4.5610:1 on dark `--raised`, `#FF6E70` is 5.1209:1, so lightening raised contrast and the arm was aimed the wrong way; one precision the note missed is that the mutation would have reddened `colorsDocumentToCode` regardless, since `DESIGN.md:169` documents the original, so *no bit* is true of the aimed oracle and not of the suite. The silent-mutation-reuse defect is settled by evidence rather than assurance: 7b's and arm 8's recorded figures reproduce to **sixteen digits**, which cannot happen without the mutation having been applied. Departures all delivered-with-reason, none scope drift — the pin is **ported not replaced** and provably so, since no call site moved. Carried as `D-g4-a`: `planning/fidelity/servers.ledger.md` still reads `25 matched, 64 pending` beside the new register. **Merged-tree gates re-run by the orchestrator rather than taken on report**: `make lint` **0 violations, 0 serious in 535 files**, `0/542 require formatting`, `no-raw-design-values: clean`; `make test` **1697 tests in 211 suites passed**, exit 0 — identical to the branch figure, so the merge introduced nothing; reconciler **0 across A–L**. |
| M22 | The Harnesses and Insights boards | `M22-harnesses-and-insights-boards.md` | `spec-M22.md` | — | To Do — triaged 2026-08-22. **Absorbs `R7-C1`**: neither board exists, no `GET /harnesses` route exists, and A36 forbids the Mac app reading a harness config itself, so the board and the route ship together or neither does. The route owes a parity row against `src/control.ts`. Three of the brief's four readings already exist in `HarnessState`; the fourth is new modelling. `PRD.md` §8.2's savings sketch is superseded by this brief, and the brief's own duty-cycle caption needs re-cutting for the same reason. Eleven assumptions, no essential question **A spec defect found by M16 and verified against source 2026-08-23, and it is M22's to correct because M22 owns both the spec and the route.** `spec-M22.md` §3.3 describes `HarnessState` as carrying `.notWired`, `.wired(route:)` and `.wiredWithDuplicates(route:count:)`. `RouterCore/Discovery/HarnessReconciliation.swift` declares **four** cases: `notWired(overlapping: Int)`, `wiredViaHTTP`, `wiredViaShim(bridge: String)` and `wiredWithDuplicates(route:count:)`. **Two errors, and the second is the dangerous one.** The spec drops `notWired`'s `overlapping` payload — the count of what adopting the harness would consolidate. And **`.wired(route:)` does not exist**: it collapses `wiredViaHTTP` and `wiredViaShim(bridge:)` into one case carrying a payload *neither* of them has. `wiredViaShim` carries a **bridge name** that `.wired(route:)` cannot express and `wiredViaHTTP` carries nothing at all, so **a route built to the spec's names could not tell an HTTP-wired harness from a shim-wired one** — which is exactly the distinction the Harnesses board exists to show. The spec then reasons *three of the brief's four readings exist in the model and one does not*, which is arithmetic on the wrong denominator: four exist. **M16's rail waits on `GET /harnesses`**, so this reaches beyond M22. Correct the spec in the same change as the route rather than after it. **Status 2026-08-23: seven substantive commits, tree clean, `swift test` 1767/221 green**, all lints clean, `parity-divergence.sh` 6 declared / 0 stale, `parity-manifest-check.sh` 94 rows, `parity-manifest-selftest.sh` 36/0. Harnesses is through M23's gate — tokens, literals, structure, geometry and type-metrics clean over 193 nodes. **It measured the sequencing rather than assuming it**: of 36 `unclassified` rows on Harnesses, 34 want a `VOUCHED_CONTROLS` entry and **0 of the 34 are covered by the merged table**; two of its seven kinds (`banner`, `section-header`) have no key on any branch and the other five sit *inside blocks m19 and m16 are changing*, so it has landed no edit and will not until it reads the merged version. **It withdrew its own earlier superset claim**, which was wrong twice — it omitted m19 and it had not checked. It also **verified and corrected `spec-M22.md` §3.3 in place**, keeping the old reading; `GET /harnesses` was built against the source so it already carries all four states, and the risk was to the next reader — M16's rail reads that route. **Remaining**: Insights fidelity artifacts, docs, UI verification, e2e. **Ready to verify 2026-08-23 at `6bd8385`, 22 commits, tree clean — both stop rules met and the session has handed over.** e2e-green: `m22-boards.sh` green twice, one backgrounded launch over both panes, `axkit front` reading Ghostty/Chrome throughout. **Driving the shipped app found one product defect and it is the result worth having from the item** — `Read now ago`, filed as **M32**, with the structural reason it survived three green instruments. Fixed at `15185eb`, guarded at both sides of the boundary, **guard armed by reintroducing the defect**. **The lane had two defects of its own**, both recorded at the wrong line rather than summarised, and both `G4`'s class in an acceptance lane. **`m22-boards.sh` enrolled in `make acceptance`** — a lane nothing dispatches passes by hand forever while reading as covered work, and `parity-stream.sh` sat executable and unrun from `R2-R` until `P3`. Gates all run rather than asserted: `swift test` **1768/221**, swiftformat, `swiftlint --strict`, three lints, `reader-accounting.py`, `null-run-gate.py` armed, reconciler A–L clean, `parity-manifest-check.sh` 94 rows, `parity-manifest-selftest.sh` 36/0, `parity-divergence.sh` 6/0, `r7-harness-reconciliation.sh` pass, `mock-fidelity-gate.sh` both surfaces. **What it names as undoable from there**: the cross-family verify, the 34 `VOUCHED_CONTROLS` pairings against the merged table, a re-measure of `harnesses` **with the role walk re-run on that surface rather than inherited**, and the `ai/m18` obligations. **The flake is unattributed** — the test name was never captured, which is weaker evidence than a named flake and the note says so. |
| M23 | The mock-to-SwiftUI conversion contract | `M23-mock-to-swiftui-contract.md` | — | — | **Merged** `6d54ce2` — **Verified — Done** (`effect-witness`, 2026-08-21), awaiting merge. Eight bounces, closed on live-process measurement throughout. The whole trace was rebuilt from `git archive` and reproduces exactly, and `53` turned out recoverable after all — the count of processes *mentioning* the engine — so the row that called it unrecoverable now under-claims rather than over-claims. Done rather than a ninth cycle because the one false sentence found is a rebuttal on which no figure rests, where the previous block was an acceptance failure. `D-m23-bq` is the one to carry forward: `swift test --filter` reports a zero-match filter as a pass |
| M24 | The storefront's own artwork — banners and app-style icons | — | — | — | **Done** (ai/m24 → main; design-only, 23 files, all under `design/`) |
| M25 | The controls row, not the columns, set the boards' width | `M25-board-columns-do-not-flex.md` | — | — | **Done** (ai/x4 broke the min-width chain, ai/x5 flexed the two controls rows) |
| M26 | The Checks board and the design's eval board are two surfaces | `M26-checks-board-framing.md` | — | — | **Done** (ai/m26 → main; owner kept the reachability board, mock amended, DEF-031 closed) |
| P7 | `control-auth-post-http` needs a real OAuth client | `P7-auth-post-oauth-client.md` | — | — | **Done** (ai/p7 → main; parity reached 82 of 83) |
| P8 | Make `install-launchd-watch`'s `reran` term attributable | `P8-launchd-watch-attributable.md` | — | — | **Done** (ai/p8 → main; the lane was shown able to go red) |
| R9 | The SDK drops an upstream's message on -32603; the router reads it off the wire | `R9-sdk-drops-upstream-message.md` | — | — | **Done** (ai/r9 → main; DEF-047 closed, 7 tests armed 5-of-7 red, parity 82/83 0 diverged) |
| R10 | `index` prints two counts that disagree, and neither is checked | `R10-index-reports-a-write-that-did-not-land.md` | — | — | **Done** — merged to `main` at `8241e0f` (`ai/r10` `f810870`; `effect-witness`; 3 follow-ups) |
| R14 | A client's Authenticate action succeeds, and says which upstreams still need authorising | `R14-client-auth-succeeds-and-reports-upstreams.md` | — | — | **Merged** `2481e05` — **Verified — Done** (`raster-visual`, 2026-08-21), awaiting merge. The whole flow driven independently against both routers: registration 201 and idempotent, `/authorize` 200, `/token` 200, replay refused, and `401` appears nowhere. The report's discriminator proved on the verifier's own fixtures — `auth.authorized` fails in **both** directions and the page ignores it. The bypass class is clean across six confusions on both routers. Merge is held by `parity-gate.sh` redding on an unrelated row R14 cannot have caused (`D-r14-f`, 5 green 4 red over nine runs). Follow-ups `D-r14-a`…`D-r14-g` |
| R15 | The Host check guards `/mcp` and nothing else | `R15-host-check-is-scoped-to-the-mcp-transport.md` | — | — | **Merged** `2481e05` — **Verified — Done** (`effect-witness`, 2026-08-21), awaiting merge with R14. Holds across all eleven routes on both routers; `/mcp`'s 403 stays byte-identical at 97 bytes. The live foreign-Host exposure on `/health`, `/status`, `/servers` and `/usage` is closed |
| R16 | Adoption reads global scope only, so a project-scoped server is invisible to it | `R16-adoption-cannot-see-project-scoped-servers.md` | — | — | To Do — triaged 2026-08-22, scoped to **shape 2**. Both halves confirmed at HEAD: `cmdImport` reads `src.mcpServers` at `src/index.ts:85` and never `projects`, while `projects?: string[]` is modelled at `src/config.ts:21` and `:126`, carried through resolution at `:164`/`:189` and **editable through the control API PATCH** at `src/control.ts:450` — so an owner can hand-scope an upstream today and the router honours it; only learning it from the harness is missing. **One thing the brief did not know, and it shrinks the item: the report criterion 3 asks for is already built** — `skipped` is collected at `src/index.ts:93` and printed as `not adoptable:` at `:153-155` with a per-server reason, and today only ever holds servers `parseServer` refused. So the work is enumeration plus the collision decision, not enumeration plus a new report. **Shape 1 is not deferred by preference**: it decides what scope this product works in, and `D-r7-i` is its mirror image (`HarnessesVerb` prints *Global scope only* while reading a project-scoped file), so building it now would settle that from the side with less evidence. Filed as **R16-C1**. Shape 3 stays refused — it changes the tool namespace the model sees. **Owner decision 2026-08-22 — per-project scope.** `R16-C1` is answered: the router adopts and serves per-project MCP configuration rather than flattening to one global set per harness, so a server may exist as several profiles under one name (the `proctor` case that raised this). **Shape 1 is unblocked and is now the target**; shape 2 stays the first slice because it stops the false claim while shape 1 is built. `D-r7-i` is this decision's mirror and inherits it — R7's already-shipped behaviour is the global-only half and must be re-read against this, not left standing. **Known cost the owner took knowingly**: the tool namespace a model sees now depends on the directory the session started in, so a prompt naming a tool can behave differently by project. That is a documentation and diagnostics obligation on whoever plans this, not a reason to narrow the scope again. |
| R16-C1 | Adopt project-scoped upstreams and serve them by caller cwd | `R16-adoption-cannot-see-project-scoped-servers.md` | — | — | Deferred (child of R16) — registered 2026-08-22 at R16's triage. Blocked on the same scope decision as `D-r7-i` and `R7-C4`, which are its mirror image. `CallerIdentity` already carries cwd and `usage` already reports per-project call counts, so it is a decision problem rather than a plumbing one. |
| G5-C1 | Repo-owned gate wrappers for the vendored campaign scripts | `G5-vendor-the-campaign-version-the-gates-run.md` | — | — | Deferred (child of G5) — registered 2026-08-22. Nothing in this repo invokes a `test-campaign` script: every hit across `Makefile`, `scripts/` and `planning/test-campaign/bin/` is a comment naming one, and no file resolves a skill directory. The skill loads its own `scripts/` from the plugin cache, so vendoring a copy does not change what runs. Wrappers `make` could call are new surface rather than a relocation, so G5 carries the code and proves it runs by hand, and this carries the wiring. |
| R17 | A failed index leaves a recorded error for one server and nothing at all for another | `R17-a-failed-index-can-leave-no-trace.md` | — | — | Merged `54666f **Merged-tree gates re-run by the orchestrator rather than taken on report**: `make lint` **0 violations, 0 serious in 536 files**, `0/543 require formatting`; `make test` **1699 tests in 212 suites passed**, exit 0; reconciler **0 across A–L**. The merge introduced nothing. |
| R18 | A failed index drops the digest, so the next success serves a changed surface unheld | `R18-a-failed-index-drops-the-digest-that-holds-the-diff.md` | — | — | To Do — triaged 2026-08-22, plan alongside `R20` (same manifest row, opposite direction). **Confirmed by reading, and the drop is incidental rather than deliberate**: `buildManifest`'s `catch` at `src/manifest.ts:258-265` constructs a whole new entry while `prev` is in scope, and the held-diff branch four lines above spreads it — so `digest` and `tools` are both dropped, and at `:236` `!prev?.digest` takes the same branch as a matching digest. The evidence is this file's own convention rather than silence in general: every deliberate retention here carries a defending comment, the first-sight branch has a six-line one, and this branch has none. So the brief's first fix is right — **keep the last good digest**, and an absent digest keeps meaning *never seen*, which is what `:236` already assumes. **The fork that remains is the plan stage's first decision**, because `unionTools` at `src/manifest.ts:326` skips on `!entry ‖ entry.tools.length === 0` and **never reads `entry.error`** — the zero-length list is load-bearing and doing the job the error field looks like it should do. Digest-only leaves a seen-then-failed server held against an empty approved set; digest-and-tools with serving gated on `error` is correct for the right reason and reaches `router.ts:168` and `:347`. Criterion 2 is what separates them, so arm it against a never-seen server **and** a seen-then-failed one. **Fork settled 2026-08-22 by the Google lane (`agy`/`gemini-3.7-flash-high`), not by the owner** — it was technical and evaluable with the evidence in the brief. **Verdict: shape B** — preserve `digest` **and** `tools`, and gate serving on `error` rather than on `tools.length === 0`. Reason given: a diff-and-approval system needs an accurate baseline, and shape A reduces `prev.tools` to `[]`, turning a precise incremental diff into a full re-approval of the whole surface. What shape A is better at, stated rather than dismissed: **blast radius and a failsafe default** — it never touches the serving path, and an empty array physically cannot serve a stale definition however the downstream logic breaks. **A third shape the lane rated above both, and the plan stage should cost it first**: split the row into a persistent `baseline {digest, tools, approvedAt}` never wiped on an index failure, and a transient `runtime {status, error, lastAttemptAt}`, with `unionTools` gating on `runtime.status === 'healthy'`. That removes the cross-talk with `R20`, which writes the same row in the opposite direction. **The failure mode B introduces, named**: a *permanent silent blackout* — if a later success writes `digest` and `tools` but never clears `error`, the server is healthy, valid and permanently unservable. The lane was explicit that this argues for the status enum rather than back to A, because using `tools: []` as a proxy for an error boolean is the anti-pattern that caused this defect. |
| R19 | A watch fire saves a manifest snapshot taken before a concurrent write | `R19-a-stale-manifest-snapshot-clobbers-a-concurrent-write.md` | — | — | Done — merge held as R19-I **Verified Done 2026-08-22.** Oracle rung **behavioural, cross-process, on the real binaries** — the verifier re-armed and reproduced the defect itself and **timed the overlap rather than assuming it**. Gates on the branch: lint **0 over 533**, `make test` **1687 in 210**, `parity-cli.sh` **17/0**. Three gate numbers that moved are each shown outside this item's diff. **The reconciler does not reproduce the check-E finding** — exit 0, clean across A–L, 26 merged branches — because `ai/g5`'s tip is no longer an ancestor of `main`; better than predicted and for a reason that is not this item's. **Merge ordering answered, and the orchestrator had already merged R17 first**: the verifier's advice was R19-first precisely because `surface.tsv` **auto-merges**, so R17's stale `cli-watch` note survives silently. Taken up as **R19-INT** rather than resolved at the merge, because the two code conflicts are **records rather than logic** and R19's base predates R17, so its side never chose to drop R17's route account. **Unsettled and declared, none blocking**: `buildManifest` still decides staleness from the outer snapshot, so a non-forced run can skip a server another process invalidated mid-run; the watcher's in-lock `delete` of a failed row can still remove a good row another writer just wrote for that name, identically on both routers and predating this item; and node now has a lock module while node-side `servers.json` (`watch.ts:311`, `control.ts:105`) still takes nothing where Swift locks it — a real asymmetry this item's scope correctly excludes and that is now cheap to close. **Verified Needs More Work 2026-08-23 at `ce980ef` (parent `eb3e42c`); gap-fix 1 dispatched — records only, no code.** **The merge is clean**, so the *a conflict is a finding* branch never fired; the verifier merged, measured and discarded, and the tree reproduces from `eb3e42c + ed37a30`. Gates re-measured on that tree: lint 0/552, `make test` **1728/216**, `parity-cli` 18 verbs 0 disagreed with all four `cli-watch` green, `parity-overlap` 2/2, reconciler clean A–L, `claim-sweep` exit 1 on 11 blocking hits **all inherited from `main` in four byte-identical files**. **What moved the verdict is arm 1 splitting**: restoring the node-side delete reddens `parity-cli` with `surface.tsv`'s recorded string verbatim and leaves `IndexFailureRecordTests` **green**, because that suite drives the Swift watcher and never executes `src/watch.ts`; arming the Swift side separately fails there with 11 issues. So the resolution is right and **the justification is false** — `planning/progress/R19-integration.md`'s *would have failed both* claims one instrument pair where **each side is pinned by one instrument**. The corrected claim is *stronger*, which is why the gap-fix asks for a rewrite rather than a hedge. Three smaller findings: `surface.tsv`'s `cli-watch` row cites `WatchIndexing.swift:206` where the save is at **`:209`** (and the file is under `RouterCore/`, not `MCPRouterKit/`); the gate-race account omits that `parity-oauth.sh` **runs twice per gate** with fail winning; and arm 5 points at `B1`/`B2` wording that `claim-sweep` excludes from its exit code. **Arm 4 came out against the record**: three full gates read red, green, green against the four-of-four on file, and the only red was at load 400 with greens at 175 and 23 — **gate and machine are confounded in both datasets**, reported rather than chased, and this cell has now stated a reproduction rate the next measurement contradicts **four times**. **Merge deferred by orchestrator decision**: R16/R18/R21 wait on this, and merging a record that asserts an instrument relationship which measurably does not hold — to save a two-sentence fix — would undo the day's work on exactly that defect class. **Gap-fix 1 delivered 2026-08-23 at `8294df4` — documents only, ready to verify.** The blocking paragraph now says each side is pinned by exactly one instrument and names which: `parity-cli.sh` pins node and **stays green under a both-side regression, being differential**; `IndexFailureRecordTests` pins Swift and **is absolute, so it catches the case the differential lane cannot**. The runner verified that statically rather than on report, and quotes the withdrawn sentence with how it was disproved. All three non-blocking items in: `surface.tsv` now cites `app/Sources/RouterCore/Watch/WatchIndexing.swift:209` **with the call text attached**, and it re-read all six sites, giving two more their paths — `ManifestIndexer.swift` is under `Service/` and `AuthRoutes.swift` under `Auth/`, **not the `Indexing/`/`HTTP/` a reader would guess**. DEF-033's double-run is named with fail-wins precedence. The claim-sweep section proves inheritance **the drift-proof way** — `git diff --name-only adfa923..eb3e42c` lists none of the four files. Arm 4 recorded as confounded with **no fifth rate**. **Three corrections to the verdict, all recorded**: its *four of the eleven hits live in the features-to-triage copies* is **seven**, counted off the script's per-class output; `ledger-reconcile.py` exits **1** on this branch (check E, `ai/g4b`) because `eb3e42c` predates main's G4-B row — branch staleness that clears on merge, and the verdict's exit 0 was on the merged tree; and `make lint` **ran** here rather than failing at `tools`, because this worktree already carries `node_modules` and `dist/index.js` — so the standing dispatch note is right that it *can* fail and wrong that it always will. `main` moved twice during the work and the blocking-pattern count was re-run against both, 11 each time. **Gap-fix 1 verified Needs More Work 2026-08-23 at `f3a14b6`; gap-fix 2 owed — records again.** **The gap-fix kept its records-only promise**, verified as git tree objects rather than file listings with a presence control on both instruments. **It fails on the very claim it was dispatched to fix.** The verifier completed the 2×2 whose bottom-left cell had never been run: the **Swift `removeEntry` arm reddens `parity-cli` too**, with the exact mirror of the node arm's diff on the same scenario row — because a differential harness sees a one-sided regression whichever side it is on. So **the Swift side is pinned by two instruments**, and the replacement sentence *`parity-cli.sh` is not what catches that one* is false, **four lines above a sentence that predicts exactly this** — `:32` to `:36` on `8294df4`, measured wrap-tolerantly because the withdrawn claim itself spans `:31-32`. **This row previously said thirteen, and that was the orchestrator's error**: the verdict states *both* — four in its opening paragraph and thirteen in F1 — and the brief carried thirteen forward into this row and into commit `01b6ef9`. The gap-fix runner caught it and correctly filed it as the orchestrator's, since this file was being written concurrently. **The orchestrator's first attempt to check it also failed**, on a line-anchored grep that could not span the claim's own hard wrap — the third time that instrument has misfired here in one day. The error **under-claims** coverage, so nothing is less guarded than the record says; it is red because the item's whole scope was that paragraph, it was dispatched to fix this defect class, and the row recorded the claim as verified **statically**. Replacement text and the matrix are in the verdict. **All three self-corrections hold** — the sweep count is 7 with the normaliser named, all eleven sites listed, and a planted control firing +5 including an instance split across a hard wrap. **Two things it reported rather than smoothed**: the worktree was handed over dirty with a previous verifier's Swift arm live and uncommitted, pasted into the verdict before gating; and **the only out-of-family lane that answered says Done with an erratum** while the in-family fable lane agrees with the red — stated that way round *because dressing that up would be the same failure I'm filing*. It declined `parity-gate.sh` because that is the instrument that would produce a fifth rate for the oauth cell, at the stated cost of having no whole-parity figure of its own. **Gap-fix 2 delivered 2026-08-23 at `2793574` — one file, 239 insertions / 19 deletions, no code, ready to verify.** The paragraph now says each side is pinned by **at least** one instrument and that `parity-cli.sh` catches both, with the completed 2×2 at `:46-51` and both mutation strings quoted in full — `0a1,2 >` for the node arm, `1,2d0 <` for the Swift one, **same scenario, same two servers, opposite sides**. The withdrawn text is cited by anchor, tree and line with its blob `af1dcb98`, **re-derived rather than taken from the verdict**, and gap-fix 1's section keeps its account as history with the false half marked withdrawn. **Two things it measured instead of transcribing.** The four-versus-thirteen correction above. And it **narrowed the verdict's ready-to-paste claim** — *`parity-cli.sh` … is the only instrument that does* becomes *of the two instruments*, because arm A was run against those two and **never against `make test`'s 1728**, and *inheriting an unmeasured exclusivity from a verdict would be the same move that produced this paragraph twice*. **Why it got through twice, stated plainly**: the ledger recorded gap-fix 1's claim as verified **statically**, and a static read cannot catch a claim reasoned from the very differential property that refutes it. **Gates on the merged tree** (`main` at `01b6ef9`): lint **0/552** with main's two new gates armed and green, `make test` **1728/216**, `parity-cli` **18/0** all four `cli-watch` green, `parity-overlap` **2/2**, manifest-check **94 rows**, **reconciler exit 0 clean A–L** — the `E — G4-B` finding cleared by the merge as the brief predicted — and `claim-sweep` exit 1 on the same inherited 11, byte-identical but for its own `A5` moving `:82` → `:110`. **Every gate ran twice**: the first pass piped its output and lost the exit codes, *because zsh does not populate `PIPESTATUS`* — this repo's own recorded hazard, caught by the runner on itself. **Declined and stated**: `parity-gate.sh` not run, inheriting the verifier's trade and its cost, and no fifth rate for the oauth cell. `src/`, `app/` and `scripts/` byte-identical to `eb3e42c` as tree objects; only `Makefile` moves, twelve lines from main's `50f3e8f`. |
| R19-INT | The R19/R17 join: three records to merge and a declaration to delete | `R19-integration.md` | — | — | Ready for AI — raised 2026-08-22 after the orchestrator aborted R19's merge deliberately. Two code-adjacent conflicts (`src/watch.ts`, `WatchIndexing.swift`) that are **records rather than logic**, plus `surface.tsv` **auto-merging** a declaration R19 makes false in three claims. R19's base predates R17's merge, so its side never chose to drop R17's route account — including the pre-registered prediction that an R19-only world predicts the inverse of what was measured, R17's strongest single piece of evidence. `parity-cli.sh` has never been run with R17's fourth scenario against R19's watcher. |
| R21 | `approve` answers 200 while discarding the write that would make it true | `R21-approve-answers-200-over-a-discarded-write.md` | — | — | Ready for AI — filed 2026-08-22 while re-measuring M28's DEF-049, which had two halves fixed and a third predicted. **The prediction is refuted and the plainer defect survives it.** `AuthRoutes.swift:120` is the last of DEF-049's three `try? ManifestIO.save` sites; the route sets `tools`/`digest`/`builtAt`, removes `pending`, discards the write and returns `(200, approved: N)` unchanged — so a refused write leaves the surface held while the caller is told it was approved, and `approved` is counted from the pending entry **before** the write. DEF-049's louder prediction (a stale `builtAt` fires `Describe.swift:218` and `/servers` reports `authorized: true`) **does not follow**: approve is reachable only with `pending` present, and both implementations clear `error` when they stage one — `src/manifest.ts:246` and `ManifestBookkeeping.swift:83` — so the `guard case let .string(text)?` at `Describe.swift:208` fails first and `:218` is never reached. The fix shape is already in this repo at `ServicePorts.swift:388-394`: catch, carry a `cacheFailure` into the response. **Do not move the status code** — `ControlApproveDispatchTests.swift:114-118` pins 200 deliberately. |
| R20 | A staged entry wipes a same-named healthy upstream and blames it for the failure | `R20-a-staged-entry-misattributes-its-error-to-a-healthy-upstream.md` | — | — | Untriaged — the one surface R17 makes **newly wrong**, measured by its verifier: a healthy `db` serving 2 tools, then a broken `db` staged, gives `/servers` an `indexError` of `spawn /nonexistent/not-a-server ENOENT` for a server whose configured command is `node`, where pre-fix it read `error: None`. The tool loss is not a regression — the old delete wiped the surface too — but the misattribution is. R7's duplicate class is the precondition, so decide it there rather than patching where the error string is filed |
| X1 | The iOS accessibility-tree harness, and two surfaces still empty | `X1-ios-a11y-harness.md` | — | — | Done (closed by X3's engine fix + the accessibility-frame row oracle; `make test-ios` 36/0) |
| X2 | The iOS on-glass instrument, and the six cases it takes off `n/a` | `X2-ios-on-glass.md` | — | — | **Done** (ai/x2 → main; lane-owned device, six green runs) |
| X3 | The iOS unit lane read an empty accessibility tree because the engine was off | `X3-ios-unit-lane-empty-tree.md` | — | — | Done (DEF-029 closed, armed three ways) |
| X6 | Cleanup's `Read first…`, the half DEF-011 was held open for | — | — | — | **Done** (ai/x6 → main; CASE-0135/0136/0137, nine mutation arms) |
| M27 | The sidebar foot's loopback readout and the child-process label | `M27-sidebar-foot-readout.md` | — | — | **Done** — merged to `main` at `cbe5cc3` (`ai/m27` `26337b8`; rung `raster-visual`; 6 follow-ups `D-m27-a`…`D-m27-f`, none blocking) — both restored, on glass 8 of 8 boards. The count card announces as ONE element — `Child processes, 1 of 4 declared servers running` — after a reversal: the branch first shipped it as two stops because A35's readout line was anchored whole, and three model families independently found that anchor was written before the label existed. A35 widened to its own stated prefix tolerance. The mock's `--live` foot dot deliberately not, and the same argument convicted the count numeral, which was painting `--live` on `0 of 4`. `mac-shell.sh` exit 0, 1552 tests, evidence in `planning/evidence/M27-acceptance.md` and `planning/evidence/M27/` |
| M28 | Five findings that need a decision rather than a runner | `M28-decision-docket.md` | — | — | Done — all five dispositioned 2026-08- **Closed 2026-08-22.** DEF-057 answered by the owner — vendor `test-campaign` only — and filed as **G5**. Four of five never reached the owner: two had been answered by work that landed after the docket was written, one was the orchestrator's to take, and DEF-033 needs nothing. The two that became work are **R21** and **G5**. |
| M29 | "Disable a server" is drawn with nothing behind it | `M29-disable-a-server-is-drawn-with-nothing-behind-it.md` | — | — | Untriaged — filed 2026-08-22 by M18's runner while inventorying every sheet in the design of record. `ServerPatch` carries **no field that disables a server**, the control API's PATCH is the Mac app's only channel, and **no ledger row claims the behaviour** — not R7's, not R16's, not M22's. M18 declined to invent it rather than shipping a control that does nothing. **This is a decision before it is a build**: disabling is a third state between adopted and removed and the product has two, so it has to answer whether a disabled server keeps its manifest row and digest (which `R18` and `R20` both write), whether `unionTools` skips it by a new flag or by the same emptiness test `R18`'s verdict has just established is doing the error field's job, and whether it is per-harness or global — **the owner's per-project decision makes 'disabled here, live there' expressible and probably intended**. Do not close it by deleting the row from the gate table: that table is generated from the design of record, and deleting a row to make an inventory agree with an implementation inverts the check. |
| M30 | Where a capability document actually comes from | `M30-capability-document-source.md` | — | — | Untriaged — filed 2026-08-23 by M19's runner as **M19's real exposure, written up rather than buried**. Allocated `M29` first and **renumbered to M30 on the orchestrator's collision**, `M29` having landed on `main` at `78a27e7` 18:47 while this runner was down in the crash window. **A trap the renumber avoided, worth carrying**: four other `M29`s live in this tree — two in `spec-F3.md`, one in `G3-gapfix-2.md`, one in `scripts/red-green.py` — and **every one is a mutation id in a red-green table, a different namespace with the same spelling**. A tree-wide `sed` would have renumbered a red-green record and **nothing would have gone red**, because those tables are prose to every gate we run. The rename named its five files explicitly instead. |
| M31 | The design of record cannot draw a disabled primary | `M31-the-design-of-record-cannot-draw-a-disabled-primary.md` | — | — | Untriaged — raised by `lukerhodes-2f` from M18's gap-fix, **verified independently by the orchestrator**. `design/mcp-router-console.html` sets `.btn:disabled` and `.btn.primary` at the **same specificity (0-2-0)** with `.primary` declared **second**, so it wins on `background`, `border-color` and `color`: a disabled primary keeps its accent fill, accent border and `--on-accent` label and differs from a live one **only by losing its shadow**. `DESIGN.md` requires *every control* to carry a disabled state and says *disabled dims in place*, so **the design of record cannot draw the state the design authority requires**. Not merely undrawn — drawn **as though enabled**, which is the exact failure the rule names. **It also inverts the instrument**: `M23`'s conversion is mock-driven and `mock-fidelity` compares a build to the frame the mock draws, so a build that correctly dims a disabled primary would be **reported as a divergence** — the gate currently rewards reproducing the defect. Reaches M18 (built, same defect from the same cause in `ProminentButtonStyle`), M19 and M22, and **none of them owns it**. Triage must settle what *dims in place* means for an accent-filled control in `DESIGN.md` itself, and sweep for built surfaces that already reproduce it. |
| M32 | A mock-driven gate is blind to what the mock does not draw | `M32-a-mock-driven-gate-is-blind-to-what-the-mock-does-not-draw.md` | — | — | Untriaged — filed 2026-08-23 from M22's app-driving pass. **The Harnesses board rendered `Read now ago`** because `shortAgo` returns the *word* `now` under five seconds and a duration above it. **It survived all three green instruments, structurally**: no test asserted the string; the board is new so no earlier rendered pass existed; and **M23's fidelity gate compares build copy against the mock, which has no freshness line at all** because that line is M22's own addition — **so there was nothing on the other side to disagree with**. `mock-fidelity` is strong on everything the mock draws and **has no opinion on what the build adds, and no opinion reads exactly like agreement**. So *clean over 193 nodes* is true and means only *nothing the mock draws diverges* — not *the surface is right*. **Drawing it in the mock closes the instance, not the class**: every conversion adding a build-side affordance the design never specified re-opens it, and M19's `path` refusal and M18's three undrawable readings are legitimate cases of the same. The remedy is that **an `extra` needs an oracle rather than only a citation**. **The same run found two defects in its own lane, same family**: a sentinel `grep` for `isn't built yet` matched the board's own disabled-control help tag (legitimately reused `CommandAvailability.surfaceAbsent` copy), and an `awk` read AX fields 5 and 7 where SwiftUI puts the label in AXDescription (6) and `enabled` is field 8 — **reporting *no Reconcile control is drawn disabled* about four that were**. *A gate reading the wrong column says the product is wrong.* |
| M33 | `swift build` exits 0 and silent on a fault `xcodebuild` calls fatal | `M33-swift-build-cannot-gate-the-app-target.md` | — | — | Untriaged — filed 2026-08-23 from M20's verifier F5, **rescued from a verdict that had no destination for it**. Measured with a **two-way planted control** at `1d9ffad`: one fault, `swift build --build-tests` exits **0 and silent**, `xcodebuild` exits **65 and fatal**. `app/Package.swift` declares **no target at `MCPRouter/`** while `app/project.yml:42-43` does, so **the SwiftPM lane cannot compile the file the app's assembly lives in** — every green `swift test` on a branch touching `app/MCPRouter/` is *silent about* that directory rather than clean over it. **The verifier names it as the mechanism behind M18's recurrence** and the fit is exact: M18's tree had also never compiled while its runner reported gates it believed meaningful. **The eighth entry in the instrument-that-cannot-fail register and the one with the widest blast radius**, because `swift test` is the gate almost every brief in this fleet names first. Nobody has swept for other branches whose green covered nothing under that directory. |
| M34 | The menu badge is unmeasurable by any lane we have | `M34-the-menu-badge-is-unmeasurable-by-any-lane.md` | — | — | Untriaged — filed 2026-08-23 from M20's verifier F4, **rescued from a verdict that had no destination for it**. **Evidence work rather than product work**: the behaviour may be correct and nothing available can say so. The badge answers the brief's *dimmed in place with the reason in the shortcut column*, and **the only thing reading it is a unit test over a menu the test itself built**. Four lanes closed, each measured: the accessibility tree exposes **no badge attribute** (19 attributes per item, five direct probes absent, `AXHelp` correct so the instrument is sound); `mac-shell.sh` has **no menu-badge assertion** and **every `badge` hit in it is a pre-existing sidebar-row badge**, so a grep reports coverage that does not exist; `AXPress` on a menu item returns `.success` **and does nothing** in a background app; and photographing it is what `UI_VERIFICATION.md` rule 1 forbids. **Six items carry a chord and a badge at once and nothing measures which the trailing area shows.** |
| X7 | The campaign's published artifacts under-report what it knows | `X7-campaign-artifacts-underreport.md` | — | — | Untriaged (**upstream**: fledgeling-plugins, not this repo) **Owner decision 2026-08-22 — fix upstream in `fledgeling-plugins`, then re-pull the vendor.** G5 vendored `test-campaign` 0.9.2 to `vendor/test-campaign/` byte-identical to upstream `28ecd67`, with a tree checksum in `vendor/README.md`; patching the copy here would restate that as *0.9.2 plus N patches* and cost the reproducibility property G5 was commissioned to buy. **The cost taken knowingly**: this repo's gates keep running the known-wrong detectors until the vendor is re-pulled, and X8's are findings the detectors cannot support — so a campaign run before the re-pull carries them and must say so. |
| X8 | Two campaign detectors report findings they cannot support | `X8-detectors-misattribute.md` | — | — | Untriaged (**upstream**: fledgeling-plugins, not this repo) **Owner decision 2026-08-22 — fix upstream in `fledgeling-plugins`, then re-pull the vendor.** G5 vendored `test-campaign` 0.9.2 to `vendor/test-campaign/` byte-identical to upstream `28ecd67`, with a tree checksum in `vendor/README.md`; patching the copy here would restate that as *0.9.2 plus N patches* and cost the reproducibility property G5 was commissioned to buy. **The cost taken knowingly**: this repo's gates keep running the known-wrong detectors until the vendor is re-pulled, and X8's are findings the detectors cannot support — so a campaign run before the re-pull carries them and must say so. |

| I5 | Prove the phone↔Mac pairing round trip, and stop there | — | — | — | **Merged** `4157bc4` (ORCHESTRATOR.md) |
| I6 | Make Mac approval fast, without moving the boundary | — | — | — | **Merged** `ef4f615` (ORCHESTRATOR.md) |
| M9 | Rename the `Evals` destination to `Checks` | — | — | — | Done inside D2 (`9e8a754`) — triaged 2026-08-21 |
| M10 | Amend `DESIGN.md` §6:279–280 | — | — | — | Done inside D2 (`9e8a754`) — triaged 2026-08-21 |
| M11 | Regenerate the M1 command inventory | — | — | — | **Merged (partial)** `2a434b9` — promoted out of the deferred register |
| M12 | Staleness and an as-of time inside a destructive dialog | — | — | — | Ready for AI — triaged 2026-08-21, measured still open **Ready to verify 2026-08-23 — 12 commits, `0f5f118`, tree clean, survived the crash with nothing redone.** Arrived with **no brief and no spec**, so the item wrote its own: brief, `spec-M12.md` (C1–C13), plan, acceptance and progress. Gates all re-run *after* the crash rather than quoted from before it: `make lint` 0 (swiftlint 0/554, null-run-gate 28 armed / 0 held), `make test` **1743 in 217** (+18 on main's 1725/215), `make build-mac` succeeded, `m7-evals-cleanup.sh` 24 passed **green twice** with MCP Router never frontmost, reconciler 0 A–L, red-green **six mutations six reds each reverted**. **Three things it declines to claim**: the stale marker is proved at model and value layers and **not** by a rendered pass (`LoadState.stale` needs a fixture that answers once then refuses; `FixtureControlAPIClient` is a stateless struct) — *do not grade that row off the acceptance run*; one acceptance run failed the invisibility invariant then did not reproduce twice, and **could not have been attributed either way because `axkit front` compares an app NAME, so any MCP Router build on this machine fails that gate identically — which reaches every worktree this fleet runs**; and `make all`/`parity`/`test-ios` were not run. **Four items routed, none blocking**: Activity's reset dialog states `records.count`, a **500-capped ring window**, where Cleanup states the unbounded tally for the *same endpoint* — on a router with 812 calls the two dialogs say **812 and 500**, and `usage.reset()` discards both plus two log files (**the out-of-family lane recommended folding it in and the runner refused with the router source**); `make build-mac` passes no `-onlyUsePackageVersionsFromResolvedFile`, so a fresh worktree floats a transitive pin — its `eventsource` floated 1.4.2→1.5.1 under an SDK pinned exact, tripping the freshness gate; a poll where `servers()` succeeds and `usageSummary()` throws **silently drops the call count with no Partial marker**, on `main` too; and `SkillProvenanceSheet` draws body text in `--attn` at **2.31:1 on light**. **Lane note for the verifier**: codex is limited to 27 Aug and grok returns 402, so only `agy`/gemini-3.7-flash-high answered — **twice**, and its plan review killed the design's relative timestamp, so gemini has seen this design twice and is **the weakest choice for an independent verdict**. |
| P5 | Close the last three closeable parity rows | — | — | — | **Merged** `e752305` (ORCHESTRATOR.md) |
| P6 | State the owner's cutover target in the gate | — | — | — | **Merged** `05296ea` (ORCHESTRATOR.md) |
| R2-R | Router: the process that actually serves | — | — | — | **Merged** `62678aa` (ORCHESTRATOR.md) |
| R2-W | Router: the `~/.claude.json` watcher and its adoption protocol | — | — | — | **Merged** `8e48a80` (ORCHESTRATOR.md) |
| R4-C | The installer cutover | — | — | — | **Superseded** — split into R4-C1 (Done, `ai/r4c` merged) and R4-C2 (Held). Not blocked: each half has its own state and its own row. The owner's target is 82 of 83 |
| X4 | Mac boards: six defects the design of record names | — | — | — | **Merged** `2ff0941` (`ai/x4`) — its work is written up under M25 |
| X5 | Discover and Skills: the controls row set the board's width | — | — | — | **Merged** `dee20da` (`ai/x5`) — its work is written up under M25 |

## Allocation notes

- **Statuses were synced from ORCHESTRATOR.md on 2026-08-21.** Twenty-six rows here read
  `Untriaged` for work that had already merged — R2, R3, R5, M1–M8, I1–I3, P1–P4, D1–D3, F4,
  G1, M13, M14, V1 and R4. A fleet reading this column would have dispatched twenty-six shipped
  items. Membership and status drift separately: `ledger-reconcile.py` catches the first and
  cannot see the second, because both files having a row for an id says nothing about the two
  rows agreeing. When you change a status, change it in both files or the next fleet re-plans
  the work.
- **M3 was triaged on 2026-08-21 and it had shipped.** The earlier note here recorded it as
  unresolved rather than open — its row said `Untriaged` with no branch while M7's dependency
  cell read `M3 ✓ M4 ✓` — and refused to guess which reading was right. The answer is the first
  one: `589ab2e` ("the breaker board — Servers is the first pane that is actually built"),
  `3b11f33` and `af77200` are all ancestors of `main`, six `ServersBoard*.swift` sources are in
  the tree and seven test files name the board. M7's tick was correct and both rows were simply
  never updated. Scheduling it would have rebuilt a shipped board — which is the cost this note
  existed to prevent, and the reason a status a fleet cannot explain gets triaged rather than
  assumed in either direction.

- **`R7` was two different items, and both ledgers reconciled clean the whole time.** This file
  carried `R7 — the router's thesis is unmet for every harness but Claude Code`, with a brief on
  disk named for it. `ORCHESTRATOR.md`'s deferred register carried a different `R7` — the skills
  write endpoint, a child of R3 filed by M7. Two items, one id.

  `ledger-reconcile.py` could not see it, and the reason is worth keeping: checks A and B ask
  whether an id appears in *both* files, and it did. **Membership and identity drift separately,
  the same way membership and status do.** Check `F` was added for this — it compares the two
  description cells for an id present in both files and reports a pair sharing no content word at
  all. That bar is deliberately low: a legitimately reworded row nearly always keeps its subject
  noun, so requiring *zero* overlap is what stops the check firing on a correct use.

  Resolution: the top-level ledger item keeps `R7`, because a brief file carrying the id in its
  filename is the stronger claim. The deferred child became `R11` and now has a row here of its
  own. It is still open — `CleanupSheets.swift:204` draws `DisabledAction(label: "Remove", …)`.

  **Check F then found two more on its first run, and the second one exposed a defect in the
  check itself.** `R8` was the merged auth-rejection item here and *server soft-delete with a
  restore endpoint* in the deferred register; it became `R12`. `R6` was the child-PATH item here
  and *a router-side behavioural eval runner* in the deferred register; it became `R13`.

  `R6` is the one worth keeping. The first version of check F read **one row per file**, and
  `ORCHESTRATOR.md` carries two `R6` rows — the child-PATH item in the wave table, and the eval
  runner in the deferred register. The row that agreed with this file was simply the earlier one,
  so the check reported clean. A collision inside a single file is the same defect as one across
  two, and a predicate that reads one row per file cannot see it. F now compares every row for an
  id against every other, whichever file each came from.

  **Fixing the collisions created a fourth defect, and check `G` was added for it.** Renumbering
  the deferred `R8` to `R12` left the *merged* `R8` — the auth-rejection item on `ai/r8` — with
  no ORCHESTRATOR row at all, because that had been its only one. Check B did not fire: B clears
  on an id being **named** anywhere, which is the right bar for "does the other file know this
  exists" and the wrong one for "can a fleet resume from that file". G asks the narrower
  question, and found `R8` plus `X4` and `X5`, two merged branches this reconciliation had given
  rows here but never there. All three now have rows. Both tables stand at 77.

  The sequence is the point: six checks reported clean over a file with three id collisions and
  three missing rows. Each new check found something on its first run, and one of them found a
  defect the previous fix had just introduced. A reconciliation is not a state you reach; it is a
  claim that only holds for the predicates you have written down.

  **Then eight checks reported clean over five stale duplicate rows**, found by `dev-09` rather
  than by this script. `ORCHESTRATOR.md` carried two rows for `M23`, `M27` and `R10` — each pair
  agreeing on the title and disagreeing on the status, with the stale one reading *earlier* than
  the truth: `Untriaged` beside `Ready to verify` for two branches sitting at 6 and 12 commits.
  A fleet slot filled from the stale row dispatches a triage run on work that is already built.

  None of A, B, F or G could see it. Their diagnosis is worth keeping verbatim: **each check
  tests a different key, and a row is only as bound as the weakest key any check uses.** A and B
  test membership, F identity, G resumability — and none tests currency, so a present, plausible,
  wrong row satisfies every one of them. That is worse than a missing row, because a missing row
  fails membership and a stale duplicate fails nothing.

  Check `H` groups rows by id and compares status cells. It indexes the column by its **header
  name** rather than its position, which is what keeps it honest: this file has one nine-column
  table carrying `Status` and a four-column deferred register carrying none, so a positional
  predicate would have compared `M11`'s register row against whatever sat at that offset. H found
  the three plus `R4-C`, whose two rows read `Blocked — needs 82 of 83` and `Wave 4 — last` — a
  wave label written into a status cell, both stale, both quoting a parity figure since
  superseded. R4-C is now one row: superseded by `R4-C1` (Done, `docs/install.sh` defaults to
  `MCPR_ROUTER=swift`) and `R4-C2` (Held).

  Fixing it also turned up `M13`: a complete nine-column row pasted into the four-column register,
  where its cells read as *Absorbed by: mac* and its outcome as a dependency list.

  **H then had the same defect it was written to catch, one level down.** `dev-09` asked what the
  parser does with a row whose field count does not match its header — and the answer was that it
  skipped it, silently. Measured: **70 rows examined, 23 skipped**, all of them four-cell
  deferred-child rows interleaved in the nine-column table with no header of their own. None of
  the 23 can disagree about a status it has no cell for, so the finding survives — but a check
  that quietly discards what it cannot parse reports clean over a subset, which is the same
  denominator failure as a campaign publishing a pass rate over the surfaces it happened to reach.

  The script now prints `H examined N rows; skipped K` on **every** run, names the skipped ids,
  and returns **2** rather than 0 when `examined == 0`. A gate that never ran is not a gate that
  passed, and the one way to reach a false clean here was for the table shape to change under it.

  A row with *more* cells than its header is placed rather than skipped, and correctly: a stray
  `|` shifts only the cells after it, so every column before the offending pipe keeps its index.
  `D3` is the live case — 11 cells against a nine-column header, `Status` still landing on
  `**Merged** \`67ae4f5\``.

  H is armed three ways: the unmutated file is clean at exit 0; a stale duplicate row injected for
  `M27` makes it name both line numbers at exit 1; and renaming the `Status` header to `State`
  makes it exit 2 saying it measured nothing, rather than reporting a clean file it could not
  read.

- **M9, M10 and M12 were triaged on 2026-08-21 by measuring the tree, not by reading their
  rows.** Two of the three had already shipped. `M10`'s amendment is in `DESIGN.md` §6, which now
  carries the correction *and* the reason — the old illustration "a skill with no evaluation reads
  'not evaluated'" named a state the product cannot be in, because there is no eval runner
  anywhere in it. `M9` is closed in `Destination.swift`: `.evals` reads `Checks`, and the
  `rawValue`, `iconName` and `?pane=evals` slug stay `evals` **on purpose**, documented in source
  — they are identifiers held in frame restoration and in every mock link, and `DESIGN.md` §6
  governs words a user reads rather than keys a machine matches. `M12` is the one still open:
  `CleanupSheets.swift` draws its destructive "Remove <name>?" dialog with a consequence figure
  carrying no staleness marker and no as-of time.

  Three items, two of them already done — the same shape as the M3 note above. A deferred
  register records what was *filed*, and a later item closing it does not write back. So a row in
  this table is a claim about the past; check it against the tree before scheduling from it.

- **Thirteen rows were added on 2026-08-21 that this file had never carried.** Eleven were
  named only in this file's prose or only in ORCHESTRATOR.md; two — X4 and X5 — were merged
  branches recorded in neither. An allocator scans the table, so a mention in prose does not
  stop an id being reissued: that is how I6 came to be allocated for a new brief while it was
  already merged at `ef4f615`. Run `planning/ledger-reconcile.py` after every allocation; it
  refuses in both directions and has six predicates, because each is blind to the others.

- **This file is not the only allocator, despite the header above.** Measured 2026-08-21:
  `ai/i5`, `ai/x4` and `ai/x5` exist as branches, `I5` holds a live worktree, and
  `ORCHESTRATOR.md` carries rows for **I5 (Merged `4157bc4`)** and **I6 (Merged `ef4f615`)** —
  none of which appear in the table above. ORCHESTRATOR.md is the live ledger; this file has
  drifted behind it. Check both, plus `git branch --list 'ai/*'`, before allocating. Two
  sessions independently reached for an id that was already taken on the same evening, which
  is what this note exists to stop.
- **X4 and X5 are taken** (branches exist and both are merged into `main`: `2ff0941`, `dee20da`),
  as are I5 and I6. New instrument work continues at X7 rather than reusing them, so an ID never
  means two things. This line used to reach for M9–M12 as the example of an unfilled gap; that
  comparison was false in both halves and the note below says why.
- **X7 and X8 cannot be closed from this repository.** Both are defects in `test-campaign`
  0.9.2, which lives in the machine's plugin cache. The vendored submodule carries 0.5.0 and
  does not contain the scripts at all (DEF-057), so a runner in an mcp-router worktree has
  nothing to edit. They are listed here because the campaign found them and the evidence
  should not be lost; closing them means a change pushed to `fledgeling-plugins` and a
  submodule bump.
- **M28 is a decision docket, not work.** A ship-feature runner should skip it. It closes by
  the owner answering four questions, after which each answer becomes an ordinary item.
- **DEF-001 and DEF-041 did not get a brief.** The pairing transport is already specified by
  M6 (Mac side) and I1 (phone side), both Untriaged. The campaign's measurements are appended
  to those two rather than duplicated into a third.

- **M9–M12 are all allocated, and this line used to deny it.** It read "unused … never allocated;
  the M series jumps from M8 to M13", which is false for all four. They are ledger items in
  **ORCHESTRATOR.md's deferred register, lines 241–244**: M9 renames `Evals` to `Checks`, M10
  amends `DESIGN.md` §6:279–280, M12 covers staleness inside a destructive dialog. **M11 was
  promoted out of that register and merged at `2a434b9`** (an ancestor of `main`). M9, M10 and
  M12 carry no branch and have not shipped. New Mac work continues at M24 rather than filling
  the gap, so an ID never means two things.
- **A range in a note hides the id inside it.** "M9–M12 are unused" absorbed a merged id and read
  clean in every membership check over this file, because nothing searches for `M11` inside the
  string `M9–M12`. Expand ranges before reconciling two id tables; a range is the one notation
  where a reconciliation reports agreement it never tested.
- **M23 blocks M15–M22.** It specifies how a mock-to-SwiftUI conversion is proved. Converting a
  board before the measurement layers exist produces a build that looks right and cannot be shown
  to be, which is the failure the brief's sources were written from. New Mac work continues at M24.
- **The skills the pipeline depends on are vendored.** `.claude/plugins/fledgeling-plugins` is a git
  submodule tracking `main`, so a runner reads `mockup-fidelity`, `mac-craft`, `design-craft` and
  `ux-craft` at a repo-relative path rather than depending on the machine. After a fresh clone:
  `git submodule update --init --recursive`.
- **M15–M22 were allocated together on 2026-08-19** from the interactive mock at
  `design/mcp-router-console.html`. They are UI specification, not defects: each names something
  the mock draws that no earlier brief covers. `design/mcp-router-console-spec.md` carries the
  audit numbers and the list of what the mock specifies rather than measures.

- **Three ids mean two things each, and R7 found it while allocating.** `ORCHESTRATOR.md`'s
  deferred register (lines ~334–337) carries rows **R6** (router-side eval runner), **R7** (skills
  write endpoint) and **R8** (server soft-delete), while this table carries **R6** (child PATH,
  ready to verify), **R7** (harness reconciliation, this item) and **R8** (auth rejection, Done).
  Six live rows, three ids. The deferred register was populated from a different allocator than
  this table and nothing reconciles the two namespaces, which is the same hole the notes above
  describe for membership — `ledger-reconcile.py` compares ids across files and a collision
  between a *ledger* id and a *deferred-register* id is not a membership disagreement, so it
  reports agreement. R7 did **not** renumber anything: three rows written by other items are not
  a runner's to renumber mid-fleet, and picking a direction silently is how the drift got here.
  R7's own children are `R7-C1`…`R7-C4`, following `R4-C1`/`R4-C2`, which cannot collide either way.
  The decision this needs is which namespace yields — that is M28's shape, not a runner's.

# M19 — the in-app GitHub-flavoured Markdown viewer

Branch `ai/m19`, worktree `.worktrees/M19`, cut from `main` at `87e16dc`. Five commits.
**Ready to verify. Not merged, not rebased, not pushed.**

## What landed

A block parser in `MCPRouterKit/Markdown/` producing typed blocks, with `AttributedString(markdown:)`
kept for the inline runs — the shape `spec-M19.md` §3.6 settles and tells the planner not to
re-open. On top of it: a shield parser that turns a badge URL into a key, a value and a tone; a
package image resolver; the `CapabilityDocument` types and a fixture package of real files; a view
per block kind in `MCPRouterUI/Document/`; the three-tab panel with its product header, its
five-cell facts strip and its capped scrolling body; and the surface's whole measurement apparatus
under M23.

The security work is the substance rather than the trimming, because the thing being rendered
arrives from a marketplace into an app that rewrites other tools' configuration. `MarkdownInline`
rebuilds the parser's output run by run against an allowlist — presentation intent, and a link only
where the scheme is `https` — so `imageURL` and anything a later toolchain adds to that scope are
gone by construction rather than by a removal list somebody has to remember to extend. `Shield` has
nowhere to put `#4c1`, which is the structural half of *the shield colours are the token values
rather than the badge's own*. `PackageImageResolver` refuses a scheme, an absolute path, an escape
and a symlink out of the package, each with its own sentence, and the resolved image reaches the
view as bytes so no view holds a path it could open.

## What I measured

| | |
|---|---|
| Suite | **1725 → 1776 tests in 219 suites**, exit 0 |
| `swiftlint --strict` | 0 violations, 0 serious, 570 files |
| `swiftformat --lint` | 0 of 577 require formatting |
| `no-raw-design-values` | clean · 132 files scanned · 91 under the geometry rules, `Document/` newly among them |
| `no-wire-codable` · `no-harness-config-writes` (+ selftest) | clean · 2 exemptions · 354 files, 27 cases held |
| `reader-accounting` · `null-run-gate` | accounted · armed |
| `ledger-reconcile` | no findings across A–L |
| `mock-fidelity-selftest` | 68 cases, all three exits observed |
| `mock-fidelity-gate.sh readme` | **exit 1, 30 findings, all eight layers run** · tokens, literals, structure, geometry and type-metrics clean |

Every one of the 30 is a declared divergence: 29 ledger rows, 29 carrying a `Dn ·` number, splitting
D1 20 · D2 3 · D3 1 · D4 1 · D5 1 · D6 1 · D7 1 · D8 1, with the thirtieth being the copy layer
reading D7 in its own words. Exit 1 is the same exit `settings` carries and for the same reason.

Eight red-green arms in `scripts/m19-red-green.py`, all biting, plus the geometry lint armed by hand
against `ShieldView.swift`. Two arms did not bite first time and both were the arm's fault rather
than the guard's, which is the whole reason to run the pass instead of asserting it.

## The one decision, and the lane that settled it

The router serves no document. Measured: `src/control.ts:279-283` admits `/servers`, `/usage` and
`/registry`, and no wire type — `RegistryEntry`, `Skill`, `PluginOrigin` — carries a read me, a
changelog, a licence or a capability table. So there is nothing observed for this panel to render,
`DESIGN.md` §6 forbids rendering what is not observed, and `spec-M19.md`'s own header gives the host
panel to M18.

Three options went out of family before the plan was written. `grok-4.6` refused with a 402 and
`codex` is recorded down to 2026-08-27; `agy` / `gemini-3.7-flash-high` answered and took the same
option I would have: ship the renderer, the panel and the measured surface, add no entry point.
It rejected a wired `Read me…` button as advertising a capability the router cannot supply, and
rejected the endpoint as a backend feature outside the triaged brief. Its refinement is taken —
name the contract in a protocol whose production arm returns unavailable, and file the endpoint as
its own item. Recorded in full at `planning/evidence/M19-review-agy.md`.

One thing the spec parked is unparked by measurement instead: M21 merged at `e121801` and
`ColorToken.shieldGood` exists, so the acceptance line the spec deferred is written here.

## What I could not settle

- **Nothing serves a document, and no user can reach this panel.** That is the item's real exposure
  and it is filed rather than buried: `planning/features-to-triage/M29-capability-document-source.md`,
  with *do nothing* named as a genuine option, because the alternative costs a new route on a trust
  boundary in two router implementations.
- **No driven pass against the running app**, for the same reason — there is no entry point to
  drive. What replaces it is a real headless render, which is weaker, and the evidence ledger says
  so at the top rather than in a footnote.
- **One measured state of three.** The panel builds a published tab, an unpublished tab and a source
  that cannot answer; the mock draws one frame for this sheet and `mock-affordances.py` exits 3
  rather than inventing another, so two states are covered by unit tests and are not rendered
  evidence.
- **`make lint` could not run as a target here.** Its `tools` prerequisite wants `node_modules` and
  `dist/index.js`, which a fresh worktree does not have. Every component was run directly instead
  and is recorded as a difference from the target rather than as the target passing. The orchestrator
  runs the whole-repo gate.
- **`orchestrator-hierarchy.html` carries no M19 node** — it holds the F/I/M1–M8/R wave only, so
  `set-status.py` had nothing to set. Left alone rather than extended from inside this item.

## What is not mine

The entry point and the sheet inventory are M18's. Collapsible `<details>` blocks and side-by-side
diffs, which `PRD.md` §7.2 names, are drawn nowhere in `sh-readme`; a document containing one renders
it as plain text, visibly. Syntax highlighting inside a fence is not built — the mock draws a
monospace ground and no colour. The iPhone draws this surface nowhere.

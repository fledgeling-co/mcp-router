# M16 — the Signal Path replaces the Breaker Column

**Branch** `ai/m16` · **Base** `main` at `87e16dc` · **Status** ready to verify — not verified here.

Spec `planning/specs/spec-M16.md` · Plan `planning/plans/plan-M16.md` · Evidence
`planning/evidence/M16-acceptance.md`.

---

## What was built

**The band.** `SignalPath.swift` draws a head — title, topology line, four-swatch legend — over a
rail of hub → arrow → jack field. `Jack.swift` draws one jack; `StatePlug.swift` draws the mark it
leads with, at the two sizes the legend, the jack, the table row and the inspector take it.

**The field packs rather than grows.** `LazyVGrid` with `GridItem(.adaptive(minimum: 132))`, the
direct translation of `repeat(auto-fill, minmax(132px, 1fr))`. Measured at 1280×820: four columns,
track pitch 140 = 132 + the grid unit, field width 1112 — which is exactly what
`SignalPathGeometry.jackFieldWidth(inBoardWidth:)` predicts.

**The geometry is a value type with invariants, not literals in a view.** `SignalPathGeometry`
replaces `BreakerGeometry` one for one — same `documentedValues` contract, same parity suite, same
argument for living in the headless target. Ten rows in a new `### Signal Path geometry` table.
`columns(inJackFieldWidth:)` is the CSS `auto-fill` arithmetic written out, which is what turns the
brief's *"two jacks fit beside an open inspector"* from prose into an assertion.

**The presentation layer is one chain.** `JackState` inherits `BreakerState`'s ordering unchanged —
running first, because the plug's meaning is the lever's — with `wantsYou` split into `held` and
`needsSignIn` because a jack carries a word as well as a colour.

**The retirement.** `Breaker.swift`, `BreakerGeometry.swift`, `BreakerState`,
`BreakerGeometryTests`, `BreakerGeometryParityTests`, and four places in `DESIGN.md` — §2's
nineteen-row `### Breaker geometry` table, replaced by ten `### Signal Path geometry` rows; §1's
outgoing-signature paragraph; §7's two breaker motion rows, replaced by one plug row; and §8's
`Space` row, re-pointed from the breaker to **Keep warm** rather than removed. Three consumers were
re-homed rather than deleted, because each carried an argument that outlived the element: the badge
bump, the loading skeleton, and A7's cross product.

**The gate.** `VOUCHED_CONTROLS` gains `jack` and `signature`; `servers.pairing.tsv` gains the band
and four jacks.

---

## Gate output

Run from the worktree root at `753670e`.

| Gate | Result |
|---|---|
| `make lint` | **exit 0** — the target, not its components alone · `0 violations, 0 serious, 554 files` |
| `no-raw-design-values.sh` | **clean** · 127 files scanned, 86 under the geometry and boundary rules |
| `swift test` | **1743 tests in 216 suites passed** |
| `mock-fidelity-gate.sh servers` | **exit 1**, 137 findings · five layers clean |
| `mock-fidelity-selftest.sh` | **exit 0** · 68 cases · all three exits observed |
| `ledger-reconcile.py` | **exit 0** · no findings across A–L |

**`make lint` means the target, not its eight components run separately.** The distinction matters
because the recipe's `tools` prerequisite is what stops a parity lane skipping silently, and it
guards on `node_modules` and `dist/index.js` — which this worktree has and a fresh one does not.
Re-confirmed exit 0 during gap-fix 1, with source at `2123e0e`; the full record is in that section.

**The base was re-measured rather than quoted, and the derivation is now in the units of the thing
being counted.** `87e16dc` discovers **1725** — the figure the work order records at `29d5111`,
confirming nothing since touched Swift. The reader is a per-file census of `@Test` lines under
`app/Tests`, run over each tree's git objects rather than the working copy:

    for f in $(git ls-tree -r --name-only <tree> -- app/Tests | grep '\.swift$'); do
      git show "<tree>:$f" | grep -c '@Test'; done | paste -sd+ - | bc

    87e16dc  1725        ai/m16  1743

**The reader is controlled before it is trusted.** Its answer at the tip, 1743, equals the count
`swift test` reports executing there, so one `@Test` line is one test on this tree; and the two ways
that could stop being true are both absent **at both trees** —
`git grep -h '@Test' <tree> -- app/Tests | grep -c 'arguments:'` is **0**, so no parameterised case
expands to several, and the same reader piped through `grep -cE '^\s*(//|\*)'` is **0**, so no
`@Test` occurrence sits inside a comment. A second normaliser agrees on the delta:
`git diff 87e16dc..ai/m16 -- app/Tests` carries **+31** and **−13** `@Test` lines, net **+18**, and
1743 − 18 = 1725.

**What this replaces was right about the number and wrong about the instrument.** This paragraph
previously derived 1725 from *"`swift test list` on the slice-A tree returned 1736 lines, eleven of
them this item's two new test files"*. `swift test list` does not emit one line per test — it emits
build-progress lines too (`Planning build`, `Building for debugging...`, `Build complete!`, and a
`Write swift-version-….txt` line), and how many depends on how warm the build is. So 1736 − 11 was
arithmetic on listing lines presented as a test count, and `planning/evidence/M16-acceptance.md`
then read it back as *"1725 in 215"*. The figure survived two independent readers; its stated
derivation did not survive one.

`make all` was not run; the work order rules it out and the orchestrator owns the whole-repo gate.

Six mutation arms, every one applied, run, restored and swept for residue. The table is in the
evidence file. Arm 3 is the one worth keeping: it proves the assertion closing the warm-countdown
defect is not vacuous.

---

## The plan's out-of-family review, and what it changed

`agy` / `gemini-3.7-flash-high` reviewed the plan adversarially over eight questions and returned
**AMEND** with four rejections. `codex` is down until 2026-08-27 and `grok`'s balance was
re-confirmed exhausted on 2026-08-22, so the Google lane is the gate rather than a downgrade.

Two rejections changed shipped behaviour and both were real:

1. **A warm running server would have drawn a countdown.** The first draft computed the jack's word
   from the same chain as its plug. `ServerSubtitle` has put `warm` above `running` since M3 because
   the reaper skips a warm server, so the band would have read `3:41 left` while the hub two inches
   above read `1 at rest` — two statements about one server on one board. The word is now computed
   from the state **plus** `warm`.
2. **`at rest` counted the warm flag rather than warm children.** `warmUp()` logs and swallows a
   start failure, so a warm server that never came up carries `warm == true` while its process is
   not on the machine. It is `warm && state == .running` now.

The other two were a slice boundary that would not have compiled, and a vouch in
`mock_fidelity.py` that would have turned the build's own row plugs into findings by destroying the
`covered-by-pair` exemption. The first: slice B renamed `ServerRowModel.breaker` while
`ServersBoardRow.swift` still read it — the `Breaker(state: row.breaker)` call, `:46` as `main`
carries that file, which this branch has since replaced.

---

## Deviations from the plan, and why

1. **`SignalPathGeometry` rather than nine `MetricToken` cases**, against the spec's disposition
   that the literals *"route through `MetricToken`"*. They route through a token layer; which one is
   settled by the doc comment on `DesignDocParser.breakerRows` as `main` carries it
   (`DesignDocParser.swift:327` there; the function is deleted on this branch), which records why
   the breaker's nineteen rows were kept out of the chrome ladder — *"mixing nineteen breaker rows
   into the chrome ladder would bury the four values a window actually lays out with."*
   `MetricToken.jackLane` stays where it is and the geometry reads it, so the lane is authored once
   and a parity test asserts it.
2. **`DesignDocParser.swift` was split.** Adding the Signal Path's reader took it to 410 lines
   against a 400-line limit, so the increased-contrast reader moved to a file of its own. That seam
   was already declared and already argued for in the comment above it. `tableLines` widened from
   `private` to internal as a consequence, which is noted at the declaration.
3. **`StatePlug` sits at `MCPRouterUI/` root rather than under `Boards/`**, where `Breaker.swift`
   sat and for the same reason: `StateContainer.swift` draws it and compiles for both platforms.
4. **The row's leading slot is the plug's width, so the table's columns moved left by 22pt.** That
   is the visible consequence of retiring a 30pt lever rather than an incidental change, and the
   column headers read the same value so the two cannot drift.
5. **AC9 was mis-specified and is recorded as not met.** It asked for the breadth finding count to
   fall; it is 116 before and after. `divergent` is a finding too, so an affordance moving from
   *not built* to *built and measured* trades one finding for another — and no fixture this build
   ships can make a jack's label equal the mock's eleven server names. What moved is the
   composition: `absent` 80 → 75, `divergent` 16 → 21, `covered-by-pair` 16 → 24, `extra` back to
   its pre-existing 10.

---

## What is open

- **The harness column is not built, and it is the one part of the brief this item does not
  deliver.** Its reading is `HarnessState` in `RouterCore/Discovery/HarnessReconciliation.swift`,
  which declares **four** cases — `notWired(overlapping:)`, `wiredViaHTTP`, `wiredViaShim(bridge:)`,
  `wiredWithDuplicates(route:count:)` (anchor `enum HarnessState` at `:52`, its four `case` lines at
  `:55-58` — on this branch and on `main` alike, which carry the file as the same blob `f190de0`).
  The brief names **three** — *"routed over HTTP, routed through a stdio shim, or not routed"*
  (`planning/features-to-triage/M16-signal-path.md:16-17` at
  `2123e0e`) — so the first three are the brief's three and `wiredWithDuplicates` is a fourth the
  brief does not name. **This sentence previously said "the brief's three exactly" while listing all
  four, which is the defect this item filed against `spec-M22.md` §3.3; see the gap-fix record at
  the end of this note.** The enum sits in a target neither app links,
  derived from harness config files. The control API serves no harness route; A36 forbids a board
  going round it. The one thing the app can see, the `client` on a call record, cannot produce *not
  routed* at all, because a harness that has never called cannot appear in a usage log — so the
  state carrying the finding is the state that reading cannot reach. **M22 absorbs `R7-C1` and the
  route**, and the column lands with it. Recorded in `DESIGN.md` §10 and in the pairing file's
  citations. The rail reads hub → jacks meanwhile, which is honest and incomplete rather than
  either alone. *(`spec-M22.md`'s §3 grounding record — `:153` as that file stands at `87e16dc` —
  spells these cases `.wired(route:)`; the source is what is quoted here.)*
- **The band's own paragraph failed A36 before it passed it.** The first draft of `SignalPath`'s doc
  comment spelled out the file-reading APIs while explaining why this view may not use them, and
  that gate is a source grep that cannot tell a mention from a call. The prose is reworded and says
  so. Relaxing the grep to skip comments would put every forbidden call one `//` away from
  invisible — worth someone deciding deliberately rather than the next runner hitting it and
  reaching for the gate.
- **The last jack measures 142.5pt against its siblings' 132.** SwiftUI's `.adaptive` gives the last
  item in a row the remainder of its computed track set, where CSS `auto-fill` would leave empty
  tracks. It is inside the specification rather than outside it — `minmax(132px, 1fr)` allows a
  track to grow, and 132 is documented as a minimum — but it is a divergence from the mock's
  behaviour and is recorded rather than absorbed.
- **The eleven-upstream case is predicted, not measured.** The recorded fixture declares four
  servers, so the packing behaviour at the count the brief actually measured — eleven, which ran the
  band 500pt deep as a single column — is covered by `columns(inJackFieldWidth:)`'s arithmetic and
  by the observed 140pt track pitch, not by a render. A fixture with eleven servers would settle it.
- **`indicator` is unvouched in `mock_fidelity.py`, so 16 mock row-plug affordances still read
  `absent` while the build draws the mark.** The citation on each says why: the harness records no
  text for a dot, so a pairing fails `readable()` on both sides and lands `unclassified` — the
  gate's own phrase for agreement between two absences — and vouching the kind would additionally
  reclassify the build's own plugs as `extra`. It is `D-m23-h`'s subject, the mock kinds
  `VOUCHED_CONTROLS` does not name, and it wants an instrument rather than a table edit.
- **Three branches edit `VOUCHED_CONTROLS` from the same base, and the safe-merge rule is
  mechanical rather than a judgement.** `ai/m19` adds eight roles, this branch adds two, `ai/m22`
  expects to add more. Measured rather than assumed, by parsing the dict out of all three trees and
  taking the union a textual merge produces:

  - **Adding entries can only make more pairings vouched, never fewer.** No already-vouched pairing
    can be un-vouched by another branch's edit, so the whole cross-branch hazard is the *other*
    direction — `MOCK_KINDS_FOR_ROLE` keys on the **build node's role**, and a role gaining a kind
    turns `declared` from 0 to non-zero, which flips that role's unpaired build nodes from
    `covered-by-pair` (silent) to `extra` (a finding).
  - So the check before extending the table is one question: **does the other branch introduce a
    role my surface already uses?** Not *does it touch the same keys* — those are different claims
    and only the second one bites.
  - **And not *does the other branch's new kind appear in my mock census* either, which is the
    plausible mistake.** `VOUCHED_CONTROLS` maps a **mock kind** to a set of `(build role, build
    kind)` pairs — anchor `VOUCHED_CONTROLS: dict[str, set[tuple[str, str]]]` — and
    `MOCK_KINDS_FOR_ROLE` inverts it at `:94-97` on this branch, anchor
    `for _kind, _pairs in VOUCHED_CONTROLS.items()`, so the quota rule is keyed on the build side.
    Where a kind and a role share a name the two screens look like one screen and disagree.
    They disagree here: Servers' own mock census **does** name a `callout` affordance —
    `git show HEAD:planning/fidelity/servers.pairing.tsv`, column 2 matching `/callout/`, returns
    `v-ideal/callout/four-more-upstreams-are-dormant-and-cost-nothing`, unpaired — so the kind-based
    screen reports Servers exposed to M19's `callout`. It is not. Walking `root` in all four
    `planning/fidelity/dumps/servers.*.json` and collecting `node["role"]` returns **0** build nodes
    of role `callout`, with the presence control that the same walker returns `jack` **4**,
    `signature` **1** and `row-indicator` **4** on the same files. (Those dumps are gitignored
    working files, written by the gate run at `753670e`; `git diff 753670e..HEAD -- app scripts
    DESIGN.md` is empty, so they still describe this tree.) Three screens are available and
    only the third is the code's: *did it touch the same keys* (a conflict test), *does its new kind
    appear in my census* (a false positive here), *does it introduce a role my surface already
    uses* (right). Stated because the middle one is what a reader reaches for first.
  - **It is one check per *surface*, not one per pair of branches, and it includes surfaces that
    edited nothing.** M22 supplied this correction and it is the half that generalises: a branch
    with zero diff on `mock_fidelity.py` has no *conflict*, which is not the same as no *exposure*
    — the quota rule keys on the build node's role, never on who made the edit. Run pairwise across
    all three, `ai/m22`'s harnesses surface carries `callout`, one of M19's eight. Harmless in the
    event, because the only node with that role is paired and the quota rule has nothing unpaired
    to reclassify — but **non-empty**, where a check that inferred stake from the diff had reported
    the hazard closed. *"Came back empty" was this surface, not a property of the merge.* Those are
    M22's numbers over M22's dumps, which this session cannot verify and does not need to: the
    correction stands on the rule rather than on the count.
  - Run for this branch against the union of all three: M19 introduces `badge`, `callout`,
    `codeblock`, `heading`, `list-item`, `sentence`, `tab`, `verified-mark`; the four servers dumps
    carry 39 distinct build roles; the intersection is **empty**, and no role this surface already
    uses gains a kind. Nothing on Servers reclassifies under the merged table, and `unclassified`
    stays at 8. Verified in the other direction by the recovery session: zero occurrences of
    `signature` or `jack` across M19's readme dump — with the stated limit that the readme surface
    has **one** frame where this one has four, so that answer is complete relative to what M19
    measured rather than to the surface.

  **A line number is only a citation in the frame it was written in.** Every reference above names
  its tree, because this item's own note cited `mock_fidelity.py`:808 without one — correct on this
  branch, seventeen lines off on `main`, where it lands in unrelated text about `D-m23-l`. A reader
  following it from `main` finds something plausible and wrong, which is worse than finding nothing.
  The shift was this item's own +18/−1 to that file. Filed by the conductor as **G7**, third
  instance tonight, which is the argument for the remedy being mechanical — quote an anchor the
  reader can grep for and name the tree — rather than a matter of anyone being more careful.

  **An anchor is only better than a line number if the sweep for it normalises wraps**, and this
  file is the case that proves it. Counting the phrase *"agreement between two absences"* in
  `mock_fidelity.py` on `main` gave **five sweeps four different answers**, and none of them is
  wrong — they answer different questions:

  | Sweep | Answer | What it is actually counting |
  |---|---|---|
  | line-anchored `grep` | **0** | occurrences on one source line. All three are wrapped, so none |
  | `[\s#]+` → space | **2** | `:44-45` (header) and `:284-285` (`readable()`'s docstring) |
  | a different **2** | **2** | `:45` and `:862` — the subset that started the correction chain |
  | per-line scan for `agreement between two` | **1** | `:284` only — `:44` wraps a word earlier |
  | letters-only, then collapse | **3** | all of them: `:44-45`, `:284-285`, `:861-862` |

  **Five sweeps, four answers, one file, one phrase — and the last row is the only complete one.**
  `:861-862` is why: it builds the sentence from two adjacent string literals,

      f"...so nothing was compared{invisible}. Agreement "
      "between two absences is not a measurement"

  so what sits mid-phrase after any whitespace-collapsing normaliser is `Agreement " "between` —
  two quote characters no `[\s#]+` pattern touches. Stripping everything that is not a letter or a
  space finds it. The phrase is in the report the tool emits and in **no contiguous run of its
  source**, which is the shape a sweep has to be built for rather than the shape it usually is.

  That sequence cost one session a false *"the phrase is a paraphrase"*, and cost this one a
  reported count of two before it was checked by reading. **The lesson is not that one normaliser
  is right.** It is that a sweep returning a count nobody cross-checks is a measurement with no
  witness, and that the number only became knowable because two sessions disagreed and went and
  looked. So: cite the anchor **and** the tree **and** the line, because each covers a different way
  the other two fail — and say which question a count answers.

  **The right long-term home for this is `mock_fidelity.py`'s own header**, beside the paragraph
  that already says extending the list is a deliberate, reviewable act — that is where someone about
  to extend it looks, and a progress note is not. It is recorded here instead because this branch is
  finished and a fourth edit to the file three live branches are rewriting would impose a conflict
  on the two still building.

  **The conductor has since filed that header edit as `G8`**, to land as one dedicated change after
  M19, M16 and M22 are all in. This note originally proposed *whichever branch merges last*; the
  ruling rejected that and the reasoning is better than the proposal — M22 has zero diff on the file
  today, so handing it the comment manufactures a conflict surface it does not currently have, and
  "last" is fragile in a way that fails quietly, because merge order can change and a stalled branch
  would strand the rule with nobody holding it. As a post-merge change it costs one commit, has no
  conflict surface, and depends on nobody's timing. **Nothing here is M16's to carry.**

- **Two of the eight `unclassified` rows will not resolve, and should not.** `v-empty/icon/unlabelled`
  and `v-error/icon/unlabelled` pair to a `state-illustration` node that **is** vouched — the
  `"icon"` entry of `VOUCHED_CONTROLS`, anchor `"icon": {("state-illustration", "leaf")}`, `:68` on
  `main` and `:85` on this branch. (**This citation read `:87` until the gap-fix.** `:87` on this
  branch is the `"column-header"` entry — a real `VOUCHED_CONTROLS` entry of identical shape, so a
  reader following it lands on plausible text rather than on nothing. It is `G7`'s dangerous variety,
  committed inside the paragraph arguing for careful citation.) The cause is
  neither the table nor the pairing: `readable()` fails on both sides, and `mock_fidelity.py`
  already declares exactly this, in the comment three lines above `layer_breadth`'s
  `vouched = vouched_pairing(...)` call — *"the harness records no glyph
  identity for an icon node, so an icon pair has nothing to compare and lands as `unclassified`
  rather than as agreement. That is the honest outcome, not the audited one."* (`:791` on `main`,
  `:808` on this branch — the +17 is this item's own edit to the file.) It is a
  declared residue of the same shape as `font-weight-face`, wanting an instrument rather than an
  edit. Recorded because a reader closing out this surface's findings will otherwise go looking for
  a defect in `derive_pairings` that is not there.

  **They are a residue only while they stay paired.** Unpairing them to tidy the `unclassified`
  count converts a true *"the instrument cannot compare these"* into a false *"absent from the
  build"* — a finding asserting the build draws no mark where it plainly draws one. M22 made
  exactly that move on its own surface and has since repaired it, citing `:791`. The same move is
  available on these two rows and should not be made.

- **`DESIGN.md` §2's `Servers row` keeps 56pt with its derivation gone.** It read
  `56pt (4 + 48 breaker housing + 4)`; the housing retired and the value did not, because the
  table's conversion is another item's and a row height changed here would move the skeleton, the
  column headers and the inspector's neighbour. §10 records the mock's 36pt as owed.

---

## Gap-fix 1 — the six record defects, and the one I filed against someone else first

Verified `Needs More Work` at `115e0f9` in `.worktrees/M16V` against the gated tree `25a6849`
(`2123e0e` plus three merges of a moving `main`). No source change, no rebuild: the verdict's own
summary is that the build is sound and every behavioural claim reproduces. Six defects, all in the
written record. All six are fixed here, all six reproduced in this worktree first, and the fixes are
at the sites below.

**`2123e0e`'s commit message closed with a categorical claim — *"This is the last record. Everything
else in the file reads as intended."*** That claim was false, and it was made in the commit that had
just corrected a section for miscounting itself. A section can be fixed by reading it; a file cannot
be certified by fixing a section.

Anchors below are the **corrected** text, so they grep in this file rather than in the one that was
wrong. The wrong text is quoted in the *What was wrong* column.

| # | Where, by anchor in this tree | What was wrong |
|---|---|---|
| 1 | `progress/M16.md` · *What is open*, first bullet · `which declares **four** cases` | said *"the brief's three exactly"* while listing all four case names of a four-case enum |
| 2 | `progress/M16.md` · `the "icon" entry of VOUCHED_CONTROLS` | cited `:87` on this branch, which is the `"column-header"` entry; `"icon"` is `:85` |
| 3 | `evidence/M16-acceptance.md` · `AC6` | described output its own **case-sensitive** `grep` does not produce |
| 4 | `evidence/M16-acceptance.md` · `AC7` | *"§1, §7 and §8 no longer name the element"* — false for §1 and §7 |
| 5 | `evidence/M16-acceptance.md` · `AC9`, `The gate classifies every breadth row` | `extra-cited` omitted from the composition, so neither column reached its own 116 |
| 6 | `progress/M16.md` · `The base was re-measured rather than quoted` | 1725 is right; it was derived in `swift test list` lines, not in tests |

### The one worth more than its correction: I committed the defect I filed

Finding 1 is the defect this item reported against another item's spec. `ORCHESTRATOR.md` carries
M16's own words about `spec-M22.md` §3.3, under the heading `SPEC — spec-M22.md §3.3 MISDESCRIBES
HarnessState` — *"`HarnessReconciliation.swift` declares **four** cases"* at `:350` and *"That is
arithmetic on the wrong denominator — four exist"* at `:362`, **as `main` stands at `1f5ad55`**. The
first bullet of this note's own *"What is open"* section then said the harness column has three
states, listed all four case names, and called them *"the brief's three exactly"*. Same enum, same
wrong denominator, in the paragraph that hands the harness column to M22 — so M22 would have
inherited the error from the item that filed it.

Being able to name a defect is not being immune to it. The reason it survived is worth more than the
fix: I filed it against a document I was reading adversarially and committed it in a document I was
reading for flow. A reviewer's attention is not a property of the reviewer, it is a property of the
pass, and this note had no adversarial pass over its own prose until someone else ran one.

**And that citation is the second lesson, unasked for.** The verdict placed the same text at
`ORCHESTRATOR.md:219-232`. That was right at all three trees it read — the anchor
`declares **four**` sits at `:219` in `e8935f2`, `c22f3f4` and `605feda` alike — and it is **off by
131 lines** at `1f5ad55`, because `main` moved again between that verification and this gap-fix. On
`1f5ad55`, `:219` reads *"`Needs More Work` against it, with the downgrade logged"* — another item's
verdict line — and the range runs on into a dispatch section about naming the session in a brief.
Verdict-shaped prose, wrong subject: the variety that gets believed. The line number decayed in under
a day while the anchor held.

### The two false rows are a different class from the other four

Findings 3 and 4 sit in `planning/evidence/M16-acceptance.md`, whose header (`:7-9`) tells every
future reader that where `git diff <that SHA>..HEAD` does not touch the files behind a row, *"that
row **is** the evidence, and the right thing to do is skip the check"*. Both files behind `AC6` and
`AC7` are untouched since `753670e`, so both rows were live substitution offers. `AC6` named a
case-sensitive `grep` and described what a case-insensitive one returns; `AC7` asserted an absence
over three sections where two of them carry the word. A false row in a file that invites
substitution is not a typo — it is a check that will now never be run, which is strictly worse than
no row.

Both rows now name their reader, report what it actually returns, and carry a presence control:
`AC6`'s sweep returns 22 / 32 / 9 for `SignalPathGeometry` / `JackState` / `StatePlug` over the same
paths, and `AC7`'s §8 range returns 0 for `breaker` and 1 for `keyboard`. An absence check with no
control behind it is the one that gets believed wrongly, and these two were the case in point.

**Correcting `AC7` turned up the sentence it inherited.** *"What was built"* said the retirement took
*"the breaker's rows in `DESIGN.md` §1, §7 and §8"*, and measuring the four sections shows §1 lost a
paragraph rather than rows, §7's two motion rows were **replaced** by one plug row, and §8's `Space`
row was **re-pointed** from the breaker to *Keep warm* rather than removed. Only §2 lost a table.
That sentence is corrected too — it is not one of the six, but it is the phrasing `AC7` was built
from, so leaving it would have left the row's source intact.

### Two counting defects of the class this fleet spent the day on

Finding 5: the `AC9` table listed seven of the gate's eight breadth categories, and the missing one
— `extra-cited`, 2 rows — is a finding category. Both columns therefore summed to 114 findings while
the paragraph above them claimed 116. The story was right and the table could not be reconciled by
the reader it was written for, which is the same shape as a count with no witness.

The verdict recorded the base column as unchecked, needing the gate built and run at `87e16dc`. It
did not: `planning/fidelity/servers.ledger.md` is **tracked**, so `git show
87e16dc:planning/fidelity/servers.ledger.md` reads the base's own eight categories out of the object
store — `absent 80 · covered-by-pair 16 · divergent 16 · extra 10 · extra-cited 2 · present 3 ·
structure-unpaired 14 · unclassified 8`, blob `86e5823`. Both columns in the corrected table are now
re-counted row by row with one `awk` census, printed in the table's own preamble, and both reconcile:
116 findings over 149 rows at the base, 116 over 157 here.

Finding 6: 1725 was correct and its stated derivation was in the wrong units — `swift test list`
line count minus eleven, where that command also emits build-progress lines whose number varies with
how warm the build is. It is now a per-file `@Test`-line census over each tree's git objects, which
returns 1725 at `87e16dc` and 1743 at `ai/m16`; the reader is controlled against the tip's observed
executed count of 1743, and against 0 parameterised `@Test(arguments:)` cases and 0 `@Test`
occurrences inside comments at **both** trees. A second normaliser — the diff's `@Test` lines, +31
and −13, net +18 — closes on the same figure. Two readers agreeing is why this is a units defect
rather than a wrong number, and it is also why the wrong instrument went unnoticed: the answer was
right.

### The cross-branch rule gains the screen it was missing

The verifier ran the rule rather than arguing with it: `VOUCHED_CONTROLS` patched to the union of all
three branches changes nothing on this surface, and vouching a role the surface *does* use moves
`covered-by-pair` 24→20 and `extra` 10→14. The zero is measured, not blind.

Their refinement is folded into the rule's bullet above, because the screen it was missing is the one
a reader reaches for first. `VOUCHED_CONTROLS` is keyed on the **mock kind** and its values are
`(build role, build kind)` pairs, so *does the other branch's new kind appear in my mock census* and
*does it introduce a build role my surface already uses* are different questions that look identical
wherever a kind and a role share a name. On Servers they give opposite answers: the mock census names
a `callout` affordance, so the kind screen reports exposure — and walking all four dumps returns 0
build nodes of role `callout`, with `jack` 4, `signature` 1 and `row-indicator` 4 as the control that
the walker can see a role at all. Three screens, one of them right, and the wrong one is the
plausible one.

### Four things carried rather than re-derived

- **`make lint` exits 0 here, and the verifier was right that it did not there.** The target's
  `tools` prerequisite guards on `node_modules` and `dist/index.js` (`Makefile:82-87`); a fresh
  worktree has neither, so `make lint` dies at `tools` and only its eight components can be run —
  which is a difference from the target passing, and the guard exists precisely to stop a parity lane
  skipping quietly. This worktree has both, so `make lint` was re-run for this gap-fix — source at
  `2123e0e`, only the two planning files differing, neither of which any lint component reads —
  and it returns **exit 0**: `0/561 files require formatting`, `0 violations, 0 serious in 554
  files`, `no-raw-design-values: clean` over 127 files, `no-wire-codable` clean with 2 exemptions,
  the harness-config gate and its 27-case selftest held, `reader-accounting.py` accounted, and
  `null-run-gate.py` armed with 28 assertions changing verdict and 0 held green. So the record's
  `exit 0` stands as a measurement of *this* worktree, and now says which worktree.
- **The loopback-listener failure is not this item's.** One full-suite run failed
  `CallbackLifecycleTests.swift:238` under concurrency; it passes 5/5 filtered, and that test file
  plus `CallbackListener.swift` and `LoopbackHTTPServer.swift` are byte-identical to `main`. Recorded
  and not chased, on the verifier's instruction and their evidence.
- **`ORCHESTRATOR.md` looking 33 lines short was `main` moving between two commands**, not a merge
  dropping a section — `git merge-tree` returned the identical tree and the false alarm is recorded
  in the verdict. `main` moved three times during that verification and will move during the next.
- **This gap-fix touched no source.** `git diff --stat 753670e..HEAD -- app scripts DESIGN.md` is
  empty at the tip and stays empty, which is the property that made this a two-file fix.

### On the verdict call, since the referral was a downgrade

The verdict rests on one out-of-family lane and **that lane disagreed with it**:
`gemini-3.7-flash-high` said `Done`, on the principle that a verifier grades behaviour and not copy.
`grok` is at HTTP 402 and `codex` is limited until 2026-08-27 — both as this note already recorded
on 2026-08-22 — and `claude-fable-5` agreeing is a different model rather than a different family,
so it corroborates without being independent. The verifier logged that downgrade instead of passing
it silently, which is the right handling.

**I agree with `Needs More Work`, and my agreement is worth little — I am the graded party, so the
argument has to carry it rather than my assent.** The argument is that gemini's principle is sound
and its premise is wrong here. It treats the record as commentary on the work. `AC6` and `AC7` are
not commentary: they sit in a file that instructs readers to accept a row *in place of* re-running
the check, and both were false about the tree. That makes them behaviour — the behaviour of the next
reader — rather than prose. Findings 1 and 2 travel further than this item on the same basis: the
harness paragraph is what M22 reads, and a citation landing on another entry of identical shape is
believed rather than noticed.

Where I would push back, and it does not change the verdict: findings 5 and 6 would not have blocked
on their own. Neither makes a false claim about the tree — one is a table that cannot be reconciled,
the other a right number with a wrong instrument named. They clear *the record is wrong* rather than
*could be better*, so they belong in the set; they are not what tipped it. What tipped it is two rows
offering themselves as substitutes for a measurement while being wrong about it.

**Ready to verify.** Two files changed since `2123e0e`, no source, gates unaffected — re-verification
need only re-read `planning/progress/M16.md` and `planning/evidence/M16-acceptance.md`.

# G9 — absolute paths that name one machine

The first half is merged: `app/Scripts/pool-mutation-gate.sh` and `planning/run-gate.sh` derive the
repository root instead of pinning `.worktrees/R2`, and `spec-R2.md`'s citation is corrected. This
records the second half — the sweep, the pre-commit hook, and the gate.

## The denominator, and which normalisation produced it

The brief said four other files carry absolute paths. The ledger said forty. Neither number
survives without the question it answers attached, so here is every reading, each with its own.

| reading | at `main` HEAD `afd823a` | at `b757a1ac`, 2026-08-23 |
|---|---|---|
| tracked files containing the literal `/Users/lukerhodes` | 44 | **40** |
| tracked files containing `/Users/` | 72 | 67 |
| tracked files containing `/Users/` or `~/` | 233 | — |
| tracked files containing any of `/Users/ ~/ /Applications/ /Library/ /opt/ /Volumes/` | 235 | — |
| tracked files containing `/Users/lukerhodes/Dev/mcp-router` | 36 | — |
| runnable tracked files containing `/Users/` or `/Volumes/` (the gate's N2) | 4 → 0 | — |

**The ledger's 40 is reproducible and was right.** Its normalisation is *tracked files containing
the literal string `/Users/lukerhodes`, counted by file, over the whole tracked corpus*, and it
returns exactly 40 at `b757a1ac` — the commit on the day G9 was swept for. It returns 44 today.

That drift is the item's own thesis arriving on schedule. Nobody edited a path; four files were
added and two were removed, and the two removed are the gate scripts G9's first half repaired. A
count of decaying things decays. It belongs in a record as a figure with a commit attached, or not
at all.

**Scope: `git ls-files` over the whole tracked corpus, no directory filter.** Stated because a
too-narrow scope is this fleet's live defect, filed as `G8`: the sweep that found G9 covered
`scripts/` only, returned a confident zero, and missed `app/Scripts/` and `planning/` — where both
offenders were. It was committed inside G9's own sweep, minutes after G8 was filed.

**And it happened again here, one layer in.** The sweep above greps the literal `/Users/lukerhodes`.
`scripts/acceptance/parity-normalise-selftest.sh` and `scripts/parity/generate-vectors.mjs` carry
`/Users/someone` and `/Users/x`, so all 44- and 40-shaped readings miss both. They were found by the
gate, which patterns on `/Users/` rather than on a username. A sweep narrowed by a username is the
same fault as a sweep narrowed by a directory, and the second one is harder to see.

## The per-file decision

Of 66 tracked files carrying a `/Users/` or `/Volumes/` path today, 4 were runnable and 62 are not.

**Derived — 4 runnable files, all fixed.** A script acts on a path rather than describing one, so
its failure is silent execution against nothing.

| file | was | now |
|---|---|---|
| `planning/watch-fleet.sh` | repo root + a pinned `~/.claude/projects/<session-uuid>` | root from `git rev-parse --show-toplevel`; the session **discovered from a run id** |
| `planning/fleet-runner.js` | the repo root pasted into every runner prompt | a derived `REPO_ROOT` interpolated |
| `planning/test-campaign/evidence/runs/capture-mocks.py` | three pinned paths | `Path(__file__).parents[4]`, raising if the prototype is not there |
| `planning/test-campaign/bin/capture-mock-refs-hidpi.py` | a pinned `file://` URL | `parents[3]` and `.as_uri()` |

`watch-fleet.sh`'s session directory is the one the brief singled out, and the fix is not to make
the `~` relative. `~/…` is user-relative and resolves anywhere; what decayed was the session UUID
inside it, which is ephemeral by design. A run id is already an argument and names exactly one
session, so looking the session up from the run id cannot go stale — the pin is removed rather than
softened.

**Legitimately absolute — 2 runnable files, waived by line with a reason.** In both, the path is the
*value under test* and nothing opens it: `parity-normalise-selftest.sh` feeds `/Users/someone/…` to
the normaliser it is testing, and `generate-vectors.mjs` uses `/Users/x/Dev/…` as `basename()`
inputs and as cwd labels the usage store groups by. Waivers are per line, carry a non-empty reason,
and are printed by name on every gate run.

**Legitimately absolute — 55 of the 62 non-runnable files, unchanged.** Rewriting any of these would
falsify what they record.

| class | files | why the path stays |
|---|---|---|
| Recorded evidence — `planning/evidence/**`, `planning/test-campaign/evidence/**`, three `planning/progress/*.md` | 21 | The path is part of the measurement: which bundle `axkit` attached to, which window a capture came from, the cwd a tool printed. A capture manifest whose paths were rewritten is no longer a record of where the capture came from. |
| Fixture and test subjects — `app/Sources/**/Fixtures`, `app/Tests/**`, `app/Tests/**/Vectors`, four `design/mocks/**` | 29 | The path is data the product handles: a server `command`, a `cwd` the activity view groups by, a discovery path a test asserts on. A fixture rewritten to be portable stops representing the thing it stands in for. |
| Generated — `planning/tailings/*.json`, `planning/test-campaign/{campaign,witness-worklist}.json` | 5 | Regenerated by their own producers; a stale path here is fixed by re-running the generator, not by a writer. Counted by name rather than dropped. |

**Adjudicated individually — the remaining 7.**

| file | decision |
|---|---|
| "`bash scripts/acceptance/parity-gate.sh` from", `planning/plans/plan-P4.md:288` and `:289` at `f366de8` | **Derived.** Those two lines named `/Users/lukerhodes/Dev/mcp-router` and `…/.worktrees/P4`, and only ever meant *the main checkout* and *the worktree root*. Reworded to say that; the frame is the pre-fix tree, because the text the decision was taken against is not in the file any more. |
| anchor `Makefile at the **worktree root**`, `planning/plans/plan-P2.md:338` at `f366de8` | **Derived**, same reading, same pre-fix frame. |
| anchor `(r'/Users/[^"]*?/mcp-router/\.worktrees/[^"/]+', '<repo>')`, `planning/plans/plan-P4.md:231` at `5e3c90d` | **Legitimately absolute.** It is a regex — the repo's own worktree-path normaliser — not a path. |
| anchor `PATH=/Users/lukerhodes/.nvm/versions/node/v22.23.1/bin`, `planning/specs/spec-R6.md:15` at `5e3c90d` and `planning/features-to-triage/R6-child-process-path.md:12` at `5e3c90d` | **Legitimately absolute.** The `PATH` a child process inherits is R6's entire subject. |
| "Reproducing the reference requires the environment", `planning/specs/spec-R2W.md:220` at `5e3c90d` | **Legitimately absolute.** The record is explicitly about needing that environment to reproduce. |
| "Worktree `/Users/lukerhodes/Dev/mcp-router/.worktrees/M20`, branch", `WORK-ORDER.md:15` at `f366de8` | **Reported, not touched — and settled since by `G6`.** It pinned `.worktrees/M20`, which is gone: G9's exact shape in a record. `G6` owned whether a dispatch artifact belongs at the repo root, and answered by moving it: `WORK-ORDER.md` does not exist at `5e3c90d`, deleted at `0cf2b18`. The frame is therefore the last tree that held it, which is what makes this row still checkable. |
| anchor `export HARBOURMASTER_SCRIPTS=/Users/lukerhodes/Dev/fledgeling-plugins`, `ORCHESTRATOR.md:401` at `5e3c90d` | **Reported, not touched.** A genuine machine pin in an instruction other sessions follow. This runner may not write that file. |

## The pre-commit hook

Two faults, and only one of them is the one the brief named.

**The unset.** Git exports `GIT_DIR` for every commit inside a linked worktree — which is every
runner commit in this fleet — so anything the hook invokes carries that git dir into whatever
directory it walks into and runs against the runner's repository rather than the one it is standing
in. That is DEF-253's mechanism. Git does *not* export either variable for a plain commit in the
main checkout, so the line is inert on that path and load-bearing on the one the fleet uses. It is
preferred to an inventory because it holds for whatever the hook is asked to invoke next.

**The bypass the unset does not close**, found while proving the unset. Git runs a hook standing in
the *work tree*, and `GIT_WORK_TREE` moves the work tree without moving the commit. Committing from
the main checkout with it aimed at a linked worktree put the hook's cwd inside that worktree while
the commit still landed on `main` — the exact thing the hook refuses — and a cwd-derived
`git rev-parse --git-dir` read `.git/worktrees/<name>`, called it a runner and allowed it. The hook
now reads `git rev-parse --absolute-git-dir`, which answers from the environment git set up for the
commit and names the repository the commit is *going to*.

One case is an expected allow, recorded so nobody later "closes" it: with `GIT_DIR` aimed at a
worktree git redirects the whole commit there, so it lands on `runner` and `main` does not move.

`planning/hooks/prove-pre-commit.sh` runs six directions and eight assertions in throwaway
repositories, never touching this one, with two presence controls that exit 2 rather than print a
pass if they agree with the treated arm.

**How a fresh clone gets it.** The mechanism already existed and was half-built: `planning/hooks/`
is tracked, and its README documented a `cp` into `.git/hooks`. What was missing is any way to find
out afterwards whether it worked — an edited tracked hook and a stale installed copy look identical
from outside, which is a control believed armed with nothing checking. `planning/hooks/install.sh`
installs from the tracked source and `--check` reports drift by sha256, exit 1 on any mismatch,
absence, or lost executable bit. It derives the root and resolves `--git-common-dir`, so one run
covers the main checkout and every worktree.

**It is not installed on this machine, deliberately.** `--check` currently reports DRIFTED for both
hooks, which is correct: this branch changed the tracked source and nothing has been installed.
`.git/hooks` is shared by every linked worktree and three runners are in flight, so installing from
inside one changes the controls under all of them. That is the orchestrator's to run.

## The gate

`planning/runnable-path-gate.py`. **A file the repository can run must not name a path under
`/Users/` or `/Volumes/`.**

The family earns a gate, but only this line of it. "No absolute paths" would red on 62 non-runnable
files that are mostly legitimate, and a gate that can never go green gets softened until it means
nothing. This one is at zero and stays there.

**It runs from `make lint`.** That is not a detail: G9's whole finding about the two R2 scripts is
*nothing went red, because nothing invoked them*, and this gate shipped invoked by nothing — no
Makefile target, no hook, no goals script — which is the same sentence one layer up. It is now the
fifth script gate in `lint`, beside `reader-accounting.py`, `null-run-gate.py`,
`py39-annotation-gate.py` and `citation-gate.py`, for the reason those are there: four seconds,
hermetic, and a lane of its own is a lane somebody runs separately from `all`. It read "fourth"
until `py39-annotation-gate.py` landed on `main` mid-flight; the recipe took the rebase and the
ordinal did not, which is the sort of number this document exists to stop carrying forward
(`grok-4.6`, 2026-08-26).

Proved from the wiring rather than standalone. G9's original defect appended verbatim to
`planning/run-gate.sh` — `cd /Users/lukerhodes/Dev/mcp-router/.worktrees/R2 || exit 90` — and then
`make lint`: N4 moved 0 → 1, the gate named the appended line — `planning/run-gate.sh:16` at
`5e3c90d` plus one appended line, the last line of the file as planted — under `BLOCKING`, printed
`FAIL 1 runnable file(s) name a path this checkout cannot rely on`, and `lint` went red. Restored
byte-identically, sha256 `a286744e42fca177952fbec7b537ed0c38e345d21027cdbf87c131029bfa5815` before
and after, and the gate returns to `PASS` at exit 0.

It sits **beside** `planning/foreign-path-gate.py`, not inside it. Two axes:

| | hand-written record | runnable file |
|---|---|---|
| **scratch root** (`/tmp`, `/var/folders`, `$TMPDIR`) | `foreign-path-gate` blocks | `foreign-path-gate`: RUNTIME, passes |
| **machine root** (`/Users`, `/Volumes`) | counted, never blocked | **this gate blocks** |

That gate's docstring declines the machine-path corner explicitly and gives the reason — *a scratch
path fails because the artifact evaporates, a machine path fails because the reader is on another
machine* — and argues that folding it in would add an uncoupled third axis to an item whose case is
that its own two halves are coupled. This is the remedy it names, for the executed half. An earlier
draft added a second, duller reason — that importing an unmerged branch's module would make this
gate's arming depend on `ai/g6` landing. `ai/g6` merged at `03c34c3`, so that clause is gone from
here and from the gate's docstring; the two-axis argument is the reason and was the reason all
along.

Counts, printed together and never blended, re-derived at `5e3c90d` by running the gate rather than
carried forward: **N1** 143 runnable tracked files · **N2** 3 with a machine
path · **N3** 18 occurrences · **N4** 0 blocking. `~/` (57 lines), system locations (8) and scratch roots (99) are
counted and let through, each with the reason it is a different defect, and 67 NON-runnable tracked
files hold a machine path and are adjudicated above.

An earlier draft of this paragraph read N1 134 · N2 2 · N3 12 and 55 · 4 · 63. Those were the tree
the gate was written against; N1 is a count over `git ls-files` and moves whenever the tree does.
The numbers are printed here with the tree that produced them for that reason.

## Proof that each check can fail

| check | plant | result | restored |
|---|---|---|---|
| `runnable-path-gate.py`, real tree | G9's original defect verbatim — `cd /Users/lukerhodes/Dev/mcp-router/.worktrees/R2 \|\| exit 90` appended to `planning/run-gate.sh` | exit **1**, naming the appended line — `:18` when this was first measured, `:16` when re-measured at `5e3c90d`, because the line number is a property of the file's length on the day and not of the finding | sha256 `a286744e42fca177952fbec7b537ed0c38e345d21027cdbf87c131029bfa5815` before and after; exit 0 again |
| `runnable-path-gate.py`, presence control | 9 instances across BLOCK / WAIVED / CLEAN / NOT_RUNNABLE in a throwaway repo, including a bare marker with no reason that must **not** waive | all 9 read exactly; exits 2 if any is missed | throwaway, nothing planted here |
| `prove-pre-commit.sh` D4 control | the pre-G9 cwd-derived hook, reconstructed | ALLOWED the `GIT_WORK_TREE` bypass, as it must for D4 to be evidence | hook sha256 `cb8b60364e602760d3f4a41360efb382da17dee8b2424e4a4e814fce26d983c5` before and after |
| `prove-pre-commit.sh` D6 control | the same hook with the `unset` line stripped | the descendant resolved the *committer's* git dir; with the unset it resolved its own | as above |
| `install.sh --check` | nothing — run against the live state | exit **1**, DRIFTED on both hooks | n/a |

Both absence sweeps carry a presence control, because an absence check cannot detect its own
blindness: a sweep that has never been shown to fire returns zero for reasons it cannot tell apart.
Both controls run on every invocation rather than behind a flag.

**The gate blocked itself the moment it was committed**, and that is the sharpest thing measured
here. It passed every run while it was untracked and failed the first run after the commit, having
not changed a byte: it reads `git ls-files`, so its own source was outside its corpus until it was
tracked. Anything keyed on the index is inert on itself until committed, and a gate proved green
before its own commit has been proved against a corpus that excludes it. Its nine planted fixtures
are its subject — the same class as the path vectors it waives in `scripts/parity` — so they are
waived per line with a reason.

Three instruments were themselves wrong first, and are recorded because the correction is the
evidence. `prove-pre-commit.sh` initially read its verdict from git's exit code, so
`ERROR:nothing-staged` was indistinguishable from a refusal and one direction reported REFUSED
having never reached the hook; it now reads the hook's own refusal text and names the other
outcomes. The D4 presence control staged its probe into a tree the effective work tree did not
include, so it reported `ERROR:nothing-staged` and correctly refused to certify D4 — a control
declining to fire is the mechanism working, not a nuisance. And the gate's waiver reader used one
regex with a greedy reason, which made "the first marker" and "the last marker" the same match, so a
fixture string carrying marker text as data supplied the reason and printed nonsense beside two
legitimate waivers; the reason now comes from the rightmost marker, and a marker with no reason
still does not waive.

One claim died to its own control and is recorded because it was believed: `GIT_DIR` is **not** a
bypass of the pre-commit hook. It reads as one — `git rev-parse --git-dir` answers from it, so the
hook classifies the commit as a runner's — but git redirects the whole commit to that worktree, so
it lands on `runner` and `main` never moves. The presence control refused to certify the direction,
which is how the claim was caught before it reached a record. The real bypass is `GIT_WORK_TREE`,
which moves the hook's cwd and not the commit.

## Reported, not done

- anchor `export HARBOURMASTER_SCRIPTS=/Users/lukerhodes/Dev/fledgeling-plugins`,
  `ORCHESTRATOR.md:401` at `5e3c90d`, pins a plugin cache path outside the repo. This runner may
  not write that file.
- "Worktree `/Users/lukerhodes/Dev/mcp-router/.worktrees/M20`, branch", `WORK-ORDER.md:15` at
  `f366de8`, pinned the deleted `.worktrees/M20`. `G6` owned whether that file should be tracked
  at the repo root and has since answered by deleting it at `0cf2b18`; it is not in the tree at
  `5e3c90d`, so this hand-off is closed by that item rather than open.
- `planning/hooks/install.sh` has not been run. `--check` reds at exit 1 until the orchestrator runs
  it, which is the correct reading: `.git/hooks` is shared by every linked worktree, so installing
  from inside one changes the controls under all of them. `.git/hooks/pre-commit` still hashes
  `581204dc6d99`, unchanged by this branch.
- `planning/features-to-triage/LEDGER.md` needs G9's residual rows closed and the 40-versus-44
  normalisation recorded; this runner may not write it.

## Gap-fix, 2026-08-26

Returned `NEEDS MORE WORK` on two findings, both about the gate's relationship to the tree rather
than to its subject. The measurement work was confirmed and is untouched.

**Gap 1 — the gate was invoked by nothing, and unlike the hook install that was not declared.**
`git grep runnable-path-gate` returned the file and this document. The "Reported, not done" list
above named three hand-offs and omitted it, so a reader had no way to learn from the record that
the gate was inert. Closed by wiring it into `make lint` and proving it red from there, above.
Nothing about the hook changed: non-installation stays a stated hand-off, and it is stated.

**Gap 2 — a clause that would be false on merge.** The docstring gave, as a second and duller
reason for sitting beside `foreign-path-gate.py`, that the latter was *"on `ai/g6` and unmerged"*.
`ai/g6` merged at `03c34c3` and that file is on `main`. The clause is deleted rather than corrected:
the two-axis argument is the reason and always was, and the second one was scheduling. The body of
this document carried the same parenthetical and it is gone too.

**Rebased onto `5e3c90d`.** The branch's base was still an earlier `Merge ai/g7`; `main` moved twice
during this gap-fix, taking in `ai/g6` and a goal-gate repair.

**Eight bare citations, all in this file, all now anchored and framed.** They were the per-file
adjudication table and the hand-off list — a `path:line` per row with nothing to check it against.
Two of them needed a *pre-fix* tree, because the text the decision was taken against is not in the
file any more, which is the case `CITATIONS.md` says renumbering cannot repair. One of them,
`WORK-ORDER.md:15`, turned out to be answered rather than open: `G6` deleted that file at `0cf2b18`,
so the row now records a closed hand-off with the last tree that held it. `planning/progress/G9.md`
no longer appears over the citation ratchet, which stands at 9 files, none of them this branch's.

### Out-of-family review, `grok-4.6` at xhigh

It read the branch rather than the summary and returned five answers. `make lint` confirmed as the
right home, for a reason I had not given: `planning/goals/lint-except-citation-ratchet.sh` parses
the `lint:` recipe rather than a copied list, so a fifth *target* would be missed by that parser —
gap 1 again, one layer out. Three real defects, all fixed above and all armed:

* **The waiver could be forged from inside a string.** `WAIVER` matched `path-gate: ok — <reason>`
  anywhere on the line, with no comment introducer, so a line carrying a machine path could waive
  its own BLOCK by echoing the marker. Now `(?:#|//)`-scoped. The control had no plant that could
  tell the difference — removing the scoping left every arm reading exactly as planted — so
  `quoted_marker.sh` is new: a machine path plus the marker inside a shell string, expected BLOCK.
  Removing the scoping now prints `MISS quoted_marker.sh expected BLOCK read WAIVED` at exit 2.
* **`NOT_RUNNABLE` could not fail in the skip direction.** It was returned both for a correct
  exclusion and for a plant that was never written or never added. The control now requires every
  plant to be on disk and in the index first. ~~adding only `notrunnable.md` makes it exit 1 with
  `NEVER REACHED THE INDEX`~~ — **that sentence was false, and the round below is about why.** The
  guard fired; the string did not print, and the 1 was a `KeyError` traceback rather than a
  verdict.
* **Two numbers in this document had gone stale again** — "fourth script gate" and N1 142 — both
  because `main` moved under the branch mid-flight.

Two limits it named are recorded rather than closed, because closing either widens the corpus and
the verifier certified these denominators: `src/*.ts` are mode 100644 with no shebang and no
interpreter suffix, so the twelve TypeScript sources that compile into the runnable `dist/index.js`
are outside N1 (none carries a machine path today); and `BLOCK` matches neither a `$USER`-style
path — arguably right, since that is `~/`'s portable shape — nor a case-folded `/users/`, which on a
case-insensitive volume is the original defect spelled differently. Both are the owner's call.
**Both now print on every run of the gate itself** — see the round below, which also refines the
first: 13 `src/*.ts` are tracked and 12 are outside N1, because `src/index.ts` carries a shebang
and is already in the denominator.

## Gap-fix round 2, 2026-08-26 — the verifier returned NEEDS MORE WORK on one arm

Everything else in this record was reproduced and confirmed by the verifier. One gap, and it was
this item's own subject reintroduced inside the fix for it.

### The blocking gap: the `NEVER REACHED THE INDEX` arm was unreachable

`control()`'s guard returned `{}, {…}` — an empty reading with a populated failed set. `main` then
ran `mark = "ok  " if got[name] == expected else "MISS"` **before** it tested `bad`, so the first
plant raised `KeyError: 'block_sh'` and the interpreter chose the exit code. It chose **1**.

That matters because this gate's own table assigns **2** to CONTROL-DID-NOT-FIRE and **1** to
FINDINGS. A tripped guard therefore produced a broken control wearing a findings exit code — which
is verbatim the defect `G8`'s gap-fix closed as its finding b, and `G8` merged on having closed it.
`bad`'s *values* were printed nowhere, so `grep` finds `NEVER REACHED THE INDEX` exactly once in
the file, at its own definition: no input could print it.

**The fix**, both halves, because either alone leaves the class alive:

* `control()`'s guard now returns a reading for **every** plant — `NEVER REACHED THE INDEX` for the
  ones that did not arrive, `NOT ASSESSED` for the ones that did. Nothing was classified once a
  plant is missing, so no plant may read `ok`; `bad` covers all ten and the run exits 2.
* `main` reads `got.get(name, "NOT ASSESSED")` rather than `got[name]`. An arm that reports a
  broken control must not be reachable only through an exception that has already chosen the wrong
  exit code for it.

**Armed both ways the failure can arrive**, reproducing the verifier's two plants against a copy of
the gate at `planning/_g9probe.py` (untracked, so outside its own corpus; deleted after):

| plant | before the fix | after the fix |
|---|---|---|
| `git add notrunnable.md` in place of `add -A` | `KeyError: 'block_sh'`, exit **1**, string never printed | 10 `MISS` rows, 9 reading `NEVER REACHED THE INDEX` and `notrunnable.md` reading `NOT ASSESSED`, then `CONTROL DID NOT FIRE`, exit **2** |
| only `notrunnable.md` written to disk | `KeyError: 'block_sh'`, exit **1**, string never printed | identical to the row above, exit **2** |

`planning/runnable-path-gate.py` sha256 `06e58300c56fe826dda3a00641a99d27a498b3b1cc2d409960c751f4d065c41f`
before and after the arming; clean run exit 0 either side.

### The same shape elsewhere in the file: none, and three arms that are merely unexercised

Every other arm is reachable by some input, and all but three have been shown to fire:
`bad` on the classifier path (armed by the verifier: `MISS quoted_marker.sh expected BLOCK read
WAIVED`, exit 2), `BLOCKING`/exit 1 (armed: N4 0→1, the appended line named in `run-gate.sh` —
see the row in *Proof that each check can fail* above for the plant and its hashes), exit 0 (today's
verdict), the `WAIVED` section (19 rows on the real corpus today), `record_diagnostic` (67), and all
three COUNTED lines (57 / 8 / 99). The three unexercised arms are the `except OSError` handlers in
`scan`, `is_runnable` and `record_diagnostic`. They are reachable by input — a tracked file that
exists but cannot be read — and none of them chooses an exit code, so none can repeat the class
above. Recorded rather than armed.

### The blind spots now print where the gate's output is read

Both recorded limits lived only in this file, where nobody reading the gate's output would meet
them, so a `NOT MEASURED HERE` block prints on every run under the counts. Every figure is
re-derived from the tree — a pinned figure is what this gate exists to argue against — and each of
its three counters has been shown to move:

| counter | plant | reading | restored |
|---|---|---|---|
| `src/*.ts` outside N1 naming a machine path | a `/Users/…` comment appended to `src/config.ts` | 0 → **1** of 12, with N2 and N4 unmoved and exit still 0 | sha256 `f463cfa98ff2de4c1291e260b41b2dad214b4381bf6df24beb13076a84b1e39f` before and after |
| `$VAR`-interpolated, and lower-case, in a runnable file | one comment line carrying both, appended to `planning/run-gate.sh` | both 0 → **1**, N2 and N4 unmoved, exit still 0 | sha256 `a286744e42fca177952fbec7b537ed0c38e345d21027cdbf87c131029bfa5815` before and after |

The unmoved N4 is the point: the block reports what the verdict does not count, so the zero above it
is read at its real width. `dist/index.js` — the artefact that actually runs — is not tracked at
all, so it is in no denominator here; the block says so rather than implying the sources stand in
for it.

### One thing the rebase onto `main` surfaced, fixed here because this branch causes it

`G8`'s `sweep-control-gate.py` reached `main` while this was in flight, and it discovers new
undisposed sweeps. Two of the files this branch adds are sweeps: `planning/runnable-path-gate.py`
(V1,V3,V4) and `planning/hooks/prove-pre-commit.sh` (V1,V2,V3). Unregistered, the branch took that
gate from `main`'s 5 UNDISPOSED / 6 FINDINGS to 7 / 8. Both carry a real presence control that runs
on every invocation, so both are registered in `planning/sweep-controls.json` with
`disposition: control` and the command that proves it — not `grandfathered`, which is a backlog
entry rather than a pass. The gate now reads 5 / 6 on this branch, identical to `main`.

Armed end to end, which is also the cleanest demonstration that the repaired arm carries the right
code where somebody else reads it: with the `add notrunnable.md` fault planted in the tracked gate,
`sweep-control-gate.py` reports

```
  FAIL  planning/runnable-path-gate.py         control exited 2 — it said: The verdict below would be an absence this instrument cannot see, so none is printed.
```

Before this round that row would have read `control exited 1` with a traceback for its last line —
a rotted control indistinguishable from a real finding. `planning/runnable-path-gate.py` sha256
`e1a12304215ea41cf93d3c4d1795e29e295fd37139ffb0fd17eaf27c4a4b9c80` before and after the plant.

### Gates, this round

| gate | exit | reading |
|---|---|---|
| `make test` | **0** | 1980 tests in 252 suites |
| `python3 planning/ledger-reconcile.py` | **0** | no findings across A–L |
| `python3 planning/citation-gate.py` | **1** | `control 0 · blocking 0 · ratchet 1` — the inherited ratchet, nothing blocking |
| `python3 planning/py39-annotation-gate.py` | **0** | — |
| `python3 planning/runnable-path-gate.py` | **0** | control fired 10/10; N1 146 · N2 3 · N3 19 · **N4 0** |
| `bash planning/hooks/prove-pre-commit.sh` | **0** | 8 assertions over 6 directions, 2 presence controls fired |
| `python3 planning/sweep-control-gate.py` | **1** | 5 UNDISPOSED / 6 FINDINGS — identical to `main` |
| `make -o tools lint` | **2** | `tools` needs `node_modules`, so `-o tools`. Red from the two rows above and nothing else |

The hook is still not installed, deliberately: `.git/hooks` is shared with the other live worktrees.
`ls .git/hooks` read 0 before and after `prove-pre-commit.sh`, which works only in throwaway
repositories.

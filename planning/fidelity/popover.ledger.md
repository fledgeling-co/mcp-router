# Breadth ledger — popover (generated by scripts/acceptance/mock-fidelity-gate.sh)

## This run did not produce a table

The gate exited 3 without completing a measurement, so there are no rows below rather than no differences. What stopped it, and at which stage, verbatim:

```
inventory[ideal]: error: #statusPopover has no '.v-ideal' block
```

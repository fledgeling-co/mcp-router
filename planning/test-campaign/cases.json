[
  {
    "id": "CASE-0001",
    "req": "REQ-003",
    "surface": "SURF-001",
    "flow": null,
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-001",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/mac-glass-capture.log",
      "evidence/ax/SURF-002.title.txt",
      "evidence/ax/SURF-003.title.txt",
      "evidence/ax/SURF-008.title.txt",
      "evidence/runs/glass-assert.json"
    ],
    "note": "Sidebar AXPress selected Activity, Servers, Skills, Discover, Inbox, Checks, Cleanup, Settings; window titles matched. Cmd+1\u20137 not part of this case. ARMED: predicate watched to fail under the SURF-001 decoy.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-001's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106."
  },
  {
    "id": "CASE-0002",
    "req": "REQ-003",
    "surface": "SURF-001",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-001",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "inputModality": "keyboard",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "n/a: posting Cmd+1\u20137 to a background pid does not change SwiftUI's focused scene (measured: title stayed Inbox then Activity); proving the shortcuts requires the window frontmost, which this campaign refuses",
    "armed": false,
    "evidence": [
      "evidence/ax/SURF-001.keyboard.txt",
      "evidence/ax/SURF-001.menu.txt",
      "evidence/runs/glass-assert.json"
    ],
    "note": "REQ-003's keyboard half, and it stays n/a because the thing the requirement promises cannot be measured from here. Posting \u23181\u20137 to a background pid does not change SwiftUI's focused scene \u2014 measured, title stayed Inbox and then Activity \u2014 and a menu item's action reaches the window through `@FocusedValue`, which an inactive app does not have. Proving the keystroke NAVIGATES needs the window frontmost, which this campaign refuses. That is a fact about the platform and this campaign's posture, not about the product, and it is why this counts against the total rather than disappearing from it.\n\nWhat was measured instead, and is now asserted rather than described: the running app's own menu bar declares all eight bindings \u2014 \u23181 Activity, \u23182 Servers, \u23183 Skills, \u23184 Discover, \u23185 Inbox, \u23186 Checks, \u23187 Cleanup, \u2318, Settings \u2014 each with an empty modifier mask beside the command key, read off `AXMenuBar` on the live process and scoped to `menuAction:` rows because the Window menu lists the same board names under `makeKeyAndOrderFront:` with no key of their own. Those eight checks ride on CASE-0001 and CASE-0101, which are armed: the SURF-001 decoy reds 34 assertions, 16 of them these bindings. A binding silently lost would now fail something. A binding that exists and does not navigate still would not."
  },
  {
    "id": "CASE-0003",
    "req": "REQ-004",
    "surface": "SURF-002",
    "flow": "FLOW-004",
    "step": "FLOW-004.01",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-002",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-002.window.txt",
      "evidence/shots/SURF-002.build.png",
      "evidence/runs/glass-assert.json"
    ],
    "note": "AX: 2 tools from 4 servers \u00b7 1 running; fixture-stdio/http dormant, fixture-tools running, fixture-oauth wants decision. No invented figure. ARMED: predicate watched to fail under the SURF-002 decoy.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-002's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106."
  },
  {
    "id": "CASE-0004",
    "req": "REQ-005",
    "surface": "SURF-009",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-009",
      "state": "stale",
      "viewport": "320x480",
      "theme": "light",
      "role": "operator",
      "dataShape": "offline",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "n/a: NSStatusItem is not an AXPress target while MCPRouter is backgrounded; this campaign never activates, so the popover cannot be opened or photographed",
    "armed": false,
    "evidence": [
      "evidence/runs/mac-glass-capture.log"
    ],
    "note": "SURF-009 stale/offline readout unmeasured on glass. Headless honesty of live-count absence is CASE-0008 / CASE-0042."
  },
  {
    "id": "CASE-0005",
    "req": "REQ-006",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "step": "FLOW-001.03",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-008",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "queued-arrival",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-008.window.txt",
      "evidence/runs/make-test-2.log",
      "evidence/runs/glass-assert.json",
      "evidence/arming/CASE-0005.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0005.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-008's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106."
  },
  {
    "id": "CASE-0006",
    "req": "REQ-008",
    "surface": "SURF-009",
    "flow": "FLOW-001",
    "step": "FLOW-001.02",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-009",
      "state": "populated",
      "viewport": "320x480",
      "theme": "light",
      "role": "operator",
      "dataShape": "arrivals-queue",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "n/a: NSStatusItem is not an AXPress target while MCPRouter is backgrounded; inbox band never opened",
    "armed": false,
    "evidence": [
      "evidence/runs/mac-glass-capture.log"
    ],
    "note": "FLOW-001.02 popover band unmeasured on glass. FLOW-001.03/.04 proved on the Inbox board and in make-test-2.log."
  },
  {
    "id": "CASE-0007",
    "req": "REQ-009",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "step": "FLOW-001.04",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-008",
      "state": "disposition",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "single-item",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0007.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0007.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted a disposition withdrawing its own banner at the moment it happens. Reverting leaves the notification standing for an item the user has already dealt with, so the banner invites a second decision on a settled thing. Mutation in Sources/MCPRouterUI/Boards/InboxBoardModel+Arrivals.swift; `swift test --filter withdrawalIsCommandedAtTheDisposition` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0008",
    "req": "REQ-007",
    "surface": "SURF-001",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-001",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "dark",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0008.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0008.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the cleanup footer's refusal to claim a memory saving \u2014 the honesty guardrail REQ-007 names. Reverting it turns the disclaimer into the fabrication. Mutation in Sources/MCPRouterKit/Cleanup/CleanupPresentation.swift; `swift test --filter footerRefusesTheFabrication` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0009",
    "req": "REQ-012",
    "surface": "SURF-011",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-011",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0009.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0009.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the IPv4-loopback pin on the control listener. Without it NWListener binds every interface and puts an endpoint that runs the user's MCP servers on the LAN. Mutation in Sources/RouterCore/HTTP/LoopbackHTTPServer.swift; `swift test --filter loopbackPinned` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0010",
    "req": "REQ-015",
    "surface": "SURF-010",
    "flow": "FLOW-002",
    "step": "FLOW-002.01",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-010",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "fail",
    "armed": false,
    "evidence": [
      "evidence/ax/SURF-010.window.txt",
      "evidence/shots/SURF-010.build.png"
    ],
    "note": "DEF-001 / REQ-015. Sheet title `Pair iPhone`; body `Pairing is not available in this build` and `this build ships no way to listen for one.` No 8-character Crockford code, no QR \u2014 checked by pattern on the dump, not by reading it. Characterised, not inventing a transport, and it stays a FAIL because REQ-015 asks for a code and a QR that this build does not have. What the build does instead is honest, and that is asserted separately as CASE-0142/0143 against REQ-019, which is a different requirement and does not discharge this one. Captured 20 Aug 2026 after the capture script's pairing press was fixed: it had been pressing from the Servers board, where the `Pairing\u2026` control does not exist, and reporting the surface unreachable."
  },
  {
    "id": "CASE-0011",
    "req": "REQ-003",
    "surface": "SURF-003",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-003",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "call-stream",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-003.title.txt",
      "evidence/ax/SURF-003.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-003's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-003 decoy."
  },
  {
    "id": "CASE-0012",
    "req": "REQ-003",
    "surface": "SURF-004",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-004",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-004.title.txt",
      "evidence/ax/SURF-004.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-004's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-004 decoy."
  },
  {
    "id": "CASE-0013",
    "req": "REQ-003",
    "surface": "SURF-005",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-005",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "catalog",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-005.title.txt",
      "evidence/ax/SURF-005.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-005's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-005 decoy."
  },
  {
    "id": "CASE-0014",
    "req": "REQ-003",
    "surface": "SURF-006",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-006",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-006.title.txt",
      "evidence/ax/SURF-006.window.txt",
      "evidence/runs/glass-assert.json",
      "evidence/runs/arm-pipeline.json",
      "evidence/arm-pipeline/mutated.SURF-006.window.txt"
    ],
    "note": "Title Checks, not Evals; copy includes 'No model-graded evaluation exists'. ARMED: predicate watched to fail under the SURF-006 decoy. Pipeline-armed on a rebuilt app, not only on a recording.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-006's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106. PIPELINE ARM, end to end: both halves of the Checks honesty guardrail were reverted in app/Sources/MCPRouterKit/Checks/CheckCopy.swift (\"it is not a graded test\" \u2192 \"it grades how well\"; \"skills are never executed by the router\" \u2192 \"skills are executed by the router\"), the Mac app was REBUILT with `make build-mac`, relaunched backgrounded and re-photographed through the same window-scoped path the campaign uses \u2014 and the suite then went red on exactly CASE-0014, CASE-0106 (checked=137 failures=4 cases=18). Source and every recorded artifact were restored and it is green again (checked=137 failures=0 cases=18). Focus was never taken (capture reported focus unchanged: True). This is the one arm that proves the build\u2192launch\u2192capture\u2192assert CHAIN bites, not merely the predicate; the mutated build's own dump and PNG are kept under evidence/arm-pipeline/ so the red is inspectable."
  },
  {
    "id": "CASE-0015",
    "req": "REQ-003",
    "surface": "SURF-007",
    "flow": "FLOW-004",
    "step": "FLOW-004.02",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-007.title.txt",
      "evidence/ax/SURF-007.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-007's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-007 decoy."
  },
  {
    "id": "CASE-0016",
    "req": "REQ-003",
    "surface": "SURF-011",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-011",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-011.title.txt",
      "evidence/ax/SURF-011.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-011's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-011 decoy."
  },
  {
    "id": "CASE-0017",
    "req": "REQ-018",
    "surface": "SURF-008",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-008",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0017.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0017.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted undo actually restoring the declined row. Reverting leaves a control labelled Undo that reports success and restores nothing. Mutation in Sources/MCPRouterUI/Boards/InboxBoardModel.swift; `swift test --filter declineIsUndoable` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0018",
    "req": "REQ-019",
    "surface": "SURF-008",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-008",
      "state": "refused",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "storage-failure",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0018.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0018.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted a refused write throwing rather than reporting success. Reverting makes a failed enqueue silently succeed, which is REQ-019's guardrail exactly. Mutation in Sources/MCPRouterKit/Discover/CapabilityQueue.swift; `swift test --filter refusedWriteThrows` then ran 2 test(s) and went RED, and 2 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0020",
    "req": "REQ-010",
    "surface": "SURF-012",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-012",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "catalog",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-012.ios.discover-populated.png"
    ],
    "note": "DEF-013 closed: the tap was issued as soon as the button existed, and existence is not hittability. `selectTab` now waits for hittable, retries up to three times, and asserts on the destination navigation bar rather than on the tab bar's `isSelected` bookkeeping. The runs that stayed red after that were DEF-020 \u2014 the lane was sharing a simulator with another project's app, which held the foreground. three consecutive green runs of `make test-ios-glass` on the lane's own simulator (MCPRouter-Glass, iPhone 17 Pro / iOS 26.5), 5 of 5 each time."
  },
  {
    "id": "CASE-0021",
    "req": "REQ-010",
    "surface": "SURF-013",
    "flow": "FLOW-001",
    "step": "FLOW-001.01",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-013",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-013.ios.triage.png",
      "evidence/shots/ios/SURF-013.ios.queue.png",
      "evidence/shots/ios/SURF-013.ios.library.png"
    ],
    "note": "DEF-013 closed: the tap was issued as soon as the button existed, and existence is not hittability. `selectTab` now waits for hittable, retries up to three times, and asserts on the destination navigation bar rather than on the tab bar's `isSelected` bookkeeping. The runs that stayed red after that were DEF-020 \u2014 the lane was sharing a simulator with another project's app, which held the foreground. three consecutive green runs of `make test-ios-glass` on the lane's own simulator (MCPRouter-Glass, iPhone 17 Pro / iOS 26.5), 5 of 5 each time."
  },
  {
    "id": "CASE-0022",
    "req": "REQ-015",
    "surface": "SURF-014",
    "flow": "FLOW-002",
    "step": "FLOW-002.02",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-014",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-014.ios.camera-preflight.png"
    ],
    "note": "DEF-X2-b was not a product defect. It was DEF-017: the lane built with CODE_SIGNING_ALLOWED=NO, so the app had no keychain access group and SecItemCopyMatching returned -34018 rather than errSecItemNotFound; `PairingRecordStore` maps those apart correctly and Settings rendered the unreadable state truthfully, about a condition the harness had created. Signed ad-hoc, the never-paired state renders and the flow proceeds. Two further product defects were behind it and are now closed \u2014 DEF-019 (the flow's model was rebuilt on every render, resetting it to the scanner) and DEF-022 (the typed surface drew no headline). three consecutive green runs of `make test-ios-glass` on the lane's own simulator (MCPRouter-Glass, iPhone 17 Pro / iOS 26.5), 5 of 5 each time."
  },
  {
    "id": "CASE-0023",
    "req": "REQ-006",
    "surface": "SURF-013",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-013",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0023.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0023.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the popularity unit saying sessions, which is what Smithery publishes. Reverting it claims installs, a number nobody measured. Mutation in Sources/MCPRouterKit/Discover/DiscoverCopyControls.swift; `swift test --filter popularityUnitIsPinned` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0030",
    "req": "REQ-001",
    "surface": "SURF-015",
    "flow": "FLOW-005",
    "step": "FLOW-005.01",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-015",
      "state": "empty",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "daemon",
      "dataShape": "zero-children",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0030.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0030.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted single-flight on a cold upstream. Reverting spawns one child per concurrent caller, so three leases start three real processes. Mutation in Sources/RouterCore/Pool/UpstreamPool.swift; `swift test --filter singleFlight` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0031",
    "req": "REQ-002",
    "surface": "SURF-015",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-015",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "daemon",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0031.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0031.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted splitting a namespaced tool name at the FIRST separator. Reverting splits at the last, which routes a__b__c to a server that does not exist. Mutation in Sources/RouterCore/Manifest/ToolUnion.swift; `swift test --filter splitMatches` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0032",
    "req": "REQ-011",
    "surface": "SURF-015",
    "flow": "FLOW-005",
    "step": "FLOW-005.02",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-015",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "daemon",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0032.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0032.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted a warm upstream never being reaped. Reverting reaps exactly the servers the user committed to paying to keep resident. Mutation in Sources/RouterCore/Pool/UpstreamPoolReaping.swift; `swift test --filter warmIsNeverReaped` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0033",
    "req": "REQ-012",
    "surface": "SURF-016",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-016",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0033.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0033.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the control API being unable to put a command line on the wire. Reverting lets a PATCH rewrite what binary a server runs. Mutation in Sources/MCPRouterKit/Control/ServerPatch.swift, Sources/MCPRouterKit/Control/ServerPatch.swift; `swift test --filter patchCannotRewriteACommandLine` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0034",
    "req": "REQ-013",
    "surface": "SURF-015",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-015",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "daemon",
      "dataShape": "concurrent-write",
      "executionPlane": "headless"
    },
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0034.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0034.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the exclusive flock that serialises config mutation. Reverting lets two holders overlap, so one writer's servers.json clobbers the other's. Mutation in Sources/RouterCore/Config/ConfigMutationLock.swift; `swift test --filter lockSerialises` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0035",
    "req": "REQ-019",
    "surface": "SURF-016",
    "flow": "FLOW-003",
    "step": "FLOW-003.01",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-016",
      "state": "refused",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "unauthenticated",
      "dataShape": "forged-write",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0035.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0035.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the token gate running ahead of routing, so an untokened mutation is 401 rather than 404. Reverting makes an unauthorized call indistinguishable from a typo. Mutation in Sources/RouterCore/Control/ControlHandler.swift; `swift test --filter untokenedAuthIs401` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0036",
    "req": "REQ-014",
    "surface": "SURF-017",
    "lane": "parity-wire",
    "cell": {
      "surface": "SURF-017",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "harness",
      "dataShape": "corpus-83",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-parity.log",
      "planning/evidence/I5-transport-proof.md",
      "evidence/runs/parity-gate-2026-08-19.log",
      "evidence/runs/parity-gate-2026-08-19-remap.log",
      "evidence/runs/parity-gate-2026-08-19-rollback.log",
      "evidence/runs/parity-install-rollback-2026-08-19.log",
      "evidence/runs/parity-install-rollback-2026-08-19.tsv",
      "evidence/runs/parity-gate-2026-08-20-cutover.log",
      "evidence/runs/parity-gate-2026-08-20-oauth-flake.log",
      "planning/evidence/P7-acceptance.md"
    ],
    "note": "parity-gate.sh on main at e5755d0, 20 Aug 2026: **82 of 83 rows proven (4 of them by suite only, not by wire comparison), 0 DIVERGED, 1 blocked.** REQ-014's contract is the owner's cutover target of 82 of 83 with `fixture-registry-search` a standing exclusion, and that is what was measured. The two rows that stood between 80 and 82 closed the same day: `install-launchd-watch` by ai/p8, which made the `reran` term attributable \u2014 it had read yes on 2 of 6 decoy trials where no stimulus could reach the watched path \u2014 and `control-auth-post-http` by ai/p7, which built a real OAuth client and now agrees with the reference across the discovery cascade, the registration bytes, the PKCE parameters, the token exchange, the callback page, the credential file and the re-authorization branch.\n\n**The gate exits 1 and that is not this case failing.** It exits 1 while any row is blocked, deliberately, and the one blocked row is the standing exclusion the requirement names. The gate prints the distinction itself: `82 proven is at or past the target of 82. That is a count, not a licence`. Read as a pass on the requirement, not as a licence to flip the installer \u2014 that is the owner's call on R4-C's evidence, which this case does not make.\n\n**Armed, and one arm was not planned.** The mechanical arm is `parity-lane-selftest`, which runs every lane against a deliberately broken Swift router and requires each to go red; it ran green in the same `make all` that carried this tree, printing `every new lane went red against a broken router. The lanes can fail.` The unplanned arm is the first whole-gate run of the day, kept at `evidence/runs/parity-gate-2026-08-20-oauth-flake.log`: the same script on the same tree reported `81 of 83 rows proven \u2026 1 DIVERGED \u2026 This is NOT a pass`, so the aggregate verdict is live rather than a constant. That run's cause is unexplained and is recorded as DEF-033 rather than absorbed into this note."
  },
  {
    "id": "CASE-0037",
    "req": "REQ-012",
    "surface": "SURF-016",
    "flow": "FLOW-003",
    "step": "FLOW-003.02",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-016",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "oauth-callback",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/runs/make-test-2.log",
      "evidence/runs/arm-suites.log",
      "evidence/arming/CASE-0037.arming.log"
    ],
    "note": "Armed 2026-08-19: mutation went RED (see evidence/arming/CASE-0037.arming.log). Restored from in-memory backup; app/Sources clean after.",
    "armedBy": "Reverted the callback actually exchanging the authorization code it received. Reverting serves the success page for a code that was never redeemed. Mutation in Sources/RouterCore/Auth/CallbackResponder.swift; `swift test --filter codeIsExchangedOverTheWire` then ran 1 test(s) and went RED, and 1 test(s) went green again after the source was restored. The run count is swift-testing's own, because the XCTest bundle prints 'Executed 0 tests' on every run and a red that ran nothing is not a red."
  },
  {
    "id": "CASE-0040",
    "req": "REQ-016",
    "surface": "SURF-014",
    "flow": "FLOW-002",
    "step": "FLOW-002.02",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-014",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "fail",
    "armed": true,
    "armedBy": "I5 tap: same process that stores the paired-Mac record made zero connections to the advertised port; a calibration connection from that process to that port succeeded. Source fact: neither side implements a pairing transport.",
    "evidence": [
      "planning/evidence/I5-transport-proof.md"
    ],
    "note": "DEF-001. Characterised, not assert-corrected. The product stores a paired-Mac record without contacting the Mac."
  },
  {
    "id": "CASE-0041",
    "req": "REQ-017",
    "surface": "SURF-002",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-002",
      "state": "empty",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "zero",
      "executionPlane": "on-glass"
    },
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-002.empty.window.txt",
      "evidence/shots/SURF-002.empty.png",
      "evidence/runs/req017-honesty.json"
    ],
    "note": "Empty: 'No servers declared yet' / '0 tools from 0 servers \u00b7 0 running'. RE-RUNG structural \u2192 metamorphic: the old assertion was that this state's copy appears in the window, which a single state can satisfy while the product is dishonest. The assertion is now the relation \u2014 zero is knowable, so the empty state PUBLISHES 0 tools from 0 servers \u00b7 0 running rather than withholding. That is the control that proves the offline absence is a decision and not a dead selector. Measured over 3 real window dumps, 10 checks, 0 failures.",
    "armedBy": "predicate arm, on a relation rather than a state. bin/req017-honesty.py runs 10 checks across three AX dumps of the SAME window under three loads (populated / empty / offline). The `--decoy absent` run makes the product behave as a readable router that withholds anyway and turns 3 of those checks red: empty publishes a counts line; empty counts are all zero; exactly the readable loads publish counts."
  },
  {
    "id": "CASE-0042",
    "req": "REQ-017",
    "surface": "SURF-002",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-002",
      "state": "offline",
      "viewport": "1024x768",
      "theme": "dark",
      "role": "operator",
      "dataShape": "offline",
      "executionPlane": "on-glass"
    },
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-002.offline.window.txt",
      "evidence/shots/SURF-002.offline.png",
      "evidence/runs/req017-honesty.json"
    ],
    "note": "Offline: 'The router isn't running' / nothing listening on the control port. Live counts absent. RE-RUNG structural \u2192 metamorphic: the old assertion was that this state's copy appears in the window, which a single state can satisfy while the product is dishonest. The assertion is now the relation \u2014 an unreachable router WITHHOLDS the counts entirely and says why, rather than rendering 0 tools from 0 servers, which a reader cannot tell apart from a running-and-empty router. Measured over 3 real window dumps, 10 checks, 0 failures.",
    "armedBy": "predicate arm, on a relation rather than a state. bin/req017-honesty.py runs 10 checks across three AX dumps of the SAME window under three loads (populated / empty / offline). The `--decoy zeroed` run makes the product behave as a router that lies with zeros and turns 3 of those checks red: offline publishes NO counts line; offline does not zero the counts; exactly the readable loads publish counts."
  },
  {
    "id": "CASE-0101",
    "req": "REQ-003",
    "surface": "SURF-001",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-001",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-001.shell.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID>, cropped to the window chrome (derivedFrom SURF-002.build.png)",
      "frameStatus": "complete"
    },
    "note": "The shell has no board of its own, so its raster is the titlebar and source list cropped from the Servers capture, with the parent named in derivedFrom.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-001's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106."
  },
  {
    "id": "CASE-0102",
    "req": "REQ-004",
    "surface": "SURF-002",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-002",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-002.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-002's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-002 decoy."
  },
  {
    "id": "CASE-0103",
    "req": "REQ-003",
    "surface": "SURF-003",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-003",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-003.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "note": "Uses SURF-003.board.png (c55161d8\u2026), not SURF-003.build.png which shares 442eea\u2026 with SURF-001.build.png. ARMED: predicate watched to fail under the SURF-003 decoy.",
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-003's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106."
  },
  {
    "id": "CASE-0104",
    "req": "REQ-003",
    "surface": "SURF-004",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-004",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-004.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-004's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-004 decoy."
  },
  {
    "id": "CASE-0105",
    "req": "REQ-003",
    "surface": "SURF-005",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-005",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-005.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-005's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-005 decoy."
  },
  {
    "id": "CASE-0106",
    "req": "REQ-003",
    "surface": "SURF-006",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-006",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-006.build.png",
      "evidence/arm-pipeline/mutated.SURF-006.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-006's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106. PIPELINE ARM, end to end: both halves of the Checks honesty guardrail were reverted in app/Sources/MCPRouterKit/Checks/CheckCopy.swift (\"it is not a graded test\" \u2192 \"it grades how well\"; \"skills are never executed by the router\" \u2192 \"skills are executed by the router\"), the Mac app was REBUILT with `make build-mac`, relaunched backgrounded and re-photographed through the same window-scoped path the campaign uses \u2014 and the suite then went red on exactly CASE-0014, CASE-0106 (checked=137 failures=4 cases=18). Source and every recorded artifact were restored and it is green again (checked=137 failures=0 cases=18). Focus was never taken (capture reported focus unchanged: True). This is the one arm that proves the build\u2192launch\u2192capture\u2192assert CHAIN bites, not merely the predicate; the mutated build's own dump and PNG are kept under evidence/arm-pipeline/ so the red is inspectable.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-006 decoy. Pipeline-armed on a rebuilt app, not only on a recording."
  },
  {
    "id": "CASE-0107",
    "req": "REQ-003",
    "surface": "SURF-007",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-007.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-007's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-007 decoy."
  },
  {
    "id": "CASE-0108",
    "req": "REQ-006",
    "surface": "SURF-008",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-008",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-008.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-008's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-008 decoy."
  },
  {
    "id": "CASE-0109",
    "req": "REQ-008",
    "surface": "SURF-009",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-009",
      "state": "populated",
      "viewport": "320x480",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "n/a: status item not pressable in background; SURF-009 popover uncaptured. Raster-visual of the popover is unreachable under the no-activate constraint",
    "armed": false,
    "note": "mac-glass-capture.log: status item not pressable in background \u2014 SURF-009 uncaptured. Proctor agent.sock absent this session.",
    "evidence": [
      "evidence/runs/mac-glass-capture.log"
    ]
  },
  {
    "id": "CASE-0110",
    "req": "REQ-015",
    "surface": "SURF-010",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-010",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "fail",
    "armed": false,
    "evidence": [
      "evidence/shots/SURF-010.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "note": "Raster of the pairing-unavailable sheet, and a FAIL against REQ-015 for what it does not show. `SURF-010.build.png`, window-scoped off the CGWindowID the launched pid owns, manifested in `evidence/shots/captures.json`. The earlier note named a sha for an image no longer on disk and cited no file at all; this one cites the capture it describes."
  },
  {
    "id": "CASE-0111",
    "req": "REQ-003",
    "surface": "SURF-011",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-011",
      "state": "populated",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-011.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm. The assertion is stated explicitly in bin/glass-assert.py and re-run there (checked=137 failures=0 cases=18). Removing SURF-011's own copy from its window dump turns this case RED, so the check is known to bite. The suite's discrimination control additionally runs every board's predicate against every other board's dump (compared 56 board/dump pairs \u00b7 leaks=0 \u00b7 shell surfaces asserted for persistence instead: 1) \u2014 a predicate that accepted a foreign window would be matching shared shell chrome rather than the board it names. This arms the PREDICATE against a recorded artifact; the build\u2192launch\u2192capture\u2192assert pipeline is armed once, end to end, on CASE-0014/CASE-0106.",
    "note": "assertion was unrecorded before this run; it is now stated in bin/glass-assert.py and reproduced there. ARMED: predicate watched to fail under the SURF-011 decoy."
  },
  {
    "id": "CASE-0120",
    "req": "REQ-010",
    "surface": "SURF-012",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-012",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-012.ios.discover-populated.png"
    ],
    "capture": {
      "method": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
      "frameStatus": "complete"
    },
    "note": "1206x2622 off the simulator's own display server, attributed to its producing test by the GLASS-CAPTURE- marker the test writes at shutter time rather than by filename. Its empty-state sibling, SURF-012.ios.discover-empty.png, is the metamorphic arm CASE-0123 reads."
  },
  {
    "id": "CASE-0121",
    "req": "REQ-010",
    "surface": "SURF-013",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-013",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-013.ios.triage.png"
    ],
    "capture": {
      "method": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
      "frameStatus": "complete"
    },
    "note": "Triage alone. The queue and library captures this case used to speak for are CASE-0124 and CASE-0125 \u2014 three routes under one case was the same understatement the tie pass found in the registry, arriving in the case file."
  },
  {
    "id": "CASE-0122",
    "req": "REQ-015",
    "surface": "SURF-014",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-014",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-014.ios.camera-preflight.png"
    ],
    "capture": {
      "method": "XCUIScreenshot via XCTAttachment, taken inside the test after the surface's own assertion passed",
      "frameStatus": "not reported by this instrument \u2014 XCUIScreenshot carries no per-frame status, so attribution stands in its place: the picture is taken in the same test, after 'Why the camera' was found"
    },
    "note": "The capture the campaign recorded as never photographed. It exists now because the surface itself was unreachable until DEF-017, DEF-019 and DEF-022 were closed \u2014 the earlier 'inconclusive' blamed the screenshot step, and the screenshot step was fine. Armed 20 Aug 2026: `PairingFlowModel.start()` reverted to ignore the camera state and always open on .scan; the pre-prompt assertion went red \u2014 'the pairing flow did not open on the camera pre-prompt'. Armed against the surface rather than against its copy: a raster-visual case claims a picture of a surface, so the arm has to be able to take the surface away. An earlier arm reverted CameraAuthorization.copyKey's .notDetermined case to nil and the test PASSED, because CameraPermissionView reads `copyKey ?? .cameraNotDetermined` \u2014 recorded, because an arm that passes is a finding about the code rather than a formality to repeat until it goes red."
  },
  {
    "id": "CASE-0123",
    "req": "REQ-006",
    "surface": "SURF-018",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-018",
      "state": "fresh-install",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "unpaired",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-018.ios.settings-never-paired.png"
    ],
    "captureMethod": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
    "frameStatus": "complete",
    "note": "1206x2622 off the simulator's display server, attributed to testEachTabRendersItsOwnSurface by xcresulttool rather than by filename. Distinct sha c3f1c31a.",
    "capture": {
      "method": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
      "frameStatus": "complete"
    }
  },
  {
    "id": "CASE-0024",
    "req": "REQ-006",
    "surface": "SURF-018",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-018",
      "state": "fresh-install",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "unpaired",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/shots/ios/SURF-018.ios.settings-never-paired.png"
    ],
    "note": "Settings is the phone's fifth tab and was never enumerated, so nothing could have failed on it; SURF-018 was added by this campaign. On glass it renders the never-paired state on a fresh install, once the lane signs \u2014 see CASE-0022 for why it did not. three consecutive green runs of `make test-ios-glass` on the lane's own simulator (MCPRouter-Glass, iPhone 17 Pro / iOS 26.5), 5 of 5 each time. Armed 20 Aug 2026: `PairingCopy.settingsNeverPaired.headline` changed to 'Not paired'; red with 'Settings did not render the never-paired state, so the pairing entry point is unproven'. The same run's on-screen dump is what confirms DEF-026 closed on glass: the narrowing appears once.",
    "armed": true
  },
  {
    "id": "CASE-0124",
    "req": "REQ-010",
    "surface": "SURF-019",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-019",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-019.ios.queue.png"
    ],
    "capture": {
      "method": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
      "frameStatus": "complete"
    },
    "note": "Split out of CASE-0121 when the tie pass found three routes under one id. The state is the empty one, which is the state the fixture queue produces and the one i3-phone-triage.html \u00a7D specifies as one sentence and one action. Armed 20 Aug 2026: `QueueCopy`'s empty headline changed to 'Nothing here'; red with \"Queue did not render 'Nothing waiting', which is its own and no other surface's\"."
  },
  {
    "id": "CASE-0125",
    "req": "REQ-010",
    "surface": "SURF-020",
    "lane": "ios-glass",
    "cell": {
      "surface": "SURF-020",
      "state": "populated",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/ios/SURF-020.ios.library.png"
    ],
    "capture": {
      "method": "XCUIScreen.main.screenshot() via XCUITest, exported by xcresulttool export attachments",
      "frameStatus": "complete"
    },
    "note": "Split out of CASE-0121. Four rows, each carrying only MCPServer fields, plus the block that states the skills index does not exist rather than drawing an empty list. Armed 20 Aug 2026: `LibraryCopy`'s skills-absent headline changed to 'No skills.'; red with \"Library did not render 'Skills are not listed here.'\"."
  },
  {
    "id": "CASE-0126",
    "req": "REQ-010",
    "surface": "SURF-018",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-018",
      "state": "instrument",
      "viewport": "393x852",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "planning/features-to-triage/X3-ios-unit-lane-empty-tree.md"
    ],
    "note": "The iOS unit lane's own instrument. `PhoneSurfaceTests.testTheAccessibilityEngineCanBeSwitchedOn` asserts that `_AXSSetAutomationEnabled` resolves and that a hosted `ScrollView { Text }` publishes its label back through the same walk every other assertion in the target uses. Without it the target's 35 surface assertions are vacuous in a way that reads exactly like a product rendering no copy \u2014 DEF-029, where they were vacuous for ten consecutive runs. Armed three ways on 20 Aug 2026: renaming the symbol fails this case alone; mutating the call to `(false)` on a device created for the arm reads 53 failures across 36 tests, which is what proves the other 35 depend on it; and the first run ever taken on a device whose engine had never been live is clean, which proves the fix needs no warm-up on a fresh device. A device left explicitly disabled does need one, and `test-ios` now runs a throwaway process before the graded one for that reason."
  },
  {
    "id": "CASE-0127",
    "req": "REQ-018",
    "surface": "SURF-003",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-003",
      "state": "reset-dialog",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/ActivityResetEntryPointTests.swift"
    ],
    "note": "DEF-016's behaviour half. `ActivityResetEntryPointTests.theDialogResetsOnlyWhenConfirmed` counts `POST /usage/reset` through `RecordingUsageClient.resets` across the dialog's whole life: opening it sends 0, cancelling sends 0, confirming sends exactly 1, and the sheet closes with no `writeError`. Counted rather than flagged, because \"the dialog sent one\" and \"every keystroke sent one\" are the same Bool and different products \u2014 which is the distinction REQ-018 turns on, since this is the named-consequence dialog reserved for an act undo cannot reverse. Armed 20 Aug 2026 by duplicating `_ = try await client.resetUsage()`, which reads `confirming the dialog sent 2 resets`. Deleting the call was tried first and rejected: it fails to compile under typed throws, which is a red for the wrong reason."
  },
  {
    "id": "CASE-0128",
    "req": "REQ-003",
    "surface": "SURF-003",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-003",
      "state": "header-affordance",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "structural-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/ActivityResetEntryPointTests.swift"
    ],
    "note": "DEF-016's affordance half and DEF-012's first rename, at the rung they honestly reach. `theResetActionIsInTheHeader` slices `ActivityBoard.header` from its declaration to `content`'s and asserts the slice carries `CleanupPresentation.resetLabel` and opens `model.sheet = .resetHistory`; `theHeaderLabelIsTheDesigns` asserts that constant is `Reset history\u2026` per `prototype.html:716` and `:930`, not the Settings-Danger wording at `:999` the build had shipped. Recorded as `structural-visual` rather than `outcome` and counted against the strict total on purpose: both read source text, and no assertion here proves a header rendered. It is written that way because the claim is about the header in **all nine** DESIGN.md \u00a75 states \u2014 the design puts the control outside the rows conditional precisely so the empty state has it \u2014 and a rendered assertion over one state would leave the other eight unchecked while buying effect credit. Watched to fail for real rather than by a staged arm: an earlier script aborted before writing the header change, and this assertion reported `the header draws no reset action` against the tree that was genuinely missing it. Reverting `resetLabel` reads `the header action is called Reset call history\u2026`."
  },
  {
    "id": "CASE-0129",
    "req": "REQ-003",
    "surface": "SURF-004",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-004",
      "state": "header-affordance",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "structural-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/ActivityResetEntryPointTests.swift"
    ],
    "note": "DEF-012's second rename. `theMarketplacesActionIsNamedOnce` asserts `SkillPresentation.marketplacesAction == \"Add marketplace\u2026\"` per `prototype.html:786`, and then that neither `SkillsBoard.swift` nor `MenuCommand.swift` writes either label as a literal any more. The second half is the assertion that bites: the board had shipped `Manage marketplaces\u2026` while the menu item opening the **same sheet** said `Add marketplace\u2026`, so a match-the-strings check would have passed the day before the fix as easily as the day after. Asserting the literals are absent is what stops them drifting apart again. `structural-visual` for the same reason as CASE-0128 \u2014 it reads source. Armed 20 Aug 2026 by restoring the literal in `SkillsBoard`, which reads `still writes the marketplaces label as a literal`."
  },
  {
    "id": "CASE-0130",
    "req": "REQ-018",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "row-removal",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/CleanupRowActionsTests.swift"
    ],
    "note": "DEF-011's behaviour half. `theRowsRemovalOpensTheDialog` drives `CleanupBoardModel` to the state the row's Remove button sets and asserts two things: the removal dialog is open, and `M7RecordingClient.writes` is still empty. The one destructive act on this board is never one click, which is REQ-018's contract for something undo cannot reverse. Stated honestly about what it does not prove: it sets `board.sheet` rather than pressing the button, so it proves the model contract and not that the button reaches it \u2014 CASE-0131's wiring assertion is what covers that half. Armed 20 Aug 2026 by making the test perform a real removal, which reads `[\"remove(alpha, keepHistory: false)\"]` \u2014 so the empty-writes predicate is live rather than vacuously true against a client nothing ever calls."
  },
  {
    "id": "CASE-0131",
    "req": "REQ-003",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "row-actions",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "structural-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/CleanupRowActionsTests.swift"
    ],
    "note": "DEF-011's affordance half \u2014 the column the design draws on every row and the build drew on none. Three assertions: `CleanupBoardRow` declares `Button(\"Inspect\"` and `Button(\"Remove\u2026\"`; the `CleanupBoardRow` declaration carries `accessibilityElement(children: .contain)` and not `.combine`, because combining flattens the buttons into the row's label and leaves nothing for VoiceOver or a UI test to press; and `CleanupBoard` passes `inspect:` and `remove:` rather than leaving them nil, since a row with no closures draws an empty column. The `.combine` assertion is scoped to that one declaration, not to the file. The first version asserted `.combine` appeared nowhere in `CleanupBoardRow.swift` and went red against `CleanupObservationTrack`, where combining is correct \u2014 it draws a bar and a number and carries no control. A predicate that fires on a correct use is a detector defect. `structural-visual`, and counted against the strict total, for CASE-0128's reason. Armed 20 Aug 2026 three ways: blanking the Inspect label reads `draws no Inspect`, switching `.contain` to `.combine` reads `combines its children` twice, and deleting the board's `inspect:` argument reads `builds its rows without actions`."
  },
  {
    "id": "CASE-0132",
    "req": "REQ-017",
    "surface": "SURF-001",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-001",
      "state": "instrument",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/ShellTestSupport.swift",
      "app/Tests/MCPRouterUITests/ActivityReconnectTests.swift",
      "app/Tests/MCPRouterUITests/DiscoverBoardTests.swift"
    ],
    "note": "The package lane's own instrument, and a relation across runs rather than a fact about one: the same source must give the same verdict on a loaded host and an idle one. It did not \u2014 `make test` reported 4 issues at a one-minute load average of ~700 and passed 1473 tests at load 46 minutes later (DEF-030). Every one of the four asserted after a fixed sleep or a 2\u20135 second deadline, so what they were measuring was the machine.\n\nClosed by replacing each deadline with a condition: `ShellTestSupport.waitUntil` and `ActivityReconnectTests.completes` keep a bound so a condition that never arrives fails rather than hangs, but at 30s rather than inside the noise; and `searchIsDebounced` waits for the debounce to fire and then holds a full debounce window of quiet, because the wait alone is satisfied by the first request and would pass against the one-per-keystroke defect it exists to catch.\n\nArmed 20 Aug 2026 three ways, each watched to fail with its own message \u2014 `.seconds(0)` on the shared waiter reads `no poll ran at all` and `five keystrokes issued 0 requests`, `.milliseconds(1)` on the completion bound reads `reconnect did not return while the feed stayed live`, and a `queryChanged()` inserted into the quiet period reads `a later request arrived`. The contention that triggered the original failure came from another project and is not reproducible on demand, so the arms are the proof rather than a repeat of the load."
  },
  {
    "id": "CASE-0133",
    "req": "REQ-017",
    "surface": "SURF-001",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-001",
      "state": "overflow",
      "viewport": "980x620",
      "theme": "dark",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "windowed"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "planning/test-campaign/bin/capture-mac-glass.sh",
      "app/Tests/MCPRouterUITests/ShellDetailWidthTests.swift"
    ],
    "note": "DEF-015's closed half \u2014 the shell's overflow treatment, which DESIGN.md \u00a75 names and the build got wrong in a way no assertion could see. Every board's `AXSplitGroup` width is read back out of the running app and compared against its own `AXWindow` width, board by board, through the channel that already writes those dumps beside the pictures. It read 3 of 8 overflowing \u2014 Checks 988, Skills 1044, Discover 1119 inside a 980pt window, each origin moved left by exactly half its excess \u2014 and reads 0 of 8 after `.frame(minWidth: 0, maxWidth: .infinity, alignment: .leading)` on `ContentZone`.\n\n`outcome` rather than `raster-visual` on purpose: the claim is a measured relation between two frames the app publishes, not a comparison of pixels, and the pictures are what made the defect *visible* rather than what proves it fixed.\n\nArmed 20 Aug 2026, three ways on the gate and two on the test. The gate exits 1 against the pre-fix dumps, 0 against the post-fix ones, and 1 with no dumps at all \u2014 `examined=0` is a check that never ran and is never a pass. Removing the modifier reddens `ShellDetailWidthTests` with `passes its boards' minimum width up to the split view`, and dropping just `alignment: .leading` reddens the second half alone. That test's first version did NOT bite: it searched the raw source and matched the doc comment naming the modifier, so deleting the modifier left it green. Comments are stripped before matching now \u2014 a predicate satisfied by the prose explaining it is not a predicate.\n\nWhat this case does not cover, because `overflowing=0` reads like `nothing is clipped` and is not that claim: content that overflows the detail PANE stays inside the window and is invisible to this oracle. That half is CASE-0134, measured with a second probe against every leaf's right edge \u2014 and that probe was wrong on its first run, reading the frame's y where it meant x and reporting clipping on seven of eight boards. The Settings capture is whole, and looking at it is what caught it."
  },
  {
    "id": "CASE-0134",
    "req": "REQ-017",
    "surface": "SURF-005",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-005",
      "state": "overflow",
      "viewport": "980x620",
      "theme": "dark",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "windowed"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/ShellDetailWidthTests.swift"
    ],
    "note": "DEF-015's other half \u2014 content that overflows the detail PANE while staying inside the window, which CASE-0133's oracle is structurally blind to. Every leaf's right edge in the accessibility dump against the window's: Discover's rows and footnote sat at 1283pt against an edge of 1160, Skills' at 1208.\n\nIt corrected the brief written from CASE-0133's numbers, which said three boards' table columns needed to flex. Cutting Discover's `nameColumn` from 216pt to 96pt moved its content width by **zero** \u2014 the driver is the controls row, a `.fixedSize()` segmented picker beside a search field pinned to one width. And Checks turned out not to be a content defect at all: its real content ends at 1152pt inside the pane, and the only thing past the edge is the `#if DEBUG` `KeyClaimProbe` overhanging by 4pt, which is the whole of the 8pt CASE-0133 measured on that board.\n\n**The probe was wrong on its first run and the pixels are what caught it.** It read the frame's y where it meant x \u2014 the last five fields are x, y, w, h and a flag \u2014 and reported clipping on seven of eight boards, including Settings, whose capture is whole from edge to edge. Seven of eight is close to uniform, which detector-defects.md names as the signature of a dead predicate rather than a broken product, and looking at one picture settled it in a minute.\n\nFixed by declaring each field `minWidth:idealWidth:maxWidth:` against a new `searchMinWidth` of four units; measured after, no leaf on any of the eight boards sits past the pane edge except a 1pt scrollbar. Armed 20 Aug 2026: restoring `.frame(width: DiscoverBoardMetrics.searchWidth)` reddens all three assertions, naming the file, the missing floor and the missing ideal separately."
  },
  {
    "id": "CASE-0135",
    "req": "REQ-003",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "row-actions-flagged",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/CleanupRowActionsTests.swift"
    ],
    "note": "DEF-011's last open half, the one the row was left open for: `Read first\u2026`, which the design keys on `k.provenance` and which `CleanupBoardModel.Candidate` had no field to key on. It now carries `provenance: String?`, read through `SkillChecks.originUnchanged` rather than off `skill.provenance` directly, so this board and the Checks board cannot disagree about what counts as moved. Three assertions, all against the model rather than the pixel. `aMovedMarketplaceReachesTheRow` compares the row's note against that check's own `reason` rather than against a literal, because a literal would stay green while the two boards drifted apart. `anUnmovedRowCarriesNoNote` is the half that makes the first one mean something \u2014 a field that is always populated cannot mark a row \u2014 and covers both an unmoved skill and a server, which has no marketplace to have moved from. `readFirstOpensItsSheet` asserts the sheet is keyed by the skill's resolved path (`prov:/skills/pr-summariser`), that `skill(atPath:)` finds the skill behind it, and that opening it leaves `M7RecordingClient.writes` empty. Armed 20 Aug 2026 four ways: making `provenanceNote` return nil reads `(moved.provenance \u2192 nil) == \"The router first saw it at \u2026\"`; dropping its guard so an unmoved skill is flagged reads `(unmoved.provenance \u2192 \"its marketplace moved\") == nil`; giving the server call site a note reads the `allSatisfy` failure; collapsing the sheet id to `\"prov\"` reads `(board.sheet?.id \u2192 \"prov\") == \"prov:/skills/pr-summariser\"`; and keying `skill(atPath:)` on the name instead of the path reads the provenance comparison failing against nil."
  },
  {
    "id": "CASE-0136",
    "req": "REQ-003",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "row-actions-substituted",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "structural-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/CleanupRowActionsTests.swift"
    ],
    "note": "`prototype.html:961` puts `Read first\u2026` **in place of** Inspect and Remove rather than beside them, and the substitution is the requirement rather than a layout preference: a removal left on that row is one click from a fact nobody has read yet. `theRowSwapsItsActions` takes the `CleanupBoardRow` declaration, finds the `if candidate.provenance != nil {` branch, and asserts Inspect and `Remove\u2026` appear nowhere inside it. Source-level for the reason the suite's header already gives \u2014 the claim is about every flagged row in every state, and a rendered assertion over one row leaves the rest unchecked. It reads the declaration with every `//` line stripped, because the first `ShellDetailWidthTests` searched raw source for a modifier its own doc comment three lines above also named, and deleting the modifier left the assertion green: a predicate satisfied by the prose explaining it is not a predicate. `structural-visual`, and counted against the strict total, for CASE-0128's reason. Armed 20 Aug 2026 twice: renaming the button reads `draws no Read first\u2026` plus the Inspect predicate firing, and adding `Remove\u2026` back into the flagged branch reads `a flagged row still draws Inspect or Remove`."
  },
  {
    "id": "CASE-0137",
    "req": "REQ-007",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "provenance-sheet",
      "viewport": "1280x800",
      "theme": "light",
      "role": "operator",
      "dataShape": "typical",
      "executionPlane": "in-process"
    },
    "oracle": "structural-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "app/Tests/MCPRouterUITests/CleanupRowActionsTests.swift"
    ],
    "note": "The sheet `Read first\u2026` opens is where REQ-007 was most at risk on this board, because the design's version of it is almost entirely invented. `prototype.html:1249` lists an owner at install (`@jbailey`), a default branch `force-pushed 11d ago`, an installed hash `a3f19c2 \u2014 no longer in history`, and `Its evals fail 5 of 8`. The router reports none of them, and one is not computable at all: `SkillProvenance` records in its own doc comment that the client's files carry a commit and never an owner, so `changed since you installed it` cannot be derived \u2014 which is why the sheet's date is labelled as the router's own first sighting and why `CleanupPresentation.provenanceLimit` says so on screen rather than leaving the reader to infer it. `theSheetReportsOnlyObservations` asserts the sheet renders all three fields `SkillProvenance` actually carries and states none of the four invented strings. The negative half enumerates them by name rather than being a blanket not-that predicate, so it fires on the specific thing that would regress. Armed 20 Aug 2026 twice: deleting the `firstSeenAt` field reads `never renders firstSeenAt`, and adding `field(\"Owner at install\", \"@jbailey\")` reads `the sheet states 'Owner at install', which the router never reported`."
  },
  {
    "id": "CASE-0138",
    "req": "REQ-003",
    "surface": "SURF-007",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "cleanup-skills",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "skills-installed-nowhere",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-007.cleanup-skills.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm, with a local control the surface cases do not have. `glass-assert.py --decoy SURF-007/cleanup-skills` blanks this state's copy from its own dump and both cases go red (6 failed assertions). The control that makes the state mean something is `state copy is absent from SURF-007.window.txt`: the default scenario photographs this same board, so a predicate that passed on both dumps would be describing the Cleanup board rather than the state, and the extra capture would have added nothing. Suite run: checked=154 failures=0 cases=20 (surfaces 9, states 1); discrimination compared 56 board/dump pairs with 0 leaks. The build\u2192launch\u2192capture\u2192assert pipeline is armed end to end on CASE-0014/CASE-0106.",
    "note": "The `Read first\u2026` substitution, rendered by a running build instead of read out of source. CASE-0136 asserts the branch exists in `CleanupBoardRow` and says in its own note why it stops there; what it cannot say is that any build ever drew it. None had. A skill is proposed for cleanup only when every readable client lacks it, and all six skills in the `populated` fixture are installed somewhere, so the Cleanup board in Debug drew three servers and no skills at all \u2014 the substitution, the skill-kind `Remove\u2026` and the candidacy reason had no rendered path in any configuration of the product. The `cleanupSkills` scenario is the fixture that gives them one, kept separate from `populated` because the Skills board publishes a count of what `populated` holds and two surfaces should not have to move together. The dump this case cites was taken from that build at `app/.derived/Build/Products/Debug/MCPRouter.app`, window title read back as Cleanup, and it carries: `Read first\u2026` on the `pr-summariser` row with no Inspect or `Remove\u2026` beside it, the router's own moved-marketplace sentence on that row, and `stale-linter` keeping `Inspect` (enabled 1) while its `Remove\u2026` is enabled 0. Product change made to earn this: one Debug-only fixture scenario and two fixture skills. No assertion was weakened and no view code was touched."
  },
  {
    "id": "CASE-0139",
    "req": "REQ-019",
    "surface": "SURF-007",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "cleanup-skills",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "skills-installed-nowhere",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-007.cleanup-skills.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm, with a local control the surface cases do not have. `glass-assert.py --decoy SURF-007/cleanup-skills` blanks this state's copy from its own dump and both cases go red (6 failed assertions). The control that makes the state mean something is `state copy is absent from SURF-007.window.txt`: the default scenario photographs this same board, so a predicate that passed on both dumps would be describing the Cleanup board rather than the state, and the extra capture would have added nothing. Suite run: checked=154 failures=0 cases=20 (surfaces 9, states 1); discrimination compared 56 board/dump pairs with 0 leaks. The build\u2192launch\u2192capture\u2192assert pipeline is armed end to end on CASE-0014/CASE-0106.",
    "note": "REQ-019 on the one control that has to refuse: the skill row's `Remove\u2026`. The control API is read-only for skills \u2014 removing one means writing files the client applications hold open \u2014 and the row does not hide the button or offer it and fail. It draws it disabled (AX enabled 0) and states that reason in its help text, which is the shape REQ-019 asks for: no success affordance for work that cannot happen, and the refusal is the server's own. Pixels: `SURF-007.cleanup-skills.png`, 519568 bytes, sha 94dd9d06f87d, window-scoped off CGWindowID 103249 owned by the launched pid, recorded in `evidence/shots/captures.json` with the scenario that produced it. Byte-identical across two consecutive capture runs. It does NOT claim agreement with the design of record: the reference is an HTML mock rasterised at another size, and no macOS API exposes a cross-process computed style."
  },
  {
    "id": "CASE-0140",
    "req": "REQ-007",
    "surface": "SURF-007",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "provenance-sheet",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "skills-installed-nowhere",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-007.provenance-sheet.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm with both halves proven. `glass-assert.py --decoy SURF-007/provenance-sheet` reds 12 assertions: the five the sheet must carry go missing, and the four the sheet must NOT state are injected, so the negative half is watched to fail rather than assumed \u2014 an always-true not-that predicate is the failure this half exists to catch. The local control is `state copy is absent from SURF-007.cleanup-skills.window.txt`: the board behind the sheet is photographed too, and a predicate passing on both would be describing the board. Suite run: checked=187 failures=0 cases=22 (surfaces 9, states 2); discrimination compared 56 board/dump pairs, 0 leaks.",
    "note": "CASE-0137's claim, rendered. That case reads `CleanupSheets.swift` and states in its own note that no assertion in it proves a sheet was drawn; this one presses `Read first\u2026` on a running build and reads the sheet that opens. `axkit press` is background-safe on a SwiftUI Button \u2014 AXPress runs the action on the element and needs no focused scene \u2014 and it exits non-zero when the press does not take, so the sheet is photographed or reported uncaptured, never assumed. The AXSheet subtree carries the three observations `SkillProvenance` actually holds (`Where the router first saw it`, `Where it resolves now`, `The router first saw it`, each with its value), and the sentence that bounds them: `The router records where a marketplace resolves, never who owns it and never what changed inside it`. It states none of the four figures `prototype.html:1249` invents \u2014 `@jbailey`, `force-pushed`, `no longer in history`, `evals fail` \u2014 each checked by name. Frontmost was Ghostty before and after; the app was never activated."
  },
  {
    "id": "CASE-0141",
    "req": "REQ-007",
    "surface": "SURF-007",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-007",
      "state": "provenance-sheet",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "skills-installed-nowhere",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-007.provenance-sheet.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm with both halves proven. `glass-assert.py --decoy SURF-007/provenance-sheet` reds 12 assertions: the five the sheet must carry go missing, and the four the sheet must NOT state are injected, so the negative half is watched to fail rather than assumed \u2014 an always-true not-that predicate is the failure this half exists to catch. The local control is `state copy is absent from SURF-007.cleanup-skills.window.txt`: the board behind the sheet is photographed too, and a predicate passing on both would be describing the board. Suite run: checked=187 failures=0 cases=22 (surfaces 9, states 2); discrimination compared 56 board/dump pairs, 0 leaks.",
    "note": "The sheet's pixels: `SURF-007.provenance-sheet.png`, 633476 bytes, sha d48520a1a99d, window-scoped off the CGWindowID the launched pid owns, recorded in `evidence/shots/captures.json` with the `cleanupSkills` scenario that produced it and the title read back off the running app. The claim is that this surface exists as drawn output rather than as a view struct: an AXSheet at 512\u00d7416 inside the window, with the copy the case above enumerates. It does NOT claim agreement with the design of record, and here that is the point rather than a caveat \u2014 the design's version of this sheet states four things the router cannot observe, so agreement with it would be the defect."
  },
  {
    "id": "CASE-0142",
    "req": "REQ-019",
    "surface": "SURF-010",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-010",
      "state": "pairing-unavailable",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "no-phone-paired",
      "executionPlane": "on-glass"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/ax/SURF-010.window.txt",
      "evidence/runs/glass-assert.json"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm, both halves. `glass-assert.py --decoy SURF-010/unavailable` reds 8: the four phrases the sheet must carry go missing, and the code-shaped pattern that must match nothing is given something to match, so the negative half is watched to fail rather than assumed. Local control: `state copy is absent from SURF-008.window.txt` \u2014 the Inbox board behind this sheet is photographed too, and the sheet's own copy must not be on it. Suite run: checked=208 failures=0 cases=24 (surfaces 9, states 3).",
    "note": "REQ-019 where DEF-001 makes it matter: this build ships no pairing transport, and the sheet says so instead of drawing a code it cannot complete a pairing with. The AXSheet carries `Pair iPhone`, `Pairing is not available in this build`, and the sentence that keeps the reader from debugging their own network \u2014 `this build ships no way to listen for one. Nothing is wrong with your phone or your network.` The negative half is the one that would catch a regression: nothing in the window matches Crockford base-32 at the length REQ-015 specifies, checked by pattern rather than against a literal, so a code this predicate's author never saw would still trip it. This case does NOT make REQ-015 pass \u2014 CASE-0010 and CASE-0110 fail against it and keep failing. The sheet is honest about a capability that is missing; the capability is still missing."
  },
  {
    "id": "CASE-0143",
    "req": "REQ-019",
    "surface": "SURF-010",
    "lane": "macos-glass",
    "cell": {
      "surface": "SURF-010",
      "state": "pairing-unavailable",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "no-phone-paired",
      "executionPlane": "on-glass"
    },
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/SURF-010.build.png"
    ],
    "capture": {
      "method": "screencapture -x -l<CGWindowID> window-scoped; no SCFrameStatus (not ScreenCaptureKit)",
      "frameStatus": "unavailable: screencapture reports no per-frame status"
    },
    "armedBy": "predicate arm, both halves. `glass-assert.py --decoy SURF-010/unavailable` reds 8: the four phrases the sheet must carry go missing, and the code-shaped pattern that must match nothing is given something to match, so the negative half is watched to fail rather than assumed. Local control: `state copy is absent from SURF-008.window.txt` \u2014 the Inbox board behind this sheet is photographed too, and the sheet's own copy must not be on it. Suite run: checked=208 failures=0 cases=24 (surfaces 9, states 3).",
    "note": "The refusal's pixels: `SURF-010.build.png`, window-scoped off the CGWindowID the launched pid owns, an AXSheet at 512\u00d7173 over the Inbox window. Recorded in `evidence/shots/captures.json` with the title read back as Inbox \u2014 the sheet does not change the window title, so the manifest carries both the readback and the `app://mac/pairing` target rather than letting one stand for the other. This surface had no picture at all for three runs: the capture script pressed for a pairing control while the app was on the Servers board, where none exists, and recorded the surface unreachable for a reason that was its own navigation. Selecting Inbox first opened it on the first attempt."
  },
  {
    "id": "CASE-0144",
    "req": "REQ-019",
    "surface": "SURF-007",
    "lane": "swiftui",
    "cell": {
      "surface": "SURF-007",
      "state": "cleanup-skills",
      "viewport": "1024x768",
      "theme": "light",
      "role": "operator",
      "dataShape": "skills-installed-nowhere",
      "executionPlane": "headless"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/arming/CASE-0144.arming.log",
      "evidence/runs/make-test-3.log"
    ],
    "armedBy": "Two mutations of the fixture the test reads, each isolating one half of the claim, logged in evidence/arming/CASE-0144.arming.log. Arm 1 installed `stale-linter` on one client, so it stopped being a cleanup candidate: `Expectation failed: (proposed.count \u2192 1) == 2 \u2014 expected two cleanup-eligible skills, got [\"pr-summariser\"]`. Arm 2 gave `stale-linter` a moved-marketplace provenance, so both rows moved: `Expectation failed: (moved.map(\\.name) \u2192 [\"pr-summariser\", \"stale-linter\"]) == [\"pr-summariser\"] \u2014 exactly one row should substitute Read first\u2026`. The source restored to sha256 0b5b67d5040c, the sha it carried before either mutation, and the test went green again. Run counts are swift-testing's own `Test run with 1 test in 1 suite`; the XCTest bundle prints `Executed 0 tests` on every run in this package and a red that ran nothing is not a red.",
    "note": "The fixture half of CASE-0138/CASE-0139, checked in the harness rather than on glass. Those two photograph the Cleanup board drawing `Read first\u2026` on one row and a disabled `Remove\u2026` on the other; this asserts the fixture behind them actually contains one moved skill and one unmoved one, which is what makes the photograph a discrimination rather than a picture of whatever the fixture happened to hold. It also guards the reason the scenario is separate: `cleanupSkills` extends `populated` rather than editing it, because SURF-004's on-glass copy asserts the count `populated` publishes (`6 skills from 4 marketplaces \u00b7 1 held for review`) and a seventh row there would have broken an existing predicate. The test additionally asserts the servers half of the board survives the scenario, so a fixture that drew skills by losing servers would fail."
  },
  {
    "id": "CASE-0145",
    "req": "REQ-001",
    "lane": "router-daemon",
    "oracle": "effect-witness",
    "cell": "execution-plane=daemon \u00b7 data-shape=one stdio upstream declared",
    "note": "The router spawning a child process, seen by two parties that are not the router. `bin/witness-effects.sh` runs `MCPRouterCLI index --force` against a scratch home holding one stdio server, and records the pid the shell launched (`$!`) beside the pid the CHILD reports for its own parent through `getppid()`. The child writes that into a file whose path arrives in its environment and which the router never reads back, so neither record passes through the product. They agree: shell launched 30913, child 30920 names 30913. A ps(1) sample runs alongside and usually misses, because the index verb spawns, handshakes and exits inside one scheduling quantum; two earlier versions of this witness depended on ps alone and could report only that the child had A parent, not whose child it was. Armed: against a config declaring no servers the same recorder reports count=0, and against the populated one count=1 with the parents agreeing.",
    "status": "pass",
    "evidence": [
      "evidence/witness/effects.json",
      "evidence/witness/witness.log",
      "evidence/witness/arming.log",
      "evidence/witness/pstree.txt"
    ],
    "armed": true,
    "witness": {
      "recorder": "the shell's $! for the router invocation and the child's own getppid(), written by the child to a path the router never reads back; ps(1) sampled alongside and usually misses",
      "effect": "subprocess",
      "count": 1
    }
  },
  {
    "id": "CASE-0146",
    "req": "REQ-011",
    "lane": "router-daemon",
    "oracle": "effect-witness",
    "cell": "execution-plane=daemon \u00b7 data-shape=one stdio upstream declared",
    "note": "REQ-011's half of the same recording. The requirement is about upstreams being opened on demand rather than held resident, and the effect it claims is the same `Process()` spawn REQ-001 claims. Recorded from one run rather than two so the count is one observation counted once, and kept as its own case because the two requirements would be closed by different work if this ever stopped being true.",
    "status": "pass",
    "evidence": [
      "evidence/witness/effects.json",
      "evidence/witness/witness.log",
      "evidence/witness/arming.log",
      "evidence/witness/pstree.txt"
    ],
    "armed": true,
    "witness": {
      "recorder": "the shell's $! for the router invocation and the child's own getppid(), written by the child to a path the router never reads back; ps(1) sampled alongside and usually misses",
      "effect": "subprocess",
      "count": 1
    }
  },
  {
    "id": "CASE-0147",
    "req": "REQ-012",
    "lane": "router-daemon",
    "oracle": "effect-witness",
    "cell": "execution-plane=daemon \u00b7 network=loopback only",
    "note": "The router binding and accepting on loopback, seen by the kernel's socket table through lsof(8). Before: 0 LISTEN rows on 127.0.0.1:8973. After `serve --port 8973`: one LISTEN row owned by pid 15760, which is the pid the shell launched. A client then held a socket open across the sample \u2014 a raw /dev/tcp connection with a real HTTP request written into it \u2014 and lsof reported 3 ESTABLISHED rows on the port, 1 of them owned by the router: the accepted half of the pair, which is the half that proves the process took the connection rather than merely reserving the port. A separate request returned HTTP 200. After the process is killed, 0 LISTEN rows. The first version of this witness sampled after curl had already closed and recorded ESTABLISHED=0 \u2014 a real accepted connection the recorder was pointed at a moment too late to see. Armed: pointed at port 8974, which nothing was told to bind, the same recorder reports 0 while reporting 1 for 8973 in the same run.",
    "status": "pass",
    "evidence": [
      "evidence/witness/effects.json",
      "evidence/witness/witness.log",
      "evidence/witness/arming.log",
      "evidence/witness/pstree.txt"
    ],
    "armed": true,
    "witness": {
      "recorder": "kernel socket table via lsof(8): LISTEN row owned by the router pid, plus the accepted ESTABLISHED half while a client held the socket open across the sample",
      "effect": "inbound-socket",
      "count": 1
    }
  },
  {
    "id": "CASE-0148",
    "req": "REQ-013",
    "lane": "router-daemon",
    "oracle": "effect-witness",
    "cell": "execution-plane=daemon \u00b7 data-shape=empty manifest",
    "note": "The router writing to the filesystem, seen by stat(2). Before: `manifest.json` absent from the scratch home. After `index --force`: present, inode 889885583, 548 bytes, mtime 2026-08-20T23:21:42Z. The recorder is filesystem metadata, which the product does not author. Armed by running the router against one home while watching a different one: the decoy path stays empty in the same run in which the real path gains the file, so the recorder is answering about the path it is pointed at rather than about whether the router ran.",
    "status": "pass",
    "evidence": [
      "evidence/witness/effects.json",
      "evidence/witness/witness.log",
      "evidence/witness/arming.log",
      "evidence/witness/pstree.txt"
    ],
    "armed": true,
    "witness": {
      "recorder": "stat(2) filesystem metadata: absent before, inode+size+mtime after",
      "effect": "filesystem-write",
      "count": 1
    }
  },
  {
    "id": "CASE-0149",
    "req": "REQ-005",
    "surface": "SURF-018",
    "lane": "ios-unit",
    "oracle": "structural",
    "cell": "surface=iOS Settings \u00b7 state=all five states carrying a recovery action",
    "note": "Arms A1, A2, A6 red then restored.",
    "status": "pass",
    "evidence": [
      "evidence/runs/def-025-028-arms.json"
    ],
    "armed": true
  },
  {
    "id": "CASE-0150",
    "req": "REQ-005",
    "surface": "SURF-018",
    "lane": "ios-unit",
    "oracle": "structural",
    "cell": "surface=iOS Settings \u00b7 state=never paired",
    "note": "Arms A4, A5 red then restored.",
    "status": "pass",
    "evidence": [
      "evidence/runs/def-025-028-arms.json"
    ],
    "armed": true
  },
  {
    "id": "CASE-0178",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "a package publishing all three documents and one figure",
      "executionPlane": "loopback-http"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/wire-document.json",
      "evidence/wire-document/wire-document.log",
      "evidence/wire-document/arming.json",
      "evidence/wire-document/arming.log"
    ],
    "note": "A served document returns its content, and the response carries NO filesystem path. Eleven assertions: 200; the read me carries the fixture's sentinel; changelog and capabilities both present; exactly one image travelled; its reference is the string the document spelled (`docs/figure.png`) rather than a resolved path; media `image/png`; the base64 decodes to the file's own bytes; nothing refused; the package root appears NOWHERE in the raw body; and no absolute path of any shape (/private/tmp, /Users, /tmp) appears in it either. The last two are M30's own assertion moved onto the wire \u2014 the app may not open a file, so a payload carrying a path is a payload that invites one. Run against BOTH implementations over a real socket: `dist/index.js serve` and the shipped `app/.build/debug/MCPRouterCLI serve`, each on 127.0.0.1:8981 with a scratch MCP_ROUTER_HOME. M30 already proves this route in process \u2014 `ControlDocumentRouteTests.swift` calls the handler and asserts on the string it returns \u2014 which is a different claim from this one: this lets the kernel carry the bytes and reads what a client actually receives, which is what the campaign asks of every other route on this lane. Observing only the TypeScript half would leave the SHIPPED router unobserved, so both are run and both pass. The server is CONSTRUCTED and labelled so: 0 of the 21 upstreams on this machine declares a package directory, so this route 404s noPackageDirectory for all of them and no real package here reaches this outcome.",
    "armedBy": "Two planted leaks, one per implementation, each watched to turn this case RED and each restored byte-identically (sha256 before == after). node `leaks-the-path`: `media: entry.path` in src/document.ts, so the response carries the path it read from under a key the app consumes. swift `swift-leaks-the-path`: an extra `root` member on the 200 object in ControlDocument.swift, carrying the package root. Both reddened `served` AND `escape` \u2014 declared, not incidental: the no-path property is asserted by two scenarios on purpose, and an arm that reddened only one of them would be the finding. A THIRD spelling was tried first and is recorded because it is a real result: adding a `path` FIELD to the pushed object does not compile \u2014 `images` is typed Array<{reference; media; base64}> and tsc refuses the excess property. So the TypeScript type already forbids this leak by ADDITION and says nothing about it by SUBSTITUTION, which is precisely the leak this wire assertion is here to catch."
  },
  {
    "id": "CASE-0179",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "refused",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "no directory declared \u00b7 a directory that is not there \u00b7 a directory publishing none of the three",
      "executionPlane": "loopback-http"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/wire-document.json",
      "evidence/wire-document/wire-document.log",
      "evidence/wire-document/arming.json",
      "evidence/wire-document/arming.log"
    ],
    "note": "Nothing served \u2014 the three ways this route has nothing to give, each reachable and each NAMING ITS OWN RULE rather than a shared 404. `noPackageDirectory` (the server declares no cwd), `packageUnreadable` (it declares one that is not on disk) and `noDocuments` (the directory is there and publishes no README.md, CHANGELOG.md or CAPABILITIES.md). Nine assertions, three per refusal: the status is 404, the `reason` is the specific rule, and a human sentence rides along. The app maps each reason to different copy, so a shared code is a refusal the reader cannot act on. Run against BOTH implementations over a real socket: `dist/index.js serve` and the shipped `app/.build/debug/MCPRouterCLI serve`, each on 127.0.0.1:8981 with a scratch MCP_ROUTER_HOME. M30 already proves this route in process \u2014 `ControlDocumentRouteTests.swift` calls the handler and asserts on the string it returns \u2014 which is a different claim from this one: this lets the kernel carry the bytes and reads what a client actually receives, which is what the campaign asks of every other route on this lane. Observing only the TypeScript half would leave the SHIPPED router unobserved, so both are run and both pass.",
    "armedBy": "Two planted faults, each red exactly where declared and restored byte-identically. `nodocs-served-anyway`: the `noDocuments` 404 block deleted from src/document.ts, so a package publishing nothing is answered with a body and the panel would draw three empty tabs over it \u2014 RED on noDocuments and nothing else. `unnamed-refusal`: `reason: 'packageUnreadable'` rewritten to a generic `'notFound'` \u2014 RED on packageUnreadable and nothing else, which is what demonstrates the case is checking WHICH rule was hit rather than merely that a 404 arrived."
  },
  {
    "id": "CASE-0180",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "refused",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "a README.md of exactly documentBytes + 1",
      "executionPlane": "loopback-http"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/wire-document.json",
      "evidence/wire-document/wire-document.log",
      "evidence/wire-document/arming.json",
      "evidence/wire-document/arming.log"
    ],
    "note": "Too large, and it names WHICH cap it hit. A README.md of 524,289 bytes \u2014 the 512 KiB documentBytes cap plus one \u2014 returns 413 with `cap: documentBytes`, `limit: 524288`, `actual: 524289` and `file: README.md`. Seven assertions. There are three caps on this route that can produce a size refusal, so 'too large' without a cap name tells a reader nothing they can act on. The fixture is built one byte over the boundary rather than comfortably over it, and the runner REFUSES TO START (exit 2) if the caps declared in src/document.ts have drifted from the three the fixture is sized for \u2014 a fixture sized past a cap that has since moved is a fixture that passes by never reaching the boundary at all. Run against BOTH implementations over a real socket: `dist/index.js serve` and the shipped `app/.build/debug/MCPRouterCLI serve`, each on 127.0.0.1:8981 with a scratch MCP_ROUTER_HOME. M30 already proves this route in process \u2014 `ControlDocumentRouteTests.swift` calls the handler and asserts on the string it returns \u2014 which is a different claim from this one: this lets the kernel carry the bytes and reads what a client actually receives, which is what the campaign asks of every other route on this lane. Observing only the TypeScript half would leave the SHIPPED router unobserved, so both are run and both pass.",
    "armedBy": "`unnamed-cap`: `cap: 'documentBytes'` rewritten to `cap: 'markdownLimits'` in src/document.ts, so the refusal still refuses and still carries a limit but no longer names the cap the reader would act on. RED on documentTooLarge and nothing else; sha256 before == after restore."
  },
  {
    "id": "CASE-0181",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "refused",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "nine adversarial references, one readable figure inside the package",
      "executionPlane": "loopback-http"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/wire-document.json",
      "evidence/wire-document/wire-document.log",
      "evidence/wire-document/arming.json",
      "evidence/wire-document/arming.log"
    ],
    "note": "A reference attempting to ESCAPE THE PACKAGE is refused rather than served. Fifteen assertions over one response. The strongest of them is not a reason code: the fixture plants a secret file outside the package, and the case asserts that the secret's path appears NOWHERE in the raw body \u2014 if anything escaped, the bytes are in the response and no status code hides that. Then exactly one image travelled and it is the one inside the package (`docs/inside.png`), and nine references are refused with the reason each earns and no more than nine: `../outside/secret.png`, `../pkg-escape-evil/secret.png` (a SIBLING package whose path is a string prefix of this one), `link-out.png` (a real symlink out), `linkdir/secret.png` (a symlinked DIRECTORY), `../../../../etc/passwd` -> escapesPackage; `/etc/hosts.png` and `~/secret.png` -> absolutePath; `https://example.invalid/secret.png` -> remote; `%2e%2e/outside/secret.png` -> notInPackage. M30 armed this check with three planted divergences and a fresh-context verifier attacked it with 23 adversarial references for zero escapes. That evidence is real and it lives in M30's record, where it was proved ONCE. This case exists so the property is checked on a schedule instead. Run against BOTH implementations over a real socket: `dist/index.js serve` and the shipped `app/.build/debug/MCPRouterCLI serve`, each on 127.0.0.1:8981 with a scratch MCP_ROUTER_HOME. M30 already proves this route in process \u2014 `ControlDocumentRouteTests.swift` calls the handler and asserts on the string it returns \u2014 which is a different claim from this one: this lets the kernel carry the bytes and reads what a client actually receives, which is what the campaign asks of every other route on this lane. Observing only the TypeScript half would leave the SHIPPED router unobserved, so both are run and both pass.",
    "armedBy": "Five planted faults \u2014 three on the reference implementation and two on the SHIPPED router \u2014 each RED on escape and nothing else, each restored byte-identically. node, src/document.ts: `contained-always` (the containment check removed outright); `contained-prefix` (compared as a STRING prefix via candidate.startsWith(base), which serves the sibling `pkg-escape-evil`); `contained-nolinks` (realpathSync dropped, so containment is compared before symlinks resolve and the link out is served). swift, DocumentPackage.swift: `swift-contained-always` (the segment-prefix guard replaced by a non-empty check); `swift-contained-nolinks` (`.resolvingSymlinksInPath()` dropped from both base and candidate). Removing the containment check turns this case red on either implementation."
  },
  {
    "id": "CASE-0182",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "one 9 MiB figure, four 2 MiB figures, one tail figure",
      "executionPlane": "loopback-http"
    },
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/wire-document.json",
      "evidence/wire-document/wire-document.log",
      "evidence/wire-document/arming.json",
      "evidence/wire-document/arming.log"
    ],
    "note": "An oversized image is refused ON ITS OWN TERMS and does NOT spend the shared budget. Ten assertions. The document names `oversize.png` (over the 2 MiB imageBytes cap) first, then a.png..d.png at exactly 2 MiB each \u2014 which together spend the 8 MiB imageBudgetBytes exactly \u2014 then tail.png. The discriminator is d.png: it travels only if the oversized figure spent nothing, because had it spent its own 2 MiB + 1 the budget would be gone before d.png was reached and d.png would come back budgetExhausted. tail.png is budgetExhausted either way and so proves nothing on its own, which is why the case does not rest on it. Also asserted: oversize.png is refused `tooLarge` naming the 2,097,152-byte per-image limit rather than `budgetExhausted`, and the budget was spent to exactly 8,388,608 bytes. Run against BOTH implementations over a real socket: `dist/index.js serve` and the shipped `app/.build/debug/MCPRouterCLI serve`, each on 127.0.0.1:8981 with a scratch MCP_ROUTER_HOME. M30 already proves this route in process \u2014 `ControlDocumentRouteTests.swift` calls the handler and asserts on the string it returns \u2014 which is a different claim from this one: this lets the kernel carry the bytes and reads what a client actually receives, which is what the campaign asks of every other route on this lane. Observing only the TypeScript half would leave the SHIPPED router unobserved, so both are run and both pass.",
    "armedBy": "`oversize-spends-budget`: `spent += size` added to the tooLarge branch of imageCapDecisions in src/document.ts, so an oversized figure charges the shared budget on its way out. With it planted, c.png and d.png came back refused and the budget read 4,195,328 instead of 8,388,608 \u2014 RED on budget and nothing else. sha256 before == after restore."
  },
  {
    "id": "CASE-0183",
    "req": "REQ-026",
    "surface": "SURF-027",
    "lane": "router-daemon",
    "cell": {
      "surface": "SURF-027",
      "state": "populated",
      "viewport": "n/a",
      "theme": "n/a",
      "role": "operator",
      "dataShape": "one constructed package \u00b7 network=loopback only",
      "executionPlane": "loopback-http"
    },
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/wire-document/witness-socket.txt",
      "evidence/wire-document/witness.log"
    ],
    "note": "The document response crossing a real kernel socket, seen by a party that is not the router. The six cases above read status codes and bodies back through urllib, which proves the route answered but takes the router's own word for how the bytes travelled; this takes the kernel's. `bin/witness-document-socket.sh` starts the SHIPPED router against a scratch home, then holds a socket open with a real `GET /servers/constructed-served/document` written into it while lsof(8) samples the socket table. Before: 1 LISTEN row on 8983 owned by the pid the shell launched, 0 on the decoy. During: 4 ESTABLISHED rows, 1 of them owned by the router pid \u2014 the ACCEPTED half, which is the half that proves the process took the connection rather than merely reserving the port; 0 on the decoy. The 589 bytes read straight off that socket begin `HTTP/1.1 200 OK` and contain the fixture's read me sentinel, so the connection lsof saw is the one that carried this route's document. After the process is killed: 0 LISTEN rows. The server is CONSTRUCTED and labelled so: 0 of the 21 upstreams on this machine declares a package directory, so this route 404s noPackageDirectory for all of them and no real package here reaches this outcome.",
    "armedBy": "The decoy port is the arm: the same recorder, in the same run, is pointed at 8990 \u2014 a port nothing was told to bind \u2014 and reports 0 LISTEN and 0 ESTABLISHED while 8983 reports 1 and 1. A recorder that answered about whether the router was alive rather than about this port would report the same on both. Two earlier spellings of the socket READ are recorded because each failed in a way that reads exactly like a route answering 200 with an empty body: `head -c 1400` blocked forever on a keep-alive connection that had already sent all ~590 of its bytes, and a line-wise `print while <STDIN>` lost the body to SIGALRM because the JSON carries no trailing newline, so the headers arrived and the body did not. The byte-wise sysread loses at most the byte in flight.",
    "witness": {
      "recorder": "kernel socket table via lsof(8), which no part of the router authors: LISTEN rows before and after, and the ESTABLISHED pair sampled while a raw /dev/tcp client held a real GET /servers/constructed-served/document open. The router-owned half is counted by pid. Armed by a decoy port nothing was told to bind, reporting 0 in the same run.",
      "effect": "inbound-socket",
      "count": 1
    }
  }
]

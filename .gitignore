# Build output — regenerated by `npm run build`
dist/
# The bare name too, for the same reason node_modules has one below: a worktree symlinks `dist`
# to the main checkout so the parity lanes can drive the reference, and a symlink is a file to
# git, which the trailing-slash pattern does not match.
dist
*.tsbuildinfo

# Dependencies
node_modules/
# The bare name too: a worktree symlinks node_modules to the main checkout, and a symlink
# is a file to git, so the trailing-slash pattern above does not match it. One was committed
# that way — an absolute path to one machine, in a public repo.
node_modules

# The router's own runtime state lives in ~/.claude/mcp-router/, never here,
# but keep these ignored in case someone runs with a local --config.
# Anchored to the repo root: unanchored, these match at every depth, and the bare
# name silently swallowed app/Sources/MCPRouterKit/Control/Fixtures/servers.json —
# a source file, gone from the branch while still present in its author's worktree,
# so its own gate passed and only the merged tree failed.
/servers.json
/manifest.json
*.log

# Generated launchd plists — install.sh writes these with machine-specific
# absolute paths (node binary, repo location), so they are never committed.
*.plist

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# macOS Finder droppings, at any depth
.DS_Store
**/.DS_Store
._*
.Spotlight-V100
.Trashes

# Fleet worktrees. `git worktree add .worktrees/<ID>` puts a whole second checkout inside this
# one, and git sees it as an untracked directory — one `git add -A` from the repo root would
# commit another branch's entire working tree into this branch.
.worktrees/

# Fidelity-loop trajectory data: the per-round score.json and candidate.svg are
# the evidence and are committed; the residual/edge bitmaps are 21 MB of
# regenerable diagnostic imagery and stay local.
design/icon/runs/**/*.png

# Xcode / Swift
.build/
DerivedData/
*.xcuserdatad
xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
.swiftpm/

# XcodeGen generates the project and the Info.plists from app/project.yml. Tracking generated
# artifacts is what produces project.pbxproj merge conflicts between parallel branches, and this
# fleet runs several at once — run `make generate` (or `xcodegen generate`) after checkout.
*.xcodeproj/
app/**/Info.plist
app/.derived/
# The ios-glass lane builds signed, into its own derived data — DEF-017.
app/.derived-signed/

# Private status pages. This repo is public and docs/ is the published site;
# these name project state and are written for one reader.
planning/status/

# The campaign's AX driver is built from scripts/acceptance/axkit.swift; the binary is an
# artifact of that source, not a second copy of it.
planning/test-campaign/bin/axkit
**/__pycache__/

# The superseded paper-green campaign is kept for its registry — the record of what a campaign
# that had never reached a display server looked like. Its captures and its embedded evidence
# page are regenerable from that registry and are 6.5 MB of a public clone.
planning/test-campaign.2026-08-19-paper/evidence/
planning/test-campaign.2026-08-19-paper/evidence.html

# Editor and tool backups.
*.bak

# M23's measurement dumps. Regenerated by `make mock-fidelity` on every run and never read from
# the tree: a committed dump is a stale artifact that diffs clean against the build it was taken
# from rather than the one in front of you, which is the same silent pass the gate's third exit
# state exists to prevent. The ledger the gate writes from them IS committed.
planning/fidelity/dumps/

# Dispatch material — work orders, gap-fix briefs, verify briefs. G6: a dispatcher wrote these
# INTO the tree under test, and they landed in three commits, one predating this fleet. `.dispatch/`
# is inside the repository so a brief can cite a repo-relative path, and outside every item's diff
# so no runner has to reinvent the isolate-the-commit-and-drop-it workaround one runner invented
# alone. The three bare root names are ignored too, anchored to the root: that is exactly where the
# three that landed were written, and habit is what put them there.
.dispatch/
/WORK-ORDER.md
/GAPFIX-BRIEF.md
/VERIFY-BRIEF.md

# better-goal
.claude/goals/
.claude/settings.local.json.bak.*

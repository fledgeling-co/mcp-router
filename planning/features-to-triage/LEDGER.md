# LEDGER — MCP Router feature pipeline

Allocation is a read-modify-write on this file: **one triage at a time**, and any runner
creating a child spec takes the ledger lock first.

**The status and the narrative share the last cell. Editing one destroys the other.**
On 2026-08-23 a script advancing the status fields did `cells[6] = <new status>` — a
replacement — and removed the entire record from eight rows at once: **≈55,000 characters**,
every verdict, finding and measurement from a whole verification wave. M12 went 5,026
characters to 62; M20 8,029 to 53. It was caught by a runner hitting the conflict while
merging forward, **not by the session that did it**, and restored from the commit before.
Two things follow. **Append to that cell; never assign it** — and if you must rewrite it,
**compare lengths before and after**, because re-reading the file shows eight well-formed
rows either way. And no gate covers this: `ledger-reconcile.py` checks structure and
agreement, and **a row with a correct id, a correct status and an empty narrative is
structurally perfect.**

**This file is not the only allocator, and a scan of its table is not a free-id check.**
It said "ids are allocated here and nowhere else" until 2026-08-21, and that sentence
produced two near-misses from two sessions inside one hour — one session reached for I6
while it was merged at `ef4f615`, another for X7/X8 against a table that records neither
X4 nor X5. `ORCHESTRATOR.md` carries live rows this table has never held (P5, P6, R2-R,
R2-W, R4-C, and the M9-M12 deferred children), and merged branches exist for ids recorded
in neither file (`ai/x4`, `ai/x5`). Before allocating, scan **both files plus
`git branch --merged main --list 'ai/*'`**, and expand any range notation first — see the
allocation notes below for why a range is the one place a reconciliation reports agreement
it never tested.

| ID | Title | Brief | Spec | Plan | Status |
|---|---|---|---|---|---|
| F1 | Swift workspace, shared kit, three targets | `F1-swift-workspace.md` | `spec-F1.md` | `plan-F1.md` | **Merged** `0924040` |
| F2 | The design system in SwiftUI | `F2-design-system.md` | `spec-F2.md` | `plan-F2.md` | **Merged** `22d1802` |
| F3 | Typed control-API client and models | `F3-control-client.md` | `spec-F3.md` | `plan-F3.md` | **Merged** `13825c9` |
| F4 | ServerStateTracker cannot report failure | `F4-tracker-failure-states.md` | — | — | **Merged** `aba30bd` |
| F5 | Skill version usage history, visualisations and telemetry | `F-skill-version-usage-history-stats.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Filed 2026-08-24 from a user directive and never registered, so it sat on disk unscheduled for three days. Granular visibility into which skills, plugins and MCP servers were invoked, their exact version at invocation, and historical trend charts. |
| F6 | Marketplace auto-update checker and changelog preview | `F-marketplace-auto-update-checker.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Filed 2026-08-24 from a user directive and never registered. |
| F7 | Session analyser and multi-harness skill recommender | `F-session-analyzer-skill-recommender.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Filed 2026-08-24 from a user directive and never registered. |
| F8 | Cross-harness skill and plugin profile synchronisation | `F-cross-harness-skill-sync.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**, so deleting the brief vetoes it. Ideation past the 2026-08-24 directive. |
| R1 | Swift router: core, config, manifest | `R1-router-core.md` | `spec-R1.md` | `plan-R1.md` | **Merged** `c30eac9` |
| R2 | Swift router: lazy pool, relay, passthrough | `R2-router-pool-relay.md` | — | — | **Merged** `a8091bb` |
| R3 | Swift router: control, usage, registry | `R3-router-control-registry.md` | — | — | **Merged** `e154bae` |
| R5 | Swift router: OAuth and the auth routes | `R5-router-auth.md` | — | — | **Merged** `b7c527c` |
| R4 | Differential parity harness and cutover | `R4-router-parity-cutover.md` | — | — | **Merged (harness only)** `e129779` |
| R4-C1 | The installer points at Swift; the TypeScript tree stays | `R4-C1-installer-points-at-swift.md` | — | — | **Done** (ai/r4c) |
| R4-C2 | Retire `src/*.ts` — held, and what it waits on | `R4-C1-installer-points-at-swift.md` | — | — | Held (owner: not on a green streak) |
| M1 | Mac window shell, menu bar, keyboard | `M1-mac-shell.md` | — | — | **Merged** `10cad44` |
| M2 | Activity: the live call log | `M2-activity.md` | — | — | **Merged** `c39c891` |
| M3 | Servers: the breaker board | `M3-servers-board.md` | — | — | Done (`589ab2e`, `3b11f33`, `af77200` on main) |
| M4 | Skills and marketplaces | `M4-skills.md` | — | — | **Merged** `7a28de8` |
| M5 | Discover: the registry | `M5-discover.md` | — | — | **Merged** `2a81c87` |
| M6 | Inbox and phone pairing (Mac side) | `M6-inbox-pairing.md` | — | — | **Merged** `6b3e940` |
| M7 | Evals and Cleanup | `M7-evals-cleanup.md` | — | — | **Merged** `85d8331` |
| M8 | Settings, menu-bar popover, quarantine | `M8-settings-quarantine.md` | — | — | **Merged** `affaed6` |
| I1 | iPhone: shell and pairing | `I1-ios-shell-pairing.md` | — | — | **Merged** `d582d43` |
| I2 | iPhone: Discover and detail | `I2-ios-discover.md` | — | — | **Merged** `ba139d4` |
| I3 | iPhone: Triage, Queue, Library, Settings | `I3-ios-triage.md` | — | — | **Merged** `b50aa8d` |
| P1 | Make the two auth routes reachable | `P1-auth-routes-reachable.md` | — | — | **Merged** `496f88c` |
| P2 | The `import` verb and the config rewrite | `P2-import-verb.md` | — | — | **Merged** `95d16f9` |
| P3 | Oracles for the usage stream and registry search | `P3-stream-and-registry-oracles.md` | — | — | **Merged** `f466020` |
| P4 | Derive the manifest rows; fix the directory-dependent normaliser | `P4-derive-manifest-rows.md` | — | — | **Merged** `8686fd6` |
| M13 | The scroll-edge separator, A34 | `M13-scroll-edge.md` | — | — | **Merged** `08b9bdf` |
| G1 | Stop the checks blaming the app for being out of date | `G1-gate-hygiene.md` | — | — | **Merged** `8cfb9e3` |
| V1 | Re-run the out-of-family review on the router items (grok) | `V1-outside-review-router.md` | — | — | **Merged** `29af3eb` |
| I4 | Let the phone install directly | `I4-phone-direct-install.md` | — | — | **Retired** — replaced by I5 (`4157bc4`) and I6 (`ef4f615`) |
| D1 | Deferred register: router side (12 children) | `D1-deferred-router.md` | — | — | **Merged** `997f7af` |
| D2 | Deferred register: Mac surfaces and design authority (14) | `D2-deferred-mac.md` | — | — | **Merged** `9e8a754` |
| D3 | Deferred register: phone copy and the harness limit (4) | `D3-deferred-phone-harness.md` | — | — | **Merged** `67ae4f5` |
| — | **BLOCKED: the Apple developer identity** | `BLOCKED-apple-identity.md` | — | — | **Needs input — `as-found`, not confirmed** |
| M14 | A shipped menu tells the user the app is not built | `M14-menu-says-not-built.md` | — | — | **Merged** `7e7ed70` |
| R6 | Children inherit launchd's minimal PATH | `R6-child-process-path.md` | `spec-R6.md` | `plan-R6.md` | **Done** — merged to `main` at `1d958b4` (`ai/r6` `7a4f15a`; rung `effect-witness`; 9 follow-ups `D-r6-d`…`D-r6-k`) — both routers append the user's own `bin` directories to a child's PATH; `r6-child-path.sh` examined=6 failures=0; parity census untouched at 83 rows, cli 17/17, pool 5/5 |
| R8 | An upstream that refuses our credentials must say so | `R8-auth-rejection-visible.md` | — | — | **Done** (ai/r8 → main; owner unfroze `src/`, A38 rewritten to guard the reference's existence; Swift half unblocked by R9; parity 82/83 control 16/16 0 diverged; auth gate examined=8 failures=0) |
| R7 | The router's thesis is unmet for every harness but Claude Code | `R7-harness-reconciliation.md` | `spec-R7.md` | `plan-R7.md` | **Merged** — **Verified — Done** (`effect-witness`, 2026-08-21). B1 closed and cross-checked against `agy mcp list`'s own twenty rows; both real Gemini configs byte-identical on six attributes across the run. All five write-gate walk-throughs now exit 1, re-planted independently. B3 and B4 closed in both directions with a seven-shape sweep finding no remaining silent zero. The stripper was checked by construction and three string-literal shapes still miss — latent, since the whole tree opens exactly one block comment and it is genuine. `make lint`'s three-pass block is a false dependency. Awaiting merge; follow-ups `D-r7-z`…`D-r7-ai` — prior-pass detail: **Ready to verify** — gap-fix 2 complete (`ai/r7` `d285298`). B1 closed against the real machine: the run reads `~/.gemini/config/mcp_config.json` and its 19 entries plus the router match `agy mcp list`'s twenty rows. The panel again caught two defects in the fix — a key rewrite that made stdio entries read as HTTP, and byte-for-byte endpoint comparison turning `/mcp` vs `/mcp/` into B4's silent loss. **The write gate's own comment stripper was blanking every line to EOF** on a Swift string containing a slash-star, so an applier under one reported clean. Selftest 22→27 cases, lane 55→59 checks. Two findings overruled on measurement. grok down as a lane at 16.5 KB, substituted with fable. Follow-up `D-r7-y` |
| R7-C1 | The Harnesses board and the `GET /harnesses` route behind it | — | `spec-M22.md` | `plan-M22.md` | **Delivered inside M22** on `ai/m22` — the route, the board, and the two figures neither could observe. Declared in the census as `div-m22-harnesses` and driven at both binaries by `parity-divergence.sh`, because a Swift-only route demands no `control` row by derivation and would otherwise be invisible to every mechanical check in `parity-manifest-check.sh`. Was: absorbed by M22 at its 2026-08-22 triage. The two rows named each other as blockers and neither could be scheduled; A36 forbids the board reading a harness config itself, so the route is not optional and the work is one piece |
| R7-C2 | Apply a reconciliation plan to a harness config, behind a human | — | — | — | Deferred (child of R7; the write R7 refuses) |
| R7-C3 | opencode's transport is unestablished | — | — | — | Deferred (child of R7) |
| R7-C4 | Project-scoped harness entries | — | — | — | Deferred (child of R7) |
| R11 | Skills write endpoint (remove/disable) with preconditions and undo | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R7, renumbered 2026-08-21) |
| R12 | Server soft-delete with a restore endpoint | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R8, renumbered 2026-08-21) |
| R13 | Router-side behavioural eval runner — servers only | — | — | — | Registered (ORCHESTRATOR.md deferred register; filed as R6, renumbered 2026-08-21) |
| G2 | The ledger table holds two row shapes, and every reader silently drops one | `G2-orchestrator-table-is-heterogeneous.md` | — | — | **Done** — 2026-08-24. Main table holds exclusively 9-column rows, all 4-column notes segregated in deferred register; check H clean with 0 unparsed rows. **Deferral re-affirmed 2026-08-22 ~18:2x, and the condition is now stronger than when it was written.** G2 restructures the tables in `ORCHESTRATOR.md` and `LEDGER.md`; it was deferred on 2026-08-21 *while agents were mid-read*. **Eight runners are reading both files right now** — six build items, two verifiers — so dispatching it would recreate the exact hazard it was parked for, against four times the readers. It is not blocked on capacity, information or a decision: it is blocked on the wave draining, and it goes out as the first item after the last runner reports. Recorded here rather than left as a silent skip, because *Ready for AI* on a row nobody dispatches reads as an oversight three sessions later. |
| G3 | `make test` is not deterministically green | `G3-make-test-is-not-deterministic.md` | — | — | **Merged** `4e18cc0` — **Verified — Done** (`metamorphic`, 2026-08-21), awaiting merge. All three shapes flip, confirmed by building the scanner standalone at both the pre-fix and delivered commits; exactly 5 call sites, all bounded, over 508 files. The directional count is wrong about which scanner it measured — nine shapes at 6/3 against the delivered artifact, not twelve at 8/4 — and the population is a convenience sample the verifier doubled in one session, though the conclusion it supports is unaffected. Nine new shapes, none blocking, because the artifact declared its bound honestly. **The title cannot be met by this item**: the one red in five runs was a port collision, not a sleep, so the residue is three mechanisms and wants its own item — prior-pass detail: **Ready to verify — gap-fix 3 complete** (`ai/g3` `94ddd73`, 4 commits on `e8c20e0`). The property is unchanged and every gate held. This pass fixed the layer the rebuild deliberately did not replace — Swift's statement and trailing-closure grammar — where a verifier measured **two misses and one false fire**, each pinned by a one-token control: a statement label made a control-flow body read as the wrapper's trailing closure; a labelled string-literal final argument produced no call site at all, because a literal blanked to whitespace is the shape of an unapplied method reference; and `Task` inside a string interpolation reddened a correct wrap. **The previous pass's directional claim is corrected where it was made** — "fails toward a red on correct source rather than toward a miss" was true of the three unreachable shapes it named and false as a statement about the layer. Measured: 2 misses to 1 false fire in the blocking shapes, and 6 misses to 3 reds across the nine further shapes the lanes found and this pass did not take (`D-g3-ah`). Stated as a count: of the twelve shapes measured against the delivered scanner, eight fail toward a miss and four toward a red, so the layer fails both ways and the direction is not predictable from it. **The completeness claim is bounded rather than closed** (`D-g3-ae`): Family A is 19 controls on a citable production list, Family B is 12 on brace nesting, and **Family C is 38 with an open population** — the count is now given family by family and the mutation matrix stated separately. 69 controls, each red under at least one of twelve mutations. **`D-g3-q`'s derivation withdrawn**: it does not reproduce, and the verifier got both sites red 4 of 4 under heavier load; deferred on scope alone. **Five duplicated register ids removed** (`D-g3-ad`), and `ledger-reconcile.py` — taken from `main` for **check K** — exits 0 at 172 register rows. Gates: `make test` **0** and **0** (7.126 s, 12.513 s at 1587 tests in 199 suites), `make lint` **0** at 0 violations in 500 files, `make parity` **0** at 358/358, `make acceptance-r6` **0** at `examined=6 failures=0`, the assigned mutation red at **exit 2** naming its own condition at `PoolReapingTests.swift:98:29` in 13.189 s. Two further `make test` runs went red in files this item does not touch — `CallbackLifecycleTests.swift:238` and `ControlStreamTests.swift:72`, `D-g3-c`'s class, recorded under `D-g3-s` as its fourth and fifth instances; **`make test` is still not deterministically green on this machine, which is this item's own title**, and what G3 fixed is the pool suite, the residue being `D-g3-c`'s ~60 unclassified sleeps elsewhere. The scan finds exactly 5 call sites, all bounded, over 499 files. Load average 421 and 0.0% idle throughout, so the timings are not representative. Two out-of-family lanes asked to break it found eight more, two of them regressions this pass had introduced; five taken, the rest registered with their direction. New follow-ups `D-g3-aa`…`D-g3-ah`. Prior: The property is unchanged and established: **a regression in this class produces a named red inside the CI bound, not a timeout** — the assigned mutation reds at exit 2 after 11.280 s naming its own condition. This pass fixed the guard, not the property. The scanner was **rebuilt rather than patched**: all seven defects the panel and the verifier found are instances of two approximations of Swift's grammar — comments recognised by a line's first three characters, and block structure read from indentation. A delexer implementing the real comment and literal grammar and a brace-balance walk replace both, and three named residues close with them. Held by **53 controls** asserting both directions, every one seen to fail: **34 single-mechanism mutations, 34 of 34 red, all 53 controls covered**. **Three out-of-family rounds asked to break it rather than review it and found twelve more defects, all fixed here** (`D-g3-y`) — including a raw literal read as a multi-line opener that was silently blanking a real file, and a CRLF desync that kept the readability guard green. `D-g3-l`/`m`/`n`/`o`/`p`/`r`/`v` closed; `D-g3-q` re-measured wider and deferred with a probe-tested reason (`awaitSessionEnded` takes its early return at 3 of 3 call sites, so the remedy is `D-g3-g`); acceptance criterion 3 deleted with its reason, as the orchestrator's error. New: `D-g3-w`, `D-g3-x`, `D-g3-y`, `D-g3-z`. `make test` **0** twice at `1587 tests in 199 suites` (4.941s, 4.130s); `make lint` **0** at `0 violations, 0 serious in 497 files`; `make parity` **0** at 358/358; `make acceptance-r6` **0** at `examined=6 failures=0`; the assigned mutation reds at **exit 2** in 10.589 s naming its own condition at `PoolReapingTests.swift:98:29`. One unrelated red in six runs — `CallbackLifecycleTests.swift:238`, a third measured `D-g3-c` instance, recorded under `D-g3-s`. Idle 0.0-44.6% with two sibling runners live What is still approximated is named — Swift's statement and trailing-closure grammar, regex literals, `#if` branches read as if all compile, and lexical containment standing in for an execution bound. All mutated files restored from `cp` backups and `git diff app/Sources/` empty across the pass |
| G4 | Assertions that do not read the quantity they are named for | `G4-assertions-that-do-not-read-their-own-quantity.md` | — | — | Merged `de1315 **Gap-fix 2 verified Done 2026-08-22 and merged at `de1315d`.** Oracle rung **differential measurement with an armed control** — the verifier armed its own sweep against a synthetic residue before trusting its clean sheet, and **every figure came from a command it ran** rather than from the runner's account. §6's arithmetic holds (`1 + 22 + 6 + 7 = 36` case lines; `31 behaved + 5 did not = 36`), every non-elided line appears verbatim in its own run, and the two absent are make's echo and make's error because it invoked the script directly. **`D-g4-b`'s thesis proved itself a fourth time while being checked**: the aligned diff of the two pastes differs in exactly one line, `merged ai/*` 29 → 27, and the verifier's own run read **28** — a count that has moved 29, 27, 27, 28 with **no tracker file edited**. `ai/g5` has since added a third commit past the merge point, so *committed twice* is already stale, and chasing it is the row's own point. **The sweep loads `claim-sweep.py` from `main` rather than copying it** — absent from this HEAD, present there, `exec`'d — and its accounting closes: **1118 scanned + 157 skipped = 1275 = `git ls-files`**, so nothing is dropped in silence. Non-blocking imprecision left: §6's diagnostic sentence attributes all five selftest failures to the stale count, and `div-r1-d3` fails a different check — the *expectation* is stale in all five, so the sentence is right about the fixture and loose about the output. |
| G4-B | The two gates G4 shipped, and the doctrine they each broke | `G4-gapfix-3.md` | — | — | Merged `92a348d` — **verified Done 2026-08-22 at `bc41e13`.** `main`'s `make lint` was red from `de1315d` and both causes were G4's own rules broken by G4's own gates. (1) `null-run-gate.py`'s three RAW arms built their scratch tree from a **hardcoded** directory list; M15 widened `GEOMETRY_DIRS` to four by adding `Settings`, so `no-raw-design-values.sh` exited early, the plant was never reached and each arm honestly reported it could not discriminate — G4's own thirteenth instance verbatim. The arms now read the list out of the script. (2) `reader-accounting.py` walked the filesystem, so it named a silent drop inside `planning/test-campaign/bin/witness-fixture/` — **untracked, another session's work-in-progress** — and reddened a shared branch over a file not in the repository. Its population is now `git ls-files`, and the docstring states the cost: a drop written this morning is invisible until it is added. **Five arms held.** The one that settles it is the boundary control: the real fixture (sha256 matched) reads **22 files exit 0** untracked and **23 files exit 1** after `git add` alone, with no byte changed — so the boundary is tracked-vs-untracked and not a `planning/test-campaign/` exclusion. The RAW derivation followed two novel directory names while `main`'s pre-fix gate **HELDs on all three poison arms** against the same widened script, which is what makes it a derivation rather than a coincidental fit. The registry row was **forced out, not removed to pass**: `analyse_file` finds `::main`'s silent drop at `:519` before and no reader after, unresolved 9 on both sides, and restoring the row exits 1 as a stale declaration. **A defect the write-up understated**: `NULL-accounting-empty` was passing on the git-failure path, since exit 2 plus `usage error` is satisfied by any non-repository; `repo=True` moves it onto the null condition it is named for. **Merged-tree gates re-run by the orchestrator**: `make lint` exit 0 with all three previously-HELD arms biting, 28 armed / 28 changed verdict / 0 held; reader-accounting 0; reconciler 0 across A–L. **Two residuals, neither blocking**: `null-run-gate.py:395` names *the Swift suite (1684 tests)* where `main` is **1725** — a stale literal in the instrument built to catch stale literals, filed as G4's **twenty-second** instance; and `script_dirs` misses a `"${ROOT}/…"` brace spelling, measured as failing loud rather than quiet. |
| G5 | Vendor the `test-campaign` version the gates actually run | `G5-vendor-the-campaign-version-the-gates-run.md` | `ai/g5` | — | **Done** — merged `34b9cf8` — `vendor/test-campaign/` is 0.9.2 pinned to `28ecd67`, byte-identical to both the plugin cache and `git archive` at that commit, with the pin and the four gate commands in `vendor/README.md`. Reproduced from a fresh clone with the submodule uninitialised and `~/.claude/plugins` denied under `sandbox-exec`: `campaign.py check` exit 0 at 69/76 armed 69/69, `strict-check.py` 62 of 76, `capture-lineage.py --gate` 16 of 16, `vacuity-check.py` 1 finding — the same numbers `0.9.4` gives. Progress at `planning/progress/G5.md`, gap-fix 1 at `planning/progress/G5-gapfix.md`, gap-fix 2 at `planning/progress/G5-gapfix-2.md`, gap-fix 3 at `planning/progress/G5-gapfix-3.md`, gap-fix 4 at `planning/progress/G5-gapfix-4.md`, gap-fix 5 in `planning/progress/G5.md`. Gap-fix 4 dropped the `installed <version>` label at twelve sites in six documents and committed `planning/installed-claim-sweep.py` to guard the pattern. Gap-fix 5 closed the verdict's seven-item work order — all record and instrument, no gate number moved: the guard could not see the word inside markdown emphasis and now can, without reading it inside an identifier; `planning/G5-citation-frames.py` gives the citation-frame control the reader and three predicates it lacked; and four figures quoted for commits turned out to be properties of a working directory or of no tree at all. Filed 2026-08-22 from **M28/DEF-057, answered by the owner**: vendor `test-campaign` only, over bumping the whole submodule or dropping the reproducibility claim. Measured: vendored `0.5.0` against the `0.9.2` every reported gate actually ran, and `plugins/test-campaign` is **8.9 MB** against the submodule's **546 MB**. 0.5.0 carries none of the scripts the campaign depends on, so a fresh clone following the documented `git submodule update --init` cannot run `campaign.py check`, `strict-check.py`, `capture-lineage.py` or `vacuity-check.py`. Verification history in `planning/verification/G5-verdict-history.md`. |
| G6 | Evidence kept in `/tmp` is not evidence | `G6-evidence-that-lives-in-tmp-is-not-evidence.md` | — | — | Untriaged — filed 2026-08-23 after the crash. **Every `/tmp` artifact this fleet cited is gone**: `sweep3.py`, `sweep2.py`, `sweep.py` and `m18-build2.log`. These were not scratch files — a sweep is the instrument proving a guard is *armed*, which is the distinction most of G4's twenty-two instances turn on. Two consequences already real: **G5 gap-fix 2's accepted verdict rests on an artifact nobody can re-run** (it recorded four planted faults producing four `FAIL` rows, plus three more the verifier planted itself, all against `/tmp/g5gf2/sweep2.py` (gone)); and **`G5V2` was sent to run a file that does not exist** — its brief opens arm 1 with *Run it* against `/tmp/g5gf3/sweep3.py` (gone) and calls that arm the claim that decides the item. **Worse than a stale literal**: G4's instances are wrong and *checkable*, but a dead path produces no wrong answer to catch — only an absence, which reads identically to *not yet run*. The fix is already precedent here: R17's `planning/claim-sweep.py` is committed and survived; G5's lived in `/tmp` and did not, and the two were written days apart for the same shape of problem. **The orchestrator's defect, not a runner's** — the briefs citing `/tmp` were written here, `GAPFIX-BRIEF.md:64` included. **Two directions, one root — measured 2026-08-23.** The matching rule (`lukerhodes-2f`) is **a brief cites a repo path or it cites nothing**; two briefs failed it *hours after being written here* — `GAPFIX-BRIEF.md:64` and `VERIFY-BRIEF.md:29`. The opposite error is the dispatch artifacts themselves: `WORK-ORDER.md`/`GAPFIX-BRIEF.md`/`VERIFY-BRIEF.md` were written **inside the tree under test** and are now **tracked in 4 of 12 worktrees** (M12, M19, M20, G5V2) and untracked in the other eight — so a third of them would merge into `main` carrying the orchestrator's prompts as project artifacts, and nobody decided that. M20 caught it and isolated the file in `bcc69dd` so it can be dropped before merge, but that is a per-runner workaround for a dispatcher's mistake and eleven runners did not each invent it. **`/tmp` is too volatile to cite, the worktree too durable to write into**, and this fleet never decided where its own working material lives. Triage must settle both directions together: fixing only the `/tmp` half moves briefs into worktrees and makes the tracked-artifact problem universal instead of one-third. — **Triaged 2026-08-25 → To Do.** Grounded: `/tmp` is cited in durable specs (`spec-P4.md:43,85,300`, `spec-F1.md:166`), not merely in transient evidence logs. And `WORK-ORDER.md` is tracked at repo root, confirming the opposite hazard (dispatch artifacts committed into the tree under test) exists on `main`. **Residual:** move surviving sweep scripts into `planning/verification/`, scrub `/tmp` citations from durable specs, and establish a dedicated ignored path for dispatch prompts. — **Developer Review 2026-08-26 (`ai/g6`, 8 commits, 24 files, no product code touched).** Both directions settled together as the brief's coupling argument requires. `planning/foreign-path-gate.py` sorts every scratch-root path into a **total partition** (`RUNTIME`/`GENERATED`/`SUBJECT`/`CITED_LIVE`/`CITED_DEAD`/`WITHDRAWN`), blocking on both citation classes — **`CITED_LIVE` blocks too, because a live scratch path is one reboot from the dead kind**. 39 withdrawals across 20 records; `WORK-ORDER.md` moved out of the repo root into `planning/dispatch-records/`; `.dispatch/` ignored with root-anchored entries for the three dispatch filenames; the rule written to `planning/practices/EVIDENCE_AND_DISPATCH.md`. **Denominators, one instrument over both trees:** 280→286 line-anchored · 280→286 wrap-tolerant · 188→193 deduped keeping the **most severe** class · 95→100 hand-written non-generated markdown · 42→44 citing-position; blocking `CITED_DEAD` **40→5** and `CITED_LIVE` **1→0**. Holes printed rather than hidden: 38 extensionless, 18 pointers read as SUBJECT, 232 files carrying machine-durable paths counted and never blocked. **16 plants all fired before any count printed**, with three negative controls that must still block and a wrap plant that fails when the two readings agree. **It corrected the orchestrator's grounding:** at `0e5ff49` there are **45 blocking occurrences, 40 of them in files this branch may write** — the brief's 40 counted *files carrying absolute paths*, the same digit answering a different question, and no file-level normalisation of this corpus yields 40 (md 44, md+json 50, planning-md 42, all-tracked 91). Gate exits **1 by design** on 5 shared-ledger citations the branch may not write, **0** once applied; `--control` 0. `claim-sweep` 1 is **pre-existing**, byte-identical hit lists at baseline and branch. — **Verified DONE and MERGED 2026-08-26 at `03c34c3`** (fresh context, rebased onto `afd823a`, out-of-family agreed). **All 8 requirements Met.** The brief's own **coupling test** was graded first and held: both directions settled together, neither quietly dropped. **Every denominator reproduced exactly** on the verifier's own instrument across both trees — N1/N2 280→286, N3 188→193, N4 95→100, N5 42→44, `CITED_DEAD` 40→5, `CITED_LIVE` 1→0 — and **the partition is genuine**, classes summing to N3 at every tree measured (188, 193, 218). **16 of 16 plants fired** and five of six gate mutations turned a control red. **`CITED_LIVE` blocking judged correct on a stronger ground than the record gave:** the cited glob resolves *today* to files from **different sessions**, so a live scratch citation can answer with an artifact that is not the one measured — silent wrong evidence, which beats absence for damage. **Five findings, all owed, none blocking:** (F1) the record's after-column is headed `ai/g6` while its numbers were measured two commits earlier — for an item whose thesis is that a pointer must resolve where it is read, its own column header does not; (F2) *39 withdrawals across 20 records* — the 39 is exact, the file count is **17**, and no normalisation yields 20, which is this item's own defect shape in its own headline; (F3) one control gap — the `--control` arm never reaches `main()`'s scanner choice, so swapping the wrap-tolerant reader for a line-anchored one **survives green**, and separately the wrap-tolerant reading buys **0 rows** on this corpus, stated without a denominator; (F4) a merge-order blocker, since M29 merged after G6 branched and shipped two live scratch citations; (F5) a doubled paren. **F4 was resolved before the merge landed** — the branch's own `G6-shared-ledger-withdrawals.py` wrote the 5 markers it could not write itself, and the 2 M29 citations were withdrawn — after which `foreign-path-gate.py` **PASSES on `main`**: `CITED_DEAD 0 · CITED_LIVE 0`. **The orchestrator verified the root-anchoring claim rather than trusting it:** `WORK-ORDER.md` is ignored at the root and **NOT** at depth, exactly as the record states. **A correction to this fleet's own reporting:** the citation figure of **1333** quoted repeatedly was a **dirty-tree** reading; clean `afd823a` is **1321**, and G6's own contribution to it is **4**. |
| G7 | A citation that does not resolve where it is read | `G7-a-citation-that-does-not-resolve-where-it-is-read.md` | — | — | Untriaged — **split out of `G6` on 2026-08-23, on `G6`'s own coupling test rather than on taste.** `G6`'s two halves are one item because each fix displaces the other: move a brief out of `/tmp` and it lands in a tree under test. This has no such relationship — fix `G6` completely and `planning/progress/G5.md` still rots at `:159`, because that file already satisfies `G6`'s rule perfectly (committed, durable, repo-pathed, and the exact artifact the defect lives in); carry the frame everywhere and not one `/tmp` file returns. **`G6` is about whether the artifact survives; this is about whether the pointer resolves**, and an abstraction shared is not a remedy shared — `G6`'s is a location decision a dispatcher makes once, this is a citation discipline every writer applies every time. **The measurement**: `G5.md:159` is correct at `a9603e5` and becomes `:170` at `43b44a2`; two agents independently ruled out revision drift after checking three frames **all on the same side of the edit**. Systemic rather than incidental — six of `D-g5-d`'s ten citations fail the same way, and gap-fix 3's own verdict carries the bare number in its opening sentence, its arm-4 paragraph and its work order, the three places a reader acts. **Do not fix by renumbering**: a number chased forward is correct until the next edit and carries no way to tell. Argument and decomposition test both `lukerhodes-2f`'s. — **Triaged 2026-08-25 → To Do.** Grounded: `planning/progress/G5.md:159` was verified to read unrelated text due to subsequent commits (file is 399 lines), landing on plausible-but-wrong text. **Form adopted:** anchor + commit SHA + line number. **Residual:** sweep existing docs for bare line references and evaluate a mechanical verification check. — **Developer Review 2026-08-26 (`ai/g7`, 4 commits from `0e5ff49`, HEAD `8d41fac`, tree clean).** `planning/citation-gate.py` sorts every `path:line` in the tracked corpus into **seven classes that partition the set**, wired into `make lint`, with the rule at `planning/practices/CITATIONS.md`. Only `DRIFTED` (anchor present at another line) and `ABSENT` exit 1 — a stated frame that does not hold is a false claim, where a bare citation is merely an absent one — and **renumbering earns nothing**, since a number with no tree and no anchor stays `BARE` however recently it was chased. Frames are read by adjacency rather than proximity. **Five denominators printed together and never blended:** line-anchored 2081 · wrap-tolerant 2081 (+0, so nothing in this corpus is wrapped mid-citation today) · deduplicated 1702 · minus 56 generated files 1495 · resolving to a tracked file **1434**. Classes over that: RESOLVES 21 · DRIFTED 0 · ABSENT 0 · NOTREE 1 · TREE_ONLY 8 · ANCHOR_ONLY 113 · **BARE 1291**, plus 499 bare continuations, 18 dropped on ambiguous basename, 43 on untracked path. **Every presence control fired:** a hermetic throwaway repo per run with 12 planted citations across all seven classes — including a wrapped-anchor row that must read `RESOLVES` wrap-tolerantly *and* be invisible line-anchored, the run failing if those agree — plus 4 corpus plants each reverted byte-identically. **It also found the grounding was worse than the brief stated:** the anchor sentence at `G5.md:159` is **no longer in the file at all**, so renumbering could not have repaired it even in principle. — **Verified NEEDS MORE WORK 2026-08-26 at `8d41fac`** (fresh context, audit-only, nothing merged). **The gate itself is sound and that was checked rather than accepted:** every denominator reproduced **exactly** on the verifier's own run (2081 · 2081 · 1702 · 1495 · 1434, classes summing to 1434), the seven classes genuinely partition — `tally` is keyed on a fixed tuple so an unknown class **raises rather than dropping silently** — `make lint` exits 0 as shipped and **2** with a planted bare citation, a renumbered-but-frameless plant stays `BARE` so renumbering earns nothing, and an **offsetting** plant leaving the total unchanged still exits 1, proving the ratchet is per-file rather than scalar. All seven presence controls fired. **The defect is a claim in the record, and it is this item's own subject turned on itself:** `planning/progress/G7.md` states *N2 == N1 says no citation in this corpus is split across a wrap today*, presenting as a **corpus measurement** what is a **tautology** — `normalise` collapses a whitespace run to a single space and `CITATION` forbids a space inside a token, so a wrap-split citation is found by **neither** reader and N1 ≡ N2 by construction. Proved by a **randomised differential over 30,000 inputs with deliberate mid-token splits: 0 divergences**, and **0 of 14** control rows contain a newline, so the citing-side wrap is unexercised — the control that warrants the claim tests anchor matching in the *target* file, a different question. Stale docstrings say *eight planted citations* where 12 are planted. **Blocking and ratchet arms unaffected — a false record claim, not a false green.** `agy` agreed independently, naming the same three. **Work order:** withdraw or re-warrant the N2==N1 claim, exercise the citing-side wrap or state it as unexercisable, correct the docstrings. — **Record claim withdrawn and re-warranted 2026-08-26 (5 commits, READY-TO-VERIFY again).** The gate's blocking logic is untouched; only records and controls moved. **`N2 == N1` is now stated as a structural identity, not a corpus measurement** — `normalise` replaces a whitespace run with a single space and `CITATION` admits no space, so a wrap-split citation collapses to `a.md: 2` and is found by **neither** reader; the printed `+0` is a fact about the tokeniser rather than about the corpus. **Withdrawn in place with the withdrawal stated**, because G7's own subject is that a record must resolve where it is read. The record names what would have to change for the two readers to differ: `CITATION` admitting whitespace, or `normalise` deleting a run rather than spacing it. **The identity is no longer prose — two new controls hold it:** a wrapped citation token must be found by neither reader *and* its unwrapped form by both (the guard on the guard), and a wrapped **frame** must read `RESOLVES` whole and `TREE_ONLY` a line at a time — so the citing-side wrap tolerance that **is** real is demonstrated rather than assumed. It re-ran the differential itself (**30,000 inputs, 0 divergences**) and counted **12 control rows, 0 containing a newline**, noting its 12 differs from the verifier's 14 because 12 is `len(rows)` in the shipped file — a denominator disagreement reported rather than smoothed. Stale docstrings corrected from *eight planted citations* to twelve in both places, and `citations_in`'s false claim replaced with what the collapsed view actually buys: **the window, not the token**. Four probes, `citation-gate.py` restored byte-identically each time (`f110aea1…1cec`). |
| G8 | A question answered at the wrong scope returns a clean answer | `G8-a-question-answered-at-the-wrong-scope.md` | — | — | Untriaged — filed 2026-08-23 beside `G7` on the test that split `G7` from `G6`: shared shape, unshared remedy. **`G7`'s fix is carry the frame; this one's is establish the set before answering about members of it.** Harder to catch because **a citation that does not resolve announces itself when someone follows it, while a question answered at the wrong scope returns a clean answer** — no dangling pointer, no red, just a correct answer to a narrower question. **Three occurrences in one evening** before either party named it: the `:159` citation ruled non-drift on three frames **all one side of the edit**; `RouterSheet.Kind`'s cases verified to exist and their *meaning* inferred without reading the tests that define it; and the `VOUCHED_CONTROLS` collision asked of *the two parties already in conversation* rather than *which branches touch the file* — complete about the wrong set, missing the largest party at `+29/−6`, and **caught only because M19's report mentioned the table in passing**. A fourth, same shape: a line-anchored `grep` as an absence sweep over hard-wrapped prose returned 0 for a phrase present twice, and the orchestrator reported a verbatim quotation as a paraphrase **to the party who had quoted it correctly, who then adopted the wrong version**. **M16 produced the positive form the same evening**: rather than asking whether a merge conflicts it derived the union of all three trees, walked its own dumps against it, and named the one thing it could not check — reducing an open-ended cross-branch question to *does the other branch introduce a role I use*, mechanical and runnable before any merge. That belongs in `mock_fidelity.py`'s header, where it **retires** the merge hazard rather than answering it once. **Routing decided 2026-08-23 — this item carries M16's rule, and it lands after all three merge.** M16 wrote the vouching-asymmetry rule into `planning/progress/M16.md` at `f83c7d8` rather than `mock_fidelity.py`'s header, **deliberately and correctly**: `ai/m16` is finished and ready to verify, and a fourth edit to a file two live branches are still rewriting buys them a conflict and reopens a closed branch to purchase a comment. It named the header as the right home and asked that the timing be the orchestrator's rather than taken from a finished branch. **Not M22's to carry**, despite being last: M22 has **zero diff** on that file today, so handing it the comment creates a conflict surface it does not have and makes its rebase harder — and *whichever merges last* is fragile, because merge order can change and a stalled M22 would strand the rule indefinitely. It lands instead as **one dedicated change once M19, M16 and M22 are all in**: zero conflict surface, no branch reopened, no dependence on anyone's timing. — **Triaged 2026-08-25 → To Do.** Grounded: only a 10-line header comment was added to `mock_fidelity.py`; **zero executable checks landed.** The actual requirements (executable merge hazard verification across all active surfaces, presence controls on absence checks, and normalizer reporting in sweeps) remain open work. — **Executable scope delivered 2026-08-26 (`ai/g8`, 5 commits, READY-TO-VERIFY).** Two gates where a header comment had been: `planning/role-intersection-gate.py` derives the union of every active branch's roles by `ast.parse` of each ref's copy and walks **every** fidelity surface against it — **including surfaces with zero diff on the gate file**, which is the rule the item exists for. Sets are established before the question is asked: 68 heads, 63 merged, **5 active**; surfaces unioned across all those refs and deduped by blob. `planning/sweep-control-gate.py` is wired into `make lint` (exit 0) and **runs** each declared control rather than trusting a disposition: 3 control · 54 grandfathered · 0 undisposed · 0 mislabelled, never blended. **Verdict on this tree is exit 3, not 0, and the reason is the item's own subject:** `popover.ledger.md` is an obituary reading *this run did not produce a table*, so a role sweep over it returns zero and **a two-state gate would read that as clean**. Six plants, each reddening its gate and restored byte-identically. **The out-of-family review caught the sharpest defect, and it was in the delivery itself:** G8's own live presence control **could not fail** — it lifted a role out of `surface.roles`, passed it back as `inject`, and checked `intersect` found it, which is `x in {x}` over tuples already in memory. *The gate against checks that cannot fail shipped containing one.* It now plants onto a real branch of a `--local --shared` clone and must reach the union through `for-each-ref` + `ast.parse`. The same review found the wrap-tolerant reader **decorative** while the report called it *the complete one*. 15 findings acted on, 1 recorded unfixed and printed every run (`run_control` reads an exit code and cannot tell a real control from `true`). **Lane note: grok stalled at 263 bytes with no verdict — one family, not two.** Branch is behind `main` and needs a rebase before merge. **VERIFIED 2026-08-26 (fresh context, agy/gemini-3.7-flash-high on-subject, agreed): NEEDS MORE WORK.** The headline fix is genuine — the verifier broke the union merge and the live presence control went `BLIND` on both rows; making it plant onto the base branch instead of a new one left the hermetic control passing 9/9 while the live one went blind, proving it is independently sensitive and no longer `x in {x}`. Exit 3 confirmed a real inconclusive rung. **The gate's finding against the orchestrator's own 3 goal scripts is judged CORRECT** — strongest case `lint-except-citation-ratchet.sh`, whose `grep -cE 'error:|Violation|FAIL|did not pass lint'` exits 0 on a lint failure matching none of the four patterns. Denominators re-derived exact on D0 114 / D1 91 / D2 60 / D5 60; heads 70 not 68 and active 7 not 5 (two `verify/*` branches created after the run); grandfathered is **55**, not 54 — a transcription slip, registry unchanged since `d7b81a4`. **BLOCKS ON: `planning/reader-accounting.py` (a pre-existing lint gate) exits 1 solely on G8's own `sweep-control-gate.py::tracked_scripts drops at :125 comprehension filter`** — exit 0 on the G9 worktree, so main is clean and this is G8-caused. One declaration row in `reader-accounting.tsv` fixes it. G8 regresses a gate in the target it wired itself into. Also raised: `design/assets/slice-tiles.py` is registered but undiscovered, so it is neither `stale` nor `mislabelled` and has **no reporting class**, silently inflating the backlog by 1; a broken ref walk raises uncaught `StopIteration` at exit 1 (loud, not fail-open, but coded as findings rather than inconclusive); control-failure and obituary-inconclusive both map to exit 3 where G9's gate distinguishes them 2 vs 1. **Verified DONE 2026-08-26** (fresh context, `grok-4.6` at xhigh, on-subject, read the branch) — **MERGED to `main` at `8795dd5`.** All five gap-fix checks confirmed and **all three presence controls fired on the verifier's own run**, each restored byte-identically. The gap is closed by **reporting rather than declaration**, confirmed independently: `reader-accounting.tsv` is byte-identical to main and carries **no** `sweep-control` row, so nothing was declared away, and `reader-accounting.py` reaches exit 0. Printing the discards surfaced **six tracked scripts the sweep never opened** — both git hooks, three `.mjs` fixtures and the TypeScript entry point — re-derived by the verifier with **conservation holding at 86 + 1463 + 117 = 1666** (figures moved with the rebase from 1662/1462/114; the gate prints live numbers so nothing false is reported, though the record's prose is now stale). `UNDISCOVERED` **blocks** rather than prints-and-passes, naming both owner calls plus the `waived` third answer. The **exit split is real and both codes were observed live**: 3 on the clean tree, 4 under a stub `git`, distinct and reachable. Two non-blocking observations the verifier raised: the shebang test runs over the extension-drop bucket only, so **9 vendored files carrying shebangs are never tested** — a caption problem rather than a hidden discard, since vendor is a counted, named drop — and one probe hash in the record (`b2d6cd85…`) matches no committed blob, an honest mid-work hash no longer checkable from the branch, though the verifier re-ran that arm itself and it fired verbatim. **Left standing, not graded down**: the UNDISPOSED goal scripts, now **five** rather than three, the rebase having brought two more onto the branch with no rows in the registry. `make test` green over three consecutive runs after rebasing onto `f013b73`; the first run hit the long-recorded loopback flake. |
| G9 | Two gates `cd` into a worktree that no longer exists | `G9-two-gates-pin-a-worktree-that-no-longer-exists.md` | — | — | Untriaged — swept for 2026-08-23 after egress rescoped the family as *a destructive git call whose target is established somewhere other than immediately before the call*. **This repo has the shape, mitigated but not harmless.** "REPO_ROOT="$(git -C "$(dirname "$0")" rev-parse --sh", `app/Scripts/pool-mutation-gate.sh:15` at `03c34c3` and "REPO_ROOT="$(git -C "$(dirname "$0")" rev-parse --sh", `planning/run-gate.sh:10` at `03c34c3` both `cd` into `.worktrees/R2`, **which does not exist** — no R2 in `git worktree list`, directory gone. Both are 0755 and both are **permanently dead**: every invocation exits 90 without reaching a check. **The `|| exit 90` is what makes this better than DEF-253**, whose script proceeded into whatever repository it landed in and ran `git reset --hard`, destroying a gate patch. **What is not saved is the gate**: `planning/specs/spec-R2.md` cites `pool-mutation-gate.sh` as runnable, so the spec asserts a mutation gate that cannot produce a result — this fleet's recurring shape once more. **It was correct when written** and a routine worktree cleanup falsified script, gate and citation without touching any file, with nothing going red because nothing invokes them. An absolute path is the slow-acting version of the fault and it decays untouched. **Also swept**: four other files carry absolute paths — none pins a worktree, but `planning/watch-fleet.sh` pins a `~/.claude/projects/…` session directory, which decays the same way. **A sweep-scope note against this session**: the first pass covered `scripts/` only and returned zero, missing `app/Scripts/` and `planning/` — `G8`'s wrong-scope failure, in its own sweep, minutes after filing G8. — **Triaged 2026-08-25 → To Do.** Fix landed and the gate is measurably alive: both scripts derive the root via `git rev-parse --show-toplevel`, and `pool-mutation-gate.sh` — which had exited 90 on every invocation — now runs and exits **1 (11 OK / 1 HOLE / 1 SKIP)**. Its findings are filed separately as `pool-mutation-gate-hole-and-stale-mutation.md`, and `spec-R2.md`'s *all thirteen guards proved load-bearing* row is corrected from that measured output. **The brief undercounts its own sweep: it says four other files carry absolute paths; the tracked count is 40**, including `WORK-ORDER.md` at the repo root — which is `G6`'s dispatch-artifact-in-the-subject direction, tracked on `main`. **Residual, unstarted:** that 40-file sweep, and the `GIT_DIR`/`GIT_WORK_TREE` unset in the pre-commit hook (hook exists, 36 lines, **0** mentions of either). **Owner note, raised by the review lane and left open rather than taken:** the brief's own preference for `run-gate.sh` was *delete* ("referenced by nothing and is the easier call"); this session repaired its path instead, which keeps an R2-specific script alive with a corrected pointer. Repair was right for the mutation gate; delete may still be right for `run-gate.sh`. — **Remaining scope delivered 2026-08-26 (`ai/g9`, 6 commits, READY-TO-VERIFY).** **The 40-vs-44 disagreement is settled, and both parties were right:** the ledger's **40 is reproducible** — *tracked files containing the literal `/Users/lukerhodes`, counted by file, over `git ls-files`, at commit `b757a1ac`*, the day G9 was swept — and the same reading is **44** at `main` today. G6's runner reported that no normalisation yields 40; it does, once the tree is named. The number needed a frame, which is G7's subject arriving inside G9's. Four other readings printed beside it (72 · 233 · 36 · 4). **The sweep failed the same way again one layer in:** keying on the literal username missed `/Users/someone` and `/Users/x` in two files — **the gate found them**. **Per-file: 4 runnable pins derived · 57 legitimately absolute** (21 recorded-evidence files where the path IS the measurement, 29 fixture subjects, 5 generated, 2 waived per line) **· 2 reported not touched** ("export HARBOURMASTER_SCRIPTS=/Users/lukerhodes/Dev/f", `ORCHESTRATOR.md:401` at `03c34c3`'s machine-pinned var, and `WORK-ORDER.md:15` which pins a deleted worktree — G6's open direction). **The hook landed in tracked source** `planning/hooks/pre-commit` with `install.sh --check` comparing installed against tracked by sha256; **deliberately NOT installed**, because `.git/hooks` is shared by every worktree and three runners were live — that is the orchestrator's to run. **Two measured findings on the hook itself:** git **does** export `GIT_DIR` for a commit inside a linked worktree, so the unset is load-bearing; and **`GIT_WORK_TREE` was a real bypass the unset does not close** — it moves the hook's cwd into a worktree while the commit still lands on `main`, now closed with `--absolute-git-dir`. **New gate** `planning/runnable-path-gate.py` sits **beside** `foreign-path-gate.py` rather than inside it, because G6's own docstring declines the machine-path corner as a different defect — and because G6 was unmerged, so importing it would have made this gate's arming depend on another branch. N1 135 · N2 3 · N3 18 · **N4 0 blocking**. **Three instruments were wrong first, and one claim died to its own control:** `GIT_DIR` is **not** a hook bypass — git redirects the whole commit so `main` never moves — and the control refused to certify it *before it reached a record*. The harness first read verdicts from git's exit code, so *nothing staged* was indistinguishable from a refusal. And **the gate blocked itself the moment it was committed**: it passed every run while untracked and failed the first run after, unchanged, because it reads `git ls-files`. **VERIFIED 2026-08-26 (fresh context, agy/gemini-3.7-flash-high on-subject, agreed): NEEDS MORE WORK.** The measurement work is the strongest of the pair. **The 40/44 claim reproduces exactly** — 40 tracked files containing `/Users/lukerhodes` over `git ls-files` at `b757a1ac`, 44 at merge-base `afd823a`, so the adjacent verifier's earlier “no normalisation yields 40” was drawn without checking out the tree. G9 takes 44→39. N1 135 / N2 3 / N3 18 exact, **and N4=0 is a measured zero**: appending a real machine path to a real tracked runnable file moved N4 0→1 and failed the gate at exit 1, while a bare waiver marker made the presence control print `MISS bare_marker.sh expected BLOCK read WAIVED` and refuse to print a verdict at exit 2 (distinct from block=1). `prove-pre-commit.sh` re-run clean: `GIT_DIR` is not a bypass (git redirects the whole commit), `GIT_WORK_TREE` **is** and is now refused, both controls fired, and removing either `--absolute-git-dir` or the `unset` line makes the prover FATAL at exit 2 rather than falsely pass — fail-closed. `install.sh --check` correctly reds at exit 1; non-installation is a stated hand-off (`progress/G9.md:122-125`, `:201`) and the verifier did **not** install it — `.git/hooks/pre-commit` still hashes `581204dc6d99`. Separation from `foreign-path-gate.py` judged right, not duplication. **BLOCKS ON: `runnable-path-gate.py` is invoked by nothing** — no Makefile target, no hook, no goals script; `git grep` returns only the file and its progress doc — **and unlike the hook install this is not declared**, the “Reported, not done” list naming three other hand-offs and omitting it. G9's own brief says of the R2 scripts *“Nothing went red, because nothing invoked them”*; its remedy is a gate nothing invokes. Second finding: the docstring's “duller reason” that `foreign-path-gate.py` is *“on `ai/g6` and unmerged”* **is already stale** — `ai/g6` merged at `03c34c3` — so the clause will be visibly false on merge. Needs rebase; adds 9 bare citations (its own progress doc). **VERIFIED 2026-08-26 (fresh context, `grok-4.6` at xhigh, which confirmed the finding independently from code alone): NEEDS MORE WORK.** Five of six checks pass and the headline work is confirmed. **Gap 1 reproduced exactly, from the wiring**: appending the original defect to `planning/run-gate.sh` and running `make -o tools lint` moved N4 **0 → 1** with `BLOCKING planning/run-gate.sh:16`, `FAIL 1 runnable file(s) name a path this checkout cannot rely on`, and `make: *** [lint] Error 1`; restored to the record's full 64-character sha with an empty diff and the gate back to `PASS`. The gate **is** a `lint:` recipe step rather than a separate target, which matters for the reason grok gave: the goal's lint script parses the recipe for `|| fail=1` steps, so a separate target would have been invisible to it. Gap 2's deletion is judged **sound** — the clause was scheduling, and amending would have left a dated fact to rot. The forgeable-waiver arm **fired**: removing the comment scoping printed `MISS quoted_marker.sh expected BLOCK read WAIVED` at exit 2. The hook is **still not installed** and the verifier did not install it. **BLOCKS ON ONE THING, and it is the item's own subject reintroduced inside the fix for it.** The `NEVER REACHED THE INDEX` control arm is **unreachable dead code**. Armed two ways, both produced an identical unhandled `KeyError: 'block_sh'` traceback at **exit 1**; the string never printed. The guard returns an empty `got`, so `main` raises on the first plant before it ever tests `bad`, and `bad`'s values are never printed anywhere — `grep` finds the string exactly once, at its own definition. **The consequence is the point: this gate's table assigns 2 to CONTROL-DID-NOT-FIRE and 1 to FINDINGS, so a tripped guard produces a broken control wearing a findings exit code** — verbatim the defect G8's gap-fix closed as its finding b. Grok, given only the code and the measurement: *"Dead. … Exit 1, and that is not the failed-control code. … Adding only the file does trip the guard and does exit 1, but the 1 is the crash, not a diagnostic."* Fix shape is small: have the control return a full result on the guard path, or read `bad` before indexing, and return 2. Second, non-blocking: the two recorded limits live **only in the progress record** — the gate prints its scope but no blind-spot block, unlike G8's sibling gate. **Verified DONE 2026-08-27 — MERGED to `main` at `5ed60f6`.** Graded in fresh context by an agent that did not write the three closing commits: it read them, then **armed the control three ways rather than taking their word**. All three plants print `NEVER REACHED THE INDEX` and exit **2** with no traceback — `git add` of the single file in place of `add -A`, that file written to disk alone, and **a third the verifying agent added itself**: a partial result with an empty failure set, which without the new guard would have printed MISS rows and fallen through to a verdict the run had not earned. Gate hash identical either side. It then checked for the same shape elsewhere and found none — the classification loop is total over every plant, the failure set derives from the result across all of them, and `main` reads through `.get`. The two recorded blind spots now print on every run rather than living only in the progress record, with figures re-derived per run. |
| G10 | `make acceptance` dies at its first lane, so every enrolment into it is inert | `G10-make-acceptance-is-red-on-main-so-every-enrolment-into-it-is-inert.md` | — | — | Untriaged — found 2026-08-23 by M22's verifier **during a `Done`-verdict residual sweep**, which is the rule earning its keep on its second use. `make acceptance` runs `shells.sh` first (`Makefile:484`) and **that lane is red**, with the blob byte-identical on `main`, `ai/m22` and its base (`6ffa54a52b…`) — **not any item's regression**. Two defects: "set v to value of e as text", `scripts/acceptance/shells.sh:216` at `03c34c3` does `set v to value of e as text` where the shell puts labels in the accessibility **description** (`G4`'s class in an acceptance gate — `mac-shell.sh` asks correctly and passes 9 of 9 boards); and **its emptiness guard cannot fire**, because `missing value` coerced `as text` is not empty. **The cost is not a red gate.** M22 enrolled `m22-boards.sh` into `acceptance` on the reasoning that *a lane nothing dispatches passes by hand forever while reading as covered work* — and **that enrolment is inert**, because the target dies four lanes earlier. So **enrolment has stopped being a way to make a lane run while continuing to read like one**, for M22 and for every lane enrolled from here. Every lane already in `acceptance` also needs re-checking, since none has been reached since `shells.sh` went red and **a lane that has never run is not known to pass**. — **Triaged 2026-08-25 → To Do. Filed as Needs More Info first; the review lane called that the wrong bucket on both grounds, and it was right.** `shells.sh` now reads `description` and `title` alongside `value` and rejects AppleScript's `missing value` — the two defects the brief names, the second being why the first survived. **But it has not been run against the running app, so it is not known to pass** — that is unfinished verification, not missing information, and the remedy needs no owner. The halt-vs-continue question is likewise evaluable rather than a taste call: run every lane, aggregate, exit non-zero — *a green summary over a red lane* is a defect in the summary rather than a property of continuing. **Residual, and the expensive part, absent from the first verdict entirely:** re-check **every lane already enrolled in `make acceptance`**, none of which has been reached since `shells.sh` went red, because a lane that has never run is not known to pass. **Do not** close this by reordering the lane list — that makes one lane run and leaves ordering as the thing deciding what gets measured. — **Remaining scope delivered 2026-08-26 (`ai/g10`, 7 commits, READY-TO-VERIFY).** **`shells.sh` was still RED on its first execution after the merged repair** — exit 1, *the macOS window's accessibility tree does not carry 'Activity'*. The attribute fix was necessary and **not sufficient**: four further defects sat behind it, each found only by fixing the last and running again, and **all five were the harness being wrong about the app rather than the app being wrong.** (2) It walked via System Events `entire contents`, which returned 61 lines carrying neither needle while `axkit dump` returned **104 rows carrying both**, same window, same minute. (3) It resolved the window by the title `MCP Router`, which **does not exist** — the console titles by destination, and CGWindowList said `Cleanup`; Screen Recording *is* granted (34/35 windows named) yet the lane blamed it. (4) It sampled ground at x=200px — **100pt on a 2x display, inside a 256pt sidebar**: (200,930) reads `#28282A` while four other points all read `#1C1C1E`. (5) It never checked build freshness — iOS read `#1E1E1E` from a bundle built 08-20, two days before `.ground` changed. **It now exits 0 green end to end.** **Per-lane table: 8 enrolled, 8 run, 5 pass, 0 fail, 3 blocked, aggregate exit 2** — a red lane cannot hide inside a green total, which was the whole point. **Frontmost unchanged throughout (`ghostty`)**, per UI_VERIFICATION rule 1. **VERIFIED 2026-08-26 (fresh context, agy/gemini-3.7-flash-high on-subject, independently returned NEEDS MORE WORK on the same two requirements): NEEDS MORE WORK.** The repair mechanism is sound and was proved twice. **A red lane cannot hide in a green total**: the real 8-lane aggregate hit a genuine red, printed `mac-shell.sh 1 FAIL` in the table and `ACCEPTANCE FAILED: 1 of 8 lanes failed an assertion`, and exited 1 with no `ACCEPTANCE PASSED` line; separately, mutating `acceptance-lanes.sh` to swallow the fail and blocked branches fired **9 assertions across 5 arms**, so the selftest is falsifiable rather than decorative. **Lane order is byte-identical to main's recipe** — the forbidden fix was not taken. All five `shells.sh` defects reproduce: System Events `entire contents` gave 105 lines with `Activity` 0 / `Running` 0 against `axkit dump` 84 rows with both present (figures differ from the recorded 61/104 because the tree mutates every 2s; direction and substance identical); 39 of 40 on-screen windows report a name so Screen Recording **is** granted and the app's layer-0 window is titled `Skills`, never `MCP Router`; (200,930) and (500,930) sample #28282A against five points at #1C1C1E = `ColorToken.ground`, with `MetricToken.sidebar` 256 so 200px = 100pt is inside it; and corrupting the Debug stamp's `sources` digest turned the new freshness check to exit 2 BLOCKED naming `make build-mac`. **Frontmost was `ghostty` at start and end throughout** — UI_VERIFICATION rule 1 held. The verifier's own first sampler converted to sRGB and read #252528 where truth is #1C1C1E — the exact Display-P3 trap `shells.sh` documents; the lane's raw sampler is right. **BLOCKS ON TWO.** (1) **The committed per-lane table is stale against its own branch**: G10's `3be638b` repaired `mac-shell.sh`'s oracle build list, moving it BLOCKED(2)→FAIL(1), so the delivered branch produces `fail: 1` / aggregate **1** where the record says `fail: 0` / aggregate **2** — and the record's prose names a *window-title* red while the verifier's run failed on an *AXHelp reason-string* assertion (`Router / Re-index Manifest` read `Re-indexing the whole manifest hasn't been built yet.`, expected `This feature hasn't been built yet.`), so that lane holds at least two reds and the record names one. The item whose whole point is that a red must not hide in a summary ships a summary reporting its own red as a block. (2) **`control-client.sh` passes vacuously**: byte-identical to main (`520fed38`), it emits only `parity-lock.sh: line 205: BASHPID: unbound variable` and **exits 0 having run zero assertions** — `BASHPID` does not exist in macOS `/bin/bash` 3.2 and the script sets `-u`. The brief scoped *“re-check every lane already enrolled — a lane that has never run is not known to pass”*; the record classes it `PASS`. This is the item's own thesis one layer down and is not in the known-inherited set. Verifier's per-lane table: shells 0 · control-client 0 (vacuous) · p1-auth-routes 2 · mac-shell **1** · r7-harness 2 · m22-boards 0 · menu-badge 0 · menu-badge-selftest 0 → `enrolled 8 run 8 pass 5 fail 1 blocked 2`, aggregate **1**. **GAP-FIX COMPLETE 2026-08-26 on `ai/g10`, +5 commits, tip `e619e67` — READY TO RE-VERIFY.** Both gaps closed. **Gap 1, the stale table**: re-ran `make acceptance` and rewrote the evidence record from what that run printed — `shells.sh` 0 PASS 12 assertions · `control-client.sh` 0 PASS 4 · `p1-auth-routes.sh` 2 BLOCKED 0 · `mac-shell.sh` **1 FAIL** 11 · `r7-harness-reconciliation.sh` 2 BLOCKED 0 · `m22-boards.sh` 0 PASS 16 · `menu-badge-lane.sh` 0 PASS 8 · `menu-badge-lane-selftest.sh` 0 PASS 3, giving `enrolled 8 · run 8 · pass 5 · fail 1 · blocked 2 · vacuous 0`. `fail` 0→1 and `blocked` 3→2 exactly as the verifier said. The record now states the aggregator's exit (**1**) and make's own recipe exit (**2**) separately, since the old "aggregate exit 2" could not be told from either. **Every red in `mac-shell.sh` enumerated** by running a scratch copy that records instead of exiting (deleted, never committed): 4 FAIL lines over 54 passing assertions, which are **two** real defects — a stale seven-name destination allow-list, and a row still naming the export command by its pre-M20 title and menu — plus two cascade lines firing on the empty field the first leaves behind. Both reported rather than fixed, because they encode product-scope decisions belonging to M1/M22, and filed as a brief on the branch. **The AXHelp mismatch is a HARNESS defect, proved and fixed harness-side with 0 product source files touched**: the app writes `command.reason(in: context)` into AXHelp while the oracle printed the generic `availability.reason`, and `reason(in:)` specialises `.featureUnbuilt` per command deliberately under D-m14-a because nine commands now carry that answer. Measured twice — compiled standalone the fixed oracle emits the app's exact string, and run live it covers 32 app-declared commands over 9 boards with **zero** reason-string reds, so the assertion is stronger and does not turn the lane green. **Gap 2, the vacuous pass, fixed at its root**: `BASHPID` is bash 4.0+ and macOS `/bin/bash` is 3.2.57, and the runner measured the precise crack — **bash 3.2 preserves the status of an explicit `exit 1` and of a `set -e` death, and loses it for a `set -u` death**. Fixed by assigning a fallback in a function rather than through command substitution, which would fork first. `control-client.sh` now runs **3 of 3 checks against a real router** where it previously asserted nothing; the lock selftest had been blind to this because its shebang resolves to Homebrew bash 5, and it now passes 14/14 under bash 3.2, bash 5, and with `BASHPID` exported. **The durable half: the aggregator counts each lane's assertions and classes exit 0 with zero of them as `VACUOUS`, reported with the failures** — exit 2 is a lane reporting it could not run, exit 0 with nothing behind it is an unearned claim. Cleanup on failure fixed too: the EXIT trap terminates the launched instance and escalates TERM→KILL, and on the run above `mac-shell.sh` exited 1 leaving **0** MCPRouter processes. **Three presence controls, each fired and restored byte-identically** (self-pid→`$$`; `lane_assertions()` forced to 1, reddening 8 assertions across two arms; the inherited-`BASHPID` guard removed), with arm 9 the two-way control requiring the same lane plus one `ok` line to be a PASS again. Out-of-family (agy/gemini-3.7-flash-high, on-subject) raised three real findings, all applied — the inherited-`BASHPID` guard, `sed` for `tr -dc` which had read an assertion declaration with a trailing comment as `52`, and the SIGKILL escalation — and its false-positive point is now in both the runner header and the record: the count is a presence test, not a tally, since `control-client.sh` makes 3 checks and is credited with 4 because its probe echoes `OK`. Gates: `make test` 0 · `ledger-reconcile` 0 · `citation-gate` 1 at main's inherited ratchet with blocking 0, this branch's citations framed and the baseline untouched · `acceptance-lanes-selftest.sh` 0 over 12 arms · `parity-lock-selftest.sh` 0 across 3 configurations. **Caveat stated by the runner: the 8-lane table predates the three review-driven edits**, none of which can change a lane outcome (the guard fires only on an exported `BASHPID`, which was not set; the `sed` fix touches only a declaration line no lane emits; SIGKILL follows only a failed TERM) and the two selftests covering that code were re-run green after — but the 20-minute GUI sweep was not re-run, on a machine at load 94. **VERIFIED 2026-08-26 (fresh context; lane `claude-fable-5` at high **RECORDED AS DEGRADED**, grok-4.6 xhigh having stalled at 280s with a 222-byte partial): NEEDS MORE WORK — narrowly, on the record rather than the mechanism.** **GAP 2 is fully closed and every control fired.** The load-bearing bash claim was re-measured and is **exact**: bash 3.2.57 preserves the status of an explicit `exit 1` and of a `set -e` death and returns **0** for a `set -u` death, where bash 5 returns 1 in all three. The lock selftest holds **14/14 under bash 3.2, bash 5, and with `BASHPID` exported**; `control-client.sh` runs **3 of 3 against a real router**; the aggregator's `VACUOUS` class **fired** on a planted silent lane, exiting 1; the two-way control passes; cleanup left **0 instances** after a non-zero run; and the assertion-count caveat is in **both** the record and the runner header. All three presence controls fired and restored byte-identically. `git diff 03c34c3..ai/g10 -- app/Sources/` is **empty** — no product copy was edited to green a lane. The AXHelp oracle ran live over **32 app-declared commands across 9 boards with zero reason-string reds**. **BLOCKS ON FOUR RECORD DEFECTS.** (1) **The rewritten table still does not survive a re-run — 5 of 8 rows differ**, bidirectionally and traceably: two lanes the record calls BLOCKED passed because the CLI existed in the shared build, and three GUI lanes blocked because that session could not get a window composited (0 windows after 15s on both Debug and Release). Isolated, `shells.sh` exits 1 with `the macOS window never appeared` — **pre-existing at `03c34c3` and untouched by G10, so inherited rather than this branch's**. The outcomes are environment-dependent and the record states none of that. (2) **One of the two "real defects" is state-dependent**: the destination allow-list red only fires when the app restores Harnesses or Insights, and on the verifier's run that assertion **passed** with `window title is 'Checks'` — the allow-list is genuinely stale, but the record and the downstream brief present a conditional red as a standing one. (3) The quoted restoration hash is **one commit stale** and matches no file on the delivered tree. (4) The presence-control figure is **understated — 11 assertions across three arms, not 8 across two.** The 2+2 split is **CONFIRMED exactly** by code reading, with both real defects located in product source. **Rule 1 held**: frontmost `TV` before and after, MCP Router never frontmost, 0 instances left. **Verified DONE 2026-08-27 — MERGED to `main` at `59ecf34`.** All five record defects reproduce exactly on the verifier's own run and every named control fired. The `ACCEPTANCE-ENV` stamp printed **verbatim** on a live 8-lane sweep, in the preamble and again under the counts, with arm 12 enforcing both facts in both places and its two plants reddening 2 and 3 assertions respectively. `Destination` ships **nine** cases against the seven-name allow-list, so the staleness is standing by reading, and the verifier's run **restored into Insights and watched the conditional red fire** with the exact quoted message. Every hash re-checked **by object type**: six resolve as commits and `520fed38` is confirmed a **blob**, which is how the fifth defect was found — it survived the earlier sweep precisely because it resolves to *something*. The presence control was re-measured independently: the fallback branch alone gives **8** reds across arms 8 and 10, both branches give **11** across arms 8, 10 and 11 at 5+3+3 with arm 9 green. |
| G11 | A gate answers differently on two checkouts of one commit | `a-gate-answers-differently-on-two-checkouts-of-one-commit.md` | — | — | **To Do** — filed 2026-08-26 by M30's runner, reproduced and diagnosed by the orchestrator. `planning/foreign-path-gate.py` reads disk instead of git blob and tests machine `/tmp` existence, failing on clean checkout while passing on dirty main. **Verified DONE 2026-08-27 (fresh context, grok-4.6 xhigh on-subject) — MERGED to `main` at `566dc5a`.** The gate now reads the committed tree at `HEAD` via `ls-tree` plus `cat-file --batch`, so its verdict is a property of the commit rather than of whoever's working tree it ran in; working-tree scanning is opt-in behind `--worktree` and says so. **The verifier reproduced the determinism rather than accepting it**: two clones, one dirtied with an unwithdrawn scratch citation in a tracked record, gave clean HEAD **0** and dirty HEAD **0**, with dirty `--worktree` **1**. It separated machine resolution from the verdict independently — a repo citing one scratch path, run with that path present and then absent, gave exit 1 both times and `CITED 1` both times, with stdout **byte-identical** once the live/dead annotations are stripped. All **17 presence plants** fired, and mutating one plant's expected class makes the gate print `control: DID NOT FIRE` and exit 2 rather than reporting a count it cannot stand behind. **ONE FINDING RECORDED, NOT BLOCKING, and a good catch**: in `planning/verification/G11-corpus-determinism.py` the 2-way assertion is **vacuous** — it clones a pristine copy but passes `--root <original>` on *both* invocations, varying only `cwd`, which the gate ignores, so it compares two identical commands. The verifier proved it by replacing the clone with an empty directory and watching the suite still print PASS and exit 0. The genuine two-root comparison exists and is load-bearing inside the gate's own `run_control`, which the verifier showed catches the exact regression — so the property is proved, just not by that file. One word fixes it. |
| G12 | The mutation harness reports success when its filter selects nothing | `mutation-harness-passes-when-it-selects-nothing.md` | — | — | **To Do** — filed 2026-08-26 from M29/G7 verify. Mutation harness returns success with 0 checks run if filter matches nothing; needs to fail-closed on empty match. |
| G13 | The pool mutation gate has a guard nothing proves, and a mutation that no longer applies | `pool-mutation-gate-hole-and-stale-mutation.md` | — | — | **To Do** — filed 2026-08-25 from tailings audit. Pool mutation gate eviction guard test passes whether guard is present or not; forced termination check no longer applies to moved code. |
| G14 | The campaign never enumerated three surfaces that shipped | `the-campaign-never-enumerated-three-shipped-surfaces.md` | — | — | **To Do** — filed 2026-08-27 by the orchestrator when asked whether the campaign covers every surface. `campaign.py check` exits 0 over *"24 enumerated, 0 with no case"*, which is true and is the wrong denominator: the surface list has not grown since it was written. `Destination.swift` ships **nine** destinations and the campaign names **seven** — `harnesses` and `insights` are absent entirely, as is the capability-document viewer (probing every surface title and name for `readme`, `document`, `capability`, `markdown` returns NO on all four). None of the three is unbuilt: each is measured against the mock by `mock-fidelity`, which carries its own `planning/fidelity/*.layers.json`. So structure is checked on all three and **no on-glass case ever opens, actuates or photographs them**. A surface never enumerated is invisible to a completeness check keyed on enumeration — the same shape as M32's census defect, where an element outside every rule could not be reported by a gate that reports by rule. **Recorded so triage does not chase them: `REQ-015`, `REQ-016` and `REQ-020` look like holes and are honest records of product state** — pairing genuinely ships no transport (`DEF-001`), `REQ-016`'s missing effect provider is missing *because* the feature is unimplemented, and `REQ-020` is `class: deferred` behind the owner's parity cutover. **The one real requirement gap is `REQ-008`**, whose two cases are both `n/a`: `NSStatusItem` is not an `AXPress` target while backgrounded and UI_VERIFICATION rule 1 forbids taking the screen, so a shipped surface is unreachable by construction — stated honestly, never decided. **DECOMPOSED 2026-08-27 by intake into five briefs, and this row is now the parent finding rather than the work.** An omnibus row bundling five differently-shaped pieces is not schedulable: enumerating a board, tracing a board's figures, covering a viewer and its flow, deriving a denominator from the product, and re-running a traversal refusal on the wire are five runs, not one. The children: `harnesses-board-has-never-been-opened-on-glass.md`, `insights-board-has-never-been-opened-on-glass.md`, `the-capability-document-viewer-has-no-case-and-no-flow.md`, `the-campaigns-surface-list-is-hand-maintained-and-has-gone-stale.md`, and `the-document-route-has-no-wire-level-campaign-case.md` (proposed-by-ai). **Two gaps were NOT filed because the backlog already carries them, and intake surfaces rather than duplicates**: `REQ-008`'s unreachable menu-bar popover is `daemon-status-popover-interactive-testability.md`, which proposes the product-side remedy of exposing accessibility actions so the popover can be actuated without stealing focus — the right shape, since the campaign's constraint is that it must never take the user's screen; and the journey/plane modelling is `campaign-journey-plane-ledger-model.md`. Neither needed a second brief. |
| G15 | The Harnesses board has never been opened on glass | `harnesses-board-has-never-been-opened-on-glass.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage from G14's decomposition. Shipped with M22; absent from the campaign's surface list entirely, so no completeness gate can report it uncovered. Its structure IS checked against the design of record by mock-fidelity, which carries `harnesses.layers.json`; what no case establishes is that it loads, populates from a real router, renders its empty and failure states, or is reachable from the sidebar. |
| G16 | The Insights board has never been opened on glass | `insights-board-has-never-been-opened-on-glass.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage from G14's decomposition. Filed separately from G15 rather than bundled, because Insights is **the board of counts and measurements** — the surface where a figure the product does not observe is hardest to spot, since a plausible number reads as a real one. DESIGN.md §6 forbids drawing a figure the router does not observe and nothing has checked that here against a live router; M32's census already found one such cell on this board. |
| G17 | The capability-document viewer has no case and no flow | `the-capability-document-viewer-has-no-case-and-no-flow.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage from G14's decomposition. **The two halves were each proved and the join between them never was**: M19's panel was measured against the mock, M30's route by route tests, parity vectors and a two-router differential. No case covers a person opening a capability, reading its README and following it to the changelog. M30's own runner made the point while building its look — every capture of that panel, M23's included, was a picture of a JSON fixture rather than a document a router served. |
| G18 | The campaign's surface list is hand-maintained, and has gone stale twice over | `the-campaigns-surface-list-is-hand-maintained-and-has-gone-stale.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage from G14's decomposition. **This is the recurrence rather than the instance**: fixing G15–G17 leaves the mechanism that lost them in place. The completeness gate asks which *enumerated* surface has no case, so a surface never enumerated is invisible to it — the shape M32 closed for mock elements by making the census total. The requirement list has the same illness at smaller scale, 20 in the input file against 25 in the inventory. **M35's verify verdict is the same defect class from the harness side**, delivered the same day. |
| G19 | The document route has no wire-level campaign case | `the-document-route-has-no-wire-level-campaign-case.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**, so deleting the brief vetoes it. Distinct from G17 because a route can be correct while the panel is wrong and the reverse. M30 armed the containment check with planted divergences and a verifier attacked it with 23 adversarial references for zero escapes — strong evidence that lives in the item's record and is proved **once** rather than continuously, which is the shape this repo keeps finding rotted. |
| G20 | The citation gate landed and immediately found forty-two unfalsifiable claims | `citation-debt-surfaced-by-its-own-gate.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage, and **still live**: measured this run the ratchet reads **BARE 1329 over N5 1519 against a baseline of 1291, with 12 files above it**. This is the debt the gate surfaced on its first run and it has been carried, not absorbed — every fleet since has reported `citation-gate` exit 1 as inherited. Absorbing it is an owner decision; the ratchet's own rule is that counts may only fall. |
| G21 | Test-campaign visual capture lineage and unpublished-reason catalog | `campaign-capture-catalog-audit.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Filed 2026-08-25 from the capture-lineage gate; the campaign's own header records a run where 20 published captures showed three unrelated documents while every gate passed, because only the filename bound a picture to a surface. |
| G22 | Test-campaign external effect provider and source-root resolver | `test-campaign-effect-provider-resolver.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. The vacuity check currently reports `providers: 7 of 8 named, 0 resolved — NOT CHECKED` because no `sourceRoot` is declared, so a provider is a string nobody read. |
| G23 | Design-of-record parity alignment with honesty guardrails | `design-of-record-honesty-alignment.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Filed 2026-08-25 from the campaign's differential review. |
| W1 | Secrets-at-rest inventory and dynamic token resolution bridge | `W1-secrets-at-rest-inventory.md` | — | — | **Merged** `b9ceec6` (`ai/w1` → `main`) — inventory of 15 secret entries across 9 upstreams in `servers.json`; router half of WAR-0057 / W1 implemented via `SecretResolver` and `WardenSecretResolver` supporting `warden://` and `op://` dynamic resolution at upstream connection time; 1815 tests passing across 227 suites. |
| W2 | Upstream authentication token migration to the system secrets broker | `upstream-tokens-warden-migration.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. W1 inventoried the secrets; this moves them. The owner's own standing note records this as **owed work rather than tidying**, and that the tokens should be rotated because they sat on disk in the clear. |
| M15 | Settings becomes its own window | `M15-settings-window.md` | `spec-M15.md` | `plan-M15.md` | Merged `29d511 **Gap-fix verified Done 2026-08-22 at the metamorphic rung and merged at `29d5111`** — BL-3's load-bearing claim flipped by a controlled single-property mutation and flipped back rather than re-run, BL-1 re-derived by regenerating the artifact, BL-2 by an independently written sweep. **A correction the orchestrator owes**: this row previously credited M15's runner with diagnosing the check-E false-RED correctly where R17's had not. `aae3033` gets the **mechanism** right and repeats the **specific** error — *seventeen commits, M21's merge and G5's among them*. There was no G5 merge: `4de2080..52f0c5c` is **18 commits with exactly one merge**, `e121801`, and `ai/g5` was cut from `2fbe062` having committed nothing, so its tip **was** a main commit. Today's green is not main moving again — `ai/g5` committed `3cd45c6` at **14:40:51, six minutes after `aae3033`**, and that is what stopped check E firing. **And the guard's claim is narrower than its comment**: armed by re-chording `Add server…` to `,`, AppKit **strips the chord from the loser**, so A19b read 35 chords on one item each against a 36/23 baseline and saw nothing; A20 caught it instead. The guard bites on the case it was built for, and `AXMenuItemCmdChar` cannot see a collision that has already been resolved — filed as G4's **nineteenth** instance. Two fixes named and not blocking: the gap-fix's reconciler paragraph, and A19b's comment. **Merged-tree gates re-run by the orchestrator**: `make lint` **0 violations in 549 files**, `0/556 require formatting`; `make test` **1725 tests in 215 suites passed**, 0 failures; reconciler **0 across A–L**. |
| M16 | The Signal Path replaces the Breaker Column | `M16-signal-path.md` | `spec-M16.md` | — | **Done** — verified `4cc7728`, awaiting merge — **To Do** — design of record settled 2026-08-22 as `design/mcp-router-console.html`. Needs More Info — triaged 2026-08-22. One essential question, and it is M21's fork applied to the signature element: does the breaker column stay. `BreakerGeometryParityTests` asserts exact equality against `DESIGN.md`'s nineteen rows, so removing the levers reddens a parity test whose oracle is the contested document. M23's Servers ledger has already enumerated the work and cites this brief by name **Ready to verify 2026-08-23 at `c50c44a`, twelve commits from `87e16dc`** — *not* `f83c7d8` with nine, which was a stale head that would have sent a verifier at the wrong tree. **No behaviour has changed since `753670e`**; everything after it is records: `87387a5` progress note and acceptance evidence, `f83c7d8` the cross-branch rule, `5ef14ef` citation frames, `1e229d9` the G8 placement ruling *with its argument rather than only its outcome*, `c50c44a` the sweep-count correction. `planning/evidence/M16-acceptance.md` still describes the tree. **It produced this fleet's most reusable result**: adding entries can only make more pairings vouched, never fewer, so no already-vouched pairing can be un-vouched by another branch's edit, and the whole cross-branch hazard is the quota rule keying on the **build node's role** — reducing an open-ended merge question to *does the other branch introduce a role my surface uses*, mechanical and runnable before any merge. Routed to `G8` for the header rather than carried by a finished branch. **It also caught its own citations**: three frame-dependent ones in its note now carry anchor and tree, and it flags that two of the three **landed on plausible wrong text rather than on nothing**, which is the variety that gets believed. **Verified Needs More Work 2026-08-23 at `115e0f9`; gap-fix 1 dispatched. The code is right; the record is not — all six defects are prose, each a one-line edit.** Every behavioural claim reproduced on the merged tree: **1743 tests in 216 suites**, all eight `lint` components exit 0, the fidelity gate returns 137 findings with the breadth composition matching exactly, and **regenerating the tracked ledger produces byte-identical output** — which the verifier calls the strongest single confirmation that the acceptance evidence still describes the tree. Claim 1 holds exactly. **The cross-branch rule is sound and was settled by execution rather than argument**: patching `VOUCHED_CONTROLS` to the three-branch union changed nothing on this surface, and a **presence control** (vouching `indicator`) moved `covered-by-pair` **24→20** and `extra` **10→14**, so the zero is measured rather than blind. **And the verifier refined the rule**: it survives a screen it does not mention — **Servers' mock census *does* name a `callout` affordance**, so screening on the *kind* would report it exposed when it is not, and keying on the **build node's role** is what matches the code. **Two of the six are the class M16 filed against `spec-M22.md` and then committed itself** — its note says the harness column has three states while listing four case names for a four-case enum, and one citation names its tree and still lands **two lines off, on another dict entry of identical shape**, which is `G7`'s dangerous variety resolving onto plausible text. **Two are false rows in the acceptance file and that is what tipped the call**: `AC6` describes output its case-sensitive `grep` does not produce, and `AC7`'s *§1, §7 and §8 no longer name the element* is false for §1 and §7 — **and that file instructs a reader to accept a row in place of re-running the check**, so a false row there is an instruction to believe something untrue rather than measure it. **The verdict overrides the only out-of-family lane still up**: `gemini` said `Done`, arguing over-blocking on prose, and the downgrade is logged rather than passed silently. **`make lint` never ran** — `tools` wants `node_modules` and `dist/index.js`, absent — so its body is green from components run directly but the target was not observed to pass. One early reading that `ORCHESTRATOR.md` looked 33 lines short was **`main` moving, not a lost merge**; `git merge-tree` cleared it and the false alarm is recorded. **Gap-fix 1 delivered 2026-08-23 at `b03d8ee`, 18 commits — ready to verify. Documents only**: `git diff 753670e..HEAD -- app scripts DESIGN.md` is still empty. **All six findings reproduced before being patched and each fix names its reader.** F1: `HarnessState` declares four cases (anchor `enum HarnessState` at `:52`, cases `:55-58`, same blob `f190de0` on this branch and `main`). **F2 corrects the verdict's own citation** — `"icon"` is at `:85` on this branch, not `:87`, and `:87` is `"column-header"`: `G7`'s dangerous variety, in the verdict that reported it. F3: AC6's case-sensitive `grep` returns 10 hits, all doc comments or forbidding assertions, **0 under `RouterCore`** — the deadlock breaker is lowercase and needs `-i` (29 total), presence control 22/32/9. F4: four breaker mentions survive in `DESIGN.md` and **§8 is the only named section that is clean**. F5: `extra-cited` restored so both columns reconcile, 116 over 149 at the base and 116 over 157 here, **the base column read out of the ledger blob tracked at `87e16dc`** rather than by rebuilding the gate. F6: 1725 is now a per-file `@Test` census over each tree's git objects, controlled against the tip's executed 1743 and against zero parameterised cases at both trees, with the diff's net +18 agreeing. **The cross-branch refinement is folded into the rule's bullet**: 0 build nodes of role `callout` on Servers against `jack` 4 / `signature` 1 / `row-indicator` 4 as the control. **Two things came out differently from the brief.** `make lint` **exits 0 here** — this worktree carries the `node_modules` and `dist/index.js` its `tools` prerequisite wants, so the target was observed; the verifier's failure was a **fresh-worktree condition** and both files now say which worktree. And **its own `ORCHESTRATOR.md:219-232` citation went stale mid-run** — `main` moved to `1f5ad55`, the text is at `:350`, **131 lines off, landing on another item's verdict line**: *the defect I was fixing, arriving on schedule*, caused by the orchestrator appending to that file concurrently. **It fixed one thing beyond the six and one beside it**, and it **agrees with the verdict while pushing back on two findings** — 5 and 6 would not have blocked alone — noting that *my agreement carries little weight, I am the graded party, so the argument is what holds it up*. **VERIFIED `Done` 2026-08-23 at `4cc7728`** — the wave's third pass. **Documents-only holds wider than the brief asked**: the diff `753670e..b03d8ee` is empty for `app scripts DESIGN.md` **and** unfiltered outside the two planning files. All six findings reproduce and all six fixes are correct, each re-derived — `enum HarnessState` at `:52` with four cases at `:55-58` (blob `f190de0` on both trees); `"icon"` at `mock_fidelity.py:85` here and `:68` on `main`, with `:87` the `"column-header"` entry; AC6's ten hits at the lines it names, presence control 22/32/9; AC7's four surviving mentions with section ranges and §8's zero **controlled by `keyboard`**; AC9 reconciling 116 over 149 and 116 over 157. **And it did something the note did not**: it **planted a parameterised case and a commented `@Test` to prove the 1725 census's two zeros are not blindness.** Merged tree green — 1743/216, all eight lint components 0, fidelity gate exit 1 with the exact recorded composition, ledger regenerates byte-identical, selftest 68 cases all three exits, reconciler A–L. **The cross-branch union changes nothing and vouching `row-indicator` moves covered-by-pair 24→20 and extra 10→14, so that zero is measured.** **The runner's pushback is upheld**: findings 5 and 6 would not have blocked alone and neither made a false claim about the tree — *what tipped the previous call was two rows offering themselves as substitutes for a check while being wrong about it*, and those rows are now true with controls behind them. **Three residuals routed, none blocking**, the first being that arm 5's *two sites* is reproducible but both are model switches — **`swift build` fails in `MCPRouterKit` and never type-checks the UI**, which is `M33`'s mechanism again, so the row's inference about the *view* does not follow from its evidence; the invariant is real and it proved it by the variant that reaches `SignalPath.swift:261`. **And the gemini lane the previous verdict overrode answered `Done` this time and reached its Finding A independently.** |
| M17 | Four states on every surface, and chrome that follows | `M17-surface-states.md` | `spec-M17.md` | `plan-M17.md` | **Done** — merged `d33924f` (`ai/m17` → `main`); FortyStateMatrixTests verifying 40 distinct cells across ten surfaces; 1937 tests in 229 suites passing. The brief's declared dependency on M1 alone is wrong: forty cells needs M22's two boards and M15's window, so it lands after both. Nine-versus-four states produces the same build either way and is recorded as an assumption. `StateContainer` already models nine and is used only in the debug gallery. Cites `D-m23-g`, `DEF-015`, `DEF-014`, `DEF-034` and `SURF-003` rather than re-deriving them. Nine assumptions, no essential question |
| M18 | Twelve sheets, and the gate each decision gets | `M18-sheets-and-gates.md` | `spec-M18.md` | — | **Needs More Work** — verified `8354a63`, gap-fix 3 owed — To Do — triaged 2026-08-22. Measured thirteen sheets in the mock, not twelve — `official` was added by M24 — and two of the mock's own sheet links resolve to nothing. The build has seven sheet sites, three still on `isPresented:`, five per-board enums and one dead case. Five of the thirteen have no host surface until M15, M19 and M22 land. Only `pair` has measured evidence and its transport is `DEF-001`, still open. Ten assumptions, no essential question **Built 2026-08-22 at `6b620b3`, eight commits — NOT ready to verify: the tree has never compiled.** The runner reported that plainly rather than dressing it up, which is the only reason it is actionable. `swift build` failed across five attempts; **two of those failures were the runner's own** and are recorded in its commits rather than quietly fixed — it queued two builds of one package which deadlocked on `app/.build`, and its wrapper piped through `head -60`, which SIGPIPEs the build. It first blamed other fleets for the lock and then corrected itself (`ee3ed39`) after finding all six occupied berths were this project's. `make lint` never ran at all: it needs `make tools`, which needs `node_modules`, and two `npm install` runs got no berth. **Out-of-family plan review (`agy`/`gemini-3.7-flash-high`, AMEND on seven findings) changed the design rather than the prose on one**: `SheetGate` as first planned was a lookup table **no call site consulted**, so its tests would have passed at full green while a button called `remove(name)` directly — the boards now route destructive actions through `request(_:)` and the acceptance test moved to the board-model seam. Five per-board `Sheet` enums and two `Bool`-shaped presentations became one `RouterSheet` nested per host, so `inboxBoard.sheet = .addServer` **fails to compile** rather than rendering something. All seven `.sheet(` sites use `item:`; the three that were `isPresented:` kept their by-id lookup by deriving the enum from existing storage, because a captured value would have satisfied the brief's letter and reintroduced the stale-subject bug both models carry a comment about closing. **Three things the mock draws that cannot be drawn as drawn**, measured here and absent from triage: the router computes its children's PATH at `src/pool.ts:168` and **publishes it nowhere**, so `path` ships as a refusal in the shape `SURF-010` established; `ConfigWriter.backupsKept = 5`, so the gate table's *30-day undo* is **a promise this router does not keep** and nothing was added to fake it; and `RegistryEntry` has no publisher field, so `official` drops its publisher grid and says so on the surface. **Also open**: Phase E's six fidelity surfaces and rendered verification of the two new sheets. Next step is a compile, not a verifier. **Gap-fix delivered 2026-08-23 at `6721e5c`, 12 commits — ready to verify.** `swift build` exit 0, `swift test` **1757 in 219 green twice**, `make lint` 0 (swiftlint 0/557, no-raw-design-values clean/127, null-run-gate 28 armed 0 held) — **+32 tests across 4 suites with nothing dropped**. **The measurement the brief asked for is still owed, and the runner said so itself rather than letting it pass**: the brief asked it to *measure* that `@Environment` actually updates a `ButtonStyle`, warning that a style is not a `View` and that a disabled button rendering at full strength is a silent defect no compile catches. It instead **removed the question by construction** — both styles now read `isEnabled` from a nested `View` where support is unambiguous — and recorded that in `planning/evidence/M18-acceptance.md` as **an argument rather than a measurement**. Right call and right disclosure, but **the property is argued, not observed**, and if the warning was worth writing the measurement is worth taking before this is called Done. **A flake named rather than absorbed**: `CallbackLifecycleTests` *a listener binds once* failed once with *the callback listener was cancelled before it bound*, passing on the same tree either side — R5's loopback socket bind, untouched by the diff, neither fixed nor folded into its own gate result. **Remaining scope**: the four hostless sheets (M19 owns readme, M22 the other three, `Kind.owner` names them), Phase E's six fidelity surfaces, and rendered verification of the two new sheets. `RouterSheetTests` compares enum and mock both ways, so a sheet shipping unmatched reddens it. Its worktree carries `GAPFIX-BRIEF.md` and `WORK-ORDER.md` untracked — `G6`'s second direction, in the eight-worktree bucket rather than the four. **Verified Needs More Work 2026-08-23 at `d482562` (on merge `c85a105`); gap-fix 2 dispatched.** **The blocker is a destructive-action defect the gap-fix introduced**: `4c320a8` moved `.keyboardShortcut(.cancelAction)` **off Cancel and onto the destructive Remove button** (`CleanupSheets.swift:106`, anchor `.keyboardShortcut(.cancelAction)`, tree `6721e5c`), **so Escape removes the installed capability instead of dismissing the sheet**. It was correct before M18. The verifier **measured it with a presence control** rather than reading it — a single-file SwiftUI reproduction of that shape fires `REMOVE` on posted keycode 53 and **the pre-M18 shape fires `CANCEL`**. **No test in `app/Tests` mentions either shortcut**, and that zero carries a planted probe, so the guard is part of the fix. **The measurement the runner owed and declined now exists, and it holds**: rendering the shipped styles through `ImageRenderer` and naming the nearest palette token, a live prominent button paints the accent hue, a disabled one paints `--f3` at `rgb(248,248,248)`, and a bare `View` reading `@Environment(\.isEnabled)` separates green from magenta. So the by-construction fix was sound and only the observation was missing. **Two instrument limits recorded as could-not-run rather than as passes**: `ImageRenderer` draws nothing inside a `ScrollView`, which is where `SheetFrame` puts every sheet body, so the empty bodies in the new sheets' renders are the instrument and not the product. **Three non-blocking**: a destructive button filled where the mock draws it unfilled; **the two new sheets have no Escape path at all, alongside six older ones — 8 of 14 sheets**, so fixing the blocker alone leaves the class one sheet over; and the `29 btn primary` count's normaliser was never named — the verifier gets **29, 33 and 35** over that one phrase and the conclusion survives all three, which is the counting discipline finding its next instance within hours of being written. `swift test` 1757/219 with **R5's ephemeral loopback bind red once (`EADDRINUSE`), green on runs 2 and 3** — *a second costume for the flake M18 had already named* — counts read from the xUnit report rather than the summary line. **Gap-fix 2 delivered 2026-08-23 at `3317f00`, seven commits plus a `main` merge — ready to verify.** **The blocker is fixed and it reproduced it first**, with a **committed** probe at `planning/evidence/M18-gapfix-2/escape-shortcut-probe.swift` (in the repo, not `/tmp`) building seven action-row shapes and posting keycode 53/36 at each: the shipped shape fires `REMOVE` on Escape, the pre-M18 control fires `CANCEL`. **That matrix killed two candidate fixes before one was written** — **one control cannot hold both shortcuts** (SwiftUI keeps the innermost and silently drops the other, both orders) and **`.onExitCommand` on a sheet's own body is inert**. So Cancel takes Escape, Remove takes no key, nothing is the default. **The cost is stated**: Return no longer dismisses the four surfaces touched — correct for a destructive confirmation with no non-destructive affirmative, and a deliberate trade on the two informational sheets, where a hidden zero-opacity twin keeping both keys was **measured working and declined on cost**. **Finding 3 scoped to four**, including `RemoveServerDialog` as the blocker's sibling because *fixing only the sheet would have left the pair disagreeing in the other direction*; six left as named open defects, guarded so a name that stops matching a sheet fails. **Nine of fifteen sheets now have an Escape path, up from six.** The guard is five source-level assertions plus a scanner armed against the shipped defect, eight arms all red. **Three figures corrected, two of them the verdict's**: `MissingSubjectSheet` sat outside its table so *8 of 14* is **9 of 15**, and `make lint` exits **0** here for the whole target because this worktree has `node_modules` and `dist/index.js` on disk **untracked** — a worktree fact saying nothing about a fresh checkout. **The third is its own and it is `G4`'s shape in its own measurement**: its first per-component lint check **read `tail`'s exit code**, so all eight read 0 while `reader-accounting.py` was actually failing on its new script — `make lint` caught it. `swift test` **1768/221, 0 failures, 0 skipped, six green runs**, R5's flake absent from all of them. **A design question it raised and declined to decide — now settled by the orchestrator from the design authority rather than left open**: it made Cancel a filled primary on `RemoveServerSheet` while Cleanup's other two confirmations kept a standard Cancel, so three sheets disagree. `DESIGN.md:441` rule 4 reads *One prominent accent-filled action per view, trailing. Cancel leads. Destructive is never the default.* **Cancel leads and the prominent action is trailing, so Cancel cannot be the accent-filled one** — and since destructive is never the default, a destructive confirmation has **no** prominent action, the rule being a ceiling rather than a requirement. **So the two standard Cancels conform and the filled one does not.** For the verifier to check rather than for an owner to decide. **Gap-fix 2 verified Needs More Work 2026-08-23 at `8354a63`; gap-fix 3 owed — two style changes, one comment, and a guard.** **The blocker is genuinely fixed**: re-running the committed probe reproduced **all fourteen cells**, and all eight mutation arms bit with the runner's exact issue counts. Every corrected figure re-derives on the verifier's own instruments — **9 of 15 sheets carry an Escape path against 6 before, two-way planted control**; 35/33/29 `btn primary` over 3,566 elements with **zero disabled and zero destructive controls carrying a key** across all of `app/Sources`. It also measured the **Settings-window shape the committed matrix does not cover** — the sheet takes Escape and the window handler stays out of the way — and notes that **its presence control there failed twice before it armed, so a lazier run would have reported an instrument as a product defect.** **What blocks is the design call the orchestrator settled, and the verifier reached the same conclusion independently with better evidence.** Cancel is the accent-filled control on `RemoveServerSheet` and `RemoveServerDialog`, **leading**, where `DESIGN.md:441` puts the accent fill **trailing** and Cancel leading — and **`DESIGN.md:433` makes a §3 violation a defect rather than a variation**. Three artifacts corroborate rather than restate: the mock draws that Cancel as a bare `btn`; the same board's two other confirmations draw it standard; and **`ServerSheets.swift:89` states the rule correctly 183 lines above the control that breaks it.** M18 introduced one site at `4c320a8`, the other is M3's, and **gap-fix 2 kept it under a comment citing §3.4 — the clause that forbids it.** Its reason for blocking rather than passing is this fleet's own rule: *passing it would leave it nowhere, since gap-fix takes failures and merge takes passes.* **Two differences from the record rather than from the work.** `make lint` exits **2** at `tools` here — the gap-fix's *exits 0 here* is true of its worktree and **of neither tree**. And `swift test` is 1768/221 green **six times in eight**, not six-for-six: run 6 was the R5 flake the brief names, and **run 3 was `OAuthWireTests.cancellationResumesTheWaiter`, whose assertion sits behind a fixed three-second sleep and which is named nowhere in this item's record** — a second, previously-unnamed flake. Both sit outside the diff; OAuthWire passes 12 of 12 in isolation. Six non-blocking findings each with a destination, including that the gap-fix's shortcut table cites `Button(` declaration lines under headers naming the modifiers, and its its `RemoveServerDialog` row at `:270` **went stale inside the commit that wrote it**, landing on a comment. **Gap-fix 3 delivered 2026-08-23 at `453a124` — ready to verify, blocker closed.** `RemoveServerSheet` (`CleanupSheets.swift:131`) and `RemoveServerDialog` (`ServerSheets.swift:278`) now draw Cancel with `StandardButtonStyle`, **neither `.keyboardShortcut(.cancelAction)` moved**, and **both confirmations now have no prominent action at all — what rule 4 produces when the only affirmative act is destructive**, which is the orchestrator's determination and the verifier's, implemented. **The inverted comment is rewritten to state §3.4 correctly and name the commit that filled the control**; the two correct §3.4 citations are untouched. **The guard exists now**: `SheetShortcutScan.Control` gains `label` and `buttonStyles` with three tests reading them — the sweep, **the population it actually covers** (a label written as an expression is invisible to it, so the seven literal-Cancel views are asserted), and **the readers themselves shown reading and shown failing**. `arm-guards.sh` is **12 arms and all twelve bit**, the scanner-blinding arm going 8 issue lines → 13. **Both record differences corrected in the honest direction**: the `make lint` figure now **names the worktree wherever it appears** rather than claiming anything about the branch (0 here, 2 in the verifier's — one carries `node_modules`/`dist`, the other neither); and `swift test` is **11 green of 11 here against the verifier's 6 of 8, pooled 17 of 19** — *and still a sample of a branch with two live flakes, not a property of it*, which is the denominator rule applied by pooling rather than replacing. **The second flake is named with its mechanism**: `OAuthWireTests.cancellationResumesTheWaiter` asserts **behind a fixed three-second sleep**, a wall-clock budget wanting a different repair from R5's port. The shortcut table now cites modifier lines under modifier-named columns, with **a committed instrument that states its normaliser**, reports 342 files / 59,999 lines and is **armed both ways**. **`make acceptance` deliberately not run — red on `main` at `shells.sh`, `G10`** — and the escape probe matrix skipped because no file any of its cells measures moved and the verifier reproduced all fourteen. Carried forward: Phase E with two new rows, rendered verification of the two sheet bodies, the six sheets with no Escape path, `M31`, and both flakes. **Gap-fix 3 verified Needs More Work 2026-08-23 at `1b4449a` — the design blocker is genuinely closed and the block is the merged tree.** **The rule reading is upheld by exhaustion rather than by agreement**: no conforming shape keeps a prominent action on a two-control destructive confirmation — fill is trailing, Cancel leads, destructive is never the default, so **all six assignments die and *neither filled* is what is left**. The macOS-alert counter-argument is real but its residue is *which control holds Return*, already open with the `hidden-cancel` probe row behind it. An independent reader found **zero accent-filled Cancels among the eight Cancel controls** in `app/Sources`, armed both ways and **without the next-control false positive that caught the first reader last pass**. **What blocks is a merge-only break, caught in advance**: `main` at `5f98e6c` carries M22's `ShimExplanationSheet`, and merged into `ai/m18` it turns `thePopulationIsTheFifteenNamed` and `everySheetOutsideTheGapCarriesAnEscapePath` **red, four runs of four** — while **neither tree is red alone**, because `HarnessSheets.swift` does not exist at `453a124` and `SheetShortcutGuardTests.swift` does not exist on `main`. Repair is two entries plus a re-stamp, and the item's own `shortcut-lines.py` already prints the corrected figures (**nine of sixteen with an Escape path, seven without**). **The merge also conflicted in `ShellTestSupport.swift`, and resolving it naively would have dropped two of M18's gate enrolments — the verifier proved `boardFileListIsComplete` catches that rather than assuming it.** **Three findings besides the blocker, and the first is an instrument that cannot fail**: `arm-guards.sh` **calls any nonzero exit a bitten arm**, so on the merged tree it printed *all arms bit* while **nine of twelve measured nothing against a baseline that was already red**. A population figure at `ServerSheets.swift:270` goes stale on the merge. And **the `role: .cancel` branch excusing the eighth Cancel from the asserted population had no arm** — the verifier armed it and it fires. **It declined the pooling this orchestrator had endorsed**: *defensible, but I would not quote 17 of 19 alone — the record itself says the populations are not interchangeable and both flakes are environment-sensitive*, so its four runs are reported as four runs and pooled with nothing. Everything else reproduced: twelve arms with recorded issue counts, **all fourteen probe cells for the third time**, and 342 files / 59,999 lines from an independent reader. Two of three out-of-family lanes down; gemini agreed on the rule reading, so that check is one family rather than three. |
| M19 | The in-app GitHub-flavoured Markdown viewer | `M19-gfm-viewer.md` | `spec-M19.md` | — | **Needs More Work** — verified `54bb0aa`, gap-fix owed — To Do — triaged 2026-08-22. No Markdown rendering of any kind exists in the app and nothing in the campaign measures this feature at all, so M23's gate is the only thing that will catch drift. The mock's readme sheet gained a product header and a facts strip after the brief was written. One acceptance line waits on M21 for the badge colour — a dependency, not an owner question. Eight assumptions, no essential question **Done** — merged `f294060` 2026-08-23 — six commits, tree clean, base `87e16dc`.** Runner measured **1776 tests in 219 suites** (up from 1725/215, re-measured at its own base rather than quoted), swiftlint `--strict` 0 over 570 files, reconciler 0 across A–L. `mock-fidelity-gate.sh readme` **exits 1 with 30 findings and that is the correct outcome** — every finding is a declared divergence; 29 ledger rows all carry a `Dn` tag and the manifest holds the recipe to re-derive the split. **Three things a verifier must not be surprised by, all recorded by the runner rather than found later**: there is **no entry point in the shipped app and that is deliberate** — `src/control.ts:279-283` serves no document, so a wired *Read me…* button would read *no document* for every capability in a Release build, which is the *no number the router does not observe* constraint one surface over (out-of-family record at `planning/evidence/M19-review-agy.md`; grok 402, codex down to 2026-08-27, `agy` answered); therefore **no driven pass**, only a real headless render plus the gate, **and the ledger opens by saying that is weaker**; and the gate measures **one of the panel's three designed states** because the mock draws one frame and `mock-affordances.py` exits 3 rather than inventing another. **It extended the shared M23 instrument** — additively and with a control: this surface has no `.v-*` frame so the census could not reach it at all, `mock_fidelity.py` now reads an optional `stateFrames` map, and five census kinds knew only the boards' spelling (`badge` had no rule, so **the shields this item exists to draw were invisible to the instrument meant to check them**). All six existing frames re-measured before and after — 87/6/8/6 and 52/4, unchanged. **An id collision, and it is the orchestrator's**: the runner filed `M29-capability-document-source.md` at `06dc629` (20:25) while `M29` had been taken on `main` at `78a27e7` (18:47) during the crash window — allocation is a read-modify-write the pipeline serialises for exactly this reason. Renumbering to **M30**; no ledger row of the runner's is affected. Verifier not yet dispatched. **A coverage asymmetry, stated before any verifier sees it.** `readme.layers.json`'s `stateFrames` is exactly `['ideal']` — **one** state frame — where M16's servers surface is measured on **four** (ideal, empty, loading, error), and `readme.ideal.json` is the only `readme.*.json` in the repo. So *M19's fidelity is measured* and *M16's fidelity is measured* are **not the same claim**, and only one of them covers the empty, loading and error states. Not a merge hazard and not a defect in the work — the gate measured what exists — but a verdict must not rest on a coverage word meaning two different things. Found by `lukerhodes-2f`; M19 has exited, so it is the orchestrator's to carry. **Cross-branch reclassification measured to zero for these two surfaces** — and *only* for these two, a limit this row previously overstated as *in both directions*. **M22 corrected it**: the role-walk hazard is **per-surface, not a property of the merge**. Over M22's 42 build roles the intersection with what the merge adds is **`callout`** — harmless there and checked rather than assumed, since the only node carrying it (`unreadable-note`, error state only) is already paired. **Anyone re-measuring after the merge runs it on their own surface rather than inheriting *empty*.**: M19 introduces eight roles (badge, callout, codeblock, heading, list-item, sentence, tab, verified-mark) and **none** intersects M16's 39 build roles, and `signature`/`jack` appear **zero** times across 21 build roles on the readme surface. **Verified Needs More Work 2026-08-23 at `54bb0aa`; gap-fix owed.** Everything the item claims reproduced — gate exit 1 with 30 findings, 29 rows all `Dn`-tagged (**parsed as a pipe table with a planted untagged row confirming the zero is measured**), 1776 in 219, the six existing census frames byte-identical in their affordance **lists** rather than only their counts, ledger regenerates byte-identical. **The blocker is one defect seen twice, and it is the eighth instrument-that-cannot-fail.** `MarkdownBlock.plainText` is **never constructed anywhere in `app/`** — the only two `.plainText(` hits are `case let` patterns in a switch. So `CapabilityDocumentFixtureTests.nothingFallsBack`, which filters for that kind and expects the result empty, **cannot fail for any input**, and it is the only guard on *with no block falling back to raw text* — half the brief's acceptance line and row 10 of the evidence table. **And the arm proving that guard is itself vacuous**: A2 **reddens by trapping** (`Fatal error: Range requires lowerBound <= upperBound` from `(1 ... 0)`) rather than by an assertion. Re-running all eight arms and **classifying the output rather than the exit code** puts seven on a real recorded issue and A2 alone on a crash. **The renderer is not in question** — every undrawn construct arrives visible as its own source, all three tabs parse to typed blocks, badges carry `--shield-good` (#1B7A38) in the dump, and the gate is live (D7 label moved findings 30→28 and back). **On the orchestrator's stated coverage asymmetry: one state frame does clear Done** — the mock draws no frame for the three unhappy states and `mock-affordances.py` exits 3 rather than inventing one. **The sharper gap is the tabs**: 1 of 3 measured, where the mock gives each tab body an id and this item's own `stateFrames` censuses them at 22/14/6 — and **D1's citation blames *rendering three documents at once* where what actually blocks it is that `CapabilityDocumentSheet` exposes no way to select the rendered tab**. Two thirds of the ledger rests on that citation. Four smaller findings including `readme.layers.json:40` declaring **five** `VOUCHED_CONTROLS` pairings where an AST diff shows **eight** added — *the file that declares them undercounts them*, and each addition is a deliberate weakening. **Gap-fix delivered 2026-08-23 at `f07dfe3` — all seven findings answered, ready to verify.** **F1 taken the opposite way from the verdict's proposal, and correctly**: the verdict named deleting the unreachable `.plainText` case as the smaller fix, but `plan-M19.md:172-173` specifies *four hashes or more is `.plainText`* and both `MarkdownBlock.swift` and the parser's own comment describe that — **the code had diverged from the plan, and deleting the case would have made the documents wrong to match the code.** `hashLine(_:)` now returns **three outcomes instead of an optional**, because the old function returned `nil` for two different reasons and only one of them is prose. **Three controls came with it**: a presence control asserting `#### deeper` produces exactly one fallback, **a second presence control sitting in the suite that does the counting**, and a negative one keeping `#hashtag` and `####hashtag` as paragraphs. **F2 — the red-green pass now runs each arm against only the test it names, requires that test green before the mutation and issue-recorded by name after, and refuses an arm that trapped, failed to compile, or whose filter selected nothing.** A2's mutation is `(1 ... 2)` and reddens by parsing; **A9 is new and guards the guard**; nine arms, all by assertion. **F4 five → eight pairings** — *five was the count of existing keys, a real quantity under the wrong noun*, which is `G4`'s subject stated exactly. F3 twenty-four → twenty by three independent derivations. F6's D1 citation now names the missing `tab` parameter and **prices** the closure rather than calling it impossible. F7 closed: heading nodes measured their own lead, reporting 42/36/35pt where the line boxes are 26/20/19. **Three disclosures.** It **copied one figure from the verdict before checking it** — *clean on four of eight layers* is **five** — caught on re-read, corrected in the three files it reached, and **recorded in the progress file rather than quietly fixed**. One `swift test` went red once **while output was being filtered, so the test's name is genuinely gone**; fourteen later runs passed, five repeating the exact sequence, and it stays recorded as unexplained. **And `D1` is not closed — it is priced**: the seam is one `init` parameter, but the closure needs two more state frames and a decision about censusing the sheet's chrome, **which would re-pair 20 of the 29 rows**. Final tree: suite **1778 in 219** green, readme gate exit 1 with 30 findings and the same breadth split, ledger regenerates byte-identical, all nine arms red. **It regenerated the `servers` and `settings` ledgers to confirm the shared-instrument edits stay additive** (breadth identical, 116 and 97) **and then restored them**, since all four rows that move belong to other items. |
| M20 | Menu bar, status item, and the notification banner | `M20-menubar-status-notification.md` | `spec-M20.md` | — | **Done** — verified `aa82593`, awaiting merge — To Do — triaged 2026-08-22. The app declares six command groups with no Router or Library menu and `⌘1`-`⌘7` over seven destinations; the mock wants nine, which waits on M22 and M15. The accelerator map disagrees three ways and the mock wins, recorded as an assumption. The status item has never been measured — `SURF-009` carries three `n/a` cases on a structural limit — and M23's harness is the route that reaches it. Eight assumptions, no essential question **Recovered and reported 2026-08-23 after the crash.** All 17 outstanding paths committed on `ai/m20`, tree clean, three commits from `main`: `f3cc64d` the plan, `0bdfcbe` the menu-bar work labelled in-body as an **unverified crash checkpoint**, and `bcc69dd` isolating `WORK-ORDER.md` so the dispatch artifact can be dropped before merge. **Four of the 17 were untracked and existed nowhere else** — `KeyChord.swift`, `MenuCommandAvailability.swift`, `MenuCommandRuleTests.swift` and the work order. It had *finished its turn at 08:10 owing that commit* rather than being cut, which is the DISPATCH hazard biting a third time. Not pushed, not verified — the checkpoint label is honest and the item still owes a verifier. **Verified Needs More Work 2026-08-23 at `984f00a`; gap-fix 1 dispatched. Two blocking reasons.** **The delivered tree does not compile** — a bare `nil` in statement position at `MenuCommandAvailability.swift:110` fails `MCPRouterKit`, `make test` exits **2** at its enumeration guard, and **no test in the suite has ever run against this branch**. That is the crash-checkpoint risk the brief named, realised: this branch's contents were committed on recovery by a session that never gated them. **And three of the plan's four slices produced nothing** — seven promised names each occur in exactly one file, and **that file is `plan-M20.md`**. Adding `return nil` turns everything green — 1730 tests in 216 suites, the macOS target builds, and the running menu bar holds every Slice A claim reachable: eight menus in the order `mac-shell.sh:394` expects **and had never run**, eleven dimmed items carrying nine distinct unbuilt sentences, `⌃W` the only new chord, `⌘R` still on Reset Server. Probe reverted and hash-verified. **An uncommitted 548-line verdict draft from a dead verifier session was already in the worktree**; this verifier re-derived rather than carrying figures across, and **six of its numbers were wrong** — `make lint` exits 2 not 1, `EXPECTED_ORDER` is line 394 not 395, its *1734 discovered* is one draw of a figure that returns **1742/1734/1733 on the same tree because it counts SwiftPM build chatter**, its return-line list dropped `:74`, and *nine dimmed items* is eleven. Its two blocking conclusions and its `.featureUnbuilt` arithmetic survived. **The load-bearing non-blocking finding is `DESIGN.md:589`**: it refuses `⌥⌘Q` under a rule that does not reach it — *Review Held Changes…* can fire — and *eight of their ten commands* is **nine of twelve**, repeated wrongly in three more places while the same source file gets the same arithmetic right twice. It also **armed the A3 and A4 drift guards the plan claimed were seen red and never were**; both bite under mutation, so the gap-fix owes the record rather than the guards. `bcc69dd`'s isolation of `WORK-ORDER.md` **does** permit dropping it — reverse-applies cleanly. **Gap-fix 1 delivered 2026-08-23, gated at `8aecd90` — ready to verify.** **Blocker 1 closed**: `return nil` at `MenuCommandAvailability.swift:110`, suite runs **1748 in 219, exit 0**, macOS target builds — **the first time any test has run against this branch**. **Blocker 2 closed by building rather than withdrawing**: slices B, C and D all landed with tests B1–B4, C1–C4 and the D1 amendment, and all seven names that previously existed only in `plan-M20.md` now occur in **3–6 files each** under `app/`, presence-controlled both ways. **Four calls differing from the plan, each recorded where it is read.** `isApprovable` gained **a third condition** — nothing the entry asks for still blank — because without it **Approve would have sent a credential-less declaration to the router**, `declaration(for:values:)` dropping empty values rather than refusing them; it bites on a recorded fixture where `smithery:deepwiki` asks for an `Authorization` header. `(prominent)` on Approve **was not taken**: `DESIGN.md:212` binds accent exposure to a live contrast deviation that `LightAppearanceTests.darkOnAccentDeviationIsPinned` measures every run, and a three-row band plus a footer already holding the prominent slot would have put **up to four accent fills in the smallest surface in the app** — Approve leads by position instead, declared `D2`. **`⌥⌘Q` stays refused and §8 gained a second limb rather than an exception**: both lanes (gemini via `agy`, and fable-5) independently returned Option A and named a mechanism **neither the brief nor the verdict had** — **`⌥⌘Q` is AppKit's own `Quit and Keep Windows`** — measured on the tree as `NSAlternateQuitMenuItem`, mask 2, with the mask legend established from five independently-documented chords in the same dump. **A namespace trap recorded**: the five drift guards now carry ids **outside the `M01…M59` namespace**, because the existing `"M20"` in `scripts/red-green.py` is **the twentieth mutation**, on `ServerStateTracker.swift` — *a trap for anyone grepping the item id*, and the second instance of this collision after M19 found four `M29`s in the same tables. All five guards report KILLED; F3's count corrected at four sites; F7's `discovered` now counts test ids so `discovered`, `executed` and the suite's own line all read **1748**. **Two limits declared rather than absorbed**, the second filed into `M32`. F4 and F5 left alone per the brief's addendum. `make lint` and `make build-mac` recorded as **targets not run** with all components green individually. **VERIFIED `Done` 2026-08-23 at `aa82593`, gated on `890021f` — the wave's first pass.** Both blockers closed: `return nil` at `MenuCommandAvailability.swift:110`, `make test` **1748 executed in 219**, and **the figure now agrees three ways** because closing F7 made `discovered`, `executed` and a static `@Test` count all read 1748. **The seven names counted independently — 4, 3, 4, 6, 6, 5, 6 — two readers agreeing exactly, controlled both directions** (near-miss names return 0), each with at least one `Sources/` **and** one `Tests/` file. **The deviation is stronger than it was presented**: `declaration(for:values:)` filters empty values and returns a declaration anyway, `smithery:deepwiki` really does ask for a secret `Authorization` header, and `InboxConformanceTests.swift:258` — already green — asserts the two meet, so **without the third condition Approve sends a credential-less declaration**. `⌥⌘Q` re-measured off the running bar as `NSAlternateQuitMenuItem` at mask 2. Five drift guards KILLED. **Residual sweep applied per the Done rule — six findings, none blocking, each with a destination.** **V1 fixed by the orchestrator at `0f1617a`** rather than through another cycle: `ShellModelInbox.swift:110` cited `InboxArrivalTests.nothingOutsideTheWindowInstalls`, which **Slice C moved onto `InboxBoundaryTests` without following the reference back** — three sites, one in shipped product source, presence-controlled both ways (declared once in `InboxBoundaryTests`, zero times in `InboxArrivalTests`) and read back after patching. **V5 is a fleet hazard and is recorded as one**: two identically-named `MCPRouter.app` bundles in DerivedData, distinguished only by an opaque hash segment — **a verifier that globs measures a different tree and gets output that looks exactly right**. It avoided that by proving its subject via `WorkspacePath` first. **It corrected its own citation on read-back** — it had cited `DESIGN.md:439-440` for rule 4, which is rule 3 — then checked every line citation in the verdict resolves to the text it claims. |
| M21 | The token layer, the split accent, and `DESIGN.md` | `M21-token-layer-and-design-md.md` | `spec-M21.md` | `plan-M21.md` | Merged `e12180 **Verified Done 2026-08-22 and merged at `e121801`.** `ColorToken` 40 cases over four resolved appearance contexts, `MetricToken` 21, `DESIGN.md` §1-2 re-authored to *Patchbay*, register regenerating **byte-identical** to the committed file at **89 rows — 70 matched, 19 pending, 0 uncited** against a plan expecting 64+. **The verification is stronger than the delivery in the two places the runner said it could not reach from its own worktree.** `mac-shell.sh`'s one scroll-edge failure is **not this branch's**: `main`'s build reports the identical `0.872 uniform` against the same 0.90 bar and passes 52/52. What M21 does move there is the separator — `main` **0.0742** to the branch's **0.0921** against a token going 7.5% → 9%, nominal 1.200 and measured 1.241, while `main` at the unchanged token differs from M13's reading by 1.9%, the run-to-run spread. An order of magnitude outside the noise and landing where the token says. And all 40 colour rows were reparsed from `DESIGN.md` alone with all **78** documented ratios recomputed in WCAG arithmetic: **zero disagreements beyond 0.05**. **Arm 7a's no-bit confirmed to four decimals** — `#FF5A5D` is 4.5610:1 on dark `--raised`, `#FF6E70` is 5.1209:1, so lightening raised contrast and the arm was aimed the wrong way; one precision the note missed is that the mutation would have reddened `colorsDocumentToCode` regardless, since `DESIGN.md:169` documents the original, so *no bit* is true of the aimed oracle and not of the suite. The silent-mutation-reuse defect is settled by evidence rather than assurance: 7b's and arm 8's recorded figures reproduce to **sixteen digits**, which cannot happen without the mutation having been applied. Departures all delivered-with-reason, none scope drift — the pin is **ported not replaced** and provably so, since no call site moved. Carried as `D-g4-a`: `planning/fidelity/servers.ledger.md` still reads `25 matched, 64 pending` beside the new register. **Merged-tree gates re-run by the orchestrator rather than taken on report**: `make lint` **0 violations, 0 serious in 535 files**, `0/542 require formatting`, `no-raw-design-values: clean`; `make test` **1697 tests in 211 suites passed**, exit 0 — identical to the branch figure, so the merge introduced nothing; reconciler **0 across A–L**. |
| M22 | The Harnesses and Insights boards | `M22-harnesses-and-insights-boards.md` | `spec-M22.md` | `plan-M22.md` | **Done** — verified `87268b2`, awaiting merge — To Do — triaged 2026-08-22. **Absorbs `R7-C1`**: neither board exists, no `GET /harnesses` route exists, and A36 forbids the Mac app reading a harness config itself, so the board and the route ship together or neither does. The route owes a parity row against `src/control.ts`. Three of the brief's four readings already exist in `HarnessState`; the fourth is new modelling. `PRD.md` §8.2's savings sketch is superseded by this brief, and the brief's own duty-cycle caption needs re-cutting for the same reason. Eleven assumptions, no essential question **A spec defect found by M16 and verified against source 2026-08-23, and it is M22's to correct because M22 owns both the spec and the route.** `spec-M22.md` §3.3 describes `HarnessState` as carrying `.notWired`, `.wired(route:)` and `.wiredWithDuplicates(route:count:)`. `RouterCore/Discovery/HarnessReconciliation.swift` declares **four** cases: `notWired(overlapping: Int)`, `wiredViaHTTP`, `wiredViaShim(bridge: String)` and `wiredWithDuplicates(route:count:)`. **Two errors, and the second is the dangerous one.** The spec drops `notWired`'s `overlapping` payload — the count of what adopting the harness would consolidate. And **`.wired(route:)` does not exist**: it collapses `wiredViaHTTP` and `wiredViaShim(bridge:)` into one case carrying a payload *neither* of them has. `wiredViaShim` carries a **bridge name** that `.wired(route:)` cannot express and `wiredViaHTTP` carries nothing at all, so **a route built to the spec's names could not tell an HTTP-wired harness from a shim-wired one** — which is exactly the distinction the Harnesses board exists to show. The spec then reasons *three of the brief's four readings exist in the model and one does not*, which is arithmetic on the wrong denominator: four exist. **M16's rail waits on `GET /harnesses`**, so this reaches beyond M22. Correct the spec in the same change as the route rather than after it. **Status 2026-08-23: seven substantive commits, tree clean, `swift test` 1767/221 green**, all lints clean, `parity-divergence.sh` 6 declared / 0 stale, `parity-manifest-check.sh` 94 rows, `parity-manifest-selftest.sh` 36/0. Harnesses is through M23's gate — tokens, literals, structure, geometry and type-metrics clean over 193 nodes. **It measured the sequencing rather than assuming it**: of 36 `unclassified` rows on Harnesses, 34 want a `VOUCHED_CONTROLS` entry and **0 of the 34 are covered by the merged table**; two of its seven kinds (`banner`, `section-header`) have no key on any branch and the other five sit *inside blocks m19 and m16 are changing*, so it has landed no edit and will not until it reads the merged version. **It withdrew its own earlier superset claim**, which was wrong twice — it omitted m19 and it had not checked. It also **verified and corrected `spec-M22.md` §3.3 in place**, keeping the old reading; `GET /harnesses` was built against the source so it already carries all four states, and the risk was to the next reader — M16's rail reads that route. **Remaining**: Insights fidelity artifacts, docs, UI verification, e2e. **Ready to verify 2026-08-23 at `6bd8385`, 22 commits, tree clean — both stop rules met and the session has handed over.** e2e-green: `m22-boards.sh` green twice, one backgrounded launch over both panes, `axkit front` reading Ghostty/Chrome throughout. **Driving the shipped app found one product defect and it is the result worth having from the item** — `Read now ago`, filed as **M32**, with the structural reason it survived three green instruments. Fixed at `15185eb`, guarded at both sides of the boundary, **guard armed by reintroducing the defect**. **The lane had two defects of its own**, both recorded at the wrong line rather than summarised, and both `G4`'s class in an acceptance lane. **`m22-boards.sh` enrolled in `make acceptance`** — a lane nothing dispatches passes by hand forever while reading as covered work, and `parity-stream.sh` sat executable and unrun from `R2-R` until `P3`. Gates all run rather than asserted: `swift test` **1768/221**, swiftformat, `swiftlint --strict`, three lints, `reader-accounting.py`, `null-run-gate.py` armed, reconciler A–L clean, `parity-manifest-check.sh` 94 rows, `parity-manifest-selftest.sh` 36/0, `parity-divergence.sh` 6/0, `r7-harness-reconciliation.sh` pass, `mock-fidelity-gate.sh` both surfaces. **What it names as undoable from there**: the cross-family verify, the 34 `VOUCHED_CONTROLS` pairings against the merged table, a re-measure of `harnesses` **with the role walk re-run on that surface rather than inherited**, and the `ai/m18` obligations. **The flake is unattributed** — the test name was never captured, which is weaker evidence than a named flake and the note says so. **VERIFIED `Done` 2026-08-23 at `87268b2`** — graded on the verifier's own merge of `main` into `6bd8385`, one conflict resolved to main's live record. **Both fidelity ledgers regenerate byte-identical**, so the 193/127 nodes and breadth splits reproduce **exactly rather than approximately**. `swift test` 1768/221 four for four; `make lint`, both parity lanes, the reconciler and `r7-harness-reconciliation.sh` green; `m22-boards.sh` green three times, never frontmost. **It armed three things rather than trusting them**: reintroducing the freshness defect turned exactly three expectations red with the string `Read now ago`; the lane's replaced `awk` returns **0** over the same window dump the shipped one returns **4** from; and planting `Savings: 99.8% processes` makes the forbidden-figure sweep fire. **The `VOUCHED_CONTROLS` restraint holds three ways** — zero diff, 36 unclassified splitting 34+2 as claimed, and 0 of the 34 covered by the union, because **m19's `callout` is `(callout, hstack)` where this surface needs `(callout, text)`** — the near-miss the runner predicted, and a sharper reason than *harmless* for why the intersection does not bite. **Residual sweep applied: eight findings, each with a destination**, the first filed as **`G10`** because it is a `main` defect rather than M22's. Two environment notes: the verifier symlinked `node_modules`/`dist` from the main checkout — the arrangement `.gitignore` anticipates — so `make lint` ran **whole** rather than in pieces; and **the flake did not recur in four runs, which it records as neither confirming nor refuting**, grading the disclosure rather than the flake. |
| M23 | The mock-to-SwiftUI conversion contract | `M23-mock-to-swiftui-contract.md` | — | — | **Merged** `6d54ce2` — **Verified — Done** (`effect-witness`, 2026-08-21), awaiting merge. Eight bounces, closed on live-process measurement throughout. The whole trace was rebuilt from `git archive` and reproduces exactly, and `53` turned out recoverable after all — the count of processes *mentioning* the engine — so the row that called it unrecoverable now under-claims rather than over-claims. Done rather than a ninth cycle because the one false sentence found is a rebuttal on which no figure rests, where the previous block was an acceptance failure. `D-m23-bq` is the one to carry forward: `swift test --filter` reports a zero-match filter as a pass |
| M24 | The storefront's own artwork — banners and app-style icons | — | — | — | **Done** (ai/m24 → main; design-only, 23 files, all under `design/`) |
| M25 | The controls row, not the columns, set the boards' width | `M25-board-columns-do-not-flex.md` | — | — | **Done** (ai/x4 broke the min-width chain, ai/x5 flexed the two controls rows) |
| M26 | The Checks board and the design's eval board are two surfaces | `M26-checks-board-framing.md` | — | — | **Done** (ai/m26 → main; owner kept the reachability board, mock amended, DEF-031 closed) |
| P7 | `control-auth-post-http` needs a real OAuth client | `P7-auth-post-oauth-client.md` | — | — | **Done** (ai/p7 → main; parity reached 82 of 83) |
| P8 | Make `install-launchd-watch`'s `reran` term attributable | `P8-launchd-watch-attributable.md` | — | — | **Done** (ai/p8 → main; the lane was shown able to go red) |
| P9 | `make parity-regen` cannot run, so the vectors are unverifiable against the reference | `parity-regen-is-broken-so-nothing-checks-the-committed-vectors.md` | — | — | **To Do** — filed 2026-08-26 from M30 build. `scripts/parity/generate-vectors.mjs` fails with `TypeError: opts.commit is not a function` at `buildManifest`. **VERIFIED 2026-08-27 (fresh context, grok-4.6 xhigh): NEEDS MORE WORK.** The named deliverables all hold mechanically — the generator supplies the missing `commit` option, `make parity-regen` exits 0, `make parity` passes 427/427, the selftest's five arms are real discriminators that fail closed, and a planted divergence in a committed vector is detected at exit 2 and named in the diff. **BLOCKS ON A VECTOR THAT CANNOT DETECT THE DRIFT IT NAMES.** This branch put a new `auth-pages` vector on the regen path, and the generator **does not import the reference's page template — it carries a hand-copied duplicate of the template literal inline**, while its own description string claims it is *"generated from the reference's own template literal"*. The verifier mutated a colour in the **built** reference and re-ran: `make parity-regen` exited **0**, reporting *"the committed vectors match the reference exactly"*. **That is the exact defect class this item exists to close, newly placed on the regen path.** The duplication predates the branch, in a separate generator; this commit is what promoted it. Smallest fix named out-of-family: export the template from the reference and require it, rather than the inlined copy. **SECOND FINDING, resolution defensible and the record missing:** regeneration genuinely diverged from a committed vector — `build-manifest.json` expects a row to keep its previous digest across an index failure and the TypeScript reference did not emit it — and the runner changed the **reference** rather than the vector. The verifier confirmed the resolution is sound (Swift's bookkeeping already preserves the digest, and R18 ratified that as security-relevant: a dropped digest lets a later success auto-approve a changed tool surface unheld) but the brief demanded that a regeneration which changes a committed vector *is* the finding and be recorded as one. It exists only as a commit-message bullet — no progress file, no ledger note, no R18 citation — and with no TypeScript suite in the repo, that vector is the change's only coverage. |
| P10 | A row count pinned by hand went stale when M30 added rows, and `make parity-selftest` has been red since | `a-row-count-pinned-by-hand-went-stale-when-m30-added-rows.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; found by P9's gap-fix and reproduced on clean `main` at `c860ca2`, where `make parity-selftest` exits **2**. `planning/parity/surface.tsv` pins `# rows: 95` and no longer holds 95; last changed by `b1160ef`, M30's document work, **merged during this session by this orchestrator, which did not notice because `parity-selftest` is not in the gate set it runs**. **Worse than one red gate**: `parity-manifest-check` then exits 1 on the unmutated tree, so `parity-manifest-selftest` prints *"NOT GREEN — every red below proves nothing"* and its **35 reds become uninformative**, and `make` aborts before anything enrolled after it. **Third instance of one mechanism in one day**, after M35's seven-of-nine destinations and G18's surface list — a hand-kept duplicate of a fact the artifact beside it already states. **Must NOT be closed by updating the pin**: that restores green and re-arms the trap, and the number feeds `PARITY_CUTOVER_TARGET`, an owner decision recorded at 82. P9's runner declined to move it and was right to. **EVIDENCE ADDED 2026-08-27, and it makes this a documented recurrence rather than a first occurrence.** `git log -L3,3` shows the pin **bumped five times** — 83 → 91 → 92 → 94 → 95 — once per change that touched the file. **It was caught in advance once and the catch did not survive**: R19's own row records on 2026-08-22 that *"the union with today's `main` puts 96 rows against `surface.tsv:3`'s `# rows: 94` pin … a merge-only break caught in advance for the first time"*, and it was then resolved the way every occurrence has been — by moving the pin. **The sharpest measurement is that the last bump was already wrong when written**: `ebe3165` set the pin to **95** at a commit where the file already held **97** data rows, so the hand-update was stale by two the moment it was made, and `b1160ef` — the same item, minutes later — inherited it. Nobody mistyped; the number is not derivable by the person editing it, and five changes in a row have demonstrated that. The gate's own message names the coupling: *"the cutover target is derived from this denominator and is pinned separately."* |
| R9 | The SDK drops an upstream's message on -32603; the router reads it off the wire | `R9-sdk-drops-upstream-message.md` | — | — | **Done** (ai/r9 → main; DEF-047 closed, 7 tests armed 5-of-7 red, parity 82/83 0 diverged) |
| R10 | `index` prints two counts that disagree, and neither is checked | `R10-index-reports-a-write-that-did-not-land.md` | — | — | **Done** — merged to `main` at `8241e0f` (`ai/r10` `f810870`; `effect-witness`; 3 follow-ups) |
| R14 | A client's Authenticate action succeeds, and says which upstreams still need authorising | `R14-client-auth-succeeds-and-reports-upstreams.md` | — | — | **Merged** `2481e05` — **Verified — Done** (`raster-visual`, 2026-08-21), awaiting merge. The whole flow driven independently against both routers: registration 201 and idempotent, `/authorize` 200, `/token` 200, replay refused, and `401` appears nowhere. The report's discriminator proved on the verifier's own fixtures — `auth.authorized` fails in **both** directions and the page ignores it. The bypass class is clean across six confusions on both routers. Merge is held by `parity-gate.sh` redding on an unrelated row R14 cannot have caused (`D-r14-f`, 5 green 4 red over nine runs). Follow-ups `D-r14-a`…`D-r14-g` |
| R15 | The Host check guards `/mcp` and nothing else | `R15-host-check-is-scoped-to-the-mcp-transport.md` | — | — | **Merged** `2481e05` — **Verified — Done** (`effect-witness`, 2026-08-21), awaiting merge with R14. Holds across all eleven routes on both routers; `/mcp`'s 403 stays byte-identical at 97 bytes. The live foreign-Host exposure on `/health`, `/status`, `/servers` and `/usage` is closed |
| R16 | Adoption reads global scope only, so a project-scoped server is invisible to it | `R16-adoption-cannot-see-project-scoped-servers.md` | — | — | **Done** — merged `2b0be30` (`ai/r16` → `main`); WatchStaging extracts project-scoped servers attaching projects field; ToolUnion filters by CWD; 1939 tests green. Both halves confirmed at HEAD: `cmdImport` reads `src.mcpServers` at `src/index.ts:85` and never `projects`, while `projects?: string[]` is modelled at `src/config.ts:21` and `:126`, carried through resolution at `:164`/`:189` and **editable through the control API PATCH** at `src/control.ts:450` — so an owner can hand-scope an upstream today and the router honours it; only learning it from the harness is missing. **One thing the brief did not know, and it shrinks the item: the report criterion 3 asks for is already built** — `skipped` is collected at `src/index.ts:93` and printed as `not adoptable:` at `:153-155` with a per-server reason, and today only ever holds servers `parseServer` refused. So the work is enumeration plus the collision decision, not enumeration plus a new report. **Shape 1 is not deferred by preference**: it decides what scope this product works in, and `D-r7-i` is its mirror image (`HarnessesVerb` prints *Global scope only* while reading a project-scoped file), so building it now would settle that from the side with less evidence. Filed as **R16-C1**. Shape 3 stays refused — it changes the tool namespace the model sees. **Owner decision 2026-08-22 — per-project scope.** `R16-C1` is answered: the router adopts and serves per-project MCP configuration rather than flattening to one global set per harness, so a server may exist as several profiles under one name (the `proctor` case that raised this). **Shape 1 is unblocked and is now the target**; shape 2 stays the first slice because it stops the false claim while shape 1 is built. `D-r7-i` is this decision's mirror and inherits it — R7's already-shipped behaviour is the global-only half and must be re-read against this, not left standing. **Known cost the owner took knowingly**: the tool namespace a model sees now depends on the directory the session started in, so a prompt naming a tool can behave differently by project. That is a documentation and diagnostics obligation on whoever plans this, not a reason to narrow the scope again. |
| R16-C1 | Adopt project-scoped upstreams and serve them by caller cwd | `R16-adoption-cannot-see-project-scoped-servers.md` | — | — | Deferred (child of R16) — registered 2026-08-22 at R16's triage. Blocked on the same scope decision as `D-r7-i` and `R7-C4`, which are its mirror image. `CallerIdentity` already carries cwd and `usage` already reports per-project call counts, so it is a decision problem rather than a plumbing one. |
| G5-C1 | Repo-owned gate wrappers for the vendored campaign scripts | `G5-vendor-the-campaign-version-the-gates-run.md` | — | — | Deferred (child of G5) — registered 2026-08-22. Nothing in this repo invokes a `test-campaign` script: every hit across `Makefile`, `scripts/` and `planning/test-campaign/bin/` is a comment naming one, and no file resolves a skill directory. The skill loads its own `scripts/` from the plugin cache, so vendoring a copy does not change what runs. Wrappers `make` could call are new surface rather than a relocation, so G5 carries the code and proves it runs by hand, and this carries the wiring. |
| R17 | A failed index leaves a recorded error for one server and nothing at all for another | `R17-a-failed-index-can-leave-no-trace.md` | — | — | Merged `54666f **Merged-tree gates re-run by the orchestrator rather than taken on report**: `make lint` **0 violations, 0 serious in 536 files**, `0/543 require formatting`; `make test` **1699 tests in 212 suites passed**, exit 0; reconciler **0 across A–L**. The merge introduced nothing. |
| R18 | A failed index drops the digest, so the next success serves a changed surface unheld | `R18-a-failed-index-drops-the-digest-that-holds-the-diff.md` | — | — | **Done** — merged `c6dd377` (`ai/r18` → `main`); ManifestBookkeeping preserves previous digest on failure; ManifestDigestHoldTests verified; 1940 tests green., plan alongside `R20` (same manifest row, opposite direction). **Confirmed by reading, and the drop is incidental rather than deliberate**: `buildManifest`'s `catch` at `src/manifest.ts:258-265` constructs a whole new entry while `prev` is in scope, and the held-diff branch four lines above spreads it — so `digest` and `tools` are both dropped, and at `:236` `!prev?.digest` takes the same branch as a matching digest. The evidence is this file's own convention rather than silence in general: every deliberate retention here carries a defending comment, the first-sight branch has a six-line one, and this branch has none. So the brief's first fix is right — **keep the last good digest**, and an absent digest keeps meaning *never seen*, which is what `:236` already assumes. **The fork that remains is the plan stage's first decision**, because `unionTools` at `src/manifest.ts:326` skips on `!entry ‖ entry.tools.length === 0` and **never reads `entry.error`** — the zero-length list is load-bearing and doing the job the error field looks like it should do. Digest-only leaves a seen-then-failed server held against an empty approved set; digest-and-tools with serving gated on `error` is correct for the right reason and reaches `router.ts:168` and `:347`. Criterion 2 is what separates them, so arm it against a never-seen server **and** a seen-then-failed one. **Fork settled 2026-08-22 by the Google lane (`agy`/`gemini-3.7-flash-high`), not by the owner** — it was technical and evaluable with the evidence in the brief. **Verdict: shape B** — preserve `digest` **and** `tools`, and gate serving on `error` rather than on `tools.length === 0`. Reason given: a diff-and-approval system needs an accurate baseline, and shape A reduces `prev.tools` to `[]`, turning a precise incremental diff into a full re-approval of the whole surface. What shape A is better at, stated rather than dismissed: **blast radius and a failsafe default** — it never touches the serving path, and an empty array physically cannot serve a stale definition however the downstream logic breaks. **A third shape the lane rated above both, and the plan stage should cost it first**: split the row into a persistent `baseline {digest, tools, approvedAt}` never wiped on an index failure, and a transient `runtime {status, error, lastAttemptAt}`, with `unionTools` gating on `runtime.status === 'healthy'`. That removes the cross-talk with `R20`, which writes the same row in the opposite direction. **The failure mode B introduces, named**: a *permanent silent blackout* — if a later success writes `digest` and `tools` but never clears `error`, the server is healthy, valid and permanently unservable. The lane was explicit that this argues for the status enum rather than back to A, because using `tools: []` as a proxy for an error boolean is the anti-pattern that caused this defect. |
| R19 | A watch fire saves a manifest snapshot taken before a concurrent write | `R19-a-stale-manifest-snapshot-clobbers-a-concurrent-write.md` | — | — | **Needs More Work** — verified `a40351d`, gap-fix 3 owed — Done — merge held as R19-I **Verified Done 2026-08-22.** Oracle rung **behavioural, cross-process, on the real binaries** — the verifier re-armed and reproduced the defect itself and **timed the overlap rather than assuming it**. Gates on the branch: lint **0 over 533**, `make test` **1687 in 210**, `parity-cli.sh` **17/0**. Three gate numbers that moved are each shown outside this item's diff. **The reconciler does not reproduce the check-E finding** — exit 0, clean across A–L, 26 merged branches — because `ai/g5`'s tip is no longer an ancestor of `main`; better than predicted and for a reason that is not this item's. **Merge ordering answered, and the orchestrator had already merged R17 first**: the verifier's advice was R19-first precisely because `surface.tsv` **auto-merges**, so R17's stale `cli-watch` note survives silently. Taken up as **R19-INT** rather than resolved at the merge, because the two code conflicts are **records rather than logic** and R19's base predates R17, so its side never chose to drop R17's route account. **Unsettled and declared, none blocking**: `buildManifest` still decides staleness from the outer snapshot, so a non-forced run can skip a server another process invalidated mid-run; the watcher's in-lock `delete` of a failed row can still remove a good row another writer just wrote for that name, identically on both routers and predating this item; and node now has a lock module while node-side `servers.json` (`watch.ts:311`, `control.ts:105`) still takes nothing where Swift locks it — a real asymmetry this item's scope correctly excludes and that is now cheap to close. **Verified Needs More Work 2026-08-23 at `ce980ef` (parent `eb3e42c`); gap-fix 1 dispatched — records only, no code.** **The merge is clean**, so the *a conflict is a finding* branch never fired; the verifier merged, measured and discarded, and the tree reproduces from `eb3e42c + ed37a30`. Gates re-measured on that tree: lint 0/552, `make test` **1728/216**, `parity-cli` 18 verbs 0 disagreed with all four `cli-watch` green, `parity-overlap` 2/2, reconciler clean A–L, `claim-sweep` exit 1 on 11 blocking hits **all inherited from `main` in four byte-identical files**. **What moved the verdict is arm 1 splitting**: restoring the node-side delete reddens `parity-cli` with `surface.tsv`'s recorded string verbatim and leaves `IndexFailureRecordTests` **green**, because that suite drives the Swift watcher and never executes `src/watch.ts`; arming the Swift side separately fails there with 11 issues. So the resolution is right and **the justification is false** — `planning/progress/R19-integration.md`'s *would have failed both* claims one instrument pair where **each side is pinned by one instrument**. The corrected claim is *stronger*, which is why the gap-fix asks for a rewrite rather than a hedge. Three smaller findings: `surface.tsv`'s `cli-watch` row cites `WatchIndexing.swift:206` where the save is at **`:209`** (and the file is under `RouterCore/`, not `MCPRouterKit/`); the gate-race account omits that `parity-oauth.sh` **runs twice per gate** with fail winning; and arm 5 points at `B1`/`B2` wording that `claim-sweep` excludes from its exit code. **Arm 4 came out against the record**: three full gates read red, green, green against the four-of-four on file, and the only red was at load 400 with greens at 175 and 23 — **gate and machine are confounded in both datasets**, reported rather than chased, and this cell has now stated a reproduction rate the next measurement contradicts **four times**. **Merge deferred by orchestrator decision**: R16/R18/R21 wait on this, and merging a record that asserts an instrument relationship which measurably does not hold — to save a two-sentence fix — would undo the day's work on exactly that defect class. **Gap-fix 1 delivered 2026-08-23 at `8294df4` — documents only, ready to verify.** The blocking paragraph now says each side is pinned by exactly one instrument and names which: `parity-cli.sh` pins node and **stays green under a both-side regression, being differential**; `IndexFailureRecordTests` pins Swift and **is absolute, so it catches the case the differential lane cannot**. The runner verified that statically rather than on report, and quotes the withdrawn sentence with how it was disproved. All three non-blocking items in: `surface.tsv` now cites `app/Sources/RouterCore/Watch/WatchIndexing.swift:209` **with the call text attached**, and it re-read all six sites, giving two more their paths — `ManifestIndexer.swift` is under `Service/` and `AuthRoutes.swift` under `Auth/`, **not the `Indexing/`/`HTTP/` a reader would guess**. DEF-033's double-run is named with fail-wins precedence. The claim-sweep section proves inheritance **the drift-proof way** — `git diff --name-only adfa923..eb3e42c` lists none of the four files. Arm 4 recorded as confounded with **no fifth rate**. **Three corrections to the verdict, all recorded**: its *four of the eleven hits live in the features-to-triage copies* is **seven**, counted off the script's per-class output; `ledger-reconcile.py` exits **1** on this branch (check E, `ai/g4b`) because `eb3e42c` predates main's G4-B row — branch staleness that clears on merge, and the verdict's exit 0 was on the merged tree; and `make lint` **ran** here rather than failing at `tools`, because this worktree already carries `node_modules` and `dist/index.js` — so the standing dispatch note is right that it *can* fail and wrong that it always will. `main` moved twice during the work and the blocking-pattern count was re-run against both, 11 each time. **Gap-fix 1 verified Needs More Work 2026-08-23 at `f3a14b6`; gap-fix 2 owed — records again.** **The gap-fix kept its records-only promise**, verified as git tree objects rather than file listings with a presence control on both instruments. **It fails on the very claim it was dispatched to fix.** The verifier completed the 2×2 whose bottom-left cell had never been run: the **Swift `removeEntry` arm reddens `parity-cli` too**, with the exact mirror of the node arm's diff on the same scenario row — because a differential harness sees a one-sided regression whichever side it is on. So **the Swift side is pinned by two instruments**, and the replacement sentence *`parity-cli.sh` is not what catches that one* is false, **four lines above a sentence that predicts exactly this** — `:32` to `:36` on `8294df4`, measured wrap-tolerantly because the withdrawn claim itself spans `:31-32`. **This row previously said thirteen, and that was the orchestrator's error**: the verdict states *both* — four in its opening paragraph and thirteen in F1 — and the brief carried thirteen forward into this row and into commit `01b6ef9`. The gap-fix runner caught it and correctly filed it as the orchestrator's, since this file was being written concurrently. **The orchestrator's first attempt to check it also failed**, on a line-anchored grep that could not span the claim's own hard wrap — the third time that instrument has misfired here in one day. The error **under-claims** coverage, so nothing is less guarded than the record says; it is red because the item's whole scope was that paragraph, it was dispatched to fix this defect class, and the row recorded the claim as verified **statically**. Replacement text and the matrix are in the verdict. **All three self-corrections hold** — the sweep count is 7 with the normaliser named, all eleven sites listed, and a planted control firing +5 including an instance split across a hard wrap. **Two things it reported rather than smoothed**: the worktree was handed over dirty with a previous verifier's Swift arm live and uncommitted, pasted into the verdict before gating; and **the only out-of-family lane that answered says Done with an erratum** while the in-family fable lane agrees with the red — stated that way round *because dressing that up would be the same failure I'm filing*. It declined `parity-gate.sh` because that is the instrument that would produce a fifth rate for the oauth cell, at the stated cost of having no whole-parity figure of its own. **Gap-fix 2 delivered 2026-08-23 at `2793574` — one file, 239 insertions / 19 deletions, no code, ready to verify.** The paragraph now says each side is pinned by **at least** one instrument and that `parity-cli.sh` catches both, with the completed 2×2 at `:46-51` and both mutation strings quoted in full — `0a1,2 >` for the node arm, `1,2d0 <` for the Swift one, **same scenario, same two servers, opposite sides**. The withdrawn text is cited by anchor, tree and line with its blob `af1dcb98`, **re-derived rather than taken from the verdict**, and gap-fix 1's section keeps its account as history with the false half marked withdrawn. **Two things it measured instead of transcribing.** The four-versus-thirteen correction above. And it **narrowed the verdict's ready-to-paste claim** — *`parity-cli.sh` … is the only instrument that does* becomes *of the two instruments*, because arm A was run against those two and **never against `make test`'s 1728**, and *inheriting an unmeasured exclusivity from a verdict would be the same move that produced this paragraph twice*. **Why it got through twice, stated plainly**: the ledger recorded gap-fix 1's claim as verified **statically**, and a static read cannot catch a claim reasoned from the very differential property that refutes it. **Gates on the merged tree** (`main` at `01b6ef9`): lint **0/552** with main's two new gates armed and green, `make test` **1728/216**, `parity-cli` **18/0** all four `cli-watch` green, `parity-overlap` **2/2**, manifest-check **94 rows**, **reconciler exit 0 clean A–L** — the `E — G4-B` finding cleared by the merge as the brief predicted — and `claim-sweep` exit 1 on the same inherited 11, byte-identical but for its own `A5` moving `:82` → `:110`. **Every gate ran twice**: the first pass piped its output and lost the exit codes, *because zsh does not populate `PIPESTATUS`* — this repo's own recorded hazard, caught by the runner on itself. **Declined and stated**: `parity-gate.sh` not run, inheriting the verifier's trade and its cost, and no fifth rate for the oauth cell. `src/`, `app/` and `scripts/` byte-identical to `eb3e42c` as tree objects; only `Makefile` moves, twelve lines from main's `50f3e8f`. **Gap-fix 2 verified Needs More Work 2026-08-23 at `a40351d`, on one finding — the third pass to fail on the same paragraph, and the pattern is now the finding.** **What held**: records-only at tree-hash exactness with **three presence controls** on the instrument (synthetic plant into a fresh index, the same command form against `main` which differs, and a live plant reverted); the 2×2 reproduces cell for cell; four-lines-not-thirteen re-derives correctly and `main`'s `1f5ad55` has accepted it. **The narrowing was necessary rather than merely cautious**: arm A run against two instruments the record never names shows `make test` stays at 1728 — it is `swift test` in `app/` and reaches no node code — **but `parity-overlap.sh` reddens** (`reference:[rows=lifeline, wanted lifeline,slowfail]`). **So pasting the previous verdict's ready-made *the only instrument that does* would have written a third false exclusivity.** **What blocks**: running both arms at once confirms the record's remaining reasoned claim and falsifies one *this pass introduced* — `:39` says what `IndexFailureRecordTests` **uniquely** holds is the both-sides-at-once case, and under A+B `parity-overlap.sh` reddens on both sides with no `disagree:` clause. **That word is new in this pass, four lines below the sentence the pass narrowed for exactly this reason**, and R19's own lane says so in its source comment at `:187-188`. **So three passes have each removed one exclusivity claim and written another** — which points at the paragraph rather than at any sentence in it, and a fourth pass should be briefed on that rather than on the new sentence alone. **The severity call went out of session and split**: `agy`/gemini said Done with a merge-time erratum, in-family `claude-fable-5` said Needs More Work, `codex` and `grok` both down — **one out-of-family read rather than three**. What tipped it: *for a records-only item the paragraph **is** the deliverable, so Done-with-an-erratum still costs the edit and only drops the check.* Gap-fix 3 needs **no measurement campaign** — the replacement text is in F1 with every number attached. Four further findings each carry a destination, including a bullet about `LEDGER.md` that **`main` has since falsified** and reconciler counts drifting 103→106. **Gap-fix 3 delivered 2026-08-23 at `dcbdc9d` — records only, ready to verify, and it changed the register rather than the sentence.** The paragraph now **states what each instrument *does* catch, arm by arm, and makes no claim about what only it catches**: `parity-cli.sh` differential and green at 18/0 on the both-sides arm; `IndexFailureRecordTests` absolute on Swift, blind to node, red at 11 issues on two arms; `parity-overlap.sh` red on all three armed rows; `make test` red on none **and structurally unable to be**. **Four instruments named where the graded text named two**, and the 2×2 is now a 3-instrument × 5-arm matrix. **It declined the verdict's ready-made option (b) text** — *every number in it is used; its framing is not* — because **`is IndexFailureRecordTests'` and `the strongest guard here` rank over the same unenumerated set that `uniquely` did.** **One correction back to the verdict, in its favour**: `parity-overlap.sh` is **not** *absolute, not differential* — it carries three assertions per row, two absolute at `:192-195` and one differential at `:196`, **and only that reading explains the `disagree:` clause in the verdict's own arm-A output.** F3's `thirteen` count re-measured **under a named normaliser** — 2 occurrences, neither the withdrawn figure — where **the case-sensitive line count returns 1 and misses a capitalised one**, the same failure that undercounted G5's sites. **F5 landed rather than threatened**: `git log --all` shows a dispatch artifact committed into the tree it was about **three times**, one of them predating this fleet — folded into `G6`. **Gates each run twice and agreeing**: lint 0/552, test 1728/216, parity-cli 18/0 with all four `cli-watch` green, parity-overlap 2/0, manifest-check 94 rows, reconciler A–L, claim-sweep exit 1 on the same 11 inherited hits in four files this branch does not write. **The exclusivity sweep returns 2 hits before and after** — `uniquely` gone, the survivor a sentence *denying* the property — **with a plant taking it to 3**. **Two things for the verifier**: `parity-manifest-check.sh` went red **once in 12 runs** on an unchanged tree with the cause unidentified after 200 reproduction attempts, **printing *stale manifest*, which is actionable and wrong**; and `main` moved to `26c3959` mid-gating, touching no code, so every figure stays stamped to `9d4da76`. **Gap-fix 3 verified Needs More Work 2026-08-23 at `eaa2695` — the fourth failure, and it is the bullet the register change added.** **F1**: the paragraph says *`make test` catches none of these arms and structurally cannot*; `make test` is an unfiltered `swift test` in `app/`, `RouterCoreTests` is a test target and `IndexFailureRecordTests` lives in it, so **`make test` is a strict superset of the instrument the same paragraph says goes red at 11 issues** — measured **14** on arms B and A+B against unarmed 1789/223 exit 0. The stated reason *executes no node code* rules out the **node** arm only and was generalised to three. **The three issues above eleven are `WatchAdoptionTests`, named nowhere in either R19 record — the fourth previously-unnamed instrument in four passes, sitting inside the instrument the paragraph declares blind.** The verifier's summary is the finding: *the register changed and the failure mode survived it — exclusivity claims went, a universal negative arrived.* **F2**: the arm-A instruction **does not reproduce** — `delete next.servers[name]` compiles only in the `failed` loop and R19 removed the run-level save, so armed alone it reaches neither disk nor the adoption gate and yields **a silent green** (parity-cli 18/0, parity-overlap 2/0); the presence control is the same delete inside the locked span at `src/watch.ts:263-266`, which reddens both. **What stands**: all four positive bullets confirmed by arming, the A+B row and the whole `parity-overlap.sh` column reproduced **on a tree whose `app/` and `scripts/` moved 51 commits under them**, and the three-assertion correction upheld and load-bearing (`:196` is the only line that can emit `disagree:`). **The refusal is upheld on a narrower ground** — the superlative ranks, the possessive does not, *Of the two* bounds it — but pasting would have imported the *absolute-not-differential* description the same pass corrected. **F3 is not R19's and blocks the merge anyway**: the union with today's `main` puts 96 rows against `surface.tsv:3`'s `# rows: 94` pin, so `parity-manifest-check.sh` exits 1 on the merged tree while both parents are green — **a merge-only break caught in advance for the first time**. |
| R19-INT | The R19/R17 join: three records to merge and a declaration to delete | `R19-integration.md` | — | — | **Done** — merged `920ff96` — raised 2026-08-22 after the orchestrator aborted R19's merge deliberately. Two code-adjacent conflicts (`src/watch.ts`, `WatchIndexing.swift`) that are **records rather than logic**, plus `surface.tsv` **auto-merging** a declaration R19 makes false in three claims. R19's base predates R17's merge, so its side never chose to drop R17's route account — including the pre-registered prediction that an R19-only world predicts the inverse of what was measured, R17's strongest single piece of evidence. `parity-cli.sh` has never been run with R17's fourth scenario against R19's watcher. |
| R21 | `approve` answers 200 while discarding the write that would make it true | `R21-approve-answers-200-over-a-discarded-write.md` | — | — | **Done** — merged `5a51776` (`ai/r21` → `main`); handleApprove answers 500 when ManifestIO.save fails; ControlApproveDispatchTests verified. while re-measuring M28's DEF-049, which had two halves fixed and a third predicted. **The prediction is refuted and the plainer defect survives it.** `AuthRoutes.swift:120` is the last of DEF-049's three `try? ManifestIO.save` sites; the route sets `tools`/`digest`/`builtAt`, removes `pending`, discards the write and returns `(200, approved: N)` unchanged — so a refused write leaves the surface held while the caller is told it was approved, and `approved` is counted from the pending entry **before** the write. DEF-049's louder prediction (a stale `builtAt` fires `Describe.swift:218` and `/servers` reports `authorized: true`) **does not follow**: approve is reachable only with `pending` present, and both implementations clear `error` when they stage one — `src/manifest.ts:246` and `ManifestBookkeeping.swift:83` — so the `guard case let .string(text)?` at `Describe.swift:208` fails first and `:218` is never reached. The fix shape is already in this repo at `ServicePorts.swift:388-394`: catch, carry a `cacheFailure` into the response. **Do not move the status code** — `ControlApproveDispatchTests.swift:114-118` pins 200 deliberately. |
| R22 | Single-action router daemon diagnostics bundle export | `daemon-diagnostics-bundle-export.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. |
| R23 | Router gateway log retention and diagnostic rotation settings | `gateway-log-rotation-size-policy.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. |
| R24 | Local-network device discovery and mutual pairing handshake | `local-network-device-pairing-handshake.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. **Touches the boundary `DESIGN.md` states as a principle** — *the phone queues; it never installs* — and `D-i4-a` records that any change there must amend that line in the open first. Triage should read that before planning. |
| R25 | Server configuration profile export with safe secret redaction | `server-profile-export-redaction.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. Overlaps W1's secrets inventory. |
| R26 | Proactive desktop notification for upstream authentication expiry | `upstream-auth-expiry-desktop-notification.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. |
| R27 | Upstream server health check and timeout configuration | `upstream-server-health-probe-settings.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. |
| R20 | A staged entry wipes a same-named healthy upstream and blames it for the failure | `R20-a-staged-entry-misattributes-its-error-to-a-healthy-upstream.md` | — | — | **Done** — merged `b637adf` (`ai/r20` → `main`); StagedErrorAttributionTests verifying healthy server preservation., measured by its verifier: a healthy `db` serving 2 tools, then a broken `db` staged, gives `/servers` an `indexError` of `spawn /nonexistent/not-a-server ENOENT` for a server whose configured command is `node`, where pre-fix it read `error: None`. The tool loss is not a regression — the old delete wiped the surface too — but the misattribution is. R7's duplicate class is the precondition, so decide it there rather than patching where the error string is filed |
| X1 | The iOS accessibility-tree harness, and two surfaces still empty | `X1-ios-a11y-harness.md` | — | — | Done (closed by X3's engine fix + the accessibility-frame row oracle; `make test-ios` 36/0) |
| X2 | The iOS on-glass instrument, and the six cases it takes off `n/a` | `X2-ios-on-glass.md` | — | — | **Done** (ai/x2 → main; lane-owned device, six green runs) |
| X3 | The iOS unit lane read an empty accessibility tree because the engine was off | `X3-ios-unit-lane-empty-tree.md` | — | — | Done (DEF-029 closed, armed three ways) |
| X6 | Cleanup's `Read first…`, the half DEF-011 was held open for | — | — | — | **Done** (ai/x6 → main; CASE-0135/0136/0137, nine mutation arms) |
| M27 | The sidebar foot's loopback readout and the child-process label | `M27-sidebar-foot-readout.md` | — | — | **Done** — merged to `main` at `cbe5cc3` (`ai/m27` `26337b8`; rung `raster-visual`; 6 follow-ups `D-m27-a`…`D-m27-f`, none blocking) — both restored, on glass 8 of 8 boards. The count card announces as ONE element — `Child processes, 1 of 4 declared servers running` — after a reversal: the branch first shipped it as two stops because A35's readout line was anchored whole, and three model families independently found that anchor was written before the label existed. A35 widened to its own stated prefix tolerance. The mock's `--live` foot dot deliberately not, and the same argument convicted the count numeral, which was painting `--live` on `0 of 4`. `mac-shell.sh` exit 0, 1552 tests, evidence in `planning/evidence/M27-acceptance.md` and `planning/evidence/M27/` |
| M28 | Five findings that need a decision rather than a runner | `M28-decision-docket.md` | — | — | Done — all five dispositioned 2026-08- **Closed 2026-08-22.** DEF-057 answered by the owner — vendor `test-campaign` only — and filed as **G5**. Four of five never reached the owner: two had been answered by work that landed after the docket was written, one was the orchestrator's to take, and DEF-033 needs nothing. The two that became work are **R21** and **G5**. |
| M29 | "Disable a server" is drawn with nothing behind it | `M29-disable-a-server-is-drawn-with-nothing-behind-it.md` | — | — | Untriaged — filed 2026-08-22 by M18's runner while inventorying every sheet in the design of record. `ServerPatch` carries **no field that disables a server**, the control API's PATCH is the Mac app's only channel, and **no ledger row claims the behaviour** — not R7's, not R16's, not M22's. M18 declined to invent it rather than shipping a control that does nothing. **This is a decision before it is a build**: disabling is a third state between adopted and removed and the product has two, so it has to answer whether a disabled server keeps its manifest row and digest (which `R18` and `R20` both write), whether `unionTools` skips it by a new flag or by the same emptiness test `R18`'s verdict has just established is doing the error field's job, and whether it is per-harness or global — **the owner's per-project decision makes 'disabled here, live there' expressible and probably intended**. Do not close it by deleting the row from the gate table: that table is generated from the design of record, and deleting a row to make an inventory agree with an implementation inverts the check. — **Triaged 2026-08-25 → To Do.** (Review lane corrected from Needs More Info). Grounded: `ServerPatch` lacks a disable field and `unionTools` skips only on `tools.length === 0`. The implementation decisions (manifest row retention, explicit `disabled` boolean flag, and per-project scope alignment) are technical architecture choices that belong to the planner rather than owner questions. Ready for implementation plan. — **Developer Review 2026-08-25 (`ai/m29`, 9 commits, tree clean).** Built by a fleet runner; the first attempt died mid-implementation and the second resumed its worktree rather than restarting. **Bundle checked by the orchestrator before queueing, not taken on the runner's word:** `swift build --build-tests` exit **0**, 0 errors — the SourceKit diagnostics reporting *No such module 'Testing'* and a missing `server:` label were IDE noise from reading the files without package context, and do not reproduce in a build. `make test` exit 2, **1960 tests in 248 suites, 2 failures**. **Both failures are inherited and their fix already exists uncommitted on `main`:** they are *every bookkeeping branch matches the reference, byte for byte* and *every registered vector file is loaded and every case in it is compared* — the two R18 parity tests repaired in `build-manifest.json` earlier the same day and never committed, so a branch taken from `bbdc14b` could not carry the repair. The runner diagnosed this correctly and unaided (*the vector still encodes pre-R18 behaviour*). **Runner-reported, not yet re-measured here:** `make lint` 0, `parity-control.sh` 56/56, red-green DIS-1…7 **7/7 killed**, `make parity` 367 vectors at floor 367. **Blocked and inherited:** `make mock-fidelity SURFACE=servers` exit 3 — `MeasureDump/main.swift:206` is a non-exhaustive switch missing `.readme`, byte-identical to `main`, so M29's redrawn row is **unmeasured against the mock**; and `parity-manifest-check.sh` exit 1 on a census pin 94 vs 96, also inherited. **Decisions honoured from the branch's own out-of-family review:** (a) manifest row, digest and approved tools all survive and `disabled` stays out of `upstreamHash`; (b) an explicit `isServed` predicate sits above the emptiness test, which stays byte-identical including its ported defect; (c) a global boolean with per-project off-ness left to `projects`, deny-list deferred as DEF-M29-a. **Two judgement calls a verifier must scrutinise, both self-reported:** `TrackerStateMatrixTests`' fixture guard demanded an empty diff against `main` — an invariant only a frozen wire satisfies, which M29 widens by design — and was re-aimed at *recordings move as a set* rather than deleted; and red-green surfaced **two plan errors** recorded rather than quietly fixed (DIS-3 named a test that compares recorded hashes and cannot see a Swift mutation; DIS-6's *one mutation, two suites* is false). Lane note: `agy` answered on-subject; **grok timed out at 400s (exit 142), logged downgrade**. **Not verified and not merged** — verify runs in fresh context and is the only route to Done. — **Verified NEEDS MORE WORK 2026-08-26 at `a526031`** (fresh context, audit-only, nothing merged). **12 of 18 oracle lines Done · 2 Partial · 1 MISSED · 3 Unverified.** **The blocking defect is new and was not self-reported:** `MenuBarPresentation.AttentionCause.causes(for:)` reads `pendingChange`/`auth`/`indexError` and **never consults `disabled`**, while `attentionRows()` maps the unfiltered list — so a disabled server holding a schema change **shows a menu-bar band row while the count reports zero**, and that row opens a sheet whose `Disable` button is dimmed. A dead end, and oracle 15's own words failing. Confirmed by executed probe, not argued: `needsAttention=false` while `attentionRows` returns `["sift|heldChange"]`. **Two more oracles proved to have no oracle by deletion probes:** deleting the whole in-flight mechanism leaves all 1960 tests passing (line 16), and deleting both Swift sweep guards does too (line 5). Line 17 carries an assertion comparing a payload-free case to itself, which cannot fail. Line 12 is **unmeasurable as built** — the sheet's copy is private and the lane that would render it is blocked. **Gates the verifier re-ran itself rather than reading:** `make test` 0 (1960/248, zero failures after the rebase onto `0e5ff49`), `make lint` 0, `make parity` 0 (367 at floor), `parity-control.sh` 0 (56/56), **red-green DIS-1…7 7/7 KILLED on its own re-run**, `npm run build` 0; `mock-fidelity` 3 and `parity-manifest-check.sh` 1, **both confirmed inherited by checking `main`** (`MeasureDump/main.swift` byte-identical, sha256 `b243a9ed…6dfc` both sides). Out-of-family lane `agy gemini-3.7-flash-high` answered **on-subject** and **agreed on every row**, independently calling the band defect internally contradictory — **not degraded**. Four probes mutated the worktree and all were reverted and hash-verified; `git status --porcelain` empty at the end. **Gap-fix work order, five items:** guard `causes(for:)` with `!disabled` and assert band suppression; assert `writesInFlight` is *set* during a write, not only cleared after; cover the sweep skip in Swift and add a reindex-on-disabled case; make the sheet's label and dimmed reason reachable to a test and assert the row's `accessibilityValue`; replace the tautological assertion. — **Gap-fix delivered 2026-08-26 at `1992efe` (15 commits on `ai/m29`), READY-TO-VERIFY again.** All five work-order items closed. The blocking band defect is fixed with `guard !server.disabled else { return [] }` — the same term `needsAttention` already carried — asserted three ways including a **16-arm cross product** of `disabled` × held × unauthorised × index-failed asserting `rows.isEmpty == !dot`, and proved by removing the guard and watching all three redden (`MenuBarPresentation.swift` sha256 `e2f1ed57…1668` before and after). An audit of every other reader of the same fields found **no second instance**. `writesInFlight` now has an oracle that observes the mark **set** from inside `patch` rather than only cleared after; the sweep guards and a reindex-on-disabled case are covered; oracles 12 and 18 became reachable statics with a **source-scan binding guard** so a static returning the right string cannot pass while `body` stops calling it; the tautology is replaced. **Gates:** `make test` **0** at **1977 in 251** (up from 1960/248) · `make lint` 0 · `make parity` 0 · `parity-control.sh` 0 (56/56) · `npm run build` 0 · **red-green DIS-1…16, 16 of 16 KILLED**. Nine proving mutations, all reverted and hash-verified. **Two things it reported rather than smoothed:** the out-of-family reviewer caught it **reintroducing the same tautology one line below** the one it had just replaced, and one arm was cut short by its own timeout leaving a file mutated — restored from git and re-run, recorded rather than quietly fixed. **Out of scope, noted:** `ShellModelBadges.menuContext` leaves *Reset Server* live for a disabled tripped server. — **Re-verified NEEDS MORE WORK 2026-08-26 at `1992efe`** (fresh context, audit-only). **17 of 18 oracle lines Done · 1 Partial · 0 Missed · 0 Unverified**, up from 12/2/1/3. **Four of five work-order items confirmed closed by the verifier's own probes rather than the gap-fixer's word:** the band guard exists at `MenuBarPresentation.swift:84` and removing it reddens **three** named suites; the `writesInFlight` mid-write hook is real and its assertion fails if the hook never fires; the sweep guards are outcome-rung, the watcher test asserting a **real child process never created its marker file** with a matched positive control; and the tautology scan came back clean — **179 assertions across 9 M29 suites, zero cannot-fail shapes**, with the only two `#expect(state == state)` sitting in `DiscoverStateTests` **byte-identical on `main`**, so inherited rather than M29's. **The one blocker is oracle 18, and it is the same defect class one surface over:** the row's spoken value became `accessibilityValueText` and the tests assert that *property*, while **nothing asserts `body` still publishes it**. Proved by deleting the single line `.accessibilityValue(accessibilityValueText)` from `body` and running the full suite — **all 1977 tests passed, the mutation SURVIVED** (file restored byte-identical, sha256 `51a98f69…f12d4` both sides). Corroborated: `grep -rn 'accessibilityValue(' app/Tests/` returns **zero** hits and `.measured(...)` captures no accessibility value, so nothing else could observe it. Oracle 12 got exactly this guard and passes (DIS-16 swaps the call for an identically-rendering literal, so only the binding guard bites); oracle 18 did not get one. **Remedy: one source-scan assertion mirroring the sheet's, plus an arm proving it reddens.** **Evidence integrity the verifier reported against itself:** `parity-control.sh` first exited 1 with M29's own row mismatching — because the Swift body carried `"hash":"0000000000000000"`, the sentinel **its own DIS-3 probe injects**, baked into a *prebuilt* `ControlDiff` binary. `red-green.py` restores source but **not build artifacts**. Rebuilt from clean source → 56/56, exit 0. A contamination caught and named rather than a false defect filed. **Gates:** `make test` 0 (1977/251) · `make lint` 0 · `make parity` 0 · `npm run build` 0 · red-green **15 KILLED plus DIS-3 effectively killed** on a 900s out-of-harness re-probe after the harness timed it out at exactly 300.0s with a synthetic *suite did not terminate*. `parity-manifest-check.sh` 1 and `mock-fidelity` 3 both re-confirmed **inherited against current `main` at `6fab22a`**, script and file hashes identical on both sides. — **Oracle-18 binding guard closed 2026-08-26 (17 commits, READY-TO-VERIFY again).** No product behaviour changed — `ServersBoardRow.swift` is byte-identical to the verifier's baseline `51a98f69…f12d4` throughout. The new test reads the source the way oracle 12's guard does, because this host exposes no accessor for a view's accessibility value, and it asserts **equality on the whole `.accessibilityValue(` population** rather than `contains` — deliberately, so the DIS-16-shaped mutant dies too: an inlined ternary that renders identically would leave every other assertion green. Three scoping anchors stop an empty or over-running extraction satisfying the equality. **New arm DIS-17 KILLED (1/1)**, deleting the exact line whose mutation survived for the verifier; file restored byte-identically. `make test` **0 at 1978/251**, `make lint` **0**, nothing weakened. **The `Reset Server` question decided OUT OF SCOPE, against the agent's own first lean, on three verified repo facts:** `spec-M29.md` decision **D3** records *no menu-bar command and no accelerator, `MenuCommand` is untouched*, with **DEF-M29-b** already filing the Router-menu half; oracle 5 deliberately keeps **user-asked reindex live on a disabled server**, and `MenuCommand.resetServer` resolves to `.reset(.reindex)` whenever `indexError` is set, so dimming that predicate would close a door M29 deliberately opened; and the one-line fix would dim *Reset* while leaving *Review Held Changes* live on the same selection, which `ServerRowAction.forServer`'s own comment calls a dead end. What it fixed instead was the **false record**: `menuContext`'s doc comment claimed *the same fact the row's Reset action branches on — one source, two readers*, which stopped being true when `forServer` gained its disabled short-circuit. **Owed by the orchestrator: a DEF-M29-d row** — the menu bar's disabled behaviour as one decision (`Reset Server`, `Review Held Changes` and the two Router verbs, with honest dimmed reasons), beside DEF-M29-b. **DEF-M29-d filed 2026-08-26**, owed to the gap-fix agent which could not write this file: *the menu bar's behaviour for a disabled server, taken as one decision* — `Reset Server`, `Review Held Changes` and the two Router verbs, each with an honest dimmed reason, and `ShellModelBadges.menuContext`'s `selectedServerIsTripped` reading the placard alone. Filed rather than fixed because `spec-M29`'s decision **D3** deliberately declined the menu-bar surface (*no menu-bar command and no accelerator; `MenuCommand` is untouched*), because oracle 5 keeps **user-asked reindex live on a disabled server** and `resetServer` resolves to `.reset(.reindex)` on `indexError`, so a naive dim would close a door M29 deliberately opened, and because dimming *Reset* alone would leave *Review Held Changes* live on the same selection — a half-measure. Sits beside **DEF-M29-b**, which already carries the Router-menu half. |
| M30 | Where a capability document actually comes from | `M30-capability-document-source.md` | — | — | Untriaged — filed 2026-08-23 by M19's runner as **M19's real exposure, written up rather than buried**. Allocated `M29` first and **renumbered to M30 on the orchestrator's collision**, `M29` having landed on `main` at `78a27e7` 18:47 while this runner was down in the crash window. **A trap the renumber avoided, worth carrying**: four other `M29`s live in this tree — two in `spec-F3.md`, one in `G3-gapfix-2.md`, one in `scripts/red-green.py` — and **every one is a mutation id in a red-green table, a different namespace with the same spelling**. A tree-wide `sed` would have renumbered a red-green record and **nothing would have gone red**, because those tables are prose to every gate we run. The rename named its five files explicitly instead. — **Triaged 2026-08-25 → Needs More Info.** Grounded: control API admits only `/servers`, `/usage`, `/registry/*`. No wire type carries markdown documentation or licenses. The brief offers 'do nothing' (leaving the viewer on fixture data) vs building two router endpoints (Swift + TS) with parity vectors and image streaming. This is a scope/cost decision for the owner. — **Owner decision 2026-08-25 → To Do.** Asked as a scope/cost fork (build the plumbing vs leave the viewer on fixture data); the owner chose **serve real documents**. The Needs More Info is discharged. What the decision licenses: a route that lets the router read a package's own files, in **both** router implementations, with the parity vectors that keep them honest. What it does not settle, and the plan owes each an answer: which of the five facts-strip cells the router genuinely observes (a licence and a *runs in* fact are derivations, and §6 forbids displaying a figure the router does not observe); whether images travel as bytes on the wire, since the viewer's resolver returns bytes precisely so the app never opens a file; and the transport size cap, because a README is unbounded on the wire and the refusal has to say which cap it hit. **BUILT 2026-08-26 on `ai/m30`, 6 commits from `03c34c3`, tip `b1c77ee` — READY TO VERIFY.** Spec committed before code. **The route is `GET /servers/:name/document`, in BOTH implementations** — `src/document.ts` plus its arm in the TypeScript control surface, and `DocumentPackage.swift` plus `ControlDocument.swift` in RouterCore. **The package root is the server's declared `cwd` and nothing else**: it is the one directory a wire type carries and the one the router already starts the child in. A root derived from `args[0]` was considered and **refused in writing** as a guess about a packaging convention the router does not otherwise use; `/skills` was ruled out because neither router serves it at all. **The three questions the triage said the plan owed an answer are answered in the spec, in doc comments, and in assertions.** (1) **One of the five facts cells survives**: `Kind`, taken from the router's own config. `Version`, `Licence`, `Runs in` and `Reads` are derivations and are absent — a test named for the rule fails if any of the four reaches the wire. Two further cells are served *because observed*: `Tools` (the length of the approved manifest list) and `Served to` (the projects restriction the router itself applies). (2) **Images travel as base64 bytes and the response carries no package path**, asserted by failing if the root string appears anywhere in the body — which forced an image-reference scan and a containment check into the router, both ported and both vectored. (3) **Three transport caps distinct from `MarkdownLimits`**: `documentBytes` 512 KiB answering 413 and naming cap, limit, actual and file; `imageBytes` 2 MiB naming that image alone; `imageBudgetBytes` 8 MiB answering `budgetExhausted` — and an oversized image does not spend the shared budget. **Parity is armed rather than assumed**: three vector files (20 + 25 + 15 cases) take the floor 367 → 427, and three divergences planted in the Swift implementation produced three reds — a dropped title (3 cases), dropped symlink resolution (the escape case became readable), and an oversized image spending the budget (3 cases) — restored byte-identically at sha256 `93e1d508…3828`. Separately `control-differential.sh` gained a real package and three rows: **59 rows compared at both binaries, 59 ok, 0 failed.** Gates: `make test` 0 (**1998 tests**, up from 1980) · `make parity` 0 (427/427) · `ledger-reconcile` 0 · `control-differential.sh` 0 · `make lint` 2 and `citation-gate` 1 on the inherited ratchet alone (BARE 1335 across clean main's same 10 files, none of them this branch's — its one added row was removed by framing the citation, not by touching the baseline) · `parity-manifest-check.sh` 1, gap 2 on clean main and still exactly 2. **Changes beyond the brief, disclosed rather than buried**: the unhosted-kind census moved four → three because hosting `readme` is precisely what this item licenses, and the replacement assertion is strictly stronger; `Identity.publisher` and `.pitch` became optional because the router observes neither, with the fixture unchanged so M23's capture is unchanged; the image resolver's refusal gained three cases plus `unrecognised`; and one handler was extracted to keep dispatch under its complexity cap. **No live-app screenshot was taken** — the panel's rendering is M23's measurement and is unchanged; what this item adds is proven by route tests, mapping tests and the two-router differential. **THREE INHERITED FAILURES REPORTED, NOT CHASED, and two are new findings:** `make parity-regen` is **broken on main** (`opts.commit is not a function`), so **nothing currently checks that the committed vectors match the reference** — filed as its own brief, and the reason this item's vector sections live in an importable module with its own entry point; `foreign-path-gate.py` **exits 1 on a pristine detached checkout of `03c34c3` with the same 7 citations and passes in the main checkout only because of its untracked working-tree state** — a gate answering differently on two checkouts of one commit; and one load-induced flake in `RealProcessTests` under five concurrent runners, green in three later full runs. **Orchestrator note:** this runner filed its parity-regen brief as `M31-parity-regen-is-broken.md`, and `M31` is allocated to a live item with its own row and branch. Renamed on the branch at `04a0da0`/`b1c77ee` — the same collision this row's own history records happening to M30, which was allocated `M29` and renumbered when `M29` landed on `main`. **VERIFIED 2026-08-26 (fresh context; the verifier ran NO out-of-family lane for this item and explicitly does not claim one): NEEDS MORE WORK — and the finding is a scope disclosure, not a code defect.** The engineering is judged sound and **the trust boundary is called the best-defended thing in either item it graded**. The verifier mounted **its own 23-reference traversal attack** with a real symlink to a file outside the package, a symlinked directory and a sibling-prefix package: **every escape refused and nothing threw** — symlink-out and symlinked-dir both `escapesPackage`, sibling-prefix `escapesPackage`, percent-encoded separators refused (nothing percent-decodes anywhere), absolute and `~` refused, scheme-prefixed refused. It then **re-planted a divergence itself**, dropping symlink resolution from the Swift candidate, and the parity vectors reddened with `expected a refusal, got .readable(…/escape.png)` — so the containment check is genuinely the thing standing between this route and arbitrary file read, **and it is armed**. It also planted the package root into the payload and watched the no-path-on-the-wire test redden. **All seven facts cells audited against §6 and all seven pass**: `Kind` from the router's own config, `Tools` gated so a failed connection yields an absent fact rather than a zero, `Served to` the router's own restriction, and the four derivations correctly absent with a test that fails if any label reaches the wire. The "strictly stronger" claim is **verified** — the old assertion made two claims, the new one makes four and retains every claim about the other kinds. Parity floor arithmetic checks at 427, and **the verifier went further than the runner**: regenerating all three vector files from the TypeScript reference produced **byte-identical output**, so the broken `parity-regen` leaves the *inherited* 367 unchecked against a reference rather than M30's 60. `parity-regen`'s break is **confirmed independently on clean main**. **THE FINDING THAT DECIDES THE VERDICT:** the package root is the declared `cwd` and nothing else, and the refusal of `args[0]` is judged **correct** — for `npx -y @scope/pkg` it is a flag, for `node /path/dist/index.js` its dirname is the build directory, and guessing would widen the readable surface. **But nothing derives `cwd`, and on this machine 0 of the 21 real upstreams declare one — so the route 404s for every server the owner actually has, and an item built under a decision to *serve real documents* currently serves none in the owner's own environment.** The panel handles it coherently, mapping the refusal to its own UI case, so nothing breaks — but it was not disclosed. **That is also why requirement 6 is a gap rather than a rung**: *"the panel's rendering is M23's measurement and unchanged"* is true about pixels and silent about reachability, and one end-to-end look would have surfaced this immediately. Thin spot: `budgetExhausted` is exercised by exactly one of the 15 caps vectors and by no route test, though the ordering property that matters is armed. Gates: `make test` **0** (1998 tests / 254 suites), `ledger-reconcile` 0, `py39-annotation-gate` 0, `control-differential` 0 (59/59, and it **refuses under bash 3.2, needing bash 5.3**), `citation-gate` 1 at BARE **1332** — *exactly* clean main, so **M30 adds zero bare citations**. **VERIFIED 2026-08-27: NEEDS MORE WORK — two narrow defects, both in the new work rather than in the corrections.** Every correction the finishing agent made to the dead agent's work is **sound and confirmed**: all **15** artifact paths the record cites are tracked (`tmp-reach.mjs` gone and cited nowhere); reach **re-derived from the live config by the verifier** as 21 upstreams, 14 stdio with no `cwd`, 7 non-stdio, **0 stdio declaring a `cwd`** → **SERVED 0 of 21**, with the seven http names matching the committed evidence exactly; and both PNGs **opened and confirmed** — served titlebar and H1 `m30-look` with the constructed harness named in three places, refused reading `m30-no-cwd has no package to read`, and M19's fixture capability `trawl` appearing **nowhere** in the evidence tree. The look **reproduces byte-identically** across all six artifacts on a re-run. **(a) The filed brief carries a wrong measured number**: option D says it would cover *"the eleven `npx` upstreams"* where the live config has **six** (6 npx + 4 `node .../dist/index.js` + 4 native binaries = 14 stdio). The brief's distinguishing claim is that it recommends against C and D **on the measurement rather than on taste**, and one of those measurements is off by five. Out-of-family (grok-4.6 xhigh, on-subject) judged option B sound, found nothing outside A–D that beats it, and held that the error **only weakens the brief** rather than changing the recommendation — even at eleven, D leaves four node dist paths, four native binaries and seven http upstreams unserved. **The brief's judgment survives; its arithmetic does not.** **(b) The promoted harness reports an environment as a failure.** The verifier's first run exited **1** with `FAIL: the harness could not render the served document`, because the shared `.build` held a non-MEASURE build: `MeasureDump` correctly reported **3/inconclusive** and the harness converted that into a red. It has a `blocked` path for a *missing* binary but none for a *present* binary built without MEASURE — the same condition. **This is precisely G10's defect-3/5 class and it is live rather than theoretical**, because `.build` is shared: running another item's `make test` between two M30 runs is enough to trigger it. **Verified DONE 2026-08-27 — MERGED to `main` at `8ca250d`.** Both narrow defects closed. The brief's option D is corrected from *"the eleven `npx` upstreams"* to **six**, re-derived from the live config along with every other figure in it rather than assuming only one was wrong (21 upstreams = 14 stdio — 6 npx, 4 `node .../dist/index.js`, 4 native — with **0 declaring `cwd`** — plus 7 http; the reach probe re-runs to `SERVED 0 of 21` identically). **The correction is recorded with the original error left visible, and the brief now states that it strengthens the argument**: at six, option D covers 6 of 21 and leaves 15 unserved. The harness now reads MeasureDump's exit and turns **3 into a `blocked` exit 2 naming the rebuild**, with every other non-zero still a failure — **armed both ways in-repo**, the pre-fix path reproducing the verifier's exact red so the new branch is demonstrably not a no-op, and the evidence tree byte-identical after the green run. **A CORRECTION TO THIS ORCHESTRATOR'S OWN PREMISE, measured by the runner and confirmed here: `app/.build` is NOT shared across worktrees** — three distinct inodes. That claim was written into several briefs this session and was wrong. The defect is no less live, because any plain `swift build` in the same worktree plants it, which is a cheaper and commoner cause than the one I asserted. |
| M31 | The design of record cannot draw a disabled primary | `M31-the-design-of-record-cannot-draw-a-disabled-primary.md` | — | — | Untriaged — raised by `lukerhodes-2f` from M18's gap-fix, **verified independently by the orchestrator**. `design/mcp-router-console.html` sets `.btn:disabled` and `.btn.primary` at the **same specificity (0-2-0)** with `.primary` declared **second**, so it wins on `background`, `border-color` and `color`: a disabled primary keeps its accent fill, accent border and `--on-accent` label and differs from a live one **only by losing its shadow**. `DESIGN.md` requires *every control* to carry a disabled state and says *disabled dims in place*, so **the design of record cannot draw the state the design authority requires**. Not merely undrawn — drawn **as though enabled**, which is the exact failure the rule names. **It also inverts the instrument**: `M23`'s conversion is mock-driven and `mock-fidelity` compares a build to the frame the mock draws, so a build that correctly dims a disabled primary would be **reported as a divergence** — the gate currently rewards reproducing the defect. Reaches M18 (built, same defect from the same cause in `ProminentButtonStyle`), M19 and M22, and **none of them owns it**. Triage must settle what *dims in place* means for an accent-filled control in `DESIGN.md` itself, and sweep for built surfaces that already reproduce it. — **Triaged 2026-08-25 → To Do.** `.btn.primary:disabled` / `.btn.primary.disabled` added to the design of record, and `MockButtonFidelityTests` re-pointed from *the mock cannot dim a primary* to *the mock dims a primary*. **The semantics are not invented here — they are M18's, verified identical:** `ProminentButtonStyle.palette(isEnabled: false)` is `Palette(label: .t4, fill: .f3, border: .line)` and the landed CSS is `color:var(--t4);background:var(--f3);border-color:var(--line)`. M18's own comment says the treatment was *derived rather than copied into the app (M31)*, so this completes what it pointed at, and its contrast argument travels with it: `--t4` on `--f3` is below 4.5:1 and claims the `disabled` exemption by name against WCAG 1.4.3's inactive-control clause. **Was going to be filed Needs More Info; the review lane refused that** — you cannot land the answer and mark the question open. **Residual:** state the semantics in `DESIGN.md` itself rather than only in a code comment and a stylesheet, with the ratio stated and the exemption claimed explicitly; and sweep built surfaces that may already reproduce the defect, which the brief names as the part most likely to be skipped. **One unratified addition:** `cursor:not-allowed` was added to the mock rule and has no counterpart in M18's palette. — **Residuals delivered 2026-08-26 (`ai/m31`, 4 commits, READY-TO-VERIFY).** **DESIGN.md §3 now states the disabled-primary semantics** as a token triple, with the ratio computed rather than carried: **`--t4` on `--f3` is 2.94:1 dark and 2.62:1 light**, claimed exempt under **WCAG 1.4.3** with the *inactive user interface component* clause quoted rather than cited. The arithmetic's control is that it reproduces DESIGN.md's own published columns exactly, and the doc says explicitly that its new figures are **not** those columns, since a disabled label is never drawn on a bare ground. **`cursor:not-allowed` removed, not ratified** — §3 rule 8 puts the arrow cursor everywhere in app chrome, macOS paints no such cursor, and it was **the only `cursor` declaration in the entire design of record**; now held at zero by an assertion rather than at *not that one*. **Sweep: examined=23 surfaces · 31 call-sites · 10 disableable · failures=0 · unproven=13**, committed as a re-derivable instrument rather than prose. Rungs stated per class: the design of record **RENDERED** through Obscura in both appearances; the store page **PASS**; Swift **SOURCE READ ONLY**, because mock-fidelity's inherited break leaves no rendered lane. **The 13 unproven are the phone, and they are the sweep's real finding:** `PhoneProminentButtonStyle` dims to **`--raised`, not `--f3`**, with no bezel — so it dims, but `--raised` is the *enabled* fill of the phone's secondary style, making **a disabled phone primary pixel-identical to an enabled secondary**, while its own comment claims *the same tokens*. It also reads `@Environment(\.isEnabled)` **on the ButtonStyle type**, the construct `Controls.swift` declines to rely on, and one call site invokes `makeBody` directly so SwiftUI never installs those dynamic properties at all. **Left unfixed on the file's own precedent** that phone-ladder gaps are reported rather than made there. **A second rendered instance of the exact M31 defect:** `.tb-btn.on` is declared after `.tb-btn:disabled` at equal specificity and probes **byte-identical** to enabled — latent, since no markup uses `.on`. **The assertion M31 replaced could not have failed on the bug it was named after:** it tested that a rule *exists*, and the bug was a rule that exists and **loses**. 3 planted arms, 3 assertions, all bit; restore byte-identical. **Self-reported and corrected mid-run:** a `governor-run` REFUSED was masked as `EXIT=0` by an `echo`, and the sweep's first version printed the store page as having **zero** primaries because it counted the wrong vocabulary — a quiet zero, caught. **VERIFIED 2026-08-26 (fresh context; rebased onto `03c34c3` clean; agy/gemini-3.7-flash-high AGREE, grok-4.6 xhigh AGREE): NEEDS MORE WORK.** The DESIGN.md deliverable is sound and **re-derivable**: `--t4` on `--f3` recomputed 2.9367→2.94 dark and 2.6154→2.62 light, and the stated control genuinely holds — the same float compositing reproduces all four published columns (3.3665→3.37, 2.7933→2.79, 1.1463→1.15, 1.0680→1.07). **The control also discriminates**: 8-bit-rounded compositing gives 2.95/2.61 and breaks DESIGN.md's own published 1.15, so float is the single method reproducing everything. **R4 confirmed with the verifier's own two-way planted control** — on a mock where `.btn.primary:disabled` exists with its body reduced to `box-shadow:none`, the old `contains(".btn.primary:disabled")` returns True while the new body assertion returns False and the control renders byte-identical enabled and disabled; the merged assertion could not have failed on its own bug. `arm-guards.sh` 3/3 arms bit on distinct assertions, restore sha256-verified. **BLOCKS ON THE SWEEP — the half the brief called “the part most likely to be skipped”.** `failures` is initialised at `sweep-prominent-disabled.py:124` and **never appended to anywhere in the file**; `grep -n failures` returns the declaration, two reads and the return. `failures=0` over `examined=23` is structurally guaranteed, and the HTML lane emits raw dicts with no verdict field. The docstring defends the zero on having a denominator, which it has; the presence control it needs does not exist. **And the defect it is credited with finding is real but was not found by it** — `.tb-btn.on` (line 440) after `.tb-btn.disabled,.tb-btn:disabled` (439), both 0-2-0, re-probed through Obscura as `BEFORE rgb(0,113,227) fg=rgb(255,255,255)` / `AFTER` identical — but the sweep's selector regex is `\.btn(\.primary)?` fullmatch, so `.tb-btn.on` was never in its search space; `render-probe.sh` found it, while the commit message says *“the finding the sweep turned up”*. **Two further live defects sit outside its denominator, unfiled**: `.trow[aria-selected]` + `.trow.disabled` renders `--t4` on `--accent-ink` measured at **1.68:1 light / 1.08:1 dark** — exactly the pairing the new §3 text says it refuses to license — and `.switch:disabled` dims by `opacity:0.45` keeping the accent fill, a different treatment from the triple just ratified. The phone claim is **confirmed but overstated**: ".fill(isEnabled ? ColorToken.accent.color : ColorTok", `app/Sources/MCPRouterUI/Phone/PhoneButtonStyle.swift:47` at `7cd54d6` does dim to `--raised`, `:32` does read `@Environment(\.isEnabled)` on the ButtonStyle type, "PhoneProminentButtonStyle(fillsWidth: fillsWidth).ma", `app/Sources/MCPRouterUI/Phone/PairingResultSurfaces.swift:23` at `7cd54d6` does invoke `makeBody` directly — but a disabled phone primary is **not** pixel-identical to an enabled secondary, since `PhoneStandardButtonStyle` enabled draws a `--t1` label (`:66`) and a `--line-strong` bezel (`:73-76`) where the disabled prominent draws `--t4` and no bezel; they share the fill only. The committed instrument states this modestly and correctly — the overstatement is ledger prose. Leaving the phone unfixed was defensible on the file's own precedent; **leaving it unfiled was not** — the brief says the sweep decides “one CSS line or a set of build corrections”, it decided *set*, and `git diff main --diff-filter=A` shows no new brief, so 13 unproven findings and one live rendered defect exist only in a text file and a ledger row. Gates on the rebase: `make test` 0 (1982 tests / 252 suites), `ledger-reconcile` 0, `make lint` 2 and `citation-gate` 1 **byte-identical to clean main** (BARE 1335 / N5 1482, same 10 files) — inherited. **GAP-FIX COMPLETE 2026-08-26 on `ai/m31`, 10 commits, rebased clean onto `03c34c3` — READY TO RE-VERIFY. All four gaps closed, and the widened sweep found a fifth defect on a shipped page.** **Gap 1, the sweep can now fail.** It no longer asks whether a dimming rule *exists* — the question `.btn.primary` itself passed while being the bug. It resolves the cascade: for every rule painting the accent ink it builds the element matching that rule plus a disabled marker, replays every rule by specificity and source order, and reads off the winning fill, colour and border. Five verdicts, four of which fail. Denominator: **12 accent-filled controls over 4 surfaces**, with 4 accent rules excluded as not-a-control (progress bar, notification glyph, shield icon) and the reason printed. Presence control `arm-sweep.sh`: **7 planted defects, one per failure verdict, each re-creating a defect this item fixed**, all red, all restored byte-identically by sha256. **The assertion is the verdict-table diff plus failure-count movement, not a grep — the runner tried grep first and it was wrong twice in one run**, once finding nothing while the sweep went red for a better reason, once matching five verdict lines already present so a no-op plant would have "passed". **Gap 2, attribution corrected at source.** `.tb-btn.on` was found by the render probe, not the sweep, whose regex was a `.btn(.primary)?` fullmatch; the branch was unpushed so the misattributing commit was amended rather than leaving a false claim in the permanent record, with the reasoning kept in the probe's header, and the search space widened to every accent-painting rule. **Gap 3, both settled by correcting the design of record rather than ratifying exceptions.** The row case drew `--t4` on `--accent-ink` at **1.68:1 light / 1.02:1 dark** — the runner's dark figure differs from the verifier's 1.08 because it uses the page's own dark root values rather than the light ones. **The switch was worse than reported**: its markup is `class="switch disabled"` while the rule was `.switch:disabled`, so it never applied at all — Obscura read the disabled switch as identical to the live one beside it, **dimmed by nothing**. DESIGN.md §3 now binds the fill rather than the button, states the refused pairing's measured cost, and maps a switch's track and knob. **Gap 4, the phone brief is filed**, with the overstatement corrected: a disabled phone primary is **not** pixel-identical to an enabled secondary; they share the fill only. **THE FIFTH DEFECT, found by the widened sweep and on a page that ships**: the store page's bare `.btn` *is* the accent-filled control, and its disabled rule kept the accent under a tint plus a cursor §3 rule 8 forbids, with a live disabled instance on the page. Fixed. **Merge caution from the runner: that file is separately dirty in the main checkout and a merge may conflict; it did not touch that copy.** **Rung discipline**: design of record and store page **RENDERED** via Obscura in both themes across all six controls, with dark reached by applying the page's own dark root block because `setEmulatedMedia` is accepted and inert in that engine — which proves the rules resolve under dark token *values*, not that the media query fires. Swift is **SOURCE READ only**, and because mock-fidelity still exits 3 on the inherited `MeasureDump` break the 13 phone rows stay **UNPROVEN rather than passing**. One probe bug caught and fixed mid-run: the segmented probe was snapshotting the wrapper, not the button. **Out-of-family review: PARTIAL, and the runner explicitly does not claim it clean.** agy failed (headless permission auto-denial, empty output); grok-4.6 at xhigh confirmed on-topic and delivered one finding before the lane was killed by timeout; fable-5 also timed out. **That one finding was real and was acted on**: the resolver tried `background` then fell back to `background-color` instead of letting the cascade choose between them, **proven exploitable** — an accent rule spelled `background-color` legitimately winning against a disabled rule spelled `background` gave `DIMS (HIDDEN)` under the old resolver and `DRAWS-AS-ENABLED` under the new. Fixed, with arm 6 as the regression. **Five design questions went unanswered by any other family** and are recorded as unreviewed: the switch knob mapping, whether fixing an unreachable rule was overreach, whether "unreachable" is a legitimate undrawn exemption, the control predicate's soundness, and whether the store-page fix was in scope. Gates: `make test` **1985 tests / 252 suites** (1982 baseline plus 3 new guards, suites unchanged) · button-fidelity tests 0 (8/8) · `ledger-reconcile` 0 · sweep 0 · sweep self-check 0, reproducing all four published DESIGN.md columns · `arm-sweep.sh` 7/7 · `arm-guards.sh` 8/8 · `citation-gate` 1 and `make lint` 2, **both the inherited ratchet at BARE 1335 over the same 10 files as clean main, none of them this branch's** — its 3 added bare citations were given anchors and trees, which also took RESOLVES 23 → 26. **Verified DONE 2026-08-26** (fresh context; lane `grok-4.6` xhigh out-of-family after one lane failure, corroborated by fable-5 recorded as degraded) — **MERGE BLOCKED ON THE OWNER'S UNCOMMITTED WORK, not on the item.** All eight checks pass. The verifier re-derived every contrast figure with its own float-compositing implementation and **settled the disputed dark ratio in the runner's favour, with a cause**: dark `--t4` on the **light** `--accent-ink` computes 1.0761 ≈ 1.08, so the earlier verifier mixed a dark token with a light one, while the runner's 1.0247 uses the page's own dark accent. All four sweep exclusions judged **honest** — each is a `div`, `span` or `<i>`, none on an interactive tag or attribute. 7/7 arm-sweep plants fired with a **distinct verdict transition each**, all restored byte-identically; arm-guards 8/8. The switch finding is confirmed and the mechanism named exactly: the markup is `role="switch"` with `aria-disabled`, and `:disabled` matches only a `disabled` **attribute**, which that button does not carry — so the rule was unreachable and the disabled switch rendered identically to the live one beside it. **The main checkout's dirty copy still carries the pre-fix rule, which the verifier cites as direct proof M31 never touched it.** Obscura produced the pixels, both themes, six controls, `darkVarsApplied=39`, no Playwright fallback needed. **THREE FINDINGS OF THE VERIFIER'S OWN, none a regression:** the sweep has **no assertion on the switch knob** (it checks descendants setting `color`; the knob sets `background`), so deleting the knob's disabled rule leaves the sweep green — caught at the test rung instead, so covered but not where a reader would look; **the reachability exemption is the real denominator softener rather than the exclusions** — four accent-filled controls have no disabled treatment and pass as `UNDRAWN/unreachable`, and planting a disabled marker on one checkbox reddened **two rows immediately**, so the exemption is mechanically honest and printed on every row but is a property of the mock's markup rather than the product; and the no-op plant is **a legible control rather than an exit-code gate** — it prints `*** DID NOT GO RED ***` with an empty verdict diff, but `arm()` returns 0 either way. **On the five design questions no other family had seen, grok and fable agreed unanimously: Q1 SOUND, Q2 SOUND, Q3 GAP, Q4 GAP, Q5 OVERREACH.** The verifier leans in-scope on Q5 against both reviewers, and confirmed their stated cost is exactly **one 1-line conflict** at the disabled-button rule. Q3 and Q4 are to be filed as a follow-up brief — policy findings about the instrument, disclosed in its own output, affecting controls that have no disabled instance on any surface today. **Also noted: grok's first run was a LANE FAILURE** — 378 bytes of narration plus a fabricated claim about files it never opened; a retry constrained to the prompt returned real verdicts. Gates: `make test` **0** (1985 tests / 252 suites, run twice), `ledger-reconcile` 0, `py39-annotation-gate` 0, `citation-gate` 1 at the inherited ratchet. The ratchet moved 1332 → 1333 and the +1 lands in this ledger, which M31 never edited — a resolution knock-on from adding a tracked file rather than new unanchored prose. |
| M32 | A mock-driven gate is blind to what the mock does not draw | `M32-a-mock-driven-gate-is-blind-to-what-the-mock-does-not-draw.md` | — | — | Untriaged — filed 2026-08-23 from M22's app-driving pass. **The Harnesses board rendered `Read now ago`** because `shortAgo` returns the *word* `now` under five seconds and a duration above it. **It survived all three green instruments, structurally**: no test asserted the string; the board is new so no earlier rendered pass existed; and **M23's fidelity gate compares build copy against the mock, which has no freshness line at all** because that line is M22's own addition — **so there was nothing on the other side to disagree with**. `mock-fidelity` is strong on everything the mock draws and **has no opinion on what the build adds, and no opinion reads exactly like agreement**. So *clean over 193 nodes* is true and means only *nothing the mock draws diverges* — not *the surface is right*. **Drawing it in the mock closes the instance, not the class**: every conversion adding a build-side affordance the design never specified re-opens it, and M19's `path` refusal and M18's three undrawable readings are legitimate cases of the same. The remedy is that **an `extra` needs an oracle rather than only a citation**. **The same run found two defects in its own lane, same family**: a sentinel `grep` for `isn't built yet` matched the board's own disabled-control help tag (legitimately reused `CommandAvailability.surfaceAbsent` copy), and an `awk` read AX fields 5 and 7 where SwiftUI puts the label in AXDescription (6) and `enabled` is field 8 — **reporting *no Reconcile control is drawn disabled* about four that were**. *A gate reading the wrong column says the product is wrong.* — **Triaged 2026-08-25 → To Do. One instance fixed; the class is untouched and is the actual item.** `EvalsInspector` composed `\(shortAgo(...)) ago`, which renders **`now ago`** under five seconds; it now reads `just now`, matching the guard `BoardCopy.readAt` already carried. That is the only unguarded site in the tree. **The brief is explicit that fixing instances does not close the class**, and the review lane found the residual as first written undercounted it: the brief documents **two** mechanisms, not one. (a) An `extra` — something the build draws that the mock never did — gets a citation but no oracle, so the gate has no opinion and *no opinion reads exactly like agreement*. (b) M20's finding, which the brief calls **worse than the first**: the mock's four header count cells match **no affordance-derivation rule at all**, so a wrong `Resident 214 MB` never enters the census as `present`, `divergent` **or** `absent` — leaving not even an `extra` row to interrogate. **Priority context for whoever plans this:** the brief suspends the M23 coverage claim on the branches carrying conversions until this lands, so *clean over N nodes* is not a coverage statement about a surface until then. **BUILT 2026-08-25→26 on `ai/m32`, tip `dd43d74`, branched from `03c34c3` — READY TO VERIFY.** The instance was left alone as briefed; both mechanisms the brief documents are closed. **(b) The census is now total.** `scripts/acceptance/mock-affordances.py` classifies every element in a frame into exactly one of `affordance` / `covered` (no rule, but inside one, so its text is carried into that affordance's label) / `uninventoried` (no rule, no affordance ancestor, draws its own string) / `container`, asserted where produced and re-derived independently by the engine; a new `census` layer in `mock_fidelity.py` makes each `uninventoried` element a finding unless a `~` pairing row waives it with a citation. **M20's case measured on the real design of record**: the Insights `v-ideal` frame draws `Resident, all children` and `214 MB` inside `<div class="stat">`, and the field meant to make that visible reported `unclassifiedElements: 1` for a frame holding twelve such strings — it skipped `div` and `span` before counting, and nothing in the engine ever read it. **Across six manifests the residue is 128 drawn strings outside every rule** (servers 8, harnesses 1, insights 12, readme 43, settings 64) — the size of the hole the brief said nobody could size. **(a) An `extra` now needs an oracle**: a `!` pairing row names a file that must actually contain the string the node drew, as a quoted literal under a test root with its `::` selector present if given, so a citation alone never removes a finding; and `covered-by-pair`, the gate's one wholly silent class, exempts a child only where an ancestor measured `present` rather than merely comparable, so a duplicated row or an invented sibling loses the exemption by changing the aggregate it rested on. **Controls: 17 new selftest cases, 68 → 85**, each planting the fault its check exists for and asserting red, plus three refutations proving the satisfied path stays quiet. Live control with hashes: removing the `sentence` rule moved servers/v-ideal from `affordance 87 / uninventoried 8` to `86 / 9` with `elements` held at 290 — a narrowed rule set cannot shrink the residue, it only moves the element into the reported class; restored byte-identically at `a733fc19d074819d`. **Out-of-family review (agy/gemini-3.7-flash-high, on-subject, quoting the diff) found five ways the first draft could not fail, all five closed in code with a selftest case each**: an oracle pointing at the implementation (the build asserting against its own literal), a bare substring matching a Swift identifier, a substring exemption made structurally unreachable by a text-combining container, private-use codepoints dropping an icon-font glyph out of the residue, and a frame whose own root matches a rule reporting a residue of zero for a structural reason. Gates: `make test` 0 (1980 tests / 252 suites), `ledger-reconcile` 0, `citation-gate` 1 at main's inherited ratchet (its own row fixed by anchoring the citation, baseline untouched), selftest 85/85 with all three exits observed. **NEW FINDING, not fixed and not this item's**: the inherited mock-fidelity exit 3 is not the popover frame — the `MCP_ROUTER_MEASURE=1` build does not compile, because a later branch added `.readme` to the `Surface` enum without updating a switch in `MeasureDump`'s entry point, so no dump can be produced on this tree and every surface correctly reports 3 over an earlier run's table. One line, in a file two branches are moving. **Consequence to carry: the new layers are armed by fixture and their input measured on the real mock, but no end-to-end run over a real dump was possible.** **VERIFIED 2026-08-26 (fresh context, lane `grok-4.6` xhigh, clean out-of-family run): NEEDS MORE WORK.** Six of eight requirements confirmed exactly, including everything the item is *for*. **The census partition is total and disjoint** — every element is appended unconditionally with no skip path, `classify()` is a single if/elif/else, asserted at production and independently re-derived by the engine, which additionally refuses on two further conditions; the verifier looked for a fifth silent path and **found none**. **The 128 residue re-derived exactly**, twice — once statically and once through a live engine run — matching per surface (servers 8, harnesses 1, insights 12, readme 43, settings 64). **M20's case reproduced exactly**: at `03c34c3` the field read `unclassifiedElements: 1` for the insights frame and the engine contained **zero** references to it, where the new census finds **12** on that same frame and names them, `Resident, all children` and `214 MB` among them. Selftest confirmed at **68 → 85 cases**, exactly +17 including three exit-0 refutations, all three exits observed. The live control **reproduced exactly**: removing the sentence rule moves affordance 87→86 and uninventoried 8→9 with elements held at 290. The instance is untouched — M32 touches **no** file under `design/` or `app/Sources/`, and the one `checked just now` hit in the mock is pre-existing JS present identically on main. **AND THE END-TO-END RUN NOW WORKS**, which is the gap the builder said was closeable and could not close: all six surfaces ran against real measure dumps, with both mechanisms firing live on real data. **BLOCKS ON TWO.** (1) **The verifier DEFEATED the oracle.** The `TEST_ROOTS` guard added specifically to close review finding 1.1 is **bypassable by path traversal**: `app/Tests/../../app/Sources/.../MenuCommand.swift` passes the root check and then opens the implementation file, which contains the literal **because it is the file that draws it** — while the naive spelling is correctly refused. The `./`-prefixed spelling works too. The selftest plants only the naive spelling, so it cannot fail on the traversal. **One line — normalising the path before the root check — closes it.** grok's verdict: *"This is the item's own defect class, not leftover hardening… the test that claims the finding closed is itself such a check."* (2) **The committed fidelity ledgers were never regenerated** — `servers.ledger.md` on the branch shows **no census row at all** where a fresh run of the delivered gate writes `census 8 finding(s)` and moves breadth 116→128, which is the same *record describing a state its own branch has left* defect as G10's gap 1. `covered-by-pair` the verifier **could not defeat** — it gates on a measured whole-aggregate equality, so a duplicated child or invented sibling changes the aggregate and every child loses the exemption. Four of the five out-of-family findings are genuinely closed; the private-use-codepoint one is **empirically** closed with a deliberate, documented asymmetry. Gates: `make test` 0 (1980 tests), selftest 0 at 85 cases, e2e 1 on five surfaces and 3 on popover (**identical on main, so not M32's**), `ledger-reconcile` 0, `py39-annotation-gate` 0 with control HELD over 44 files, `citation-gate` 1 at the inherited ratchet with **none of the 9 files M32's**. **VERIFIED 2026-08-27: NEEDS MORE WORK — and the finding is a denominator that hides its own omission.** The oracle fix is **confirmed sharp**: the verifier threw **16 attack spellings** at the fixed resolver — deep `..`, interleaved dot-segments, double-slash, absolute-to-implementation, a **file symlink** and a **directory symlink** inside a test root, symlinks out of the repo, case variants, root-prefix lookalikes — for **0 bypasses**, with the control still accepted; and reverting the engine alone fails **exactly** the two planted traversal cases and nothing else, returning 0 where the case expects 1. Selftest 87, exit 0. **BUT THERE ARE SIX FIDELITY LEDGERS, NOT FIVE.** The branch regenerated **four** (servers, insights, harnesses, readme); popover legitimately did not change; **`settings.ledger.md` was never re-run** — last written at a commit predating this branch. **The record's sentence names five surfaces and accounts for five, so the sixth never enters the denominator and the omission is invisible.** It is not cosmetic: `settings.layers.json` **was** changed on this branch to add `census: required`, so the committed ledger describes a run **under a manifest that no longer exists** and carries **no census row** — the exact defect the gap-fix was raised to close on servers, left standing on settings, where the verifier's regeneration writes **64 findings**. **Verified DONE 2026-08-27 — MERGED to `main` at `630a602`.** The hidden sixth ledger is closed. The surface list is now derived from the manifests on disk rather than from prose: **six**, not five. All six were regenerated, each its own invocation, and **only `settings` changed** — the other five reproduce byte-identically, which is itself the evidence that settings was the whole of the gap. It gained `census` \| `64 finding(s)` and moved `literals 125 → 146 files`; **that second pair proves the old table described an older *tree*, not merely an older manifest.** Provenance confirmed in git: `settings.layers.json` gained `census: required` on this branch while `settings.ledger.md` was last written at a commit predating it. The record now states six with the mechanism named — the sentence named five and accounted for five, so the sixth never entered the denominator — and gives each ledger's producing run in a table. The oracle fix was separately confirmed sharp by a verifier throwing **16 attack spellings** at it for **0 bypasses**. |
| M33 | `swift build` exits 0 and silent on a fault `xcodebuild` calls fatal | `M33-swift-build-cannot-gate-the-app-target.md` | — | — | Untriaged — filed 2026-08-23 from M20's verifier F5, **rescued from a verdict that had no destination for it**. Measured with a **two-way planted control** at `1d9ffad`: one fault, `swift build --build-tests` exits **0 and silent**, `xcodebuild` exits **65 and fatal**. `app/Package.swift` declares **no target at `MCPRouter/`** while `app/project.yml:42-43` does, so **the SwiftPM lane cannot compile the file the app's assembly lives in** — every green `swift test` on a branch touching `app/MCPRouter/` is *silent about* that directory rather than clean over it. **The verifier names it as the mechanism behind M18's recurrence** and the fit is exact: M18's tree had also never compiled while its runner reported gates it believed meaningful. **The eighth entry in the instrument-that-cannot-fail register and the one with the widest blast radius**, because `swift test` is the gate almost every brief in this fleet names first. Nobody has swept for other branches whose green covered nothing under that directory. — **Triaged 2026-08-25 → To Do, and armed.** `MCPRouterApp` added to `app/Package.swift` as an executable target over `MCPRouter/`, so the SwiftPM lane compiles the directory the app's assembly lives in. **Proved by a two-way planted control rather than asserted** — the review lane flagged that an `executableTarget` not depended on by any test might not be built by `swift test`, which would have made this the same *not known to pass* sin as G10. Measured: with one planted type error in `MCPRouterApp.swift`, `swift test` exits **1** and names `MCPRouterApp.swift:234:45: error: cannot convert value of type 'String' to specified type 'Int'`; restored to a byte-identical file (sha `a81f3d66d60c` both ends) it exits **0** at 1941 tests in 244 suites. **Residual, unstarted:** the brief also asks that `make test` *say which description it compiled* — it does not, and the brief is explicit that a second lane passing silently over a directory it does not read leaves the reader where they started. And the sweep for other branches whose green covered nothing under `app/MCPRouter/` has not been run. **Assumption:** adding the target beats making `xcodebuild` mandatory — cheaper, and closes the hole at source rather than at the gate. — **Remaining scope delivered 2026-08-26 (`ai/m33`, 4 commits, READY-TO-VERIFY).** `make test` now names **`app/Package.swift (SwiftPM)`**, lists all 11 targets with per-target compiled counts, and ends `build-description-verdict: OK`. **The brief's premise was understated:** `project.yml` declares **four** source paths, not one. Arming closed `MCPRouter/`; the three `MCPRouterIOS*` paths sit in the same position and are excused **only because `make all` runs the iOS lanes** — and that excuse is **checked**, so if those lanes ever leave `all`, the three become `COVERED BY NOTHING` and the gate reddens. The report line is protected by its own **10 poison arms** plus a `grep` over what was actually emitted, so it cannot go missing while the target passes — which was the precise trap, since closing this item by adding a silent second lane would have rebuilt the defect one level up. Final run exit 0, 1980 tests in 252 suites. **VERIFIED 2026-08-26 (fresh context; rebased onto `03c34c3` clean): NEEDS MORE WORK, narrowly — one line.** All three requirements pass with every control firing, and **the poison arming is the strongest the verifier saw across either item**. `make test` exits 0, prints `selftest: 10/10 arms bit`, names `app/Package.swift (SwiftPM, package MCPRouterKit)`, lists 11 targets with per-target counts including `MCPRouterApp executable MCPRouter 1/1 sources`, and ends `build-description-verdict: OK — 1 project.yml path(s) compiled by this lane, 3 deferred..., 0 covered by nothing`. The verifier extracted the recipe verbatim and ran **its own three arms** against stubs: prints nothing → exit 1 *“did not say which description it compiled”*; marker indented so not at line head → exit 1; marker correct but script exits 3 → exit 3 via `pipefail`; real script → 0. **The report line cannot go missing while the target passes.** M33's correction to its own brief is confirmed — `project.yml` declares **four** source paths (`:43`, `:86`, `:135`, `:169`), not one — and **the `make all` excuse is genuinely checked, not asserted**: editing the real `Makefile:73` to drop `test-ios test-ios-glass` from `all:` turned all three `MCPRouterIOS*` paths to `COVERED BY NOTHING` at exit 1, each cited to its `project.yml` line, with the Makefile restored to matching sha256. Sweep reports `examined=102 affected=7 covered=0 n/a=95`, never a bare `affected=0`, and says the 95 are `n/a` rather than `clean`; selftest 7/7. The delta from the recorded `examined=100 n/a=93` is the verifier's own two `verify/*` branches, `affected=7` unchanged. M33's correction that M18 does not reproduce is independently confirmed — `ai/m18`'s range touches **0** files under `app/MCPRouter/`. **BLOCKS ON ONE ROW.** Clean main lists 10 files over the citation ratchet (BARE 1335); rebased M33 lists **11** (BARE 1336). The extra is `planning/evidence/M33-acceptance.md  1, baseline 0` — a file M33 itself creates, bare citation at `:94` quoting `app/project.yml:43` inside gate output with no anchor or tree. At M33's own unrebased tip the gate lists 4 files over baseline and that same file is one of them. Yet "ratchet only, 3 files above baseline and all three are M29's", `planning/evidence/M33-acceptance.md:214` at `b5f2227` certifies *“`planning/citation-gate.py` \| 1 \| **inherited** — `blocking 0`, ratchet only, 3 files above baseline and all three are M29's”*, which is false at the exact tree the record certifies. The KNOWN-INHERITED exemption covers what main already fails, and this row is not on main — a lane reporting clean over something it did not read, which is the shape M33 exists to close. **The out-of-family lanes split**: agy/gemini-3.7-flash-high AGREE (*“a falsified acceptance record certification requires remediation before approval”*), grok-4.6 xhigh DISAGREE for DONE (*“named requirements passed with firing controls; false inherited-gate sentence is a record error not a failed requirement”*). The verifier took gemini's side on the gate row rather than the sentence. **It also voided its own first agy call**, which had been given the premise “gate failures were strictly inherited” before that was checked; both recorded verdicts come from re-runs on corrected evidence. **VERIFIED 2026-08-26 (fresh context, `grok-4.6` at xhigh): NEEDS MORE WORK, narrowly.** Three of four checks confirmed. `:94` carries its anchor at `570910a`, the acceptance record is **off** the over-baseline list, and **`planning/citation-ratchet.json` has no diff and no commit against main** — the baseline was not touched. **The orchestrator's pipe-free anchor re-cut closed the blocker**: rebased onto main, `citation-gate.py` reports `exit: control 0 · blocking 0 · ratchet 1` with `ABSENT 0` and `DRIFTED 0`. The runner was right to report it rather than write a file it was forbidden to touch. Three of the four review corrections are visible in the record, including that `b5f2227` is **dangling** so whether merging reddens the finding depends on the object database rather than the tree, with the fresh-clone consequence spelled out. **BLOCKS ON TWO NARROW THINGS.** (1) **One figure is asserted against a tree the record never names** — the phrase *"and 4 at the tree that made the claim"* appears twice as prose with no output shown and the claim-tree named nowhere, which is the corrected defect at reduced scale. Two further figures are true and checkable but unfenced. (2) The **fourth** review correction — that two of the runner's own new citations were `TREE_ONLY` because nested backticks defeat the anchor parser — lives **only in the commit message**, not in the record; its fix is visible (the anchor is now double-quote delimited) but the reason is not. The verifier's own judgement on the rest is worth carrying: restating a figure from a fence directly above it at the same named tree is **reading evidence, not asserting it**, and that part is legitimate. **A CORRECTION TO THIS ORCHESTRATOR'S OWN FIGURE:** the over-baseline list is **9 files** on all three branches and on committed main, not the 10 quoted in every brief this session — *"the brief's '10 on clean main' counts the uncommitted `RUN-2026-08-20.md`"*. BARE is **1332**, not 1341. Both of the numbers I have been circulating were dirty-tree readings, which is the same error as the `1333`-vs-`1321` correction made earlier in this run. **STILL BLOCKED 2026-08-27, on a defect in its own gate that only became visible after M31 merged — and it is the inverse of this item's subject.** The gap-fix work is confirmed complete: all four verifier points are closed, the claim tree is named (`b5f2227`) and fenced with all four readings attributed, the `bbe8df1` fence was **reproduced byte-for-byte from a fresh checkout**, the nested-backtick `TREE_ONLY` reason is in the record rather than only in a commit message, and no legitimate restatement was stripped. **But `make test` now exits 2 on this branch, and the cause is `build-description-report.py`'s own staleness oracle.** It reports `MCPRouterUI 141/142 sources <-- NOT COMPILED` and `[stale-object] … object(s) older than their source: Controls.swift. The green describes a previous tree.` **The object is not stale.** Measured directly: it contains **30 `Palette` symbols**, the type M31 added to that file, so it is current in content. What is older is only its **mtime**. **The mechanism is this repository's own worktree convention**: `.build` is shared across every worktree, so `.worktrees/M31` compiled that source at 19:21 on 26 Aug, and merging M31 to `main` then stamped main's *source* at 00:00 on 27 Aug — leaving an object that is content-current and mtime-older. Confirmed twice more: `swift build` prints `Compiling MCPRouterUI Controls.swift` **without advancing the object's mtime**, and a content change to that source leaves the object's sha256 unchanged at `befcc994ffa38a28`. **So the gate uses mtime as a freshness oracle where SwiftPM does not maintain one, and reports RED over a fresh object.** That is a false alarm rather than a false pass — the safer direction, and still wrong, and it means the gate cannot distinguish `the green describes a previous tree` from `another worktree compiled this first`. Everything else on the branch is green: `ledger-reconcile` 0, `citation-gate` blocking 0, `py39-annotation-gate` 0, its own two registered sweeps `control` and exit 0, and `sweep-control-gate` exit 1 with 8 findings **identical on main**. **Verified DONE 2026-08-27 — MERGED to `main` at `8a057ba`.** The oracle is replaced and **the runner corrected the orchestrator's diagnosis with the more useful fact**. A single worktree reproduces the defect: append a **comment** to a source and rebuild, and `swift build` prints `Compiling MCPRouterUI Controls.swift` — the compile really ran — yet the object's mtime does not move and its sha256 is unchanged, leaving it older than the source it perfectly describes; append a real declaration and both move. **So an object's mtime tracks changes to the object's CONTENT, not the occurrence of a compile**, and anything that stamps a source without changing its meaning — a comment, a re-wrap, a checkout, a merge — strands the object permanently. The shared `.build` is one instance of that law, not the law. **Two alternatives rejected with reasons**: sibling mtimes rest on an undocumented accident (`.dia` and `.d` are rewritten unconditionally while `.o` and `.swiftdeps` are written only when their bytes change, and nothing in their shape says which is which), and `build.db` was rejected because a misparse of its custom binary encoding **reads as clean**, the one failure mode this file may not have. The oracle is now **llbuild's own verdict** — run the build again and require it to do nothing — with `--build-tests` load-bearing because `swift test` and `swift build` write the same plan the report reads two steps later. **Armed in three directions**: a real declaration with no build gives exit 1 and 12 findings cascading into two dependent targets; the same tree on a second run gives 142/142; and a touched source over an object **438 seconds mtime-older** stays green where the old comparison reads RED. **One property recorded rather than hidden: the probe repairs what it reports**, since SwiftPM has no dry-run, so a red is not reproducible by re-running the gate. No out-of-family lane returned — grok produced 384 bytes of narration and no verdict — and the record says so rather than implying a review. |
| M34 | The menu badge is unmeasurable by any lane we have | `M34-the-menu-badge-is-unmeasurable-by-any-lane.md` | — | — | Untriaged — filed 2026-08-23 from M20's verifier F4, **rescued from a verdict that had no destination for it**. **Evidence work rather than product work**: the behaviour may be correct and nothing available can say so. The badge answers the brief's *dimmed in place with the reason in the shortcut column*, and **the only thing reading it is a unit test over a menu the test itself built**. Four lanes closed, each measured: the accessibility tree exposes **no badge attribute** (19 attributes per item, five direct probes absent, `AXHelp` correct so the instrument is sound); `mac-shell.sh` has **no menu-badge assertion** and **every `badge` hit in it is a pre-existing sidebar-row badge**, so a grep reports coverage that does not exist; `AXPress` on a menu item returns `.success` **and does nothing** in a background app; and photographing it is what `UI_VERIFICATION.md` rule 1 forbids. **Six items carry a chord and a badge at once and nothing measures which the trailing area shows.** — **Triaged 2026-08-25 → To Do.** Grounded: every `badge` assertion in `mac-shell.sh` inspects sidebar rows; no menu bar badge check exists. The options (direct accessibility attribute reads, in-process render inspection, or formal structural limit recording) are test engineering tasks. Ready for plan. — **Developer Review 2026-08-26 (`ai/m34`, 3 commits, tree clean, frontmost unchanged throughout).** **The brief's central premise was wrong and the runner proved it.** Its first closed lane — *the accessibility tree exposes no badge attribute, 19 attributes per item, five direct probes absent* — is true in its measurement and false in its conclusion: the badge **is** on the plane, folded into **`AXTitle`'s value** as `Title, Badge`. The earlier probe enumerated attribute *names*, found the same nineteen, and stopped — **the brief's own `grep badge` trap, one layer up**. macOS corroborates unasked in the same dump: the Apple menu reads `App Store…, 29 updates`. **Re-measured, the limit is narrower than the one inherited:** the plane is **open for AppKit-built menus** (badge folds in at construction and post-hoc, enabled and disabled) and **closed for SwiftUI-built ones**, which is this app's menu bar — and that is now a tripwire rather than a note. Photography is not merely forbidden but **unnecessary**: `NSMenu.size` lays a menu out in-process with no window and no tracking session. **The claim nobody could settle is settled, measured:** `Install Command-Line Tool` at plain 194 · chord 235 · badge 300 · **both 319** points — strictly wider than either alone, so AppKit reserves trailing space for both and **neither displaces the other**. Asserted per-command across all **six** commands carrying a chord and a badge — the brief's number, now counted rather than asserted. The badge behaviour is shown correct through a four-link chain ending in a post-hoc badge reading back in-process, so it is **unexposed rather than lost**. — **Verified DONE 2026-08-26** (fresh context, out-of-family agreed, repo byte-identical to `65192b1` afterwards). **The builder's refutation of the brief holds under the verifier's own independent instruments:** the badge folds into **`AXTitle`'s value**, confirmed on a menu nobody in this project built — macOS's own Apple menu read from a running Safari — with the same 19 attributes the brief enumerated. The trailing-area claim is confirmed across **all six real commands and again with each command's real chord**: `both > badgeOnly` and `both > chordOnly` throughout, so AppKit reserves space for both. Six was **re-derived from source** rather than inherited. Three new assertions were mutated and all fired; `scripts/acceptance/menu-badge-lane.sh` and its selftest are **wired into `make acceptance`**, so this is a lane rather than a note. The screen was never taken — frontmost sampled at 1 Hz across launch-and-quit, MCPRouter never frontmost. **Three documentation-grade caveats carried, none a requirement failure:** the headline evidence row is labelled *Measured on `Install Command-Line Tool`*, which reads as one of this app's commands and is not one — the number is real, the label misleading; the shipped test uses a stand-in chord rather than each command's real one; and **nothing automated asserts the shipping app's badge**, which is by design — the AX plane provably cannot carry it for a SwiftUI-built menu, so the brief's third option, recording the limit structurally, is the honest floor and is what landed. |
| M35 | `mac-shell.sh` hand-names two facts the app has since changed, and both are red | `mac-shell-hand-named-rows-went-stale-when-m20-and-m22-shipped.md` | — | — | **To Do** — filed 2026-08-26 from G10 gap-fix. Destination allow-list is 7 names instead of 9; Export library was moved from File to Library in M20. **VERIFIED 2026-08-27 (fresh context): NEEDS MORE WORK — and the brief pre-empted the delivered diff in writing.** The values are all correct: three sites now read the nine destinations in sidebar order matching `Destination.swift` exactly, and the export row matches `MenuCommand.exportLibrary`'s menu and title exactly. Selftest exit 0 over 13 arms, no screen taken. **But the brief's Scope asked for the destination set to be DERIVED from the app's own source, and named this diff as the non-repair**: *"the repair on the table is not 'change File to Library' — it is that a hand-named row duplicating a fact the model already carries will go stale again."* The delivered 15-line change hand-updates the literals and derives nothing. **The staleness mechanism reproduced itself inside the delivered change**: roughly eight comments and one failure-message string still say *seven* while the adjacent pass message was updated to nine — none load-bearing in an assertion, and all of it the same defect one layer down. The item had no spec and no plan and its row was still `To Do` with a one-line description, so no stage narrowed the scope in writing and the runner defaulted to the option the brief had already ruled out. **Coverage limit stated by the verifier:** the changed assertions were never executed, because the lane drives the real app and takes the screen; the values are confirmed by reading against the product's own source, not by a run. **This is the same defect class as `the-campaigns-surface-list-is-hand-maintained-and-has-gone-stale.md`**, filed the same day from the campaign side — a hand-maintained duplicate of a fact the product already states, going stale exactly as predicted. |
| M36 | The phone control ladder dims a disabled primary to a token `DESIGN.md` §3 does not ratify | `phone-prominent-disabled-uses-an-unratified-token.md` | — | — | **To Do** — filed 2026-08-26 from M31. `PhoneProminentButtonStyle` disabled fill lands on `--raised` rather than `--f3`. |
| M37 | Upstream server disable and pause action | `server-disable-toggle-action.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. Related to M29, which established that *Disable a server* is drawn with nothing behind it; this is the affordance itself. |
| M38 | Visual diff review and approval for staged upstream tool changes | `staged-tool-diff-visual-review.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage; **proposed-by-ai**. |
| M39 | Mobile companion pairing QR presentation and expiry countdown | `phone-pairing-qr-render-and-countdown.md` | — | — | **To Do** — allocated 2026-08-27 by serial pre-triage. **Blocked in fact by DEF-001**: the build ships no pairing transport, which is why `REQ-015` carries two standing campaign FAILs. This item is the render half; the transport is not in it. |
| X7 | The campaign's published artifacts under-report what it knows | `X7-campaign-artifacts-underreport.md` | — | — | Untriaged (**upstream**: fledgeling-plugins, not this repo) **Owner decision 2026-08-22 — fix upstream in `fledgeling-plugins`, then re-pull the vendor.** G5 vendored `test-campaign` 0.9.2 to `vendor/test-campaign/` byte-identical to upstream `28ecd67`, with a tree checksum in `vendor/README.md`; patching the copy here would restate that as *0.9.2 plus N patches* and cost the reproducibility property G5 was commissioned to buy. **The cost taken knowingly**: this repo's gates keep running the known-wrong detectors until the vendor is re-pulled, and X8's are findings the detectors cannot support — so a campaign run before the re-pull carries them and must say so. |
| X8 | Two campaign detectors report findings they cannot support | `X8-detectors-misattribute.md` | — | — | Untriaged (**upstream**: fledgeling-plugins, not this repo) **Owner decision 2026-08-22 — fix upstream in `fledgeling-plugins`, then re-pull the vendor.** G5 vendored `test-campaign` 0.9.2 to `vendor/test-campaign/` byte-identical to upstream `28ecd67`, with a tree checksum in `vendor/README.md`; patching the copy here would restate that as *0.9.2 plus N patches* and cost the reproducibility property G5 was commissioned to buy. **The cost taken knowingly**: this repo's gates keep running the known-wrong detectors until the vendor is re-pulled, and X8's are findings the detectors cannot support — so a campaign run before the re-pull carries them and must say so. |
| X9 | Upstream: a verification probe reports a present file as missing | `upstream-citation-probe-basename-false-positive.md` | — | — | Untriaged (**upstream**: fledgeling-plugins, not this repo) — registered 2026-08-27 by an intake sweep that found 24 briefs on disk with no ledger row. Routed upstream beside `X7` and `X8`; this tree carries the vendored copy, not the source. Measured today, `citation-gate.py` reports `control 0 · blocking 0`, so the false positive costs this tree no blocking class. |

| I5 | Prove the phone↔Mac pairing round trip, and stop there | — | — | — | **Merged** `4157bc4` (ORCHESTRATOR.md) |
| I6 | Make Mac approval fast, without moving the boundary | — | — | — | **Merged** `ef4f615` (ORCHESTRATOR.md) |
| M9 | Rename the `Evals` destination to `Checks` | — | — | — | Done inside D2 (`9e8a754`) — triaged 2026-08-21 |
| M10 | Amend `DESIGN.md` §6:279–280 | — | — | — | Done inside D2 (`9e8a754`) — triaged 2026-08-21 |
| M11 | Regenerate the M1 command inventory | — | — | — | **Merged (partial)** `2a434b9` — promoted out of the deferred register |
| M12 | Staleness and an as-of time inside a destructive dialog | `M12-staleness-in-destructive-dialogs.md` | `spec-M12.md` | `plan-M12.md` | **Done** — merged `a040b17` — verified `Needs More Work` at `3367f72`, all five findings closed 2026-08-23 on `ai/m12` at merge `9d4da76`. **The product was never the problem and one line of `app/Sources` moved, a doc comment.** The blocker was A15c's C7 row, which claimed to prove *the stamp is absolute* and greped `reading taken $shape` against a sentence hardcoding `at `; it now reads the text after `reading taken at ` and requires a clock time, and it is **armed by planting rather than by reading**: `shortAgo(date, from: now) + " ago"` makes the app speak `'now ago'` and the gate exit 1, reverted to a byte-identical file (sha256 `44bc6250…` both ends), `25 passed, 0 failed`, exit 0 twice. The removal dialog's stamp gains no absoluteness assertion and that is **declared, not closed** — `fail` exits, so one plant arms one row. The unit table is restamped `876dc20` and says in the row why `eaf4352` was contaminated. **22 citations swept with a named regex and a looser control grep; six wrong** — `CleanupBoard.swift` is one blob at `87e16dc` and `0f5f118`, so `:79` and `:34` were wrong when written rather than drifted, and every repointed citation now carries anchor **and** tree **and** line while the two tracker citations lost their line numbers outright. The state matrix and the doc comment now call the reset dialog unreachable without a reading, with the 13 `.resetHistory` sites classified. The route default is 200, not 500, and the argument built on it is unchanged. **`make lint` and `make build-mac` ran as composed targets**, closing the composition the verdict left unproven; `make test` 1743/217, reconciler 0 across A–L. One run in three blocked at LaunchServices `-609` after A15c had passed, load 321–452 on 16 cores, not chased per the brief. No out-of-family lane: this pass executed findings already argued and measured, and there was no open fork in it. — **Ready to verify 2026-08-23 — 12 commits, `0f5f118`, tree clean, survived the crash with nothing redone.** Arrived with **no brief and no spec**, so the item wrote its own: brief, `spec-M12.md` (C1–C13), plan, acceptance and progress. Gates all re-run *after* the crash rather than quoted from before it: `make lint` 0 (swiftlint 0/554, null-run-gate 28 armed / 0 held), `make test` **1743 in 217** (+18 on main's 1725/215), `make build-mac` succeeded, `m7-evals-cleanup.sh` 24 passed **green twice** with MCP Router never frontmost, reconciler 0 A–L, red-green **six mutations six reds each reverted**. **Three things it declines to claim**: the stale marker is proved at model and value layers and **not** by a rendered pass (`LoadState.stale` needs a fixture that answers once then refuses; `FixtureControlAPIClient` is a stateless struct) — *do not grade that row off the acceptance run*; one acceptance run failed the invisibility invariant then did not reproduce twice, and **could not have been attributed either way because `axkit front` compares an app NAME, so any MCP Router build on this machine fails that gate identically — which reaches every worktree this fleet runs**; and `make all`/`parity`/`test-ios` were not run. **Four items routed, none blocking**: Activity's reset dialog states `records.count`, a **500-capped ring window**, where Cleanup states the unbounded tally for the *same endpoint* — on a router with 812 calls the two dialogs say **812 and 500**, and `usage.reset()` discards both plus two log files (**the out-of-family lane recommended folding it in and the runner refused with the router source**); `make build-mac` passes no `-onlyUsePackageVersionsFromResolvedFile`, so a fresh worktree floats a transitive pin — its `eventsource` floated 1.4.2→1.5.1 under an SDK pinned exact, tripping the freshness gate; a poll where `servers()` succeeds and `usageSummary()` throws **silently drops the call count with no Partial marker**, on `main` too; and `SkillProvenanceSheet` draws body text in `--attn` at **2.31:1 on light**. **Lane note for the verifier**: codex is limited to 27 Aug and grok returns 402, so only `agy`/gemini-3.7-flash-high answered — **twice**, and its plan review killed the design's relative timestamp, so gemini has seen this design twice and is **the weakest choice for an independent verdict**. **Verified Needs More Work 2026-08-23 at `3367f72`; gap-fix owed — five record claims, product sound.** **The verifier could not break the product**: all thirteen clauses hold, all six red-green mutations go red **under patch strings it wrote itself rather than reusing the runner's**, and `resetConsequence` is byte-identical by blob SHA at both `87e16dc` and HEAD. Merged tree: `make test` 1743/217, all eight lint components clean, `BUILD SUCCEEDED`. **The blocker is the ninth instrument-that-cannot-fail, and it guards this item's central design decision.** `A15c`'s own C7 row claims to prove *the stamp is absolute* and **greps for `reading taken $shape` against a sentence that hardcodes `at `** — so the exact regression the plan review killed renders `taken at 3m ago` and slips past. **Proved by planting rather than by reading the regex**: the defect live in the running app, `24 passed, 0 failed`, exit 0 — then **M7 planted as a negative control failed correctly**, so eight of nine rows have teeth and one does not. **Four more**: a green unit run **stamped to `eaf4352`, the commit carrying the M3 mutation, where those tests are red**; three of fourteen citations landing on plausible wrong text (`CleanupBoard.swift:79` reads `case let .stale(_, error):` where `StaleReadingBanner` sits at `:95`) — `G7`'s dangerous variety; a state-matrix row **and a shipped doc comment** calling the reset dialog reachable while loading when `CleanupBoard.swift:179` **disables its only button there**; and a route default of 200 written as 500, on an argument that is nonetheless correct — checked at source before weighing the item's refusal of the Gemini lane's scope recommendation as right. **The invisibility flake reproduces and its cause is now measured rather than argued**: one failure in ten runs plus two blocks, and **a pid-aware readback found the frontmost `MCP Router` during those blocks was `.worktrees/M22W`'s build, not its own** — the direct measurement the runner's unattributability argument lacked, and the same family as `V5`'s duplicate bundles. It left that process alone. **`make lint` and `make build-mac` cannot run on this worktree at all**, both dying at `tools` wanting `node_modules` **for parity lanes neither target runs**; ten components run by hand, recorded as a difference from the target, **with the composition left explicitly unproven**. Lane: only `agy`/gemini answered, logged as a downgrade **since it graded the findings rather than the tree and had already seen this design twice** — it reached `Needs More Work` independently. |
| P5 | Close the last three closeable parity rows | — | — | — | **Merged** `e752305` (ORCHESTRATOR.md) |
| P6 | State the owner's cutover target in the gate | — | — | — | **Merged** `05296ea` (ORCHESTRATOR.md) |
| R2-R | Router: the process that actually serves | — | — | — | **Merged** `62678aa` (ORCHESTRATOR.md) **Orphan brief `R2R-router-daemon.md` resolved by this row**: registered 2026-08-27 during an intake sweep that found 24 briefs on disk with no ledger row. That brief is this item's original filing and the item is merged, so it needed a pointer rather than an id. |
| R2-W | Router: the `~/.claude.json` watcher and its adoption protocol | — | — | — | **Merged** `8e48a80` (ORCHESTRATOR.md) |
| R4-C | The installer cutover | — | — | — | **Superseded** — split into R4-C1 (Done, `ai/r4c` merged) and R4-C2 (Held). Not blocked: each half has its own state and its own row. The owner's target is 82 of 83 |
| X4 | Mac boards: six defects the design of record names | — | — | — | **Merged** `2ff0941` (`ai/x4`) — its work is written up under M25 |
| X5 | Discover and Skills: the controls row set the board's width | — | — | — | **Merged** `dee20da` (`ai/x5`) — its work is written up under M25 |

## Allocation notes

- **Statuses were synced from ORCHESTRATOR.md on 2026-08-21.** Twenty-six rows here read
  `Untriaged` for work that had already merged — R2, R3, R5, M1–M8, I1–I3, P1–P4, D1–D3, F4,
  G1, M13, M14, V1 and R4. A fleet reading this column would have dispatched twenty-six shipped
  items. Membership and status drift separately: `ledger-reconcile.py` catches the first and
  cannot see the second, because both files having a row for an id says nothing about the two
  rows agreeing. When you change a status, change it in both files or the next fleet re-plans
  the work.
- **M3 was triaged on 2026-08-21 and it had shipped.** The earlier note here recorded it as
  unresolved rather than open — its row said `Untriaged` with no branch while M7's dependency
  cell read `M3 ✓ M4 ✓` — and refused to guess which reading was right. The answer is the first
  one: `589ab2e` ("the breaker board — Servers is the first pane that is actually built"),
  `3b11f33` and `af77200` are all ancestors of `main`, six `ServersBoard*.swift` sources are in
  the tree and seven test files name the board. M7's tick was correct and both rows were simply
  never updated. Scheduling it would have rebuilt a shipped board — which is the cost this note
  existed to prevent, and the reason a status a fleet cannot explain gets triaged rather than
  assumed in either direction.

- **`R7` was two different items, and both ledgers reconciled clean the whole time.** This file
  carried `R7 — the router's thesis is unmet for every harness but Claude Code`, with a brief on
  disk named for it. `ORCHESTRATOR.md`'s deferred register carried a different `R7` — the skills
  write endpoint, a child of R3 filed by M7. Two items, one id.

  `ledger-reconcile.py` could not see it, and the reason is worth keeping: checks A and B ask
  whether an id appears in *both* files, and it did. **Membership and identity drift separately,
  the same way membership and status do.** Check `F` was added for this — it compares the two
  description cells for an id present in both files and reports a pair sharing no content word at
  all. That bar is deliberately low: a legitimately reworded row nearly always keeps its subject
  noun, so requiring *zero* overlap is what stops the check firing on a correct use.

  Resolution: the top-level ledger item keeps `R7`, because a brief file carrying the id in its
  filename is the stronger claim. The deferred child became `R11` and now has a row here of its
  own. It is still open — `CleanupSheets.swift:204` draws `DisabledAction(label: "Remove", …)`.

  **Check F then found two more on its first run, and the second one exposed a defect in the
  check itself.** `R8` was the merged auth-rejection item here and *server soft-delete with a
  restore endpoint* in the deferred register; it became `R12`. `R6` was the child-PATH item here
  and *a router-side behavioural eval runner* in the deferred register; it became `R13`.

  `R6` is the one worth keeping. The first version of check F read **one row per file**, and
  `ORCHESTRATOR.md` carries two `R6` rows — the child-PATH item in the wave table, and the eval
  runner in the deferred register. The row that agreed with this file was simply the earlier one,
  so the check reported clean. A collision inside a single file is the same defect as one across
  two, and a predicate that reads one row per file cannot see it. F now compares every row for an
  id against every other, whichever file each came from.

  **Fixing the collisions created a fourth defect, and check `G` was added for it.** Renumbering
  the deferred `R8` to `R12` left the *merged* `R8` — the auth-rejection item on `ai/r8` — with
  no ORCHESTRATOR row at all, because that had been its only one. Check B did not fire: B clears
  on an id being **named** anywhere, which is the right bar for "does the other file know this
  exists" and the wrong one for "can a fleet resume from that file". G asks the narrower
  question, and found `R8` plus `X4` and `X5`, two merged branches this reconciliation had given
  rows here but never there. All three now have rows. Both tables stand at 77.

  The sequence is the point: six checks reported clean over a file with three id collisions and
  three missing rows. Each new check found something on its first run, and one of them found a
  defect the previous fix had just introduced. A reconciliation is not a state you reach; it is a
  claim that only holds for the predicates you have written down.

  **Then eight checks reported clean over five stale duplicate rows**, found by `dev-09` rather
  than by this script. `ORCHESTRATOR.md` carried two rows for `M23`, `M27` and `R10` — each pair
  agreeing on the title and disagreeing on the status, with the stale one reading *earlier* than
  the truth: `Untriaged` beside `Ready to verify` for two branches sitting at 6 and 12 commits.
  A fleet slot filled from the stale row dispatches a triage run on work that is already built.

  None of A, B, F or G could see it. Their diagnosis is worth keeping verbatim: **each check
  tests a different key, and a row is only as bound as the weakest key any check uses.** A and B
  test membership, F identity, G resumability — and none tests currency, so a present, plausible,
  wrong row satisfies every one of them. That is worse than a missing row, because a missing row
  fails membership and a stale duplicate fails nothing.

  Check `H` groups rows by id and compares status cells. It indexes the column by its **header
  name** rather than its position, which is what keeps it honest: this file has one nine-column
  table carrying `Status` and a four-column deferred register carrying none, so a positional
  predicate would have compared `M11`'s register row against whatever sat at that offset. H found
  the three plus `R4-C`, whose two rows read `Blocked — needs 82 of 83` and `Wave 4 — last` — a
  wave label written into a status cell, both stale, both quoting a parity figure since
  superseded. R4-C is now one row: superseded by `R4-C1` (Done, `docs/install.sh` defaults to
  `MCPR_ROUTER=swift`) and `R4-C2` (Held).

  Fixing it also turned up `M13`: a complete nine-column row pasted into the four-column register,
  where its cells read as *Absorbed by: mac* and its outcome as a dependency list.

  **H then had the same defect it was written to catch, one level down.** `dev-09` asked what the
  parser does with a row whose field count does not match its header — and the answer was that it
  skipped it, silently. Measured: **70 rows examined, 23 skipped**, all of them four-cell
  deferred-child rows interleaved in the nine-column table with no header of their own. None of
  the 23 can disagree about a status it has no cell for, so the finding survives — but a check
  that quietly discards what it cannot parse reports clean over a subset, which is the same
  denominator failure as a campaign publishing a pass rate over the surfaces it happened to reach.

  The script now prints `H examined N rows; skipped K` on **every** run, names the skipped ids,
  and returns **2** rather than 0 when `examined == 0`. A gate that never ran is not a gate that
  passed, and the one way to reach a false clean here was for the table shape to change under it.

  A row with *more* cells than its header is placed rather than skipped, and correctly: a stray
  `|` shifts only the cells after it, so every column before the offending pipe keeps its index.
  `D3` is the live case — 11 cells against a nine-column header, `Status` still landing on
  `**Merged** \`67ae4f5\``.

  H is armed three ways: the unmutated file is clean at exit 0; a stale duplicate row injected for
  `M27` makes it name both line numbers at exit 1; and renaming the `Status` header to `State`
  makes it exit 2 saying it measured nothing, rather than reporting a clean file it could not
  read.

- **M9, M10 and M12 were triaged on 2026-08-21 by measuring the tree, not by reading their
  rows.** Two of the three had already shipped. `M10`'s amendment is in `DESIGN.md` §6, which now
  carries the correction *and* the reason — the old illustration "a skill with no evaluation reads
  'not evaluated'" named a state the product cannot be in, because there is no eval runner
  anywhere in it. `M9` is closed in `Destination.swift`: `.evals` reads `Checks`, and the
  `rawValue`, `iconName` and `?pane=evals` slug stay `evals` **on purpose**, documented in source
  — they are identifiers held in frame restoration and in every mock link, and `DESIGN.md` §6
  governs words a user reads rather than keys a machine matches. `M12` is the one still open:
  `CleanupSheets.swift` draws its destructive "Remove <name>?" dialog with a consequence figure
  carrying no staleness marker and no as-of time.

  Three items, two of them already done — the same shape as the M3 note above. A deferred
  register records what was *filed*, and a later item closing it does not write back. So a row in
  this table is a claim about the past; check it against the tree before scheduling from it.

- **Thirteen rows were added on 2026-08-21 that this file had never carried.** Eleven were
  named only in this file's prose or only in ORCHESTRATOR.md; two — X4 and X5 — were merged
  branches recorded in neither. An allocator scans the table, so a mention in prose does not
  stop an id being reissued: that is how I6 came to be allocated for a new brief while it was
  already merged at `ef4f615`. Run `planning/ledger-reconcile.py` after every allocation; it
  refuses in both directions and has six predicates, because each is blind to the others.

- **This file is not the only allocator, despite the header above.** Measured 2026-08-21:
  `ai/i5`, `ai/x4` and `ai/x5` exist as branches, `I5` holds a live worktree, and
  `ORCHESTRATOR.md` carries rows for **I5 (Merged `4157bc4`)** and **I6 (Merged `ef4f615`)** —
  none of which appear in the table above. ORCHESTRATOR.md is the live ledger; this file has
  drifted behind it. Check both, plus `git branch --list 'ai/*'`, before allocating. Two
  sessions independently reached for an id that was already taken on the same evening, which
  is what this note exists to stop.
- **X4 and X5 are taken** (branches exist and both are merged into `main`: `2ff0941`, `dee20da`),
  as are I5 and I6. New instrument work continues at X7 rather than reusing them, so an ID never
  means two things. This line used to reach for M9–M12 as the example of an unfilled gap; that
  comparison was false in both halves and the note below says why.
- **X7 and X8 are editable here since G5, and still closable only upstream.** Both are defects in
  `test-campaign` 0.9.2. Until 2026-08-22 a runner in an mcp-router worktree had nothing to edit —
  the submodule pin carries 0.5.0 and lacks the scripts entirely (DEF-057). `vendor/test-campaign/`
  now holds the real 0.9.2, so the blocker has changed shape rather than lifted: an edit that lands
  only in this repository is a fork of the instrument, and a script that behaves one way here and
  another way everywhere else stops being evidence. Closing them still means a change pushed to
  `fledgeling-plugins`, after which the pin in `vendor/README.md` moves to the new commit. G5
  touched neither.
- **M28 is a decision docket, not work.** A ship-feature runner should skip it. It closes by
  the owner answering four questions, after which each answer becomes an ordinary item.
- **DEF-001 and DEF-041 did not get a brief.** The pairing transport is already specified by
  M6 (Mac side) and I1 (phone side), both Untriaged. The campaign's measurements are appended
  to those two rather than duplicated into a third.

- **M9–M12 are all allocated, and this line used to deny it.** It read "unused … never allocated;
  the M series jumps from M8 to M13", which is false for all four. They are ledger items in
  **ORCHESTRATOR.md's deferred register, lines 241–244**: M9 renames `Evals` to `Checks`, M10
  amends `DESIGN.md` §6:279–280, M12 covers staleness inside a destructive dialog. **M11 was
  promoted out of that register and merged at `2a434b9`** (an ancestor of `main`). M9, M10 and
  M12 carry no branch and have not shipped. New Mac work continues at M24 rather than filling
  the gap, so an ID never means two things.
- **A range in a note hides the id inside it.** "M9–M12 are unused" absorbed a merged id and read
  clean in every membership check over this file, because nothing searches for `M11` inside the
  string `M9–M12`. Expand ranges before reconciling two id tables; a range is the one notation
  where a reconciliation reports agreement it never tested.
- **M23 blocks M15–M22.** It specifies how a mock-to-SwiftUI conversion is proved. Converting a
  board before the measurement layers exist produces a build that looks right and cannot be shown
  to be, which is the failure the brief's sources were written from. New Mac work continues at M24.
- **One skill is vendored; four are machine dependencies.** `vendor/test-campaign/` carries
  `test-campaign` **0.9.2**, pinned to `28ecd6753386ff6d480a98d6646a5b73c62dc299` of
  fledgeling-plugins and byte-identical to it; the four campaign gates run from there and
  `vendor/README.md` carries the pin, the tree checksum and the commands. `mockup-fidelity`,
  `mac-craft`, `design-craft` and `ux-craft` are **not** vendored in any usable sense: they exist
  only inside the `.claude/plugins/fledgeling-plugins` submodule, and no worktree may run
  `git submodule update --init` because populating 546 MB of plugin skills where Claude Code loads
  them kills the runner (ORCHESTRATOR.md, DISPATCH — WORKTREE SUBMODULE). This entry used to claim
  all five were read at a repo-relative path. **What that cost, measured 2026-08-22:** the
  submodule pin carries `test-campaign` 0.5.0, which has no `capture-lineage.py` and no
  `vacuity-check.py` at all, and whose `strict-check.py` prints **58 of 76 (76%)** on this
  registry — the exact figure `strict-ratchet.json` records — because it does not know the
  `effect-witness` rung and silently drops all four such cases. 0.9.2 and 0.9.4 both
  print **62 of 76 (82%)**. A clone following the old instruction would have read agreement off an
  instrument measuring something laxer.
- **M15–M22 were allocated together on 2026-08-19** from the interactive mock at
  `design/mcp-router-console.html`. They are UI specification, not defects: each names something
  the mock draws that no earlier brief covers. `design/mcp-router-console-spec.md` carries the
  audit numbers and the list of what the mock specifies rather than measures.

- **Three ids mean two things each, and R7 found it while allocating.** `ORCHESTRATOR.md`'s
  deferred register (lines ~334–337) carries rows **R6** (router-side eval runner), **R7** (skills
  write endpoint) and **R8** (server soft-delete), while this table carries **R6** (child PATH,
  ready to verify), **R7** (harness reconciliation, this item) and **R8** (auth rejection, Done).
  Six live rows, three ids. The deferred register was populated from a different allocator than
  this table and nothing reconciles the two namespaces, which is the same hole the notes above
  describe for membership — `ledger-reconcile.py` compares ids across files and a collision
  between a *ledger* id and a *deferred-register* id is not a membership disagreement, so it
  reports agreement. R7 did **not** renumber anything: three rows written by other items are not
  a runner's to renumber mid-fleet, and picking a direction silently is how the drift got here.
  R7's own children are `R7-C1`…`R7-C4`, following `R4-C1`/`R4-C2`, which cannot collide either way.
  The decision this needs is which namespace yields — that is M28's shape, not a runner's.

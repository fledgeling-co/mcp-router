# Breadth ledger — settings (generated by scripts/acceptance/mock-fidelity-gate.sh)

Every row is derived on the run that wrote this file: the mock side from
`scripts/acceptance/mock-affordances.py`, the build side from the measurement harness's
dump. Nothing here is hand-maintained, which is what stops a row being deleted to make a
finding disappear.

## Layers

| Layer | Result | Detail |
|---|---|---|
| `tokens` | clean | 25 matched, 64 pending, of 89 rows |
| `literals` | clean | scanning 125 files |
| `structure` | clean | 53 nodes across 2 states · 13 declared axis/axes corroborated against child geometry |
| `geometry` | clean | 53 frames |
| `type-metrics` | clean | 9 per-role comparison(s) over 12 text nodes · 3 roles · Subheadline=14pt Callout=15pt Title1=26pt · 3 multi-line node(s) excluded from the per-role check |
| `copy` | clean | 3 paired strings |
| `breadth` | 97 finding(s) | absent 53 · extra-cited 44 · present 3 · structure-unpaired 4 |
| `font-weight-face` | inconclusive (substituted) | font-weight-face: String(describing:) of a Font built through Font.system(size:weight:) returns "Font(provider: SwiftUI.FontBox<SwiftUI.Font.SystemProvider>)" — no size, no weight, no face |

## Present / divergent / absent

| State | Affordance | Kind | Status | Mock value | Build value | Citation |
|---|---|---|---|---|---|---|
| ideal | v-ideal/heading/router | heading | present | mock="Router" kind=heading | build=settings.ideal/settings-window/detail/pane-hero/pane-title text="Router" role=board-title kind=text |  |
| ideal | v-ideal/sentence/the-endpoint-your-harnesses-point-at-and-how-lon | sentence | present | mock="The endpoint your harnesses point at, and how long a child s" kind=sentence | build=settings.ideal/settings-window/detail/pane-hero/pane-subtitle text="The endpoint your harnesses point at, and how long a child s" role=board-subtitle kind=text |  |
| ideal | v-ideal/button/copy | button | present | mock="Copy" kind=button | build=settings.ideal/settings-window/detail/group-router/copy-endpoint text="Copy" role=state-action kind=leaf |  |
| ideal | v-ideal/button/5-minutes | button | absent | mock="5 minutes" | build=no paired node | D4 · app/Sources/MCPRouterKit/Shell/SettingsPresentation.swift:12-19 — "There is no control-API endpoint that writes any of these"; ControlToken.swift:13-19 admits /servers, /usage and /registry only |
| ideal | v-ideal/icon/unlabelled | icon | absent | mock="" | build=no paired node | D4 · app/Sources/MCPRouterKit/Shell/SettingsPresentation.swift:12-19 — "There is no control-API endpoint that writes any of these"; ControlToken.swift:13-19 admits /servers, /usage and /registry only |
| ideal | v-ideal/button/choose | button | absent | mock="Choose…" | build=no paired node | D6 · planning/features-to-triage/M18-sheets-and-gates.md owns every sheet a pane would open |
| ideal | v-ideal/button/start-at-login | button | absent | mock="Start at login" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'SMAppService\|LSSharedFileList\|launchd' over app/Sources and app/MCPRouter returned nothing |
| ideal | v-ideal/button/inspect | button | absent | mock="Inspect…" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/Models.swift:114-121 — ServersResponse carries port, idleMs, since, pendingAuth and servers[] and nothing else |
| ideal | v-ideal/heading/harnesses | heading | absent | mock="Harnesses" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/what-this-app-may-do-to-other-tools-configuratio | sentence | absent | mock="What this app may do to other tools’ configuration files, an" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/adopt-new-servers-automatically | button | absent | mock="Adopt new servers automatically" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — the control API owns /servers, /usage and /registry; grep for a harnesses route over app/Sources on 2026-08-22 returned nothing |
| ideal | v-ideal/button/warn-about-duplicates | button | absent | mock="Warn about duplicates" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — the control API owns /servers, /usage and /registry; grep for a harnesses route over app/Sources on 2026-08-22 returned nothing |
| ideal | v-ideal/button/reconcile-without-asking | button | absent | mock="Reconcile without asking" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — the control API owns /servers, /usage and /registry; grep for a harnesses route over app/Sources on 2026-08-22 returned nothing |
| ideal | v-ideal/button/every-hour | button | absent | mock="Every hour" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — the control API owns /servers, /usage and /registry; grep for a harnesses route over app/Sources on 2026-08-22 returned nothing |
| ideal | v-ideal/icon/unlabelled#2 | icon | absent | mock="" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — the control API owns /servers, /usage and /registry; grep for a harnesses route over app/Sources on 2026-08-22 returned nothing |
| ideal | v-ideal/heading/session-analyst | heading | absent | mock="Session analyst" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/which-model-reads-your-sessions-how-often-it-run | sentence | absent | mock="Which model reads your sessions, how often it runs, and whic" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/analyse-my-sessions | button | absent | mock="Analyse my sessions" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/button/claude-cli-haiku | button | absent | mock="Claude CLI · haiku" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/icon/unlabelled#3 | icon | absent | mock="" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/button/grok-cli-grok-4-6-medium | button | absent | mock="Grok CLI · grok-4.6 medium" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/icon/unlabelled#4 | icon | absent | mock="" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/button/choose#2 | button | absent | mock="Choose…" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/button/every-6-hours | button | absent | mock="Every 6 hours" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/icon/unlabelled#5 | icon | absent | mock="" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/button/notify-me-about-findings | button | absent | mock="Notify me about findings" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'analyst' over app/Sources, src/ and app/MCPRouter returned only comments describing this mock — there is no analyst in the product |
| ideal | v-ideal/heading/updates | heading | absent | mock="Updates" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/how-often-this-app-and-the-capabilities-you-have | sentence | absent | mock="How often this app and the capabilities you have installed l" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/every-12-hours | button | absent | mock="Every 12 hours" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'updateCheck\|appcast\|Sparkle\|CFBundleShortVersionString' over app/Sources, app/MCPRouter and src/ returned nothing but Info.plist keys |
| ideal | v-ideal/icon/unlabelled#6 | icon | absent | mock="" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'updateCheck\|appcast\|Sparkle\|CFBundleShortVersionString' over app/Sources, app/MCPRouter and src/ returned nothing but Info.plist keys |
| ideal | v-ideal/button/install-updates-automatically | button | absent | mock="Install updates automatically" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'updateCheck\|appcast\|Sparkle\|CFBundleShortVersionString' over app/Sources, app/MCPRouter and src/ returned nothing but Info.plist keys |
| ideal | v-ideal/button/hold-a-version-that-wants-more | button | absent | mock="Hold a version that wants more" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'updateCheck\|appcast\|Sparkle\|CFBundleShortVersionString' over app/Sources, app/MCPRouter and src/ returned nothing but Info.plist keys |
| ideal | v-ideal/button/check-now | button | absent | mock="Check now" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'updateCheck\|appcast\|Sparkle\|CFBundleShortVersionString' over app/Sources, app/MCPRouter and src/ returned nothing but Info.plist keys |
| ideal | v-ideal/heading/security | heading | absent | mock="Security" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/what-a-new-version-has-to-prove-before-anything- | sentence | absent | mock="What a new version has to prove before anything is allowed t" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/rotate | button | absent | mock="Rotate…" | build=no paired node | D4 · app/Sources/MCPRouterUI/Settings/SettingsWindowModel.swift — forget()'s docstring: "There is no rotate endpoint and this is not one" |
| ideal | v-ideal/button/hold-schema-changes | button | absent | mock="Hold schema changes" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/Models.swift — pendingChange and heldChanges ship as behaviour; no route makes the holding itself a setting |
| ideal | v-ideal/button/manage | button | absent | mock="Manage…" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/90-days | button | absent | mock="90 days" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'retention\|keepHistory\|historyWindow' over app/Sources returned only remove(keepHistory:), which is per-server |
| ideal | v-ideal/icon/unlabelled#7 | icon | absent | mock="" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'retention\|keepHistory\|historyWindow' over app/Sources returned only remove(keepHistory:), which is per-server |
| ideal | v-ideal/heading/menu-bar | heading | absent | mock="Menu bar" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/what-the-status-item-shows-and-what-it-lets-you- | sentence | absent | mock="What the status item shows, and what it lets you approve wit" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/show-in-the-menu-bar | button | absent | mock="Show in the menu bar" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/show-the-dock-icon | button | absent | mock="Show the Dock icon" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'setActivationPolicy\|LSUIElement' over app/Sources and app/MCPRouter returned only MeasureDump's .prohibited |
| ideal | v-ideal/button/approve-from-the-popover | button | absent | mock="Approve from the popover" | build=no paired node | D4 · absence, established 2026-08-22: grep -ri 'setActivationPolicy\|LSUIElement' over app/Sources and app/MCPRouter returned only MeasureDump's .prohibited |
| ideal | v-ideal/heading/advanced | heading | absent | mock="Advanced" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/sentence/the-log-the-local-store-and-the-values-you-would | sentence | absent | mock="The log, the local store, and the values you would only ever" | build=no paired node | D3 · design/mcp-router-console.html:1553-1745 — the mock keeps all seven panes in the DOM and toggles `.active`; a NavigationSplitView detail renders the selected pane only, so this run measures the pane the source list has selected |
| ideal | v-ideal/button/reveal-in-finder | button | absent | mock="Reveal in Finder" | build=no paired node | D4 · scripts/lint/no-raw-design-values.sh — A36's one-channel rule forbids this layer naming FileManager, Bundle or a file URL, so revealing a file in Finder is not this window's to offer |
| ideal | v-ideal/button/reveal-in-finder#2 | button | absent | mock="Reveal in Finder" | build=no paired node | D4 · scripts/lint/no-raw-design-values.sh — A36's one-channel rule forbids this layer naming FileManager, Bundle or a file URL, so revealing a file in Finder is not this window's to offer |
| ideal | v-ideal/button/re-index | button | absent | mock="Re-index…" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlAPIClient.swift — reindex(_:) is per-server; there is no bulk re-index route on the control API |
| ideal | v-ideal/button/restore | button | absent | mock="Restore…" | build=no paired node | D4 · app/Sources/MCPRouterKit/Control/ControlToken.swift:13-19 — no route writes a harness config back, and scripts/lint/no-harness-config-writes.sh forbids the app doing it |
| ideal | v-ideal/callout/mcp-router-1-4-0-build-2026-08-19-developer-id-s | callout | absent | mock="MCP Router 1.4.0 (build 2026.08.19) · Developer ID signed an" | build=no paired node | D9 · Makefile:11-22 — every build in this repository is unsigned, so "Developer ID signed and notarised" is two facts nothing observes; the version half is drawn in Advanced, which this dump does not render |
| ideal | settings.ideal/settings-window | settings-window | structure-unpaired | mock=no affordance of a declared kind | build=settings.ideal/settings-window (2 children, at least one paired) |  |
| ideal | settings.ideal/settings-window/source-list | source-list | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-router | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-router | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-harnesses | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-harnesses | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-analyst | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-analyst | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-updates | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-updates | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-security | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-security | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-menubar | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-menubar | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/source-list/pane-row-advanced | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/source-list/pane-row-advanced | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| ideal | settings.ideal/settings-window/detail | detail | structure-unpaired | mock=no affordance of a declared kind | build=settings.ideal/settings-window/detail (3 children, at least one paired) |  |
| ideal | settings.ideal/settings-window/detail/pane-hero | title-block | structure-unpaired | mock=no affordance of a declared kind | build=settings.ideal/settings-window/detail/pane-hero (2 children, at least one paired) |  |
| ideal | settings.ideal/settings-window/detail/group-router | settings-group | structure-unpaired | mock=no affordance of a declared kind | build=settings.ideal/settings-window/detail/group-router (4 children, at least one paired) |  |
| ideal | settings.ideal/settings-window/detail/group-router/group-title | group-title | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/group-title | D8 · planning/specs/spec-M8.md — the four named group headers Router / Menu bar / Warm set / Control token are M8's shipped surface and scripts/acceptance/m8-settings-menubar.sh reads all four back off the rendered window; the mock draws one unlabelled .form-group per pane |
| ideal | settings.ideal/settings-window/detail/group-router/card | table | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/card | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-router/card/row-Endpoint | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/card/row-Endpoint | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-router/card/row-Home | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/card/row-Home | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-router/card/row-Idle reaper | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/card/row-Idle reaper | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-router/card/row-Counting since | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/card/row-Counting since | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-router/router-help | state-detail | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-router/router-help | D7 · planning/specs/spec-M8.md — routerHelp and the warm-set help are M8's approved "read from the router, not set here" sentences; the mock's equivalent prose is a <span> inside .fl, which no affordance rule matches |
| ideal | settings.ideal/settings-window/detail/group-warm-set | settings-group | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set | D8 · planning/specs/spec-M8.md — the four named group headers Router / Menu bar / Warm set / Control token are M8's shipped surface and scripts/acceptance/m8-settings-menubar.sh reads all four back off the rendered window; the mock draws one unlabelled .form-group per pane |
| ideal | settings.ideal/settings-window/detail/group-warm-set/group-title | group-title | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set/group-title | D8 · planning/specs/spec-M8.md — the four named group headers Router / Menu bar / Warm set / Control token are M8's shipped surface and scripts/acceptance/m8-settings-menubar.sh reads all four back off the rendered window; the mock draws one unlabelled .form-group per pane |
| ideal | settings.ideal/settings-window/detail/group-warm-set/warm-card | table | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set/warm-card | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-warm-set/warm-card/row-Kept resident | table-row | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set/warm-card/row-Kept resident | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| ideal | settings.ideal/settings-window/detail/group-warm-set/show-in-servers | state-action | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set/show-in-servers | D8 · planning/specs/spec-M8.md — `Show in Servers` is M8's shipped action and the switch it points at is on each server's own row; the mock offers `Choose…`, which opens a sheet planning/features-to-triage/M18-sheets-and-gates.md owns |
| ideal | settings.ideal/settings-window/detail/group-warm-set/warm-help | state-detail | extra-cited | mock=nothing | build=settings.ideal/settings-window/detail/group-warm-set/warm-help | D7 · planning/specs/spec-M8.md — routerHelp and the warm-set help are M8's approved "read from the router, not set here" sentences; the mock's equivalent prose is a <span> inside .fl, which no affordance rule matches |
| empty | v-empty/icon/unlabelled | icon | absent | mock="" | build=no paired node | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open"; the mock refuses the whole window |
| empty | v-empty/heading/settings-are-unavailable-while-the-router-is-sto | heading | absent | mock="Settings are unavailable while the router is stopped" | build=no paired node | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open"; the mock refuses the whole window |
| empty | v-empty/sentence/most-of-this-pane-reports-what-the-router-observ | sentence | absent | mock="Most of this pane reports what the router observes, and it i" | build=no paired node | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open"; the mock refuses the whole window |
| empty | v-empty/button/start-the-router | button | absent | mock="Start the router" | build=no paired node | D4 · planning/specs/spec-M8.md — the offline branch carries no action deliberately: nothing in this tree can start a router, and ServersBoardModel.cannotStartRouterReason is the sentence that says so |
| empty | settings.empty/settings-window | settings-window | extra-cited | mock=nothing | build=settings.empty/settings-window | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/source-list | source-list | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-router | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-router | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-harnesses | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-harnesses | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-analyst | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-analyst | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-updates | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-updates | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-security | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-security | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-menubar | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-menubar | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/source-list/pane-row-advanced | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/source-list/pane-row-advanced | D7 · design/mcp-router-console.html:1524-1551 — the mock draws its pane list as <nav class="swin-side"> INSIDE .swin-split and OUTSIDE every .v-* frame, so no state census can reach it; the list is the surface's chrome in the mock and part of the split view here |
| empty | settings.empty/settings-window/detail | detail | extra-cited | mock=nothing | build=settings.empty/settings-window/detail | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/pane-hero | title-block | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/pane-hero | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/pane-hero/pane-title | board-title | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/pane-hero/pane-title | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/pane-hero/pane-subtitle | board-subtitle | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/pane-hero/pane-subtitle | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/group-router | settings-group | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-router | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/group-router/group-title | group-title | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-router/group-title | D8 · planning/specs/spec-M8.md — the four named group headers Router / Menu bar / Warm set / Control token are M8's shipped surface and scripts/acceptance/m8-settings-menubar.sh reads all four back off the rendered window; the mock draws one unlabelled .form-group per pane |
| empty | settings.empty/settings-window/detail/group-router/router-offline | state-detail | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-router/router-offline | D5 · planning/specs/spec-M8.md — the offline branch renders ControlAPIError's headline and advice with no action button, recorded there as a deliberate DESIGN.md §5 deviation because nothing in this tree can start a router |
| empty | settings.empty/settings-window/detail/group-router/router-offline-detail | state-detail | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-router/router-offline-detail | D5 · planning/specs/spec-M8.md — the offline branch renders ControlAPIError's headline and advice with no action button, recorded there as a deliberate DESIGN.md §5 deviation because nothing in this tree can start a router |
| empty | settings.empty/settings-window/detail/group-warm-set | settings-group | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set | D5 · planning/specs/spec-M15.md:29 — assumption: "With the router stopped the window still opens and says so, rather than refusing to open". The mock's v-empty frame replaces the whole window with a refusal, so everything the build keeps live reads as extra |
| empty | settings.empty/settings-window/detail/group-warm-set/group-title | group-title | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set/group-title | D8 · planning/specs/spec-M8.md — the four named group headers Router / Menu bar / Warm set / Control token are M8's shipped surface and scripts/acceptance/m8-settings-menubar.sh reads all four back off the rendered window; the mock draws one unlabelled .form-group per pane |
| empty | settings.empty/settings-window/detail/group-warm-set/warm-card | table | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set/warm-card | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| empty | settings.empty/settings-window/detail/group-warm-set/warm-card/row-Kept resident | table-row | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set/warm-card/row-Kept resident | D7 · scripts/acceptance/mock-affordances.py:29-50 — the affordance rules name headings, rows (.trow), jacks, buttons, search fields, section headers, column headers, banners, cards (.card/.tablecard/.signalpath), fields (.kv), callouts, code blocks, progress, skeleton rows, dots, sprite icons and <p>. The mock's .form-group and .form-row match none of them, so the structure the build draws these from is not inventoried on the mock side at all |
| empty | settings.empty/settings-window/detail/group-warm-set/show-in-servers | state-action | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set/show-in-servers | D8 · planning/specs/spec-M8.md — `Show in Servers` is M8's shipped action and the switch it points at is on each server's own row; the mock offers `Choose…`, which opens a sheet planning/features-to-triage/M18-sheets-and-gates.md owns |
| empty | settings.empty/settings-window/detail/group-warm-set/warm-help | state-detail | extra-cited | mock=nothing | build=settings.empty/settings-window/detail/group-warm-set/warm-help | D7 · planning/specs/spec-M8.md — routerHelp and the warm-set help are M8's approved "read from the router, not set here" sentences; the mock's equivalent prose is a <span> inside .fl, which no affordance rule matches |

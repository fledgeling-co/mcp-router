# Ideation Log — 2026-08-26, from test-campaign + tailings + reckon

- [kept] Re-anchor the campaign to the current tree. THE finding of this pass: the campaign was written
  2026-08-24 05:11 and main has moved 43 commits since, so every number it reports describes a tree that
  no longer exists — and the reckoning that reads it therefore cannot see a day of merged work.
- [kept] Reconcile the defect vocabulary so the reckoning gate can pass. Measured: 12 distinct defect
  statuses in the registry, 9 of which reckon cannot classify, forcing exit 4 every run all session.
- [kept] Upstream: tailings' R2 probe resolves a `file.md:NN` citation by basename against the repo root,
  reporting a tracked, present spec as "exists nowhere in the repo or its history". Same shape as X7/X8.
- [dropped] A brief for the 45 capture-lineage failures — already covered by campaign-capture-catalog-audit.md.
- [dropped] A brief for sourceRoot/testRoot being undeclared — already covered by
  test-campaign-effect-provider-resolver.md.
- [dropped] A brief for Planes/Journeys/Controls NOT DECLARED — already covered by
  campaign-journey-plane-ledger-model.md.
- [dropped] A brief for the empty SwiftUI actions in M34's fixture — read them; they are `.disabled(true)`
  fixture buttons that exist to carry a badge for the AX probe and are never pressed. Correct as written.
